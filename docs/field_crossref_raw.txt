================================================================================
GROUPTRACK — FIELD-LEVEL CROSS-REFERENCE
================================================================================
Auto-generated by recommit_docs_v10.sh
Upload this file to Claude at session start for full codebase context.

LIFECYCLE PHASES:
  INIT    = Activity.onCreate → Hilt DI → ViewModel init{} — NO PERMISSIONS YET
  TICK    = Every 3-5s from startTick() — requires ACCESS_FINE_LOCATION guard
  MAP-LOAD = WebView.onPageFinished — permissions should be granted
  RECORD  = User taps Record → ConvoyGpsService — requires FINE_LOCATION + FOREGROUND_SERVICE
  RADIO   = Radio BLE connect/disconnect — requires BLUETOOTH_SCAN + BLUETOOTH_CONNECT
  USER    = User-initiated button/menu action — varies by action

RULES:
  INIT functions must NOT require permissions, network, or launch activities.
  TICK functions must have permission guard if accessing GPS/Location.
  Never call startActivity() during INIT or TICK.

STATUS KEY:
  [ADDED]      = New code not in stock Meshtastic
  [CHANGED]    = Modified from Meshtastic base
  [REFERENCE]  = Stock Meshtastic, referenced by convoy code
  [REMOVED]    = Was present, now removed (noted for history)

Generated: 2026-05-29 00:12
Branch: feature/convoy-event-ride
Last commit: 1d126d972 docs: recommit all project documents + cross-reference — 2026-05-29 00:12

================================================================================
SECTION 1: MESHTASTIC BASE LAYER MODIFICATIONS
================================================================================

--- MainActivity.kt ---
[CHANGED] Main entry point. GroupTrack modifications:
  137:        android.util.Log.d("TrackImport", "handleIntent action=${intent.action} data=${intent.data} type=${intent.type}")
  144:                val isConvoyFile = mimeType == "application/x-convoy-ride" ||
  145:                    appLinkData?.path?.endsWith(".convoy") == true
  148:                } else if (isTrackFile(appLinkData, mimeType)) {
  149:                    appLinkData?.let { handleTrackFileImport(it) }
  194:            val importDir = java.io.File(filesDir, "convoy_import").also { it.mkdirs() }
  195:            val fileName  = "ride_${System.currentTimeMillis()}.convoy"
  200:            // Peek at convoyDocType to confirm valid convoy file
  202:            val docType = json.optString("convoyDocType", "unknown")
  205:            android.util.Log.e("ConvoyImport", "Failed to copy convoy file: ${e.message}")
  209:    private fun isTrackFile(uri: Uri?, mimeType: String?): Boolean {
  228:    private fun handleTrackFileImport(uri: Uri) {
  257:                val result = com.geeksville.mesh.convoy.ConvoyTrackOps.importTrackFile(tempFile)
  262:                    is com.geeksville.mesh.convoy.ConvoyTrackOps.ImportResult.Success ->
  265:                    is com.geeksville.mesh.convoy.ConvoyTrackOps.ImportResult.PartialSuccess ->
  267:                    is com.geeksville.mesh.convoy.ConvoyTrackOps.ImportResult.Failed ->
  270:                android.util.Log.i("TrackImport", msg)
  273:                android.util.Log.e("TrackImport", "Import error: ${e.message}")

--- ui/Main.kt ---
[CHANGED] Navigation host. GroupTrack modifications:
  21:import com.geeksville.mesh.convoy.ConvoyConfig
  86:import com.geeksville.mesh.convoy.ConvoyViewModel
  88:import com.geeksville.mesh.convoy.ConvoySubMenu
  89:import com.geeksville.mesh.navigation.convoyGraph
  90:import org.meshtastic.core.navigation.ConvoyRoutes
  157:    Convoy(Res.string.map, MeshtasticIcons.Map, ConvoyRoutes.Convoy),
  158:    EventRideServices(Res.string.map, MeshtasticIcons.Wifi, ConvoyRoutes.Convoy),
  170:fun MainScreen(uIViewModel: UIViewModel = hiltViewModel(), scanModel: ScannerViewModel = hiltViewModel(), convoyViewModel: ConvoyViewModel = hiltViewModel()) {
  171:    var showConvoyMenu by remember { mutableStateOf(false) }
  173:    val convoyMenuSheetState = rememberModalBottomSheetState(skipPartiallyExpanded = true)
  311:            .filter { it != TopLevelDestination.EventRideServices || ConvoyConfig.V3_FEATURES_ENABLED }
  412:                            text = when (destination) { TopLevelDestination.Convoy -> "GroupTrack"; TopLevelDestination.EventRideServices -> "Services"; else -> stringResource(destination.label) },
  447:                                TopLevelDestination.Convoy -> {
  448:                                    navController.navigate(ConvoyRoutes.Convoy) {
  454:                                        showConvoyMenu = true
  460:                            if (destination == TopLevelDestination.Convoy) {
  461:                                navController.navigate(ConvoyRoutes.Convoy) {
  467:                                    showConvoyMenu = true
  485:            startDestination = ConvoyRoutes.Convoy,
  491:            convoyGraph(navController, convoyViewModel)
  498:        // ── Convoy submenu overlay ────────────────────────────────────────────
  499:        if (showConvoyMenu) {
  500:            ConvoySubMenu(
  501:                sheetState                = convoyMenuSheetState,
  502:                onDismiss                 = { showConvoyMenu = false },
  503:                onCreateEventRide         = { showConvoyMenu = false },
  504:                onTransferConfig          = { showConvoyMenu = false; navController.navigate(ConvoyRoutes.ConvoyTransferRide) },
  506:                    navController.navigate(ConvoyRoutes.ConvoyEmailGate)
  509:                    showConvoyMenu = false
  510:                    navController.navigate(ConvoyRoutes.ConvoySettingsPanel)
  513:                    showConvoyMenu = false
  514:                    navController.navigate(ConvoyRoutes.ConvoyApplyRadio)
  517:                    showConvoyMenu = false
  518:                    navController.navigate(ConvoyRoutes.ConvoyArchiveRestore)
  521:                    showConvoyMenu = false
  522:                    navController.navigate(ConvoyRoutes.ConvoyMapViewer)
  525:                    navController.navigate(ConvoyRoutes.ConvoyTracks)
  532:            com.geeksville.mesh.convoy.ConvoyServicesChooser(
  537:                    val route = com.geeksville.mesh.convoy.ConvoySessionManager.resolveLaunchRoute(servicesContext, true)
  539:                        com.geeksville.mesh.convoy.ConvoySessionManager.LaunchRoute.TERMS        -> ConvoyRoutes.ConvoyTerms
  540:                        com.geeksville.mesh.convoy.ConvoySessionManager.LaunchRoute.PRIVACY      -> ConvoyRoutes.ConvoyPrivacy
  541:                        com.geeksville.mesh.convoy.ConvoySessionManager.LaunchRoute.SUBSCRIPTION -> ConvoyRoutes.ConvoySubscription
  542:                        com.geeksville.mesh.convoy.ConvoySessionManager.LaunchRoute.DASHBOARD    -> ConvoyRoutes.ConvoyDashboard
  543:                        else                                                                      -> ConvoyRoutes.ConvoySignIn
  549:                    navController.navigate(ConvoyRoutes.ConvoyFieldRadio)

--- navigation/ConvoyNavigation.kt ---
[ADDED] All convoy navigation routes:
  10:import androidx.navigation.compose.composable
  50:import org.meshtastic.core.navigation.ConvoyRoutes
  60:    composable<ConvoyRoutes.Convoy> {
  62:            onNavigateToMapViewer = { navController?.navigate(ConvoyRoutes.ConvoyMapViewer) },
  63:            onNavigateToTrackExport = { navController?.navigate(ConvoyRoutes.ConvoyTracks) },
  64:            onNavigateToTrackImport = { navController?.navigate(ConvoyRoutes.ConvoyTrackImport) },
  66:                navController?.navigate(ConvoyRoutes.ConvoySettings)
  72:    composable<ConvoyRoutes.ConvoySettings> {
  75:            onNavigateToMapSources = { navController?.navigate(ConvoyRoutes.ConvoyMapSources) }
  80:    composable<ConvoyRoutes.ConvoyTrailSources> {
  87:    composable<ConvoyRoutes.ConvoyRouteCreate> {
  94:    composable<ConvoyRoutes.ConvoyMapSources> {
  101:    composable<ConvoyRoutes.ConvoyEnrollment> {
  109:    composable<ConvoyRoutes.ConvoyEmailGate> {
  111:            onProceed       = { navController?.navigate(ConvoyRoutes.ConvoyCreateEvent) },
  114:                navController?.navigate(ConvoyRoutes.ConvoyEnrollment)
  119:    composable<ConvoyRoutes.ConvoyCreateEvent> {
  129:    composable<ConvoyRoutes.ConvoySettingsPanel> {
  140:                onNavigateToCapture          = { navController?.navigate(ConvoyRoutes.ConvoyMasterCapture) },
  141:                onNavigateToApplyList        = { navController?.navigate(ConvoyRoutes.ConvoyApplyList) },
  142:                onNavigateToArchiveRestore   = { navController?.navigate(ConvoyRoutes.ConvoyArchiveRestore) },
  143:                onNavigateToApplyListMaint   = { navController?.navigate(ConvoyRoutes.ConvoyApplyListMaint) },
  144:                onNavigateToCaptureMaint     = { navController?.navigate(ConvoyRoutes.ConvoyMasterCaptureMaint) }
  150:    composable<ConvoyRoutes.ConvoyApplyList> {
  153:            onCaptureNewMaster = { navController?.navigate(ConvoyRoutes.ConvoyMasterCapture) }
  161:    composable<ConvoyRoutes.ConvoyApplyRadio> {
  171:    composable<ConvoyRoutes.ConvoyTransferRide> {
  178:    composable<ConvoyRoutes.ConvoyArchiveRestore> {
  181:            onDone = { navController?.navigate(ConvoyRoutes.Convoy) { popUpTo(ConvoyRoutes.Convoy) { inclusive = false } } },
  187:    composable<ConvoyRoutes.ConvoyApplyListMaint> {
  193:    composable<ConvoyRoutes.ConvoyMasterCaptureMaint> {
  200:    composable<ConvoyRoutes.ConvoyTracks> {
  203:            onNavigateToTrackImport = { navController?.navigate(ConvoyRoutes.ConvoyTrackImport) }
  207:    composable<ConvoyRoutes.ConvoyMapViewer> {
  210:            onNavigateToTrackExport = { navController?.navigate(ConvoyRoutes.ConvoyTracks) },
  211:            onNavigateToTrackImport = { navController?.navigate(ConvoyRoutes.ConvoyTrackImport) },
  212:            onNavigateToTrailSources = { navController?.navigate(ConvoyRoutes.ConvoyTrailSources) }
  216:    composable<ConvoyRoutes.ConvoyTrackImport> {
  222:    composable<ConvoyRoutes.ConvoyReconnectWait> {
  226:            onProceed = { navController?.navigate(ConvoyRoutes.ConvoyWriteVerify) },
  227:            onCancel  = { navController?.navigate(ConvoyRoutes.Convoy) { popUpTo(ConvoyRoutes.Convoy) { inclusive = false } } }
  236:    composable<ConvoyRoutes.ConvoyWriteVerify> {
  241:            return@composable
  247:                navController?.navigate(ConvoyRoutes.Convoy) {
  248:                    popUpTo(ConvoyRoutes.Convoy) { inclusive = false }
  256:    composable<ConvoyRoutes.ConvoyMasterSuccess> {
  259:                navController?.navigate(ConvoyRoutes.ConvoyApplyList) {
  260:                    popUpTo(ConvoyRoutes.ConvoyMasterCapture) { inclusive = true }
  264:                navController?.navigate(ConvoyRoutes.ConvoyMasterCapture) {
  265:                    popUpTo(ConvoyRoutes.ConvoyMasterCapture) { inclusive = true }
  272:    composable<ConvoyRoutes.ConvoyMasterCapture> {
  278:                    navController?.navigate(ConvoyRoutes.ConvoyMasterSuccess) {
  279:                        popUpTo(ConvoyRoutes.ConvoyMasterCapture) { inclusive = true }
  290:    composable<ConvoyRoutes.ConvoySignIn> {
  295:                    val route = ConvoySessionManager.resolveLaunchRoute(context, true)
  297:                        ConvoySessionManager.LaunchRoute.TERMS        -> ConvoyRoutes.ConvoyTerms
  298:                        ConvoySessionManager.LaunchRoute.PRIVACY      -> ConvoyRoutes.ConvoyPrivacy
  299:                        ConvoySessionManager.LaunchRoute.SUBSCRIPTION -> ConvoyRoutes.ConvoySubscription
  300:                        ConvoySessionManager.LaunchRoute.DASHBOARD    -> ConvoyRoutes.ConvoyDashboard
  301:                        else                                          -> ConvoyRoutes.ConvoySignIn
  304:                        popUpTo(ConvoyRoutes.ConvoySignIn) { inclusive = true }
  311:                    val route = ConvoySessionManager.resolveLaunchRoute(context, true)
  313:                        ConvoySessionManager.LaunchRoute.TERMS        -> ConvoyRoutes.ConvoyTerms
  314:                        ConvoySessionManager.LaunchRoute.PRIVACY      -> ConvoyRoutes.ConvoyPrivacy
  315:                        ConvoySessionManager.LaunchRoute.SUBSCRIPTION -> ConvoyRoutes.ConvoySubscription
  316:                        else                                          -> ConvoyRoutes.ConvoyDashboard
  319:                        popUpTo(ConvoyRoutes.ConvoySignIn) { inclusive = true }
  323:                    navController?.navigate(ConvoyRoutes.Convoy) {
  324:                        popUpTo(ConvoyRoutes.ConvoySignIn) { inclusive = true }
  333:    composable<ConvoyRoutes.ConvoyMyRides> {
  335:            onNavigateToRideDetail = { navController?.navigate(ConvoyRoutes.ConvoyRideDetail) },
  336:            onNavigateToCreateRide = { navController?.navigate(ConvoyRoutes.ConvoyCreateRide) },
  337:            onNavigateToFieldRadio = { navController?.navigate(ConvoyRoutes.ConvoyFieldRadio) },
  340:    composable<ConvoyRoutes.ConvoyCreateRide> {
  342:            onRideCreated = { navController?.navigate(ConvoyRoutes.ConvoyRideDetail) },
  343:            onNavigateToFieldRadio = { navController?.navigate(ConvoyRoutes.ConvoyFieldRadio) },
  344:            onApplyMasterConfig = { navController?.navigate(ConvoyRoutes.ConvoyApplyRadio) },
  345:            onArchiveRestore = { navController?.navigate(ConvoyRoutes.ConvoyArchiveRestore) },
  348:    composable<ConvoyRoutes.ConvoyRideDetail> {
  351:            onNavigateToSendInvite = { navController?.navigate(ConvoyRoutes.ConvoyInviteSend) },
  352:            onNavigateToBroadcast  = { navController?.navigate(ConvoyRoutes.ConvoyBroadcast) },
  353:            onNavigateToCreateRide = { navController?.navigate(ConvoyRoutes.ConvoyCreateRide) },
  354:            onNavigateToFieldRadio = { navController?.navigate(ConvoyRoutes.ConvoyFieldRadio) },
  355:            onApplyMasterConfig = { navController?.navigate(ConvoyRoutes.ConvoyApplyRadio) },
  356:            onArchiveRestore = { navController?.navigate(ConvoyRoutes.ConvoyArchiveRestore) },
  359:    composable<ConvoyRoutes.ConvoyProfile> {
  362:            onMyOrganizers = { navController?.navigate(ConvoyRoutes.ConvoyMyOrganizers) },
  363:            onApplyMasterConfig = { navController?.navigate(ConvoyRoutes.ConvoyApplyRadio) },
  364:            onArchiveRestore = { navController?.navigate(ConvoyRoutes.ConvoyArchiveRestore) }
  367:    composable<ConvoyRoutes.ConvoyExplore> {
  370:    composable<ConvoyRoutes.ConvoyTerms> {
  372:            onAccept = { navController?.navigate(ConvoyRoutes.ConvoyPrivacy) { popUpTo(ConvoyRoutes.ConvoyTerms) { inclusive = true } } },
  373:            onDecline = { navController?.navigate(ConvoyRoutes.Convoy) { popUpTo(ConvoyRoutes.ConvoyTerms) { inclusive = true } } }
  376:    composable<ConvoyRoutes.ConvoyPrivacy> {
  378:            onAccept = { navController?.navigate(ConvoyRoutes.ConvoyDashboard) { popUpTo(ConvoyRoutes.ConvoyPrivacy) { inclusive = true } } },
  379:            onDecline = { navController?.navigate(ConvoyRoutes.Convoy) { popUpTo(ConvoyRoutes.ConvoyPrivacy) { inclusive = true } } }
  382:    composable<ConvoyRoutes.ConvoySubscription> {
  387:                navController?.navigate(ConvoyRoutes.ConvoyDashboard) {
  388:                    popUpTo(ConvoyRoutes.ConvoySubscription) { inclusive = true }
  392:                navController?.navigate(ConvoyRoutes.Convoy) {
  393:                    popUpTo(ConvoyRoutes.ConvoySubscription) { inclusive = true }
  402:    composable<ConvoyRoutes.ConvoyDashboard> {
  405:            isSubscribed = ConvoySessionManager.isSubscribed(context),
  406:            onNavigateToRides       = { navController?.navigate(ConvoyRoutes.ConvoyRideDetail) },
  407:            onNavigateToExplore     = { navController?.navigate(ConvoyRoutes.ConvoyExplore) },
  408:            onNavigateToTracks      = { navController?.navigate(ConvoyRoutes.ConvoyTracks) },
  409:            onNavigateToProfile     = { navController?.navigate(ConvoyRoutes.ConvoyProfile) },
  410:            onNavigateToFieldRadio  = { navController?.navigate(ConvoyRoutes.ConvoyFieldRadio) },
  411:            onShowSubscription      = { navController?.navigate(ConvoyRoutes.ConvoySubscription) },
  413:            onApplyMasterConfig     = { navController?.navigate(ConvoyRoutes.ConvoyApplyRadio) },
  414:            onArchiveRestore        = { navController?.navigate(ConvoyRoutes.ConvoyArchiveRestore) },
  415:            onDownloadRideConfig    = { navController?.navigate(ConvoyRoutes.ConvoyDownloadRideConfig) },
  418:                navController?.navigate(ConvoyRoutes.ConvoyCompletedRides)
  420:            onNavigateToSearchByArea = { navController?.navigate(ConvoyRoutes.ConvoySearchByArea) }
  424:    composable<ConvoyRoutes.ConvoyInviteSend> {
  426:            onNavigateToCreateRide = { navController?.navigate(ConvoyRoutes.ConvoyCreateRide) },
  427:            onNavigateToFieldRadio = { navController?.navigate(ConvoyRoutes.ConvoyFieldRadio) },
  430:    composable<ConvoyRoutes.ConvoyBroadcast> {
  432:            onNavigateToCreateRide = { navController?.navigate(ConvoyRoutes.ConvoyCreateRide) },
  433:            onNavigateToFieldRadio = { navController?.navigate(ConvoyRoutes.ConvoyFieldRadio) },
  438:    composable<ConvoyRoutes.ConvoyFieldRadio> {
  440:            onNavigateToApplyMaster = { navController?.navigate(ConvoyRoutes.ConvoyApplyRadio) },
  441:            onNavigateToVerify      = { navController?.navigate(ConvoyRoutes.ConvoyWriteVerify) },
  445:    composable<ConvoyRoutes.ConvoyDownloadRideConfig> {
  450:    composable<ConvoyRoutes.ConvoyMyOrganizers> {
  455:    composable<ConvoyRoutes.ConvoyCompletedRides> {
  460:                navController?.navigate(ConvoyRoutes.ConvoyCompletedRideDetail)
  465:    composable<ConvoyRoutes.ConvoySearchByArea> {
  470:    composable<ConvoyRoutes.ConvoyCompletedRideDetail> {
  473:            onNavigateToMap = { navController?.navigate(ConvoyRoutes.Convoy) },

--- app/build.gradle.kts ---
  52:    signingConfigs {
  65:        versionCode =
  71:        versionName =
  165:            versionName = "${defaultConfig.versionName} (${defaultConfig.versionCode}) $name"
  175:                signingConfig = signingConfigs.named("release").get()
  177:                signingConfig = signingConfigs.getByName("debug")
  196:        variant.flavorName?.let { flavor -> variant.applicationId = "com.grouptrack.android.$flavor.debug" }
  213:        "Version code is set to: ${extensions.getByType<ApplicationExtension>().defaultConfig.versionCode}",

--- AndroidManifest.xml ---
  61:    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="replace" />
  62:    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:node="replace" />
  63:    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
  66:    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  67:    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
  68:    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
  69:    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
  162:            <intent-filter>
  164:            </intent-filter>
  168:            android:name="com.geeksville.mesh.convoy.ConvoyGpsService"
  188:            <intent-filter>
  191:            </intent-filter>
  193:            <intent-filter>
  197:            </intent-filter>
  199:            <intent-filter>
  206:            </intent-filter>
  207:            <intent-filter>
  211:                <data android:mimeType="application/vnd.google-earth.kml+xml" />
  212:            </intent-filter>
  213:            <intent-filter>
  217:                <data android:mimeType="application/gpx+xml" />
  218:            </intent-filter>
  219:            <intent-filter>
  224:            </intent-filter>
  225:            <intent-filter>
  230:            </intent-filter>
  231:            <intent-filter>
  236:            </intent-filter>
  238:            <intent-filter android:autoVerify="true">
  259:            </intent-filter>
  261:            <intent-filter>
  263:            </intent-filter>
  274:            <intent-filter>
  284:            </intent-filter>
  287:            <intent-filter>
  293:            </intent-filter>
  302:            <intent-filter>
  304:            </intent-filter>
  311:            android:name="com.geeksville.mesh.convoy.ConvoyFileReceiver"
  314:            <intent-filter>
  317:                <data android:scheme="content" android:mimeType="application/octet-stream" android:pathPattern=".*\.convoy" />
  318:            </intent-filter>
  319:            <intent-filter>
  322:                <data android:scheme="content" android:mimeType="application/x-convoy-ride" />
  323:            </intent-filter>
  326:        <!-- V3 Phase A - Invite deep link handler (intent-filter wired in Phase B) -->
  328:            android:name="com.geeksville.mesh.convoy.ConvoyInviteHandler"
  337:            <intent-filter android:label="@string/app_name">
  339:            </intent-filter>
  360:                android:resource="@xml/convoy_file_paths" />
  367:            android:name=".convoy.ConvoyDownloadActionReceiver"

================================================================================
SECTION 2: CONVOY FILE INVENTORY
================================================================================

FILE | LINES | FUNCTIONS | PURPOSE
---- | ----- | --------- | -------
AliasManager.kt | 11 | 1 |  -----------------------------------------------------------
ArtifactListPanel.kt | 357 | 3 |  Detail panel state
AuditLogger.kt | 11 | 1 |  -----------------------------------------------------------
ConvoyApiClient.kt | 433 | 12 |  ===========================================================
ConvoyApplyList.kt | 136 | 4 |  ── LoRa fields ─────────────�
ConvoyApplyListMaintenanceScreen.kt | 284 | 4 |  ── Header ───────────────
ConvoyApplyListScreen.kt | 594 | 6 |  "MASTER" or "RIDE"
ConvoyApplyRadioScreen.kt | 599 | 5 |  Header
ConvoyArchiveRestoreScreen.kt | 381 | 5 |  Load archive list
ConvoyArtifactDetailPanel.kt | 33 | 1 |  V2.5 Scaffold (Pass 1) -- ScreenReference v5 section 5
ConvoyArtifactOps.kt | 73 | 11 |  -----------------------------------------------------------
ConvoyArtifactsPanel.kt | 222 | 2 |  -----------------------------------------------------------
ConvoyBroadcastScreen.kt | 97 | 2 |  ===========================================================
ConvoyCompletedRideDetailScreen.kt | 243 | 3 |  ===========================================================
ConvoyCompletedRidesScreen.kt | 427 | 6 |  ===========================================================
ConvoyConfig.kt | 104 | 1 | ConvoyConfig {
ConvoyCreateEventScreen.kt | 409 | 4 |  0=idle 1=read 2=backup 3=write 4=done
ConvoyCreateRideScreen.kt | 571 | 4 |  ===========================================================
ConvoyDashboardScreen.kt | 882 | 10 |  ── Header ───────────────
ConvoyDevLaunchScreen.kt | 168 | 3 |  ===========================================================
ConvoyDevSeeder.kt | 321 | 27 |  ===========================================================
ConvoyDisplayPanel.kt | 218 | 2 |  -----------------------------------------------------------
ConvoyDownloadActionReceiver.kt | 38 | 1 | ConvoyDownloadActionReceiver : BroadcastReceiver() {
ConvoyDownloadConfirm.kt | 166 | 1 | TileDownloadRequest(
ConvoyDownloadNotification.kt | 94 | 6 | ConvoyDownloadNotification {
ConvoyDownloadPanel.kt | 465 | 9 |  -----------------------------------------------------------
ConvoyDownloadQueue.kt | 431 | 21 |  ===========================================================
ConvoyDownloadQueuePanel.kt | 309 | 6 |  -----------------------------------------------------------
ConvoyDownloadRideConfigScreen.kt | 284 | 1 |  ===========================================================
ConvoyDownloadWorker.kt | 201 | 2 | ConvoyDownloadWorker(
ConvoyEmailGateScreen.kt | 316 | 2 |  Meshtastic palette
ConvoyEngine.kt | 178 | 10 | ConvoyEngine {
ConvoyEnrollment.kt | 318 | 2 |  ── Header ───────────────
ConvoyEnrollmentQueue.kt | 109 | 12 |  ===========================================================
ConvoyEventConfig.kt | 195 | 6 | ConvoyEventConfig(
ConvoyExploreScreen.kt | 25 | 1 | 
ConvoyFieldRadioScreen.kt | 169 | 2 |  ===========================================================
ConvoyFileReceiver.kt | 107 | 1 | ConvoyFileReceiver : Activity() {
ConvoyGpsService.kt | 509 | 32 | ConvoyGpsService : Service() {
ConvoyInviteHandler.kt | 183 | 5 |  ===========================================================
ConvoyInviteSendScreen.kt | 91 | 2 |  ===========================================================
ConvoyMapBar.kt | 130 | 1 |  -----------------------------------------------------------
ConvoyMapSourceScreen.kt | 528 | 8 |  Refresh state
ConvoyMapViewerScreen.kt | 1325 | 13 |  Three-state display: OFF=0, ON=1, SELECTED=2
ConvoyMarkerRenderer.kt | 28 | 0 | is retained only as the bridge for node tap callbacks from
ConvoyMasterApply.kt | 103 | 4 | ConvoyMasterApply {
ConvoyMasterCaptureMaintScreen.kt | 21 | 1 | 
ConvoyMasterCaptureScreen.kt | 646 | 3 |  ── BL-02: US915 LONG_FAST frequency conversion ──�
ConvoyMasterConfig.kt | 272 | 5 | ConvoyMasterConfig(
ConvoyMasterSuccessScreen.kt | 166 | 2 |  ── CAPTURE NEW MASTER ───────────
ConvoyMyOrganizersScreen.kt | 177 | 1 |  ===========================================================
ConvoyMyRidesScreen.kt | 149 | 2 |  ===========================================================
ConvoyNavArgs.kt | 8 | 0 |  Lightweight holder for nav arguments that cannot be passed 
ConvoyNode.kt | 66 | 0 | ConvoyStatus {
ConvoyNodeFilter.kt | 54 | 2 | ConvoyNodeFilter {
ConvoyPendingRidesScreen.kt | 159 | 1 |  ===========================================================
ConvoyPrivacyScreen.kt | 48 | 2 | 
ConvoyProfileBuilder.kt | 368 | 6 | ConvoyProfileBuilder {
ConvoyProfileScreen.kt | 291 | 4 |  ===========================================================
ConvoyQueuesPanel.kt | 59 | 2 |  V2.5 Scaffold (Pass 1) -- ScreenReference v5 section 8
ConvoyRadioConfigScreens.kt | 829 | 13 |  ── Radio connection state ─────────�
ConvoyRadioManager.kt | 175 | 5 | ConvoyRadioManager {
ConvoyRadioWriter.kt | 248 | 7 | WriteStep {
ConvoyReconnectWaitScreen.kt | 278 | 2 |  60s countdown + auto BT cycle + auto-proceed to verify
ConvoyRideDetailScreen.kt | 542 | 8 |  ===========================================================
ConvoyRidesScreen.kt | 174 | 2 |  ===========================================================
ConvoyRouteCreateScreen.kt | 27 | 1 |  V2.5 Scaffold (Pass 1) -- Decision Log v3 section 11
ConvoyScreen.kt | 2142 | 26 | RecordingState { IDLE, RECORDING, PAUSED, SLEEPING }
ConvoySearchByAreaScreen.kt | 110 | 1 |  ===========================================================
ConvoyServicesChooser.kt | 189 | 1 |  ===========================================================
ConvoySessionManager.kt | 117 | 25 | ConvoySessionManager {
ConvoySettingsPanel.kt | 266 | 5 |  ── Password gate screen ──────────�
ConvoySettingsRepository.kt | 118 | 8 | ConvoySettingsRepository @Inject constructor(
ConvoySettingsScreen.kt | 356 | 4 |  ── Map Sources ─────────────�
ConvoySettingsViewModel.kt | 93 | 8 | ConvoySettingsUiState(
ConvoySharePromptDialog.kt | 55 | 1 |  -----------------------------------------------------------
ConvoySignInScreen.kt | 396 | 6 |  ===========================================================
ConvoySimulation.kt | 123 | 4 | ConvoySimulation {
ConvoySubMenu.kt | 331 | 3 |  Track which top-level item is expanded (null = none)
ConvoySubscriptionScreen.kt | 400 | 4 |  ===========================================================
ConvoyTermsScreen.kt | 45 | 2 | 
ConvoyTileCalculator.kt | 169 | 9 | ConvoyTileCalculator {
ConvoyTileDownloader.kt | 225 | 9 |  ===========================================================
ConvoyTrackDialogs.kt | 128 | 4 | 
ConvoyTrackExportSheet.kt | 329 | 3 | FilterMode { ALL, SAVED, IN_PROGRESS }
ConvoyTrackImportScreen.kt | 630 | 6 |  -- State --------------------------------------------------
ConvoyTrackOps.kt | 642 | 20 | ConvoyTrackOps {
ConvoyTrackSaveOps.kt | 33 | 4 |  -----------------------------------------------------------
ConvoyTracksScreen.kt | 25 | 1 | 
ConvoyTrailSourceScreen.kt | 636 | 6 |  -----------------------------------------------------------
ConvoyTransferRideScreen.kt | 245 | 2 |  Header
ConvoyUser.kt | 182 | 11 | ConvoyUserType { ORGANIZER, RIDER }
ConvoyVerifyConfigScreen.kt | 355 | 2 | VerifyResult(
ConvoyViewModel.kt | 1005 | 55 | HudMode {
ConvoyWaypointCreateDialog.kt | 35 | 1 |  V2.5 Scaffold (Pass 1) -- ScreenReference v5 section 6
GroupTrackTheme.kt | 454 | 10 |  ── GroupTrack Brand Colors ─────────�
MapSourceManager.kt | 288 | 26 | MapSourceManager {
RouteManager.kt | 11 | 1 |  -----------------------------------------------------------
SpatialDbManager.kt | 956 | 51 | SpatialDbManager {
TrackManager.kt | 11 | 1 |  -----------------------------------------------------------
TrailImporter.kt | 555 | 21 | TrailImporter {
WaypointManager.kt | 11 | 1 |  -----------------------------------------------------------

================================================================================
SECTION 3: HTML MAP ASSETS
================================================================================

--- convoy_map.html ---
  39:    function setTileUrl(url, label) {
  58:    function clearOverlayLayers() {
  62:    function setOverlayLayers(jsonStr) {
  79:    function initDrawTool() {
  95:    function activateDrawMode() {
  100:    function cancelDrawMode() {
  105:    function clearAreaBoundary() { if (drawnItems) drawnItems.clearLayers(); }
  106:    function showDownloadArea(n, s, e, w) {
  115:    function showDownloadedAreas(bounds) {
  127:    function clearDownloadedAreas() {
  130:    function getMapBounds() {
  137:    function showSearchCenter(lat, lng) {
  148:    function clearSearchCenter() {
  153:    function setView(lat, lon, zoom) { map.setView([lat, lon], zoom); }
  154:      function panTo(lat, lon) { map.panTo([lat, lon]); }
  155:    function fitBounds(lats, lons) {
  162:      function findNearestMarker(screenX, screenY) {
  174:    function clearMarkers() {
  178:    function addMarker(id, lat, lon, color, label, isMine, offTrack) {
  192:    function initDeviceLocation() {
  230:    function loadTrackFile(name, coordsJson, color) {
  239:    function removeTrackFile(name) {
  243:    function clearAllTrackFiles() {
  248:    function fitTrackFile(name) {
  252:    function fitAllTrackFiles() {
  258:    function drawTrack(segments) {
  271:    function clearTrack() {
  316:function triggerViewportUpdate() {
  325:function trailColor(cartoCode) {
  337:function trailWeight(cartoCode) {
  342:function loadTrails(geojsonData) {
  373:function showTrails() {
  376:function hideTrails() {
  379:function toggleTrails() {
  383:function updateTrails(geojsonData) {
  391:function clearTrails() {
  399:function loadTracks(geojsonData) {
  412:function showTracks() {
  415:function hideTracks() {
  418:function toggleTracks() {
  422:function updateTracks(geojsonData) {
  430:function clearTracks() {
  438:function waypointColor(type) {
  453:function waypointSymbol(type) {
  469:function loadWaypoints(geojsonData) {
  498:function showWaypoints() {
  501:function hideWaypoints() {
  504:function toggleWaypoints() {
  508:function updateWaypoints(geojsonData) {
  516:function clearWaypoints() {
  524:function loadRoutes(geojsonData) {
  537:function showRoutes() {
  540:function hideRoutes() {
  543:function toggleRoutes() {
  547:function updateRoutes(geojsonData) {
  555:function clearRoutes() {

--- grouptrack_map.html ---
  14:    function showProximityPopup(lat, lon, html) {
  28:      function checkLayer(layer, typeName, color) {
  78:      function checkLayer(layer, typeName, color) {
  143:    function trailColor(cartoCode) {
  156:    function trailWeight(cartoCode) {
  162:    function setTileUrl(url, label) {
  185:    function clearOverlayLayers() {
  189:    function setOverlayLayers(jsonStr) {
  206:    function trailColor(code) {
  218:    function loadTrails(geojsonData) {
  259:    function showTrails() {
  266:    function hideTrails() {
  273:    function toggleTrails() {
  279:    function updateTrails(geojsonData) {
  290:    function loadTracks(geojsonData) {
  302:    function showTracks() {
  305:    function hideTracks() {
  308:    function updateTracks(geojsonData) {
  316:    function clearTracks() {
  321:    function clearTrails() {
  325:    function triggerViewportUpdate() {
  334:    function waypointColor(type) {
  350:    function waypointSymbol(type) {
  366:    function loadWaypoints(geojsonData) {
  393:    function showWaypoints() {
  396:    function hideWaypoints() {
  399:    function updateWaypoints(geojsonData) {
  407:    function clearWaypoints() {
  411:    function toggleWaypoints() {
  419:    function loadRoutes(geojsonData) {
  432:    function showRoutes() {
  435:    function hideRoutes() {
  438:    function updateRoutes(geojsonData) {
  446:    function clearRoutes() {
  450:    function toggleRoutes() {
  459:    function loadTrackFile(name, coordsJson, color) {
  468:    function removeTrackFile(name) {
  475:    function clearAllTrackFiles() {
  480:    function hideAllTrackFiles() {
  485:    function showAllTrackFiles() {
  490:    function toggleTrackFiles() {
  495:    function fitTrackFile(name) {
  501:    function fitAllTrackFiles() {
  509:    function drawTrack(segments) {
  521:    function clearTrack() {
  530:    function initDrawTool() {
  545:    function activateDrawMode() {
  549:    function cancelDrawMode() {
  553:    function clearAreaBoundary() { if (drawnItems) drawnItems.clearLayers(); }
  554:    function showDownloadArea(n, s, e, w) {
  565:    function showQueuedAreas(boundsJson) {
  580:    function clearQueuedAreas() {
  585:        function showDownloadedAreas(bounds) {
  597:    function clearDownloadedAreas() {
  600:    function clearSearchCenter() {
  605:    function getMapBounds() {
  610:    function showSearchCenter(lat, lng) {
  621:    function clearSearchCenter() {
  628:    function setView(lat, lon, zoom) { map.setView([lat, lon], zoom); }
  629:    function panTo(lat, lon) { map.panTo([lat, lon]); }
  630:    function fitBounds(lats, lons) {
  638:    function fitTrailBounds() {
  647:    function initDeviceLocation() {
  678:        function setMaxOfflineZoom(z) {
  716:      function checkLayer(layer, typeName, color) {

================================================================================
SECTION 4: PER-FILE FUNCTION DETAIL
================================================================================

========================================
FILE: AliasManager.kt
========================================

  [10] fun stub() { /* Pass 1 scaffold -- functions in Pass 2 */ }

========================================
FILE: ArtifactListPanel.kt
========================================

  [42] fun ArtifactListPanel(
    WORK FIELDS (local computed values):
      [58] val aMono = FontFamily.Monospace
      [59] val aGreen = Color(0xFF39FF14)
      [60] val aBlue = Color(0xFF4DA6FF)
      [61] val aOrange = Color(0xFFFF8C42)
      [62] val aDim = Color(0xFF7A8DA0)
      [63] val aBg = Color(0xEE131820)
      [64] val aItem = Color(0xFF1A2233)
      [67] var detailArtifactId by remember { mutableStateOf<String?>(null) }
      [68] var showRenameDialog by remember { mutableStateOf(false) }
      [69] var renameText by remember { mutableStateOf("") }
      [70] var showDeleteConfirm by remember { mutableStateOf(false) }
      [71] var showTypeChooser by remember { mutableStateOf(false) }
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → mutableStateOf
      → onDeselectAll
      → onDismiss
      → onFitToSelected
      → onSelectAll
      → padding

  [300] private fun ArtifactListItem(
    CALLS OUT:
      → CheckboxDefaults.colors
      → Color
      → DetailActionButton
      → RoundedCornerShape
      → Spacer
      → fillMaxWidth
      → onClick
      → onNameClick
      → onToggle
      → padding
      → type.isNotEmpty

  [348] private fun DetailActionButton(label: String, color: Color, onClick: () -> Unit) {
    CALLS OUT:
      → fillMaxWidth
      → onClick
      → padding

========================================
FILE: AuditLogger.kt
========================================

  [10] fun stub() { /* Pass 1 scaffold -- functions in Pass 2 */ }

========================================
FILE: ConvoyApiClient.kt
========================================

  [41] suspend fun registerUser(
    WORK FIELDS (local computed values):
      [48] val body = JSONObject().apply {
      [55] val request = Request.Builder()
      [60] val response = client.newCall(request).execute()
      [61] val responseBody = response.body?.string() ?: ""
      [65] val json = JSONObject(responseBody)
      [66] val userId = json.optString("user_id", "")
      [86] val body = JSONObject().apply {
      [92] val request = Request.Builder()
      [97] val response = client.newCall(request).execute()
      [113] val body = JSONObject().apply {
      [118] val request = Request.Builder()
    CALLS OUT:
      → Exception
      → JSONObject
      → Log.d
      → Log.e
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → execute
      → post
      → put
      → string
      → toRequestBody
      → url

  [80] suspend fun acceptTerms(
    WORK FIELDS (local computed values):
      [86] val body = JSONObject().apply {
      [92] val request = Request.Builder()
      [97] val response = client.newCall(request).execute()
      [113] val body = JSONObject().apply {
      [118] val request = Request.Builder()
      [123] val response = client.newCall(request).execute()
      [137] val request = Request.Builder()
      [142] val response = client.newCall(request).execute()
      [143] val responseBody = response.body?.string() ?: ""
      [147] val json = JSONObject(responseBody)
      [148] val version = json.optString("version", "1.0")
      [149] val text = json.optString("terms_text", "")
    CALLS OUT:
      → Exception
      → JSONObject
      → Log.d
      → Log.e
      → Pair
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → execute
      → get
      → getTerms
      → post
      → put

  [108] suspend fun becomeOrganizer(
    WORK FIELDS (local computed values):
      [113] val body = JSONObject().apply {
      [118] val request = Request.Builder()
      [123] val response = client.newCall(request).execute()
      [137] val request = Request.Builder()
      [142] val response = client.newCall(request).execute()
      [143] val responseBody = response.body?.string() ?: ""
      [147] val json = JSONObject(responseBody)
      [148] val version = json.optString("version", "1.0")
      [149] val text = json.optString("terms_text", "")
      [177] val body = JSONObject().apply {
    CALLS OUT:
      → Exception
      → JSONObject
      → Log.d
      → Log.e
      → Pair
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → execute
      → get
      → getTerms
      → post
      → put

  [135] suspend fun getTerms(type: String): Result<Pair<String, String>> = withContext(Dispatchers.IO) {
    WORK FIELDS (local computed values):
      [137] val request = Request.Builder()
      [142] val response = client.newCall(request).execute()
      [143] val responseBody = response.body?.string() ?: ""
      [147] val json = JSONObject(responseBody)
      [148] val version = json.optString("version", "1.0")
      [149] val text = json.optString("terms_text", "")
      [177] val body = JSONObject().apply {
      [192] val request = Request.Builder()
      [197] val response = client.newCall(request).execute()
      [198] val responseBody = response.body?.string() ?: ""
      [202] val json = JSONObject(responseBody)
      [203] val rideId = json.optString("ride_id", "")
    CALLS OUT:
      → Exception
      → JSONObject
      → Log.d
      → Log.e
      → Pair
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → execute
      → get
      → post
      → put
      → rideId.isNotEmpty

  [162] suspend fun createRide(
    WORK FIELDS (local computed values):
      [177] val body = JSONObject().apply {
      [192] val request = Request.Builder()
      [197] val response = client.newCall(request).execute()
      [198] val responseBody = response.body?.string() ?: ""
      [202] val json = JSONObject(responseBody)
      [203] val rideId = json.optString("ride_id", "")
      [222] val body = JSONObject().apply {
      [226] val request = Request.Builder()
      [231] val response = client.newCall(request).execute()
      [232] val responseBody = response.body?.string() ?: ""
      [236] val json = JSONObject(responseBody)
      [237] val inviteUrl = json.optString("invite_url", "")
    CALLS OUT:
      → Exception
      → JSONObject
      → Log.d
      → Log.e
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → execute
      → inviteUrl.isNotEmpty
      → post
      → put
      → rideId.isNotEmpty
      → string

  [217] suspend fun createInvite(
    WORK FIELDS (local computed values):
      [222] val body = JSONObject().apply {
      [226] val request = Request.Builder()
      [231] val response = client.newCall(request).execute()
      [232] val responseBody = response.body?.string() ?: ""
      [236] val json = JSONObject(responseBody)
      [237] val inviteUrl = json.optString("invite_url", "")
      [254] val request = Request.Builder()
      [259] val response = client.newCall(request).execute()
      [260] val responseBody = response.body?.string() ?: ""
      [280] val body = JSONObject().apply {
      [287] val request = Request.Builder()
      [292] val response = client.newCall(request).execute()
    CALLS OUT:
      → Exception
      → JSONObject
      → Log.d
      → Log.e
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → downloadRide
      → execute
      → get
      → inviteUrl.isNotEmpty
      → post
      → put

  [252] suspend fun downloadRide(token: String): Result<String> = withContext(Dispatchers.IO) {
    WORK FIELDS (local computed values):
      [254] val request = Request.Builder()
      [259] val response = client.newCall(request).execute()
      [260] val responseBody = response.body?.string() ?: ""
      [280] val body = JSONObject().apply {
      [287] val request = Request.Builder()
      [292] val response = client.newCall(request).execute()
      [312] val body = JSONObject().apply {
      [318] val kmlObj = JSONObject().apply {
      [332] val request = Request.Builder()
    CALLS OUT:
      → Exception
      → JSONObject
      → Log.d
      → Log.e
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → execute
      → get
      → notes.isNotEmpty
      → post
      → put
      → responseBody.take

  [273] suspend fun enrollRider(
    WORK FIELDS (local computed values):
      [280] val body = JSONObject().apply {
      [287] val request = Request.Builder()
      [292] val response = client.newCall(request).execute()
      [312] val body = JSONObject().apply {
      [318] val kmlObj = JSONObject().apply {
      [332] val request = Request.Builder()
      [337] val response = client.newCall(request).execute()
      [352] val body = JSONObject().apply {
    CALLS OUT:
      → JSONObject
      → Log.d
      → Log.e
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → execute
      → notes.isNotEmpty
      → post
      → put
      → toRequestBody
      → url
      → withContext

  [304] suspend fun submitSurvey(
    WORK FIELDS (local computed values):
      [312] val body = JSONObject().apply {
      [318] val kmlObj = JSONObject().apply {
      [332] val request = Request.Builder()
      [337] val response = client.newCall(request).execute()
      [352] val body = JSONObject().apply {
      [356] val request = Request.Builder()
      [361] val response = client.newCall(request).execute()
      [376] val body = JSONObject().apply {
      [380] val request = Request.Builder()
    CALLS OUT:
      → JSONObject
      → Log.d
      → Log.e
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → delete
      → execute
      → notes.isNotEmpty
      → post
      → put
      → toRequestBody
      → url

  [347] suspend fun followOrganizer(
    WORK FIELDS (local computed values):
      [352] val body = JSONObject().apply {
      [356] val request = Request.Builder()
      [361] val response = client.newCall(request).execute()
      [376] val body = JSONObject().apply {
      [380] val request = Request.Builder()
      [385] val response = client.newCall(request).execute()
      [402] val body = JSONObject().apply {
      [406] val request = Request.Builder()
      [411] val response = client.newCall(request).execute()
      [425] val routeName: String,
      [426] val description: String,
      [427] val zipCode: String,
    CALLS OUT:
      → Exception
      → JSONObject
      → Log.d
      → Log.e
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → delete
      → execute
      → post
      → put
      → toRequestBody
      → url

  [371] suspend fun unfollowOrganizer(
    WORK FIELDS (local computed values):
      [376] val body = JSONObject().apply {
      [380] val request = Request.Builder()
      [385] val response = client.newCall(request).execute()
      [402] val body = JSONObject().apply {
      [406] val request = Request.Builder()
      [411] val response = client.newCall(request).execute()
      [425] val routeName: String,
      [426] val description: String,
      [427] val zipCode: String,
      [428] val state: String,
      [429] val fileName: String,
      [430] val fileSizeKb: Int,
    CALLS OUT:
      → Exception
      → JSONObject
      → Log.d
      → Log.e
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → delete
      → execute
      → post
      → put
      → toRequestBody
      → url

  [397] suspend fun broadcastRide(
    WORK FIELDS (local computed values):
      [402] val body = JSONObject().apply {
      [406] val request = Request.Builder()
      [411] val response = client.newCall(request).execute()
      [425] val routeName: String,
      [426] val description: String,
      [427] val zipCode: String,
      [428] val state: String,
      [429] val fileName: String,
      [430] val fileSizeKb: Int,
      [431] val fileHash: String,        // MD5 computed on device before upload
      [432] val fileDataBase64: String   // base64 encoded file content
    CALLS OUT:
      → Exception
      → JSONObject
      → Log.d
      → Log.e
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → client.newCall
      → execute
      → post
      → put
      → toRequestBody
      → url
      → withContext

========================================
FILE: ConvoyApplyList.kt
========================================

  [97] fun toJson(): JSONObject = JSONObject().apply {
    WORK FIELDS (local computed values):
      [110] val file = java.io.File(context.filesDir, FILE_NAME)
      [113] val obj = JSONObject(file.readText())
      [115] val arr   = obj.optJSONArray(key) ?: return values.toSet()
      [116] val names = (0 until arr.length()).map { arr.getString(it) }.toSet()
    CALLS OUT:
      → JSONObject
      → arr.length
      → file.exists
      → io.File
      → load
      → obj.optJSONArray
      → put
      → save
      → writeText

  [109] fun load(context: Context): ConvoyApplyList {
    WORK FIELDS (local computed values):
      [110] val file = java.io.File(context.filesDir, FILE_NAME)
      [113] val obj = JSONObject(file.readText())
      [115] val arr   = obj.optJSONArray(key) ?: return values.toSet()
      [116] val names = (0 until arr.length()).map { arr.getString(it) }.toSet()
    CALLS OUT:
      → JSONObject
      → arr.length
      → file.exists
      → io.File
      → obj.optJSONArray
      → save
      → writeText

  [114] fun <T : Enum<T>> parseSet(key: String, values: Array<T>): Set<T> {
    WORK FIELDS (local computed values):
      [115] val arr   = obj.optJSONArray(key) ?: return values.toSet()
      [116] val names = (0 until arr.length()).map { arr.getString(it) }.toSet()
    CALLS OUT:
      → arr.length
      → io.File
      → obj.optJSONArray
      → save
      → writeText

  [132] fun save(context: Context, list: ConvoyApplyList) {
    CALLS OUT:
      → io.File
      → writeText

========================================
FILE: ConvoyApplyListMaintenanceScreen.kt
========================================

  [37] fun ConvoyApplyListMaintenanceScreen(
    WORK FIELDS (local computed values):
      [40] val context = LocalContext.current
      [41] var applyList by remember { mutableStateOf(ConvoyApplyList.load(context)) }
      [42] var loraOpen     by remember { mutableStateOf(true) }
      [43] var channelOpen  by remember { mutableStateOf(true) }
      [44] var deviceOpen   by remember { mutableStateOf(false) }
      [45] var positionOpen by remember { mutableStateOf(false) }
      [46] var displayOpen  by remember { mutableStateOf(false) }
      [47] var moduleOpen   by remember { mutableStateOf(false) }
      [48] var statusMsg    by remember { mutableStateOf("") }
      [97] val checked = field in applyList.loraFields
      [115] val locked = field == ChannelField.CHANNEL_NAME || field == ChannelField.ENCRYPTION_KEY
      [116] val checked = locked || field in applyList.channelFields
    CALLS OUT:
      → AnimatedVisibility
      → Box
      → Color
      → MaintAccordionHeader
      → MaintFieldRow
      → Spacer
      → Text
      → background
      → fillMaxSize
      → mutableStateOf
      → onBack
      → padding
      → saveAndSync
      → shrinkVertically
      → statusMsg.isNotEmpty

  [50] fun saveAndSync() {
    WORK FIELDS (local computed values):
      [97] val checked = field in applyList.loraFields
      [115] val locked = field == ChannelField.CHANNEL_NAME || field == ChannelField.ENCRYPTION_KEY
      [116] val checked = locked || field in applyList.channelFields
    CALLS OUT:
      → AnimatedVisibility
      → Box
      → Color
      → MaintAccordionHeader
      → MaintFieldRow
      → Spacer
      → Text
      → background
      → fillMaxSize
      → onBack
      → padding
      → saveAndSync
      → shrinkVertically
      → statusMsg.isNotEmpty
      → verticalScroll

  [233] private fun MaintAccordionHeader(title: String, expanded: Boolean, onClick: () -> Unit) {
    CALLS OUT:
      → CheckboxDefaults.colors
      → Color
      → MaintFieldRow
      → RoundedCornerShape
      → Spacer
      → Text
      → clickable
      → copy
      → fillMaxWidth
      → onClick
      → onToggle
      → padding

  [254] private fun MaintFieldRow(label: String, checked: Boolean, locked: Boolean = false, onToggle: () -> Unit) {
    CALLS OUT:
      → CheckboxDefaults.colors
      → Color
      → Spacer
      → Text
      → clickable
      → copy
      → fillMaxWidth
      → onToggle
      → padding

========================================
FILE: ConvoyApplyListScreen.kt
========================================

  [27] fun ConvoyApplyListScreen(
    WORK FIELDS (local computed values):
      [31] val context = LocalContext.current
      [32] var applyList    by remember { mutableStateOf(ConvoyApplyList.load(context)) }
      [33] var applyMode    by remember { mutableStateOf("MASTER") } // "MASTER" or "RIDE"
      [34] var selectedRide by remember { mutableStateOf<ConvoyEventConfig?>(null) }
      [35] val rides        = remember { ConvoyEventStore.loadAll(context).sortedBy { it.eventDate } }
      [36] var longName     by remember { mutableStateOf("") }
      [37] var showConfirm  by remember { mutableStateOf(false) }
      [39] var loraOpen     by remember { mutableStateOf(true) }
      [40] var channelOpen  by remember { mutableStateOf(true) }
      [41] var deviceOpen   by remember { mutableStateOf(false) }
      [42] var positionOpen by remember { mutableStateOf(false) }
      [43] var displayOpen  by remember { mutableStateOf(false) }
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → ConvoyEventStore.loadAll
      → ConvoyMasterConfig.load
      → RoundedCornerShape
      → Row
      → Spacer
      → background
      → fillMaxSize
      → height
      → listOf
      → mutableStateOf
      → padding
      → verticalScroll

  [454] private fun ConfirmHeader() {
    WORK FIELDS (local computed values):
      [469] val ruleColor = when (rule) {
    CALLS OUT:
      → AccordionHeader
      → Color
      → ConfirmRow
      → RoundedCornerShape
      → Row
      → Text
      → getLoraValue
      → onClick
      → padding

  [468] private fun ConfirmRow(field: String, current: String, newVal: String, rule: String, changing: Boolean) {
    WORK FIELDS (local computed values):
      [469] val ruleColor = when (rule) {
    CALLS OUT:
      → AccordionHeader
      → Color
      → RoundedCornerShape
      → Row
      → Text
      → getLoraValue
      → onClick
      → padding

  [499] private fun getLoraValue(master: ConvoyMasterConfig, field: LoraField): String = when (field) {
    CALLS OUT:
      → AccordionHeader
      → CheckboxDefaults.colors
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → clickable
      → onClick
      → onToggle
      → padding

  [514] private fun AccordionHeader(title: String, expanded: Boolean, onClick: () -> Unit) {
    CALLS OUT:
      → CheckboxDefaults.colors
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → clickable
      → onClick
      → onToggle
      → padding

  [543] private fun FieldRow(
    CALLS OUT:
      → CheckboxDefaults.colors
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → clickable
      → onToggle
      → padding

========================================
FILE: ConvoyApplyRadioScreen.kt
========================================

  [31] fun ConvoyApplyRadioScreen(
    WORK FIELDS (local computed values):
      [38] val context      = LocalContext.current
      [39] val scope        = rememberCoroutineScope()
      [40] val applyList    = remember { ConvoyApplyList.load(context) }
      [41] val masterConfig = remember { ConvoyMasterConfig.load(context) }
      [42] val rides        = remember { ConvoyEventStore.loadAll(context).sortedBy { it.eventDate } }
      [44] val myNodeInfo  by uiViewModel.myNodeInfo.collectAsStateWithLifecycle()
      [45] val localConfig by channelViewModel.localConfig.collectAsStateWithLifecycle()
      [46] val channels    by channelViewModel.channels.collectAsStateWithLifecycle()
      [47] val isConnected = myNodeInfo != null
      [48] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [49] val writerState     by ConvoyRadioWriter.state.collectAsStateWithLifecycle()
      [51] var applyMode    by remember { mutableStateOf("MASTER") }
    CALLS OUT:
      → Box
      → Color
      → ConvoyEventStore.loadAll
      → ConvoyMasterConfig.load
      → RoundedCornerShape
      → Row
      → Spacer
      → Surface
      → Text
      → background
      → height
      → hiltViewModel
      → isNotBlank
      → longName.isNotBlank
      → mutableStateOf

  [417] fun <T> rule(field: Any, masterVal: T, radioVal: T): T =
    WORK FIELDS (local computed values):
      [423] val wconfig = WorkingConfig(
      [452] gpsUpdateSecs            = if (PositionField.GPS_UPDATE_INTERVAL in al.positionFields) m?.gpsUpdateSecs ?: 1 else pos?.gps_update_interval ?: 1,
      [466] telemetryDeviceInterval  = if (ModuleField.TELEMETRY_DEVICE_INTERVAL in al.moduleFields) m?.telemetryDeviceInterval ?: 0 else 0,
      [467] telemetryEnvInterval     = if (ModuleField.TELEMETRY_ENV_INTERVAL in al.moduleFields) m?.telemetryEnvInterval ?: 0 else 0,
      [490] val archiveResult = ConvoyMasterApply.archiveCurrentRadio(
    CALLS OUT:
      → ConvoyMasterApply.archiveCurrentRadio
      → Log.e
      → Log.i
      → archiveResult.exceptionOrNull
      → archiveResult.getOrThrow

  [543] private fun ConfirmHeader() {
    WORK FIELDS (local computed values):
      [558] val ruleColor = when (rule) {
    CALLS OUT:
      → ApplyAccordionHeader
      → Color
      → ConfirmRow
      → RoundedCornerShape
      → Row
      → Surface
      → Text
      → onClick
      → padding

  [557] private fun ConfirmRow(field: String, current: String, newVal: String, rule: String, changing: Boolean) {
    WORK FIELDS (local computed values):
      [558] val ruleColor = when (rule) {
    CALLS OUT:
      → ApplyAccordionHeader
      → Color
      → RoundedCornerShape
      → Row
      → Surface
      → Text
      → onClick
      → padding

  [586] private fun ApplyAccordionHeader(title: String, expanded: Boolean, onClick: () -> Unit) {
    CALLS OUT:
      → Color
      → RoundedCornerShape
      → Row
      → Surface
      → Text
      → onClick
      → padding

========================================
FILE: ConvoyArchiveRestoreScreen.kt
========================================

  [50] fun ConvoyArchiveRestoreScreen(
    WORK FIELDS (local computed values):
      [56] val context         = LocalContext.current
      [57] val scope           = rememberCoroutineScope()
      [58] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [59] val myNodeInfo      by convoyViewModel.myNodeInfo.collectAsStateWithLifecycle()
      [60] val isConnected     = connectionState.toString().contains("Connected", ignoreCase = true)
      [62] val nodeId   = myNodeInfo?.myNodeNum?.let { "!%08x".format(it) } ?: ""
      [63] val archiveDir = remember(nodeId) {
      [70] val archives = remember(archiveDir) {
      [76] var selectedFile    by remember { mutableStateOf<File?>(null) }
      [77] var phase           by remember { mutableStateOf("SELECT") } // SELECT, CONFIRM, RESTORING, WAITING, DONE, FAILED
      [78] var countdown       by remember { mutableStateOf(60) }
      [79] var statusMsg       by remember { mutableStateOf("") }
    CALLS OUT:
      → ADAPTER.decode
      → File
      → Log.i
      → addLog
      → contains
      → convoyViewModel.installProfileToRadio
      → date.substring
      → file.length
      → file.readBytes
      → hiltViewModel
      → mutableStateOf
      → nameWithoutExtension.split
      → nodeId.isNotEmpty
      → remember
      → rememberCoroutineScope

  [82] fun addLog(msg: String) {
    WORK FIELDS (local computed values):
      [89] val parts = file.nameWithoutExtension.split("_")
      [91] val date = parts[1] // 20260319
      [92] val time = parts[2] // 125157
      [101] val bytes = file.length()
      [112] val nodeNum = myNodeInfo?.myNodeNum
      [120] val profileBytes = file.readBytes()
      [121] val profile = DeviceProfile.ADAPTER.decode(profileBytes)
    CALLS OUT:
      → ADAPTER.decode
      → BackHandler
      → Box
      → Column
      → Log.i
      → Row
      → Spacer
      → Text
      → addLog
      → background
      → convoyViewModel.installProfileToRadio
      → date.substring
      → delay
      → file.length
      → file.readBytes

  [87] fun formatFileName(file: File): String {
    WORK FIELDS (local computed values):
      [89] val parts = file.nameWithoutExtension.split("_")
      [91] val date = parts[1] // 20260319
      [92] val time = parts[2] // 125157
      [101] val bytes = file.length()
      [112] val nodeNum = myNodeInfo?.myNodeNum
      [120] val profileBytes = file.readBytes()
      [121] val profile = DeviceProfile.ADAPTER.decode(profileBytes)
    CALLS OUT:
      → ADAPTER.decode
      → BackHandler
      → Box
      → Color
      → Column
      → Row
      → Spacer
      → Text
      → addLog
      → background
      → convoyViewModel.installProfileToRadio
      → date.substring
      → delay
      → file.length
      → file.readBytes

  [100] fun formatFileSize(file: File): String {
    WORK FIELDS (local computed values):
      [101] val bytes = file.length()
      [112] val nodeNum = myNodeInfo?.myNodeNum
      [120] val profileBytes = file.readBytes()
      [121] val profile = DeviceProfile.ADAPTER.decode(profileBytes)
    CALLS OUT:
      → ADAPTER.decode
      → BackHandler
      → Box
      → Color
      → Column
      → RoundedCornerShape
      → Row
      → Spacer
      → Surface
      → Text
      → addLog
      → background
      → convoyViewModel.installProfileToRadio
      → delay
      → file.length

  [105] fun restoreArchive(file: File) {
    WORK FIELDS (local computed values):
      [112] val nodeNum = myNodeInfo?.myNodeNum
      [120] val profileBytes = file.readBytes()
      [121] val profile = DeviceProfile.ADAPTER.decode(profileBytes)
    CALLS OUT:
      → ADAPTER.decode
      → BackHandler
      → Box
      → Color
      → Column
      → RoundedCornerShape
      → Row
      → Spacer
      → Surface
      → Text
      → addLog
      → archives.isEmpty
      → background
      → convoyViewModel.installProfileToRadio
      → delay

========================================
FILE: ConvoyArtifactDetailPanel.kt
========================================

  [19] fun ConvoyArtifactDetailPanel(
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → RoundedCornerShape
      → Surface
      → Text

========================================
FILE: ConvoyArtifactOps.kt
========================================

  [19] fun fit(artifactType: String, artifactId: String) {
    CALLS OUT:
      → Log.d
      → addAlias
      → changeType
      → delete
      → download
      → editPoints
      → rename
      → setTrailhead
      → toRoute
      → toTrack
      → upload

  [24] fun rename(artifactType: String, artifactId: String, newName: String) {
    CALLS OUT:
      → Log.d
      → addAlias
      → changeType
      → delete
      → download
      → editPoints
      → setTrailhead
      → toRoute
      → toTrack
      → upload

  [29] fun delete(context: Context, artifactType: String, artifactId: String): Boolean {
    CALLS OUT:
      → Log.d
      → addAlias
      → changeType
      → download
      → editPoints
      → setTrailhead
      → toRoute
      → toTrack
      → upload

  [35] fun toRoute(trackId: String) {
    CALLS OUT:
      → Log.d
      → addAlias
      → changeType
      → download
      → editPoints
      → setTrailhead
      → toTrack
      → upload

  [40] fun toTrack(routeId: String) {
    CALLS OUT:
      → Log.d
      → addAlias
      → changeType
      → download
      → editPoints
      → setTrailhead
      → upload

  [45] fun upload(artifactType: String, artifactId: String) {
    CALLS OUT:
      → Log.d
      → addAlias
      → changeType
      → download
      → editPoints
      → setTrailhead

  [50] fun download(artifactType: String, artifactId: String) {
    CALLS OUT:
      → Log.d
      → addAlias
      → changeType
      → editPoints
      → setTrailhead

  [55] fun changeType(waypointId: String, newTypeId: String) {
    CALLS OUT:
      → Log.d
      → addAlias
      → editPoints
      → setTrailhead

  [60] fun editPoints(routeId: String) {
    CALLS OUT:
      → Log.d
      → addAlias
      → setTrailhead

  [65] fun addAlias(artifactType: String, artifactId: String, alias: String) {
    CALLS OUT:
      → Log.d
      → setTrailhead

  [70] fun setTrailhead(routeId: String, waypointId: String) {
    CALLS OUT:
      → Log.d

========================================
FILE: ConvoyArtifactsPanel.kt
========================================

  [44] fun ConvoyArtifactsPanel(
    WORK FIELDS (local computed values):
      [54] var expanded by remember { mutableStateOf(false) }
      [55] var offsetX by remember { mutableFloatStateOf(0f) }
      [56] var offsetY by remember { mutableFloatStateOf(0f) }
    CALLS OUT:
      → Box
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → aTxtD.copy
      → change.consume
      → expandVertically
      → fillMaxWidth
      → height
      → mutableStateOf
      → onCreateRoute
      → padding
      → pointerInput
      → shrinkVertically

  [174] private fun ArtifactRow(
    WORK FIELDS (local computed values):
      [181] val aMono = FontFamily.Monospace
      [182] val state = displayStates[typeName] ?: 0
    CALLS OUT:
      → Color
      → RoundedCornerShape
      → Spacer
      → Surface
      → Text
      → onEditDisplay
      → padding

========================================
FILE: ConvoyBroadcastScreen.kt
========================================

  [25] fun ConvoyBroadcastScreen(
    WORK FIELDS (local computed values):
      [30] val context = LocalContext.current
      [31] var broadcastSent by remember { mutableStateOf(false) }
    CALLS OUT:
      → Arrangement.spacedBy
      → BcastNavBtn
      → Box
      → Color
      → Column
      → GroupTrackHeader
      → Row
      → Text
      → Toast.makeText
      → background
      → clip
      → copy
      → mutableStateOf
      → onBack
      → onClick

  [90] private fun BcastNavBtn(icon: String, label: String, onClick: () -> Unit) {
    CALLS OUT:
      → Text
      → onClick
      → padding

========================================
FILE: ConvoyCompletedRideDetailScreen.kt
========================================

  [32] fun ConvoyCompletedRideDetailScreen(
    WORK FIELDS (local computed values):
      [37] val context     = LocalContext.current
      [38] val scrollState = rememberScrollState()
      [41] val rideData = remember(rideId) {
      [50] val ride = rideData[rideId] ?: rideData["ride-001"]!!
      [52] var surveyExpanded by remember { mutableStateOf(false) }
      [53] var surveyRating   by remember { mutableStateOf(0) }
      [54] var surveyComment  by remember { mutableStateOf("") }
      [55] var surveySaved    by remember { mutableStateOf(false) }
      [58] val intent = Intent(Intent.ACTION_SENDTO, Uri.parse("mailto:$address"))
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → DetailField
      → Row
      → Text
      → background
      → clip
      → context.startActivity
      → launchEmail
      → mapOf
      → material3.HorizontalDivider
      → mutableStateOf
      → onBack

  [57] fun launchEmail(address: String) {
    WORK FIELDS (local computed values):
      [58] val intent = Intent(Intent.ACTION_SENDTO, Uri.parse("mailto:$address"))
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → DetailField
      → Row
      → Text
      → background
      → clip
      → context.startActivity
      → launchEmail
      → material3.HorizontalDivider
      → onBack
      → padding
      → verticalScroll

  [237] private fun DetailField(label: String, value: String) {
    CALLS OUT:
      → Column
      → Text

========================================
FILE: ConvoyCompletedRidesScreen.kt
========================================

  [47] fun ConvoyCompletedRidesScreen(
    WORK FIELDS (local computed values):
      [52] val context     = LocalContext.current
      [53] val scrollState = rememberScrollState()
      [54] var activeTab   by remember { mutableStateOf(initialTab) }
      [55] val tabs = listOf("HISTORY", "BY NAME", "BY ORGANIZER", "BY DATE")
      [57] val userRadius = remember { ConvoySessionManager.getSearchRadius(context).toFloat() }
      [60] val allRides = remember {
      [70] val organizers = remember { allRides.map { it.organizer to it.organizerId }.distinct() }
      [73] var nameQuery       by remember { mutableStateOf("") }
      [74] var nameSearched    by remember { mutableStateOf(false) }
      [75] var nameRadius      by remember { mutableStateOf(userRadius) }
      [76] var nameAllRides    by remember { mutableStateOf(false) }
      [78] var orgRadius       by remember { mutableStateOf(userRadius) }
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → CompletedRide
      → ConvoySessionManager.getSearchRadius
      → Row
      → Text
      → background
      → distinct
      → listOf
      → material3.HorizontalDivider
      → mutableStateOf
      → onBack
      → padding

  [291] private fun FilterBar(
    WORK FIELDS (local computed values):
      [338] val location = buildString {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → RoundedCornerShape
      → Row
      → SearchButton
      → SliderDefaults.colors
      → Text
      → append
      → background
      → city.isNotEmpty
      → clip
      → location.isEmpty
      → location.isNotEmpty

  [367] private fun SearchButton(enabled: Boolean, onSearch: () -> Unit) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → CompletedRideCard
      → EmptyState
      → Row
      → SearchPrompt
      → Text
      → background
      → clip
      → onClick
      → onSearch
      → padding
      → then

  [380] private fun SearchPrompt(message: String) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Column
      → CompletedRideCard
      → EmptyState
      → Row
      → Text
      → background
      → clip
      → onClick
      → padding

  [390] private fun EmptyState(message: String) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Column
      → CompletedRideCard
      → Row
      → Text
      → background
      → clip
      → onClick
      → padding

  [400] fun CompletedRideCard(ride: CompletedRide, onClick: () -> Unit) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Column
      → Row
      → Text
      → background
      → clip
      → onClick
      → padding

========================================
FILE: ConvoyConfig.kt
========================================

  [43] fun migrateTiles(context: android.content.Context) {
    WORK FIELDS (local computed values):
      [44] val prefs = context.getSharedPreferences("grouptrack", android.content.Context.MODE_PRIVATE)
      [47] val oldDir = java.io.File(context.getExternalFilesDir(null), "tiles")
      [48] val newDir = TILE_DIR
      [52] val sources = oldDir.listFiles() ?: emptyArray()
      [53] var moved = 0
      [56] val dest = java.io.File(newDir, sourceDir.name)
      [62] val ok = sourceDir.renameTo(dest)
      [86] var SHOW_DOWNLOADED_ON_OPEN = false
      [92] var SIGNAL_DROP_MINUTES = 2f
      [93] var LOST_MINUTES = 10f
      [94] var OFF_TRACK_MILES = 0.028f  // 150 feet
      [99] var TRACK_EXPORT_FORMAT = "GPX"  // GPX = default (Garmin/Strava/AllTrails), KML = Google Earth/route donation
    CALLS OUT:
      → Log.d
      → Log.e
      → apply
      → context.getExternalFilesDir
      → context.getSharedPreferences
      → dest.exists
      → emptyArray
      → io.File
      → isEmpty
      → oldDir.delete
      → oldDir.exists
      → oldDir.listFiles
      → prefs.edit
      → source
      → sourceDir.renameTo

========================================
FILE: ConvoyCreateEventScreen.kt
========================================

  [58] fun ConvoyCreateEventScreen(
    WORK FIELDS (local computed values):
      [62] val context = LocalContext.current
      [63] val scope   = rememberCoroutineScope()
      [65] var eventName    by remember { mutableStateOf("") }
      [66] var eventDate    by remember { mutableStateOf("") }
      [67] var description  by remember { mutableStateOf("") }
      [68] val channelName  = remember { "CONVOY-" + (1..4).map { "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789".random() }.joinToString("") }
      [69] var savedPsk     by remember { mutableStateOf("") }
      [70] var statusMsg    by remember { mutableStateOf("") }
      [71] var statusOk     by remember { mutableStateOf(true) }
      [72] var isProcessing by remember { mutableStateOf(false) }
      [73] var step         by remember { mutableStateOf(0) }   // 0=idle 1=read 2=backup 3=write 4=done
      [75] val organizer    = remember { ConvoyUserStore.getActiveUser(context) }
    CALLS OUT:
      → Box
      → CellLabel
      → Color
      → Column
      → ConvoyMasterConfig.load
      → ConvoyUserStore.getActiveUser
      → RoundedCornerShape
      → Spacer
      → background
      → eventDate.isNotBlank
      → eventName.isNotBlank
      → fillMaxSize
      → height
      → imePadding
      → mutableStateOf

  [361] private fun CellLabel(text: String) {
    WORK FIELDS (local computed values):
      [375] val color = when {
      [380] val prefix = when {
    CALLS OUT:
      → Color
      → RadioStep
      → RoundedCornerShape
      → StatusBanner

  [374] private fun RadioStep(label: String, active: Boolean, done: Boolean) {
    WORK FIELDS (local computed values):
      [375] val color = when {
      [380] val prefix = when {
    CALLS OUT:
      → Color
      → RoundedCornerShape
      → StatusBanner

  [395] private fun StatusBanner(msg: String, ok: Boolean) {
    CALLS OUT:
      → Color
      → RoundedCornerShape

========================================
FILE: ConvoyCreateRideScreen.kt
========================================

  [37] fun ConvoyCreateRideScreen(
    WORK FIELDS (local computed values):
      [47] val context     = LocalContext.current
      [48] val scope       = rememberCoroutineScope()
      [49] val scrollState = rememberScrollState()
      [52] var rideName      by remember { mutableStateOf("") }
      [53] var rideDate      by remember { mutableStateOf("") }
      [54] var arriveTime    by remember { mutableStateOf("") }
      [55] var rolloutTime   by remember { mutableStateOf("") }
      [56] var city          by remember { mutableStateOf("") }
      [57] var state         by remember { mutableStateOf("") }
      [58] var trailhead     by remember { mutableStateOf("") }
      [59] var zipCode       by remember { mutableStateOf(ConvoySessionManager.getZipCode(context)) }
      [60] var description   by remember { mutableStateOf("") }
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → ConvoyMasterConfig.load
      → ConvoyUserStore.getActiveUser
      → CreateSectionLabel
      → Text
      → arriveTime.isNotBlank
      → background
      → city.isNotBlank
      → clip
      → material3.HorizontalDivider
      → mutableStateOf
      → onBack

  [530] private fun RadioStep(label: String, active: Boolean, done: Boolean) {
    WORK FIELDS (local computed values):
      [531] val color = when {
      [536] val prefix = when {
    CALLS OUT:
      → Color
      → CreateSectionLabel
      → OutlinedTextFieldDefaults.colors
      → Text

  [547] private fun CreateSectionLabel(label: String) {
    CALLS OUT:
      → Color
      → OutlinedTextFieldDefaults.colors
      → Text

  [554] private fun CreateField(
    CALLS OUT:
      → Color
      → OutlinedTextFieldDefaults.colors
      → Text

========================================
FILE: ConvoyDashboardScreen.kt
========================================

  [26] fun ConvoyDashboardScreen(
    WORK FIELDS (local computed values):
      [42] val context = LocalContext.current
      [43] var seedKey by remember { mutableStateOf(0) }
      [45] val firstName     = remember(seedKey) { ConvoySessionManager.getFirstName(context) }
      [46] val isOrganizer   = remember(seedKey) { ConvoySessionManager.isOrganizer(context) }
      [47] var activeRole    by remember { mutableStateOf(if (isOrganizer) "ORGANIZER" else "RIDER") }
      [48] val currentUserId = remember { ConvoySessionManager.getUserId(context) ?: "" }
      [49] var zipCode       by remember { mutableStateOf(ConvoySessionManager.getZipCode(context)) }
      [50] var radius        by remember { mutableStateOf(ConvoySessionManager.getSearchRadius(context).toString()) }
      [52] val notifCount  = remember(seedKey) { ConvoyDevSeeder.getNotifCount(context) }
      [53] val rideCount   = remember(seedKey) { ConvoyDevSeeder.getRideCount(context) }
      [54] val inviteCount = remember(seedKey) { ConvoyDevSeeder.getInviteCount(context) }
      [55] val pubCount    = remember(seedKey) { ConvoyDevSeeder.getPublicRideCount(context) }
    CALLS OUT:
      → Column
      → ConvoyDevSeeder.getInviteCount
      → ConvoyDevSeeder.getPublicRideCount
      → ConvoyDevSeeder.getRideCount
      → ConvoyEnrollmentQueue.followOrganizer
      → ConvoyEnrollmentQueue.unfollowOrganizer
      → ConvoySessionManager.getFirstName
      → ConvoySessionManager.getUserId
      → ConvoySessionManager.isOrganizer
      → Spacer
      → apply
      → background
      → context.getSharedPreferences
      → firstName.isNotEmpty
      → foundation.Image

  [68] fun toggleFollow(organizerId: String, organizerName: String) {
    WORK FIELDS (local computed values):
      [70] val prefs = context.getSharedPreferences("grouptrack_follows", android.content.Context.MODE_PRIVATE)
      [81] var showDevMenu by remember { mutableStateOf(false) }
      [97] val ctx = androidx.compose.ui.platform.LocalContext.current
    CALLS OUT:
      → Box
      → Color
      → Column
      → ConvoyEnrollmentQueue.followOrganizer
      → ConvoyEnrollmentQueue.unfollowOrganizer
      → Row
      → Spacer
      → apply
      → background
      → clip
      → context.getSharedPreferences
      → firstName.isNotEmpty
      → firstName.uppercase
      → foundation.Image
      → material3.HorizontalDivider

  [585] private fun RideDisplayPanel(
    WORK FIELDS (local computed values):
      [605] val canRespond = rideStatus.lowercase() == "open"
      [606] val showFollow = orgId.isNotEmpty() && orgId != currentUserId
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → RideStatusBadge
      → Row
      → VisibilityBadge
      → append
      → background
      → city.isNotEmpty
      → clip
      → orgId.isNotEmpty
      → padding
      → rideStatus.lowercase
      → size
      → trailhead.isNotEmpty

  [757] private fun RideActionButton(
    WORK FIELDS (local computed values):
      [761] val borderColor = when {
      [766] val textColor = when {
      [789] val color = if (visibility.uppercase() == "PUBLIC") Color(0xFF4AB8E8) else Color(0xFFF59E0B)
      [800] val (color, label) = when (status.lowercase()) {
    CALLS OUT:
      → Box
      → Color
      → RideStatusBadge
      → Row
      → Text
      → VisibilityBadge
      → background
      → border
      → clip
      → onClick
      → padding
      → status.lowercase
      → then
      → visibility.uppercase

  [788] private fun VisibilityBadge(visibility: String) {
    WORK FIELDS (local computed values):
      [789] val color = if (visibility.uppercase() == "PUBLIC") Color(0xFF4AB8E8) else Color(0xFFF59E0B)
      [800] val (color, label) = when (status.lowercase()) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → DashBadge
      → RideStatusBadge
      → Row
      → Text
      → background
      → clip
      → onClick
      → padding
      → status.lowercase
      → visibility.uppercase

  [799] private fun RideStatusBadge(status: String) {
    WORK FIELDS (local computed values):
      [800] val (color, label) = when (status.lowercase()) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → DashBadge
      → Row
      → Text
      → background
      → clip
      → dashStatusColor
      → onClick
      → padding
      → status.lowercase

  [817] private fun NotifAccordion(label: String, color: Color, expanded: Boolean, onClick: () -> Unit) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → DashBadge
      → Row
      → Text
      → background
      → clip
      → dashStatusColor
      → onClick
      → padding
      → status.lowercase

  [829] private fun AccordionHeader(
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → DashBadge
      → Row
      → Text
      → background
      → clip
      → dashStatusColor
      → onClick
      → padding
      → status.lowercase

  [864] private fun DashBadge(label: String, color: Color) {
    CALLS OUT:
      → Box
      → Color
      → background
      → dashStatusColor
      → padding
      → status.lowercase

  [872] private fun dashStatusColor(status: String) = when (status.lowercase()) {
    CALLS OUT:
      → Color

========================================
FILE: ConvoyDevLaunchScreen.kt
========================================

  [35] fun ConvoyDevLaunchScreen(onLaunch: () -> Unit) {
    WORK FIELDS (local computed values):
      [41] val context = LocalContext.current
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → ConvoyDevSeeder.clear
      → ConvoyDevSeeder.seed
      → ConvoySessionManager.clearSession
      → ConvoySessionManager.saveUser
      → Toast.makeText
      → background
      → onLaunch
      → padding
      → seedSignedInUser
      → show
      → weight

  [131] private fun seedSignedInUser(context: Context, subscribed: Boolean) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Column
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → Text
      → background
      → clip
      → onClick
      → padding
      → setDevSubscribed

  [141] private fun DevScenarioButton(
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Column
      → Text
      → background
      → clip
      → onClick
      → padding

========================================
FILE: ConvoyDevSeeder.kt
========================================

  [20] fun seed(ctx: Context) {
    CALLS OUT:
      → Log.i
      → apply
      → clear
      → ctx.getSharedPreferences
      → edit
      → getInviteCount
      → getInviteField
      → getPublicRideCount
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideCount
      → getRideDate
      → getRideEmail
      → getRideEnrolled
      → getRideField

  [29] fun clear(ctx: Context) {
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → Log.i
      → System.currentTimeMillis
      → apply
      → clear
      → ctx.getSharedPreferences
      → edit
      → getInviteCount
      → getInviteField

  [36] fun getRideCount(ctx: Context)       = ctx.getSharedPreferences(PREFS_RIDES,  Context.MODE_PRIVATE).getInt("ride_count", 0)
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteCount
      → getInviteField
      → getPublicRideCount
      → getPublicRideField
      → getPublicRideInviteRequired

  [37] fun getNotifCount(ctx: Context)      = ctx.getSharedPreferences(PREFS_NOTIF,  Context.MODE_PRIVATE).getInt("notif_count", 0)
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteCount
      → getInviteField
      → getPublicRideCount
      → getPublicRideField
      → getPublicRideInviteRequired

  [38] fun getInviteCount(ctx: Context)     = ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).getInt("invite_count", 0)
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideCount
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideDate

  [39] fun getPublicRideCount(ctx: Context) = ctx.getSharedPreferences(PREFS_PUBLIC, Context.MODE_PRIVATE).getInt("pub_count", 0)
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideDate
      → getRideEmail

  [41] fun getRideField(ctx: Context, i: Int, field: String): String =
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideDate
      → getRideEmail

  [43] fun getRideInt(ctx: Context, i: Int, field: String): Int =
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideDate
      → getRideEmail

  [47] fun getRideName(ctx: Context, i: Int)      = getRideField(ctx, i, "name")
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideDate
      → getRideEmail

  [48] fun getRideStatus(ctx: Context, i: Int)    = getRideField(ctx, i, "my_status")
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideDate
      → getRideEmail

  [49] fun getRideDate(ctx: Context, i: Int)      = getRideField(ctx, i, "date")
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideEmail
      → getRideEnrolled

  [50] fun getRideOrganizer(ctx: Context, i: Int) = getRideField(ctx, i, "organizer")
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideEmail
      → getRideEnrolled

  [51] fun getRideEmail(ctx: Context, i: Int)     = getRideField(ctx, i, "email")
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideEnrolled
      → getRideField

  [52] fun getRideEnrolled(ctx: Context, i: Int)  = getRideInt(ctx, i, "enrolled")
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → getRideField
      → getRideTime

  [53] fun getRideTime(ctx: Context, i: Int)      = getRideField(ctx, i, "time")
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → p.apply
      → pad

  [55] fun getNotif(ctx: Context, i: Int, field: String): String =
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → p.apply
      → pad

  [57] fun getNotifInt(ctx: Context, i: Int, field: String): Int =
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getInviteField
      → getPublicRideField
      → getPublicRideInviteRequired
      → p.apply
      → pad

  [60] fun getInviteField(ctx: Context, i: Int, field: String): String =
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
      [138] val p = ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getPublicRideField
      → getPublicRideInviteRequired
      → p.apply
      → pad
      → seedInvites

  [63] fun getPublicRideField(ctx: Context, i: Int, field: String): String =
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
      [138] val p = ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → getPublicRideInviteRequired
      → p.apply
      → pad
      → seedInvites
      → seedUser

  [65] fun getPublicRideInviteRequired(ctx: Context, i: Int): Boolean =
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
      [138] val p = ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → p.apply
      → pad
      → seedInvites
      → seedUser

  [68] private fun pad(i: Int) = String.format("%03d", i + 1)
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
      [138] val p = ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → p.apply
      → seedInvites
      → seedUser

  [71] private fun seedUser(ctx: Context) {
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
      [138] val p = ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ConvoySessionManager.acceptPrivacy
      → ConvoySessionManager.acceptTerms
      → ConvoySessionManager.saveUser
      → ConvoySessionManager.setOrganizer
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setSubscriptionExpiry
      → ConvoySessionManager.setZipCode
      → System.currentTimeMillis
      → ctx.getSharedPreferences
      → edit
      → p.apply
      → seedInvites

  [84] private fun seedNotifications(ctx: Context) {
    WORK FIELDS (local computed values):
      [85] val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
      [138] val p = ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ctx.getSharedPreferences
      → edit
      → p.apply
      → seedInvites

  [137] private fun seedInvites(ctx: Context) {
    WORK FIELDS (local computed values):
      [138] val p = ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).edit()
      [172] val p = ctx.getSharedPreferences(PREFS_RIDES, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ctx.getSharedPreferences
      → edit
      → p.apply
      → seedRides

  [171] private fun seedRides(ctx: Context) {
    WORK FIELDS (local computed values):
      [172] val p = ctx.getSharedPreferences(PREFS_RIDES, Context.MODE_PRIVATE).edit()
    CALLS OUT:
      → ctx.getSharedPreferences
      → edit
      → p.apply

  [254] private fun seedPublicRides(ctx: Context) {
    WORK FIELDS (local computed values):
      [255] val p = ctx.getSharedPreferences(PREFS_PUBLIC, Context.MODE_PRIVATE).edit()
      [260] val k = "pub_${String.format("%03d", i)}_"
    CALLS OUT:
      → ctx.getSharedPreferences
      → edit
      → p.apply

  [257] fun pub(i: Int, name: String, org: String, orgId: String, email: String,
    WORK FIELDS (local computed values):
      [260] val k = "pub_${String.format("%03d", i)}_"
    CALLS OUT:
      → p.apply

========================================
FILE: ConvoyDisplayPanel.kt
========================================

  [45] fun ConvoyDisplayPanel(
    WORK FIELDS (local computed values):
      [55] var expanded by remember { mutableStateOf(false) }
      [56] var offsetX by remember { mutableFloatStateOf(0f) }
      [57] var offsetY by remember { mutableFloatStateOf(0f) }
    CALLS OUT:
      → Arrangement.spacedBy
      → Column
      → RoundedCornerShape
      → change.consume
      → expandVertically
      → fillMaxWidth
      → mutableStateOf
      → padding
      → pointerInput
      → shrinkVertically
      → width

  [157] private fun DisplayToggleRow(
    WORK FIELDS (local computed values):
      [165] val alpha = if (enabled) 1f else 0.45f
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → RoundedCornerShape
      → SwitchDefaults.colors
      → clickable
      → fillMaxWidth
      → onToggle
      → padding
      → width

========================================
FILE: ConvoyDownloadActionReceiver.kt
========================================

  [20] override fun onReceive(context: Context, intent: Intent) {
    WORK FIELDS (local computed values):
      [24] val entryId = intent.getStringExtra(EXTRA_ENTRY_ID) ?: return
      [29] val queue = DownloadQueueManager.queue.value
      [30] val cancellable = setOf(QueueStatus.DOWNLOADING, QueueStatus.QUEUED)
    CALLS OUT:
      → DownloadQueueManager.cancel
      → DownloadQueueManager.init
      → Log.i
      → setOf

========================================
FILE: ConvoyDownloadConfirm.kt
========================================

  [41] fun ConvoyDownloadConfirm(
    WORK FIELDS (local computed values):
      [51] var selectedSlots by remember {
      [54] var replaceExisting by remember { mutableStateOf(initialReplaceExisting) }
      [55] var showDetail by remember { mutableStateOf(false) }
      [79] val checked = slot.slotName in selectedSlots
    CALLS OUT:
      → CheckboxDefaults.colors
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → Text
      → areaDesc.isNotEmpty
      → mutableStateOf
      → padding

========================================
FILE: ConvoyDownloadNotification.kt
========================================

  [23] fun cancelIntent(context: Context, entryId: String): PendingIntent {
    WORK FIELDS (local computed values):
      [24] val intent = Intent(ConvoyDownloadActionReceiver.ACTION_CANCEL).apply {
      [35] val intent = Intent(ConvoyDownloadActionReceiver.ACTION_CANCEL_ALL).apply {
      [46] val existing = (context.getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager)
      [49] val channel = NotificationChannel(
      [63] val pct = if (total > 0) (downloaded * 100 / total) else 0
    CALLS OUT:
      → context.getSystemService
      → createChannel
      → entryId.hashCode
      → putExtra
      → setAutoCancel
      → setContentText
      → setContentTitle
      → setGroup
      → setOngoing
      → setPackage
      → setProgress
      → setShowBadge
      → setSilent
      → setSmallIcon

  [34] fun cancelAllIntent(context: Context): PendingIntent {
    WORK FIELDS (local computed values):
      [35] val intent = Intent(ConvoyDownloadActionReceiver.ACTION_CANCEL_ALL).apply {
      [46] val existing = (context.getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager)
      [49] val channel = NotificationChannel(
      [63] val pct = if (total > 0) (downloaded * 100 / total) else 0
    CALLS OUT:
      → context.getSystemService
      → createChannel
      → setAutoCancel
      → setContentText
      → setContentTitle
      → setGroup
      → setOngoing
      → setPackage
      → setProgress
      → setShowBadge
      → setSilent
      → setSmallIcon

  [44] fun createChannel(context: Context) {
    WORK FIELDS (local computed values):
      [46] val existing = (context.getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager)
      [49] val channel = NotificationChannel(
      [63] val pct = if (total > 0) (downloaded * 100 / total) else 0
    CALLS OUT:
      → context.getSystemService
      → setAutoCancel
      → setContentText
      → setContentTitle
      → setGroup
      → setOngoing
      → setProgress
      → setShowBadge
      → setSilent
      → setSmallIcon

  [60] fun progressNotification(
    WORK FIELDS (local computed values):
      [63] val pct = if (total > 0) (downloaded * 100 / total) else 0
    CALLS OUT:
      → setAutoCancel
      → setContentText
      → setContentTitle
      → setGroup
      → setOngoing
      → setProgress
      → setSilent
      → setSmallIcon

  [74] fun completeNotification(
    CALLS OUT:
      → setAutoCancel
      → setContentText
      → setContentTitle
      → setGroup
      → setSmallIcon

  [85] fun failedNotification(
    CALLS OUT:
      → setAutoCancel
      → setContentText
      → setContentTitle
      → setSmallIcon

========================================
FILE: ConvoyDownloadPanel.kt
========================================

  [40] fun toDisplayN() = String.format("%.2f\u00b0N", north)
    WORK FIELDS (local computed values):
      [69] val mono = FontFamily.Monospace
      [70] val blue = Color(0xFF4DA6FF)
      [71] val green = Color(0xFF3fb950)
      [72] val red = Color(0xFFf85149)
      [73] val orange = Color(0xFFd29922)
      [74] val purple = Color(0xFFbc8cff)
      [75] val dimText = Color(0xFF4A6080)
      [76] val panelBg = Color(0xEE131820)
      [77] val rowBg = Color(0xFF1A2A3A)
      [78] val sectionBorder = Color(0xFF30363d)
      [81] var isNetMode by remember { mutableStateOf(false) }
      [83] var flyoverMileage by remember { mutableFloatStateOf(0f) }
    CALLS OUT:
      → Color
      → Column
      → DownloadBbox
      → RoundedCornerShape
      → Row
      → Spacer
      → math.abs
      → mutableStateOf
      → padding
      → toDisplayE
      → toDisplayS
      → toDisplayW

  [41] fun toDisplayS() = String.format("%.2f\u00b0N", south)
    WORK FIELDS (local computed values):
      [69] val mono = FontFamily.Monospace
      [70] val blue = Color(0xFF4DA6FF)
      [71] val green = Color(0xFF3fb950)
      [72] val red = Color(0xFFf85149)
      [73] val orange = Color(0xFFd29922)
      [74] val purple = Color(0xFFbc8cff)
      [75] val dimText = Color(0xFF4A6080)
      [76] val panelBg = Color(0xEE131820)
      [77] val rowBg = Color(0xFF1A2A3A)
      [78] val sectionBorder = Color(0xFF30363d)
      [81] var isNetMode by remember { mutableStateOf(false) }
      [83] var flyoverMileage by remember { mutableFloatStateOf(0f) }
    CALLS OUT:
      → Color
      → Column
      → DownloadBbox
      → RoundedCornerShape
      → Row
      → Spacer
      → math.abs
      → mutableStateOf
      → padding
      → toDisplayE
      → toDisplayW

  [42] fun toDisplayE() = String.format("%.2f\u00b0W", kotlin.math.abs(east))
    WORK FIELDS (local computed values):
      [69] val mono = FontFamily.Monospace
      [70] val blue = Color(0xFF4DA6FF)
      [71] val green = Color(0xFF3fb950)
      [72] val red = Color(0xFFf85149)
      [73] val orange = Color(0xFFd29922)
      [74] val purple = Color(0xFFbc8cff)
      [75] val dimText = Color(0xFF4A6080)
      [76] val panelBg = Color(0xEE131820)
      [77] val rowBg = Color(0xFF1A2A3A)
      [78] val sectionBorder = Color(0xFF30363d)
      [81] var isNetMode by remember { mutableStateOf(false) }
      [83] var flyoverMileage by remember { mutableFloatStateOf(0f) }
    CALLS OUT:
      → Color
      → Column
      → DownloadBbox
      → RoundedCornerShape
      → Row
      → Spacer
      → SwitchDefaults.colors
      → math.abs
      → mutableStateOf
      → padding
      → toDisplayW

  [43] fun toDisplayW() = String.format("%.2f\u00b0W", kotlin.math.abs(west))
    WORK FIELDS (local computed values):
      [69] val mono = FontFamily.Monospace
      [70] val blue = Color(0xFF4DA6FF)
      [71] val green = Color(0xFF3fb950)
      [72] val red = Color(0xFFf85149)
      [73] val orange = Color(0xFFd29922)
      [74] val purple = Color(0xFFbc8cff)
      [75] val dimText = Color(0xFF4A6080)
      [76] val panelBg = Color(0xEE131820)
      [77] val rowBg = Color(0xFF1A2A3A)
      [78] val sectionBorder = Color(0xFF30363d)
      [81] var isNetMode by remember { mutableStateOf(false) }
      [83] var flyoverMileage by remember { mutableFloatStateOf(0f) }
    CALLS OUT:
      → Color
      → Column
      → DownloadBbox
      → RoundedCornerShape
      → Row
      → Spacer
      → SwitchDefaults.colors
      → mutableStateOf
      → padding

  [47] fun ConvoyDownloadPanel(
    WORK FIELDS (local computed values):
      [69] val mono = FontFamily.Monospace
      [70] val blue = Color(0xFF4DA6FF)
      [71] val green = Color(0xFF3fb950)
      [72] val red = Color(0xFFf85149)
      [73] val orange = Color(0xFFd29922)
      [74] val purple = Color(0xFFbc8cff)
      [75] val dimText = Color(0xFF4A6080)
      [76] val panelBg = Color(0xEE131820)
      [77] val rowBg = Color(0xFF1A2A3A)
      [78] val sectionBorder = Color(0xFF30363d)
      [81] var isNetMode by remember { mutableStateOf(false) }
      [83] var flyoverMileage by remember { mutableFloatStateOf(0f) }
    CALLS OUT:
      → Color
      → Column
      → DownloadBbox
      → RoundedCornerShape
      → Row
      → Spacer
      → SwitchDefaults.colors
      → blue.copy
      → mutableStateOf
      → padding
      → purple.copy

  [334] private fun PanelSectionHeader(title: String, borderColor: Color) {
    CALLS OUT:
      → CheckboxDefaults.colors
      → Color
      → RoundedCornerShape
      → Spacer
      → alpha
      → badge.isNotEmpty
      → border
      → color.copy
      → copy
      → foundation.BorderStroke
      → onClick
      → padding

  [348] private fun ArtifactCheckRow(
    CALLS OUT:
      → CheckboxDefaults.colors
      → Color
      → RoundedCornerShape
      → Spacer
      → alpha
      → badge.isNotEmpty
      → color.copy
      → copy
      → fillMaxWidth
      → foundation.BorderStroke
      → onClick
      → padding

  [399] private fun PanelActionButton(
    CALLS OUT:
      → CheckboxDefaults.colors
      → Color
      → RoundedCornerShape
      → Spacer
      → background
      → color.copy
      → copy
      → fillMaxWidth
      → foundation.BorderStroke
      → onClick
      → padding

  [432] private fun OverlayToggleRow(
    CALLS OUT:
      → CheckboxDefaults.colors
      → Color
      → RoundedCornerShape
      → Spacer
      → background
      → color.copy
      → padding

========================================
FILE: ConvoyDownloadQueue.kt
========================================

  [41] fun toJson(): JSONObject = JSONObject().apply {
    WORK FIELDS (local computed values):
      [104] val current = _queue.value.toMutableList()
      [105] var changed = false
    CALLS OUT:
      → System.currentTimeMillis
      → UUID.randomUUID
      → _queue.asStateFlow
      → copy
      → fromJson
      → init
      → loadQueue
      → put
      → saveQueue
      → startNextIfAvailable

  [60] fun fromJson(j: JSONObject): QueueEntry = QueueEntry(
    WORK FIELDS (local computed values):
      [104] val current = _queue.value.toMutableList()
      [105] var changed = false
      [127] val tiles = ConvoyTileCalculator.calculateTiles(north, south, east, west)
      [129] val layerCount = MapSourceManager.getDownloadSources().sumOf { it.second.size }
      [130] val totalTiles = tiles.size * layerCount
      [132] val entry = QueueEntry(
      [137] val current = _queue.value.toMutableList()
    CALLS OUT:
      → ConvoyTileCalculator.calculateTiles
      → System.currentTimeMillis
      → UUID.randomUUID
      → _queue.asStateFlow
      → copy
      → current.add
      → init
      → loadQueue
      → saveQueue
      → startNextIfAvailable

  [97] fun init(context: Context) {
    WORK FIELDS (local computed values):
      [104] val current = _queue.value.toMutableList()
      [105] var changed = false
      [127] val tiles = ConvoyTileCalculator.calculateTiles(north, south, east, west)
      [129] val layerCount = MapSourceManager.getDownloadSources().sumOf { it.second.size }
      [130] val totalTiles = tiles.size * layerCount
      [132] val entry = QueueEntry(
      [137] val current = _queue.value.toMutableList()
      [156] val cells = ConvoyTileDownloader.gridCells(north, south, east, west)
      [159] val entry = QueueEntry(
      [166] val current = _queue.value.toMutableList()
      [173] val current = _queue.value.toMutableList()
      [175] val entry = QueueEntry(
    CALLS OUT:
      → ConvoyTileCalculator.calculateTiles
      → ConvoyTileDownloader.gridCells
      → Log.i
      → cells.isEmpty
      → copy
      → current.add
      → init
      → loadQueue
      → saveQueue
      → startNextIfAvailable

  [120] fun enqueue(
    WORK FIELDS (local computed values):
      [127] val tiles = ConvoyTileCalculator.calculateTiles(north, south, east, west)
      [129] val layerCount = MapSourceManager.getDownloadSources().sumOf { it.second.size }
      [130] val totalTiles = tiles.size * layerCount
      [132] val entry = QueueEntry(
      [137] val current = _queue.value.toMutableList()
      [156] val cells = ConvoyTileDownloader.gridCells(north, south, east, west)
      [159] val entry = QueueEntry(
      [166] val current = _queue.value.toMutableList()
      [173] val current = _queue.value.toMutableList()
      [175] val entry = QueueEntry(
      [197] val bounds = ConvoyTileDownloader.tileBoundsLatLon(slotName) ?: return 0
      [198] val cells = ConvoyTileDownloader.gridCells(bounds[0], bounds[1], bounds[2], bounds[3])
    CALLS OUT:
      → ConvoyTileCalculator.calculateTiles
      → ConvoyTileDownloader.gridCells
      → ConvoyTileDownloader.tileBoundsLatLon
      → Log.i
      → cells.isEmpty
      → current.add
      → init
      → saveQueue
      → startNextIfAvailable

  [149] fun enqueueArea(
    WORK FIELDS (local computed values):
      [156] val cells = ConvoyTileDownloader.gridCells(north, south, east, west)
      [159] val entry = QueueEntry(
      [166] val current = _queue.value.toMutableList()
      [173] val current = _queue.value.toMutableList()
      [175] val entry = QueueEntry(
      [197] val bounds = ConvoyTileDownloader.tileBoundsLatLon(slotName) ?: return 0
      [198] val cells = ConvoyTileDownloader.gridCells(bounds[0], bounds[1], bounds[2], bounds[3])
      [201] val current = _queue.value.toMutableList()
      [203] val entry = QueueEntry(
      [221] val entry = _queue.value.find { it.id == entryId } ?: return
    CALLS OUT:
      → ConvoyTileDownloader.gridCells
      → ConvoyTileDownloader.tileBoundsLatLon
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → cancel
      → cancelWorkById
      → cells.isEmpty
      → current.add
      → init
      → saveQueue
      → startNextIfAvailable

  [191] fun enqueueRefresh(
    WORK FIELDS (local computed values):
      [197] val bounds = ConvoyTileDownloader.tileBoundsLatLon(slotName) ?: return 0
      [198] val cells = ConvoyTileDownloader.gridCells(bounds[0], bounds[1], bounds[2], bounds[3])
      [201] val current = _queue.value.toMutableList()
      [203] val entry = QueueEntry(
      [221] val entry = _queue.value.find { it.id == entryId } ?: return
      [231] val current = _queue.value.toMutableList()
      [251] val current = _queue.value.toMutableList()
      [262] val completed = current.filter { it.status == QueueStatus.COMPLETE }
      [268] val entry = _queue.value.find { it.id == entryId } ?: return
    CALLS OUT:
      → ConvoyTileDownloader.gridCells
      → ConvoyTileDownloader.tileBoundsLatLon
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → cancel
      → cancelAll
      → cancelWorkById
      → cells.isEmpty
      → current.add
      → holdQueue
      → init
      → isQueuePaused
      → removeEntry
      → resumeQueue

  [220] fun cancel(entryId: String) {
    WORK FIELDS (local computed values):
      [221] val entry = _queue.value.find { it.id == entryId } ?: return
      [231] val current = _queue.value.toMutableList()
      [251] val current = _queue.value.toMutableList()
      [262] val completed = current.filter { it.status == QueueStatus.COMPLETE }
      [268] val entry = _queue.value.find { it.id == entryId } ?: return
      [277] val current = _queue.value.toMutableList()
      [286] val terminal = setOf(QueueStatus.COMPLETE, QueueStatus.CANCELLED, QueueStatus.FAILED)
      [287] val current = _queue.value.toMutableList()
      [295] val current = _queue.value.toMutableList()
      [296] val idx = current.indexOfFirst { it.id == entryId }
    CALLS OUT:
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → cancelAll
      → cancelWorkById
      → clearCompleted
      → copy
      → holdQueue
      → isQueuePaused
      → removeEntry
      → resumeQueue
      → saveQueue
      → setOf
      → startNextIfAvailable
      → updateProgress

  [240] fun holdQueue() {
    WORK FIELDS (local computed values):
      [251] val current = _queue.value.toMutableList()
      [262] val completed = current.filter { it.status == QueueStatus.COMPLETE }
      [268] val entry = _queue.value.find { it.id == entryId } ?: return
      [277] val current = _queue.value.toMutableList()
      [286] val terminal = setOf(QueueStatus.COMPLETE, QueueStatus.CANCELLED, QueueStatus.FAILED)
      [287] val current = _queue.value.toMutableList()
      [295] val current = _queue.value.toMutableList()
      [296] val idx = current.indexOfFirst { it.id == entryId }
      [311] val entry = _queue.value.find { it.id == entryId }
      [314] val current = _queue.value.toMutableList()
    CALLS OUT:
      → Log.d
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → cancelAll
      → cancelWorkById
      → clearCompleted
      → copy
      → isQueuePaused
      → markComplete
      → removeEntry
      → resumeQueue
      → saveQueue
      → setOf
      → startNextIfAvailable

  [244] fun resumeQueue() {
    WORK FIELDS (local computed values):
      [251] val current = _queue.value.toMutableList()
      [262] val completed = current.filter { it.status == QueueStatus.COMPLETE }
      [268] val entry = _queue.value.find { it.id == entryId } ?: return
      [277] val current = _queue.value.toMutableList()
      [286] val terminal = setOf(QueueStatus.COMPLETE, QueueStatus.CANCELLED, QueueStatus.FAILED)
      [287] val current = _queue.value.toMutableList()
      [295] val current = _queue.value.toMutableList()
      [296] val idx = current.indexOfFirst { it.id == entryId }
      [311] val entry = _queue.value.find { it.id == entryId }
      [314] val current = _queue.value.toMutableList()
    CALLS OUT:
      → Log.d
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → cancelAll
      → cancelWorkById
      → clearCompleted
      → copy
      → isQueuePaused
      → it.copy
      → markComplete
      → removeEntry
      → saveQueue
      → setOf
      → startNextIfAvailable

  [249] fun isQueuePaused(): Boolean = queuePaused
    WORK FIELDS (local computed values):
      [251] val current = _queue.value.toMutableList()
      [262] val completed = current.filter { it.status == QueueStatus.COMPLETE }
      [268] val entry = _queue.value.find { it.id == entryId } ?: return
      [277] val current = _queue.value.toMutableList()
      [286] val terminal = setOf(QueueStatus.COMPLETE, QueueStatus.CANCELLED, QueueStatus.FAILED)
      [287] val current = _queue.value.toMutableList()
      [295] val current = _queue.value.toMutableList()
      [296] val idx = current.indexOfFirst { it.id == entryId }
      [311] val entry = _queue.value.find { it.id == entryId }
      [314] val current = _queue.value.toMutableList()
    CALLS OUT:
      → Log.d
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → cancelAll
      → cancelWorkById
      → clearCompleted
      → copy
      → it.copy
      → markComplete
      → removeEntry
      → saveQueue
      → setOf
      → startNextIfAvailable
      → updateEntry

  [250] fun cancelAll() {
    WORK FIELDS (local computed values):
      [251] val current = _queue.value.toMutableList()
      [262] val completed = current.filter { it.status == QueueStatus.COMPLETE }
      [268] val entry = _queue.value.find { it.id == entryId } ?: return
      [277] val current = _queue.value.toMutableList()
      [286] val terminal = setOf(QueueStatus.COMPLETE, QueueStatus.CANCELLED, QueueStatus.FAILED)
      [287] val current = _queue.value.toMutableList()
      [295] val current = _queue.value.toMutableList()
      [296] val idx = current.indexOfFirst { it.id == entryId }
      [311] val entry = _queue.value.find { it.id == entryId }
      [314] val current = _queue.value.toMutableList()
    CALLS OUT:
      → Log.d
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → cancelWorkById
      → clearCompleted
      → copy
      → it.copy
      → markComplete
      → removeEntry
      → saveQueue
      → setOf
      → startNextIfAvailable
      → updateEntry
      → updateProgress

  [267] fun removeEntry(entryId: String) {
    WORK FIELDS (local computed values):
      [268] val entry = _queue.value.find { it.id == entryId } ?: return
      [277] val current = _queue.value.toMutableList()
      [286] val terminal = setOf(QueueStatus.COMPLETE, QueueStatus.CANCELLED, QueueStatus.FAILED)
      [287] val current = _queue.value.toMutableList()
      [295] val current = _queue.value.toMutableList()
      [296] val idx = current.indexOfFirst { it.id == entryId }
      [311] val entry = _queue.value.find { it.id == entryId }
      [314] val current = _queue.value.toMutableList()
      [345] val current = _queue.value
      [346] val running = current.count { it.status == QueueStatus.DOWNLOADING }
    CALLS OUT:
      → Log.d
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → cancelWorkById
      → clearCompleted
      → copy
      → it.copy
      → markComplete
      → markFailed
      → saveQueue
      → setOf
      → startNextIfAvailable
      → updateEntry
      → updateProgress

  [285] fun clearCompleted() {
    WORK FIELDS (local computed values):
      [286] val terminal = setOf(QueueStatus.COMPLETE, QueueStatus.CANCELLED, QueueStatus.FAILED)
      [287] val current = _queue.value.toMutableList()
      [295] val current = _queue.value.toMutableList()
      [296] val idx = current.indexOfFirst { it.id == entryId }
      [311] val entry = _queue.value.find { it.id == entryId }
      [314] val current = _queue.value.toMutableList()
      [345] val current = _queue.value
      [346] val running = current.count { it.status == QueueStatus.DOWNLOADING }
      [349] val slotsAvailable = MAX_CONCURRENT - running
      [350] val queued = current.filter { it.status == QueueStatus.QUEUED }
      [357] val inputData = Data.Builder()
    CALLS OUT:
      → Data.Builder
      → Log.d
      → Log.e
      → Log.i
      → copy
      → it.copy
      → launchWorker
      → markComplete
      → markFailed
      → queued.take
      → saveQueue
      → setOf
      → startNextIfAvailable
      → updateEntry
      → updateProgress

  [294] fun updateProgress(entryId: String, downloaded: Int, failed: Int) {
    WORK FIELDS (local computed values):
      [295] val current = _queue.value.toMutableList()
      [296] val idx = current.indexOfFirst { it.id == entryId }
      [311] val entry = _queue.value.find { it.id == entryId }
      [314] val current = _queue.value.toMutableList()
      [345] val current = _queue.value
      [346] val running = current.count { it.status == QueueStatus.DOWNLOADING }
      [349] val slotsAvailable = MAX_CONCURRENT - running
      [350] val queued = current.filter { it.status == QueueStatus.QUEUED }
      [357] val inputData = Data.Builder()
      [368] val constraints = Constraints.Builder()
      [372] val workRequest = OneTimeWorkRequestBuilder<ConvoyDownloadWorker>()
    CALLS OUT:
      → Constraints.Builder
      → Data.Builder
      → Log.d
      → Log.e
      → Log.i
      → build
      → copy
      → it.copy
      → launchWorker
      → markComplete
      → markFailed
      → queued.take
      → saveQueue
      → setConstraints
      → setInputData

  [310] fun markComplete(entryId: String, downloaded: Int, failed: Int) {
    WORK FIELDS (local computed values):
      [311] val entry = _queue.value.find { it.id == entryId }
      [314] val current = _queue.value.toMutableList()
      [345] val current = _queue.value
      [346] val running = current.count { it.status == QueueStatus.DOWNLOADING }
      [349] val slotsAvailable = MAX_CONCURRENT - running
      [350] val queued = current.filter { it.status == QueueStatus.QUEUED }
      [357] val inputData = Data.Builder()
      [368] val constraints = Constraints.Builder()
      [372] val workRequest = OneTimeWorkRequestBuilder<ConvoyDownloadWorker>()
    CALLS OUT:
      → Constraints.Builder
      → Data.Builder
      → Log.d
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → addTag
      → build
      → enqueue
      → it.copy
      → launchWorker
      → markFailed
      → queued.take
      → saveQueue
      → setConstraints

  [333] fun markFailed(entryId: String, message: String) {
    WORK FIELDS (local computed values):
      [345] val current = _queue.value
      [346] val running = current.count { it.status == QueueStatus.DOWNLOADING }
      [349] val slotsAvailable = MAX_CONCURRENT - running
      [350] val queued = current.filter { it.status == QueueStatus.QUEUED }
      [357] val inputData = Data.Builder()
      [368] val constraints = Constraints.Builder()
      [372] val workRequest = OneTimeWorkRequestBuilder<ConvoyDownloadWorker>()
      [392] val current = _queue.value.toMutableList()
      [393] val idx = current.indexOfFirst { it.id == entryId }
      [404] val dir = File(appCtx.filesDir, "download_queue")
      [406] val file = File(dir, QUEUE_FILE)
      [407] val arr = JSONArray()
    CALLS OUT:
      → Constraints.Builder
      → Data.Builder
      → File
      → JSONArray
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → addTag
      → arr.put
      → build
      → dir.mkdirs
      → enqueue
      → entry.toJson
      → file.writeText
      → it.copy

  [342] private fun startNextIfAvailable() {
    WORK FIELDS (local computed values):
      [345] val current = _queue.value
      [346] val running = current.count { it.status == QueueStatus.DOWNLOADING }
      [349] val slotsAvailable = MAX_CONCURRENT - running
      [350] val queued = current.filter { it.status == QueueStatus.QUEUED }
      [357] val inputData = Data.Builder()
      [368] val constraints = Constraints.Builder()
      [372] val workRequest = OneTimeWorkRequestBuilder<ConvoyDownloadWorker>()
      [392] val current = _queue.value.toMutableList()
      [393] val idx = current.indexOfFirst { it.id == entryId }
      [404] val dir = File(appCtx.filesDir, "download_queue")
      [406] val file = File(dir, QUEUE_FILE)
      [407] val arr = JSONArray()
    CALLS OUT:
      → Constraints.Builder
      → Data.Builder
      → File
      → JSONArray
      → Log.e
      → Log.i
      → WorkManager.getInstance
      → addTag
      → arr.length
      → arr.put
      → build
      → dir.mkdirs
      → enqueue
      → entry.toJson
      → file.exists

  [356] private fun launchWorker(entry: QueueEntry) {
    WORK FIELDS (local computed values):
      [357] val inputData = Data.Builder()
      [368] val constraints = Constraints.Builder()
      [372] val workRequest = OneTimeWorkRequestBuilder<ConvoyDownloadWorker>()
      [392] val current = _queue.value.toMutableList()
      [393] val idx = current.indexOfFirst { it.id == entryId }
      [404] val dir = File(appCtx.filesDir, "download_queue")
      [406] val file = File(dir, QUEUE_FILE)
      [407] val arr = JSONArray()
      [418] val file = File(appCtx.filesDir, "download_queue/$QUEUE_FILE")
      [420] val arr = JSONArray(file.readText())
      [421] val entries = mutableListOf<QueueEntry>()
    CALLS OUT:
      → Constraints.Builder
      → Data.Builder
      → File
      → JSONArray
      → Log.e
      → Log.i
      → QueueEntry.fromJson
      → WorkManager.getInstance
      → addTag
      → arr.getJSONObject
      → arr.length
      → arr.put
      → build
      → dir.mkdirs
      → enqueue

  [391] private fun updateEntry(entryId: String, transform: (QueueEntry) -> QueueEntry) {
    WORK FIELDS (local computed values):
      [392] val current = _queue.value.toMutableList()
      [393] val idx = current.indexOfFirst { it.id == entryId }
      [404] val dir = File(appCtx.filesDir, "download_queue")
      [406] val file = File(dir, QUEUE_FILE)
      [407] val arr = JSONArray()
      [418] val file = File(appCtx.filesDir, "download_queue/$QUEUE_FILE")
      [420] val arr = JSONArray(file.readText())
      [421] val entries = mutableListOf<QueueEntry>()
    CALLS OUT:
      → File
      → JSONArray
      → Log.e
      → Log.i
      → QueueEntry.fromJson
      → arr.getJSONObject
      → arr.length
      → arr.put
      → dir.mkdirs
      → entries.add
      → entry.toJson
      → file.exists
      → file.writeText
      → loadQueue
      → saveQueue

  [402] private fun saveQueue() {
    WORK FIELDS (local computed values):
      [404] val dir = File(appCtx.filesDir, "download_queue")
      [406] val file = File(dir, QUEUE_FILE)
      [407] val arr = JSONArray()
      [418] val file = File(appCtx.filesDir, "download_queue/$QUEUE_FILE")
      [420] val arr = JSONArray(file.readText())
      [421] val entries = mutableListOf<QueueEntry>()
    CALLS OUT:
      → File
      → JSONArray
      → Log.e
      → Log.i
      → QueueEntry.fromJson
      → arr.getJSONObject
      → arr.length
      → arr.put
      → dir.mkdirs
      → entries.add
      → entry.toJson
      → file.exists
      → file.writeText
      → loadQueue

  [416] private fun loadQueue() {
    WORK FIELDS (local computed values):
      [418] val file = File(appCtx.filesDir, "download_queue/$QUEUE_FILE")
      [420] val arr = JSONArray(file.readText())
      [421] val entries = mutableListOf<QueueEntry>()
    CALLS OUT:
      → File
      → JSONArray
      → Log.e
      → Log.i
      → QueueEntry.fromJson
      → arr.getJSONObject
      → arr.length
      → entries.add
      → file.exists

========================================
FILE: ConvoyDownloadQueuePanel.kt
========================================

  [46] fun DownloadQueuePanel(
    WORK FIELDS (local computed values):
      [51] val queue by DownloadQueueManager.queue.collectAsState()
      [52] val active = queue.filter { it.status == QueueStatus.DOWNLOADING }
      [53] val queued = queue.filter { it.status == QueueStatus.QUEUED }
      [54] val completed = queue.filter { it.status in setOf(QueueStatus.COMPLETE, QueueStatus.FAILED, QueueStatus.CANCELLED) }
      [83] val isPaused = DownloadQueueManager.isQueuePaused()
    CALLS OUT:
      → ActionButton
      → ActiveSlotBar
      → Column
      → DownloadQueueManager.cancelAll
      → DownloadQueueManager.clearCompleted
      → DownloadQueueManager.holdQueue
      → DownloadQueueManager.isQueuePaused
      → DownloadQueueManager.resumeQueue
      → QueuedItemRow
      → RoundedCornerShape
      → Spacer
      → active.isEmpty
      → active.isNotEmpty
      → completed.isNotEmpty
      → expandVertically

  [147] private fun QueueSummaryBar(
    WORK FIELDS (local computed values):
      [169] val parts = mutableListOf<String>()
      [178] val totalRemaining = active.sumOf { it.totalTiles - it.downloadedTiles }
      [196] val pct = if (entry.totalTiles > 0) entry.downloadedTiles.toFloat() / entry.totalTiles else 0f
      [197] val pctInt = (pct * 100).toInt()
    CALLS OUT:
      → ActiveSlotBar
      → Arrangement.spacedBy
      → Column
      → DownloadQueueManager.cancel
      → Spacer
      → active.isNotEmpty
      → fillMaxWidth
      → height
      → material3.CircularProgressIndicator
      → onClick
      → padding
      → parts.add
      → queued.isNotEmpty

  [195] private fun ActiveSlotBar(entry: QueueEntry) {
    WORK FIELDS (local computed values):
      [196] val pct = if (entry.totalTiles > 0) entry.downloadedTiles.toFloat() / entry.totalTiles else 0f
      [197] val pctInt = (pct * 100).toInt()
      [266] val statusText = when (entry.status) {
      [272] val statusColor = when (entry.status) {
    CALLS OUT:
      → Color
      → CompletedItemRow
      → Date
      → DownloadQueueManager.cancel
      → QueuedItemRow
      → Row
      → Spacer
      → fillMaxWidth
      → height
      → padding

  [234] private fun QueuedItemRow(entry: QueueEntry, position: Int) {
    WORK FIELDS (local computed values):
      [266] val statusText = when (entry.status) {
      [272] val statusColor = when (entry.status) {
    CALLS OUT:
      → ActionButton
      → Color
      → CompletedItemRow
      → Date
      → DownloadQueueManager.cancel
      → RoundedCornerShape
      → Row
      → fillMaxWidth
      → onClick
      → padding

  [265] private fun CompletedItemRow(entry: QueueEntry) {
    WORK FIELDS (local computed values):
      [266] val statusText = when (entry.status) {
      [272] val statusColor = when (entry.status) {
    CALLS OUT:
      → ActionButton
      → Color
      → RoundedCornerShape
      → fillMaxWidth
      → onClick
      → padding

  [296] private fun ActionButton(text: String, color: Color, onClick: () -> Unit) {
    CALLS OUT:
      → Color
      → RoundedCornerShape
      → onClick

========================================
FILE: ConvoyDownloadRideConfigScreen.kt
========================================

  [45] fun ConvoyDownloadRideConfigScreen(
    WORK FIELDS (local computed values):
      [49] val context     = LocalContext.current
      [50] val scrollState = rememberScrollState()
      [52] val masterConfig = remember { ConvoyMasterConfig.load(context) }
      [53] val organizer    = remember { ConvoyUserStore.getActiveUser(context) }
      [57] val eligibleRides = remember {
      [58] val count = ConvoyDevSeeder.getRideCount(context)
      [60] val s = ConvoyDevSeeder.getRideStatus(context, i).lowercase()
      [74] var selectedId     by remember { mutableStateOf<String?>(null) }
      [75] var downloadStatus by remember { mutableStateOf("idle") }
      [76] var errorMsg       by remember { mutableStateOf("") }
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → ConvoyDevSeeder.getRideCount
      → ConvoyDevSeeder.getRideDate
      → ConvoyDevSeeder.getRideField
      → ConvoyDevSeeder.getRideName
      → ConvoyDevSeeder.getRideOrganizer
      → ConvoyDevSeeder.getRideStatus
      → ConvoyMasterConfig.load
      → ConvoyUserStore.getActiveUser
      → Row
      → Text
      → background

========================================
FILE: ConvoyDownloadWorker.kt
========================================

  [31] override suspend fun doWork(): Result {
    WORK FIELDS (local computed values):
      [32] val entryId = inputData.getString("entry_id") ?: return Result.failure()
      [33] val north = inputData.getDouble("north", 0.0)
      [34] val south = inputData.getDouble("south", 0.0)
      [35] val east = inputData.getDouble("east", 0.0)
      [36] val west = inputData.getDouble("west", 0.0)
      [37] val label = inputData.getString("label") ?: "Map tiles"
      [38] val refreshMode = inputData.getBoolean("refresh_mode", false)
      [39] val refreshSlot = inputData.getString("refresh_slot") ?: ""
      [54] val source = MapSourceManager.getSourceByKey(refreshSlot)
      [55] val layers = source?.layers ?: emptyList()
      [58] val cellTiles = ConvoyTileCalculator.calculateTiles(north, south, east, west)
      [61] val existingTiles = cellTiles.filter { tile ->
    CALLS OUT:
      → ConvoyTileCalculator.calculateTiles
      → ConvoyTileDownloader.downloadTiles
      → ConvoyTileDownloader.tilePath
      → Data.Builder
      → DownloadQueueManager.init
      → DownloadQueueManager.markComplete
      → DownloadQueueManager.updateProgress
      → Log.e
      → Log.i
      → Result.failure
      → Result.success
      → build
      → existingTiles.isEmpty
      → exists
      → refreshSlot.isNotEmpty

  [177] private fun showProgress(entryId: String, label: String, downloaded: Int, total: Int) {
    WORK FIELDS (local computed values):
      [179] val notification = ConvoyDownloadNotification
      [186] val info = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
    CALLS OUT:
      → ForegroundInfo
      → Log.d
      → entryId.hashCode
      → setForegroundAsync

========================================
FILE: ConvoyEmailGateScreen.kt
========================================

  [66] fun ConvoyEmailGateScreen(
    WORK FIELDS (local computed values):
      [71] val context  = LocalContext.current
      [72] val keyboard = LocalSoftwareKeyboardController.current
      [74] var email by remember { mutableStateOf("") }
      [75] var state by remember { mutableStateOf<GateState>(GateState.Idle) }
      [79] val trimmed = email.trim().lowercase()
      [80] val users   = ConvoyUserStore.loadAll(context)
      [81] val match   = users.firstOrNull { it.email.lowercase() == trimmed }
      [123] when (val s = state) {
    CALLS OUT:
      → ConvoyUserStore.loadAll
      → GateState.NeedUpgrade
      → KeyboardActions
      → OutlinedTextFieldDefaults.colors
      → Spacer
      → Text
      → email.lowercase
      → email.trim
      → fillMaxSize
      → hide
      → lowercase
      → mutableStateOf
      → onProceed
      → padding

  [77] fun validate() {
    WORK FIELDS (local computed values):
      [79] val trimmed = email.trim().lowercase()
      [80] val users   = ConvoyUserStore.loadAll(context)
      [81] val match   = users.firstOrNull { it.email.lowercase() == trimmed }
      [123] when (val s = state) {
    CALLS OUT:
      → ConvoyUserStore.loadAll
      → GateState.NeedUpgrade
      → KeyboardActions
      → OutlinedTextFieldDefaults.colors
      → Spacer
      → Text
      → email.lowercase
      → email.trim
      → fillMaxSize
      → hide
      → lowercase
      → onProceed
      → padding

========================================
FILE: ConvoyEngine.kt
========================================

  [18] fun empty() = ConvoyState()
    WORK FIELDS (local computed values):
      [23] val startLat: Double = 0.0,
      [24] val startLon: Double = 0.0,
      [25] val endLat: Double = 0.0,
      [26] val endLon: Double = 0.0,
      [27] val color: String = "#000000",
      [28] val nodeId: String = ""
      [40] val withStatus = nodes.map { it.copy(status = computeStatus(it, nowMs)) }
      [41] val heading = computeHeading(withStatus)
      [42] val sorted = computeSortPositions(withStatus, heading)
      [43] val withRoles = assignLeadTail(sorted, lockedLeadNodeId, tailNodeId)
      [44] val lead = withRoles.firstOrNull { it.isLead }
      [45] val tail = withRoles.firstOrNull { it.isTail }
    CALLS OUT:
      → ConvoyState.empty
      → Math.toRadians
      → System.currentTimeMillis
      → assignLeadTail
      → computeHeading
      → computeProximity
      → computeSortPositions
      → computeSpan
      → computeStatus
      → cos
      → it.copy
      → moving.isEmpty
      → moving.sorted
      → node.copy
      → nodes.isEmpty

  [31] fun compute(
    WORK FIELDS (local computed values):
      [40] val withStatus = nodes.map { it.copy(status = computeStatus(it, nowMs)) }
      [41] val heading = computeHeading(withStatus)
      [42] val sorted = computeSortPositions(withStatus, heading)
      [43] val withRoles = assignLeadTail(sorted, lockedLeadNodeId, tailNodeId)
      [44] val lead = withRoles.firstOrNull { it.isLead }
      [45] val tail = withRoles.firstOrNull { it.isTail }
      [46] val span = computeSpan(lead, tail)
      [47] val withProximity = computeProximity(withRoles, myCartId)
      [48] val activeCount = withProximity.count { it.status == ConvoyStatus.ACTIVE }
      [49] val lostCount = withProximity.count { it.status == ConvoyStatus.LOST }
      [63] val ageMs = nowMs - node.lastSeenMs
      [64] val lostMs = (ConvoyConfig.LOST_MINUTES * 60_000f).toLong()
    CALLS OUT:
      → ConvoyState.empty
      → Math.toRadians
      → System.currentTimeMillis
      → active.isEmpty
      → assignLeadTail
      → computeHeading
      → computeProximity
      → computeSortPositions
      → computeSpan
      → computeStatus
      → cos
      → it.copy
      → moving.isEmpty
      → moving.sorted
      → node.copy

  [62] fun computeStatus(node: ConvoyNode, nowMs: Long): ConvoyStatus {
    WORK FIELDS (local computed values):
      [63] val ageMs = nowMs - node.lastSeenMs
      [64] val lostMs = (ConvoyConfig.LOST_MINUTES * 60_000f).toLong()
      [65] val dropMs = (ConvoyConfig.SIGNAL_DROP_MINUTES * 60_000f).toLong()
      [74] val moving = nodes
      [78] val sorted = moving.sorted()
      [83] val rad = Math.toRadians(headingDeg.toDouble())
      [84] val dx = sin(rad)
      [85] val dy = cos(rad)
      [86] val active = nodes.filter { it.status != ConvoyStatus.LOST }
      [88] val lost = nodes.filter { it.status == ConvoyStatus.LOST }
      [89] val all = active + lost
      [104] val active = nodes.filter { it.status == ConvoyStatus.ACTIVE }
    CALLS OUT:
      → Math.toRadians
      → active.isEmpty
      → computeHeading
      → computeProximity
      → computeSortPositions
      → computeSpan
      → cos
      → haversineMiles
      → moving.isEmpty
      → moving.sorted
      → node.copy
      → nodes.indexOf
      → sin

  [73] fun computeHeading(nodes: List<ConvoyNode>): Float {
    WORK FIELDS (local computed values):
      [74] val moving = nodes
      [78] val sorted = moving.sorted()
      [83] val rad = Math.toRadians(headingDeg.toDouble())
      [84] val dx = sin(rad)
      [85] val dy = cos(rad)
      [86] val active = nodes.filter { it.status != ConvoyStatus.LOST }
      [88] val lost = nodes.filter { it.status == ConvoyStatus.LOST }
      [89] val all = active + lost
      [104] val active = nodes.filter { it.status == ConvoyStatus.ACTIVE }
      [107] val leadNode = if (lockedLeadNodeId != null)
      [112] val tailNode = if (tailNodeId != null)
      [137] val idx = nodes.indexOf(node)
    CALLS OUT:
      → Math.toRadians
      → active.isEmpty
      → computeProximity
      → computeSortPositions
      → computeSpan
      → cos
      → haversineMiles
      → moving.isEmpty
      → moving.sorted
      → node.copy
      → nodes.indexOf
      → sin

  [82] fun computeSortPositions(nodes: List<ConvoyNode>, headingDeg: Float): List<ConvoyNode> {
    WORK FIELDS (local computed values):
      [83] val rad = Math.toRadians(headingDeg.toDouble())
      [84] val dx = sin(rad)
      [85] val dy = cos(rad)
      [86] val active = nodes.filter { it.status != ConvoyStatus.LOST }
      [88] val lost = nodes.filter { it.status == ConvoyStatus.LOST }
      [89] val all = active + lost
      [104] val active = nodes.filter { it.status == ConvoyStatus.ACTIVE }
      [107] val leadNode = if (lockedLeadNodeId != null)
      [112] val tailNode = if (tailNodeId != null)
      [137] val idx = nodes.indexOf(node)
      [138] val ahead = if (idx > 0) nodes[idx - 1] else null
      [139] val behind = if (idx < nodes.size - 1) nodes[idx + 1] else null
    CALLS OUT:
      → Math.toRadians
      → active.isEmpty
      → computeProximity
      → computeSpan
      → cos
      → haversineMiles
      → node.copy
      → nodes.indexOf
      → sin

  [99] fun assignLeadTail(
    WORK FIELDS (local computed values):
      [104] val active = nodes.filter { it.status == ConvoyStatus.ACTIVE }
      [107] val leadNode = if (lockedLeadNodeId != null)
      [112] val tailNode = if (tailNodeId != null)
      [137] val idx = nodes.indexOf(node)
      [138] val ahead = if (idx > 0) nodes[idx - 1] else null
      [139] val behind = if (idx < nodes.size - 1) nodes[idx + 1] else null
      [140] val lead = nodes.firstOrNull { it.isLead }
      [141] val tail = nodes.firstOrNull { it.isTail }
      [164] val colorMap = nodes.associate { it.nodeId to it.markerColor }
      [171] val R = 3958.8
      [172] val dLat = Math.toRadians(lat2 - lat1)
      [173] val dLon = Math.toRadians(lon2 - lon1)
    CALLS OUT:
      → Math.toRadians
      → active.isEmpty
      → asin
      → computeProximity
      → computeSpan
      → cos
      → haversineMiles
      → node.copy
      → nodes.indexOf
      → pow
      → seg.copy
      → sin

  [130] fun computeSpan(lead: ConvoyNode?, tail: ConvoyNode?): Float {
    WORK FIELDS (local computed values):
      [137] val idx = nodes.indexOf(node)
      [138] val ahead = if (idx > 0) nodes[idx - 1] else null
      [139] val behind = if (idx < nodes.size - 1) nodes[idx + 1] else null
      [140] val lead = nodes.firstOrNull { it.isLead }
      [141] val tail = nodes.firstOrNull { it.isTail }
      [164] val colorMap = nodes.associate { it.nodeId to it.markerColor }
      [171] val R = 3958.8
      [172] val dLat = Math.toRadians(lat2 - lat1)
      [173] val dLon = Math.toRadians(lon2 - lon1)
      [174] val a = sin(dLat / 2).pow(2) +
    CALLS OUT:
      → Math.toRadians
      → asin
      → computeProximity
      → cos
      → haversineMiles
      → node.copy
      → nodes.indexOf
      → pow
      → seg.copy
      → sin

  [135] fun computeProximity(nodes: List<ConvoyNode>, myCartId: String): List<ConvoyNode> {
    WORK FIELDS (local computed values):
      [137] val idx = nodes.indexOf(node)
      [138] val ahead = if (idx > 0) nodes[idx - 1] else null
      [139] val behind = if (idx < nodes.size - 1) nodes[idx + 1] else null
      [140] val lead = nodes.firstOrNull { it.isLead }
      [141] val tail = nodes.firstOrNull { it.isTail }
      [164] val colorMap = nodes.associate { it.nodeId to it.markerColor }
      [171] val R = 3958.8
      [172] val dLat = Math.toRadians(lat2 - lat1)
      [173] val dLon = Math.toRadians(lon2 - lon1)
      [174] val a = sin(dLat / 2).pow(2) +
    CALLS OUT:
      → Math.toRadians
      → asin
      → cos
      → haversineMiles
      → node.copy
      → nodes.indexOf
      → pow
      → seg.copy
      → sin

  [160] fun colorSegmentsByNode(
    WORK FIELDS (local computed values):
      [164] val colorMap = nodes.associate { it.nodeId to it.markerColor }
      [171] val R = 3958.8
      [172] val dLat = Math.toRadians(lat2 - lat1)
      [173] val dLon = Math.toRadians(lon2 - lon1)
      [174] val a = sin(dLat / 2).pow(2) +
    CALLS OUT:
      → Math.toRadians
      → asin
      → cos
      → haversineMiles
      → pow
      → seg.copy
      → sin

  [170] fun haversineMiles(lat1: Double, lon1: Double, lat2: Double, lon2: Double): Float {
    WORK FIELDS (local computed values):
      [171] val R = 3958.8
      [172] val dLat = Math.toRadians(lat2 - lat1)
      [173] val dLon = Math.toRadians(lon2 - lon1)
      [174] val a = sin(dLat / 2).pow(2) +
    CALLS OUT:
      → Math.toRadians
      → asin
      → cos
      → pow
      → sin

========================================
FILE: ConvoyEnrollment.kt
========================================

  [54] fun ConvoyEnrollmentScreen(
    WORK FIELDS (local computed values):
      [58] val context = LocalContext.current
      [60] var firstName   by remember { mutableStateOf("") }
      [61] var lastName    by remember { mutableStateOf("") }
      [62] var email       by remember { mutableStateOf(initialEmail) }
      [63] var cellPhone   by remember { mutableStateOf("") }
      [64] var vehicleType by remember { mutableStateOf("") }
      [65] var userType    by remember { mutableStateOf(ConvoyUserType.RIDER) }
      [66] var errorMsg    by remember { mutableStateOf("") }
      [68] val allFilled = firstName.isNotBlank() && lastName.isNotBlank() &&
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Spacer
      → background
      → cellPhone.isNotBlank
      → email.isNotBlank
      → fillMaxSize
      → firstName.isNotBlank
      → imePadding
      → lastName.isNotBlank
      → mutableStateOf
      → padding
      → vehicleType.isNotBlank
      → verticalScroll
      → weight

  [291] fun ConvoyTextField(
    CALLS OUT:
      → Color
      → KeyboardActions
      → KeyboardOptions
      → OutlinedTextFieldDefaults.colors
      → Text

========================================
FILE: ConvoyEnrollmentQueue.kt
========================================

  [20] fun checkPendingDistributions(ctx: Context) {
    WORK FIELDS (local computed values):
      [23] // val response = ConvoyApiClient.getPendingInvites(userId)
      [63] // val intent = Intent(Intent.ACTION_INSERT).apply {
    CALLS OUT:
      → ConvoyApiClient.deleteFollow
      → ConvoyApiClient.getPendingInvites
      → ConvoyApiClient.postFollow
      → ConvoyApiClient.postOrganizerStatus
      → ConvoySessionManager.setOrganizer
      → ConvoyTileDownloader.downloadTiles
      → Log.d
      → Toast.makeText
      → addRideToCalendar
      → ctx.startActivity
      → distributeRideJson
      → followOrganizer
      → postOrganizerStatus
      → processSurveyQueue
      → putExtra

  [29] fun queueMapDownload(ctx: Context, rideId: String, boundsNorth: Double, boundsSouth: Double, boundsEast: Double, boundsWest: Double) {
    WORK FIELDS (local computed values):
      [63] // val intent = Intent(Intent.ACTION_INSERT).apply {
    CALLS OUT:
      → ConvoyApiClient.deleteFollow
      → ConvoyApiClient.postFollow
      → ConvoyApiClient.postOrganizerStatus
      → ConvoySessionManager.setOrganizer
      → ConvoyTileDownloader.downloadTiles
      → Log.d
      → Toast.makeText
      → addRideToCalendar
      → apply
      → ctx.getSharedPreferences
      → ctx.startActivity
      → edit
      → followOrganizer
      → getEnrollmentStatus
      → postOrganizerStatus

  [39] fun processMapQueue(ctx: Context) {
    WORK FIELDS (local computed values):
      [63] // val intent = Intent(Intent.ACTION_INSERT).apply {
    CALLS OUT:
      → ConvoyApiClient.deleteFollow
      → ConvoyApiClient.postFollow
      → ConvoyApiClient.postOrganizerStatus
      → ConvoySessionManager.setOrganizer
      → Log.d
      → Toast.makeText
      → addRideToCalendar
      → apply
      → ctx.getSharedPreferences
      → ctx.startActivity
      → edit
      → followOrganizer
      → getEnrollmentStatus
      → postOrganizerStatus
      → processSurveyQueue

  [46] fun setRadioConfigPending(ctx: Context, rideId: String) {
    WORK FIELDS (local computed values):
      [63] // val intent = Intent(Intent.ACTION_INSERT).apply {
    CALLS OUT:
      → ConvoyApiClient.deleteFollow
      → ConvoyApiClient.postFollow
      → ConvoyApiClient.postOrganizerStatus
      → ConvoySessionManager.setOrganizer
      → Log.d
      → Toast.makeText
      → addRideToCalendar
      → apply
      → ctx.getSharedPreferences
      → ctx.startActivity
      → edit
      → followOrganizer
      → getEnrollmentStatus
      → postOrganizerStatus
      → processSurveyQueue

  [53] fun setRadioConfigApplied(ctx: Context, rideId: String) {
    WORK FIELDS (local computed values):
      [63] // val intent = Intent(Intent.ACTION_INSERT).apply {
    CALLS OUT:
      → ConvoyApiClient.deleteFollow
      → ConvoyApiClient.postFollow
      → ConvoyApiClient.postOrganizerStatus
      → ConvoySessionManager.setOrganizer
      → Log.d
      → Toast.makeText
      → addRideToCalendar
      → apply
      → ctx.getSharedPreferences
      → ctx.startActivity
      → edit
      → followOrganizer
      → getEnrollmentStatus
      → postOrganizerStatus
      → processSurveyQueue

  [59] fun addRideToCalendar(ctx: Context, rideName: String, date: String, time: String, location: String = "") {
    WORK FIELDS (local computed values):
      [63] // val intent = Intent(Intent.ACTION_INSERT).apply {
    CALLS OUT:
      → ConvoyApiClient.deleteFollow
      → ConvoyApiClient.postFollow
      → ConvoyApiClient.postOrganizerStatus
      → ConvoySessionManager.setOrganizer
      → Log.d
      → Toast.makeText
      → apply
      → ctx.getSharedPreferences
      → ctx.startActivity
      → edit
      → followOrganizer
      → getEnrollmentStatus
      → postOrganizerStatus
      → processSurveyQueue
      → putExtra

  [72] fun postOrganizerStatus(ctx: Context) {
    CALLS OUT:
      → ConvoyApiClient.deleteFollow
      → ConvoyApiClient.postFollow
      → ConvoyApiClient.postOrganizerStatus
      → ConvoySessionManager.setOrganizer
      → Log.d
      → Toast.makeText
      → apply
      → ctx.getSharedPreferences
      → edit
      → followOrganizer
      → getEnrollmentStatus
      → processSurveyQueue
      → setEnrollmentStatus
      → show
      → unfollowOrganizer

  [80] fun processSurveyQueue(ctx: Context) {
    CALLS OUT:
      → ConvoyApiClient.deleteFollow
      → ConvoyApiClient.postFollow
      → Log.d
      → Toast.makeText
      → apply
      → ctx.getSharedPreferences
      → edit
      → followOrganizer
      → getEnrollmentStatus
      → setEnrollmentStatus
      → show
      → unfollowOrganizer

  [87] fun followOrganizer(ctx: Context, organizerId: String, organizerName: String) {
    CALLS OUT:
      → ConvoyApiClient.deleteFollow
      → ConvoyApiClient.postFollow
      → Log.d
      → Toast.makeText
      → apply
      → ctx.getSharedPreferences
      → edit
      → getEnrollmentStatus
      → setEnrollmentStatus
      → show
      → unfollowOrganizer

  [93] fun unfollowOrganizer(ctx: Context, organizerId: String, organizerName: String) {
    CALLS OUT:
      → ConvoyApiClient.deleteFollow
      → Log.d
      → Toast.makeText
      → apply
      → ctx.getSharedPreferences
      → edit
      → getEnrollmentStatus
      → setEnrollmentStatus
      → show

  [100] private fun setEnrollmentStatus(ctx: Context, rideId: String, task: String, status: String) {
    CALLS OUT:
      → apply
      → ctx.getSharedPreferences
      → edit
      → getEnrollmentStatus

  [105] fun getEnrollmentStatus(ctx: Context, rideId: String, task: String): String {
    CALLS OUT:
      → ctx.getSharedPreferences

========================================
FILE: ConvoyEventConfig.kt
========================================

  [55] fun toJson(): JSONObject = JSONObject().apply {
    CALLS OUT:
      → fromJson
      → put

  [89] fun fromJson(obj: JSONObject) = ConvoyEventConfig(
    WORK FIELDS (local computed values):
      [137] val fmt    = DateTimeFormatter.ISO_LOCAL_DATE
      [138] val today  = LocalDate.now()
      [139] val evDate = try { LocalDate.parse(eventDate, fmt) } catch (e: Exception) { today }
    CALLS OUT:
      → LocalDate.now
      → LocalDate.parse
      → UUID.randomUUID
      → channelName.trim
      → evDate.plusDays
      → eventDescription.trim
      → eventName.trim

  [126] fun createFromMaster(
    WORK FIELDS (local computed values):
      [137] val fmt    = DateTimeFormatter.ISO_LOCAL_DATE
      [138] val today  = LocalDate.now()
      [139] val evDate = try { LocalDate.parse(eventDate, fmt) } catch (e: Exception) { today }
      [180] val safeName = event.eventName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [181] val safeDate = event.eventDate.replace("/", "-").replace(" ", "_")
      [182] val fileName = "${event.eventId}_${safeName}_${safeDate}.json"
      [183] val file = java.io.File(eventsDir(context), fileName)
    CALLS OUT:
      → ConvoyEventConfig.fromJson
      → LocalDate.now
      → LocalDate.parse
      → Regex
      → UUID.randomUUID
      → channelName.trim
      → evDate.plusDays
      → event.toJson
      → eventDate.replace
      → eventDescription.trim
      → eventName.replace
      → eventName.trim
      → eventsDir
      → file.writeText
      → io.File

  [177] fun eventsDir(context: android.content.Context): java.io.File =
    WORK FIELDS (local computed values):
      [180] val safeName = event.eventName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [181] val safeDate = event.eventDate.replace("/", "-").replace(" ", "_")
      [182] val fileName = "${event.eventId}_${safeName}_${safeDate}.json"
      [183] val file = java.io.File(eventsDir(context), fileName)
    CALLS OUT:
      → ConvoyEventConfig.fromJson
      → Regex
      → event.toJson
      → eventDate.replace
      → eventName.replace
      → eventsDir
      → file.writeText
      → io.File
      → it.mkdirs
      → it.readText
      → json.JSONObject
      → loadAll
      → mkdirs
      → replace
      → save

  [179] fun save(context: android.content.Context, event: ConvoyEventConfig) {
    WORK FIELDS (local computed values):
      [180] val safeName = event.eventName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [181] val safeDate = event.eventDate.replace("/", "-").replace(" ", "_")
      [182] val fileName = "${event.eventId}_${safeName}_${safeDate}.json"
      [183] val file = java.io.File(eventsDir(context), fileName)
    CALLS OUT:
      → ConvoyEventConfig.fromJson
      → Regex
      → event.toJson
      → eventDate.replace
      → eventName.replace
      → eventsDir
      → file.writeText
      → io.File
      → it.readText
      → json.JSONObject
      → loadAll
      → mkdirs
      → replace

  [187] fun loadAll(context: android.content.Context): List<ConvoyEventConfig> {
    CALLS OUT:
      → ConvoyEventConfig.fromJson
      → eventsDir
      → it.readText
      → json.JSONObject

========================================
FILE: ConvoyExploreScreen.kt
========================================

  [15] fun ConvoyExploreScreen(onBack: () -> Unit) {
    CALLS OUT:
      → Box
      → Column
      → GroupTrackHeader
      → Text
      → background
      → onBack
      → padding
      → weight

========================================
FILE: ConvoyFieldRadioScreen.kt
========================================

  [40] fun ConvoyFieldRadioScreen(
    CALLS OUT:
      → Arrangement.spacedBy
      → GroupTrackHeader
      → background
      → fillMaxSize
      → fillMaxWidth
      → onBack
      → padding
      → weight

  [122] fun FieldRadioButton(
    WORK FIELDS (local computed values):
      [130] val bgColor = if (enabled) Color(0xFF1A3050) else Color(0xFF0F1E2E)
      [131] val textColor = if (enabled) Color.White else Color(0xFF334455)
      [132] val subColor = if (enabled) Color(0xFFAABBCC) else Color(0xFF223344)
    CALLS OUT:
      → Color
      → Row
      → Spacer
      → Text
      → background
      → clickable
      → clip
      → fillMaxWidth
      → height
      → lockedReason.isNotEmpty
      → onClick
      → padding

========================================
FILE: ConvoyFileReceiver.kt
========================================

  [30] override fun onCreate(savedInstanceState: Bundle?) {
    WORK FIELDS (local computed values):
      [34] val uri = intent?.data
      [44] val content = contentResolver.openInputStream(uri)?.use { input ->
      [62] val json = try {
      [70] val docType = json.optString("convoyDocType", "unknown")
      [74] val importDirName = when (docType) {
      [87] val importDir = File(filesDir, importDirName).also { it.mkdirs() }
      [88] val fileName = "convoy_${System.currentTimeMillis()}.convoy"
      [89] val destFile = File(importDir, fileName)
    CALLS OUT:
      → File
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → System.currentTimeMillis
      → content.contains
      → content.isNullOrBlank
      → contentResolver.openInputStream
      → destFile.writeText
      → finish
      → input.bufferedReader
      → it.mkdirs
      → readText
      → super.onCreate

========================================
FILE: ConvoyGpsService.kt
========================================

  [47] fun getService(): ConvoyGpsService = this@ConvoyGpsService
    WORK FIELDS (local computed values):
      [54] var state: State = State.IDLE
      [57] var currentTempFile: File? = null
      [61] var onLocationUpdate: ((lat: Double, lon: Double, alt: Double) -> Unit)? = null
      [63] var totalDistanceMiles: Double = 0.0
      [67] val R = 3958.8
      [68] val dLat = Math.toRadians(lat2 - lat1)
      [69] val dLon = Math.toRadians(lon2 - lon1)
      [70] val a = Math.sin(dLat/2)*Math.sin(dLat/2) + Math.cos(Math.toRadians(lat1))*Math.cos(Math.toRadians(lat2))*Math.sin(dLon/2)*Math.sin(dLon/2)
      [82] var lastLat: Double? = null
      [88] var onSleepTriggered: (() -> Unit)? = null
      [91] var lastLon: Double? = null
      [104] val intent = Intent(context, ConvoyGpsService::class.java)
    CALLS OUT:
      → LocalBinder
      → Log.i
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → context.startForegroundService
      → context.stopService
      → haversineFeet
      → haversineMiles
      → onBind
      → onCreate
      → onStartCommand

  [50] override fun onBind(intent: Intent): IBinder = binder
    WORK FIELDS (local computed values):
      [54] var state: State = State.IDLE
      [57] var currentTempFile: File? = null
      [61] var onLocationUpdate: ((lat: Double, lon: Double, alt: Double) -> Unit)? = null
      [63] var totalDistanceMiles: Double = 0.0
      [67] val R = 3958.8
      [68] val dLat = Math.toRadians(lat2 - lat1)
      [69] val dLon = Math.toRadians(lon2 - lon1)
      [70] val a = Math.sin(dLat/2)*Math.sin(dLat/2) + Math.cos(Math.toRadians(lat1))*Math.cos(Math.toRadians(lat2))*Math.sin(dLon/2)*Math.sin(dLon/2)
      [82] var lastLat: Double? = null
      [88] var onSleepTriggered: (() -> Unit)? = null
      [91] var lastLon: Double? = null
      [104] val intent = Intent(context, ConvoyGpsService::class.java)
    CALLS OUT:
      → Log.i
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → context.startForegroundService
      → context.stopService
      → haversineFeet
      → haversineMiles
      → onCreate
      → onStartCommand
      → pauseTrack
      → resumeTrack

  [66] private fun haversineMiles(lat1: Double, lon1: Double, lat2: Double, lon2: Double): Double {
    WORK FIELDS (local computed values):
      [67] val R = 3958.8
      [68] val dLat = Math.toRadians(lat2 - lat1)
      [69] val dLon = Math.toRadians(lon2 - lon1)
      [70] val a = Math.sin(dLat/2)*Math.sin(dLat/2) + Math.cos(Math.toRadians(lat1))*Math.cos(Math.toRadians(lat2))*Math.sin(dLon/2)*Math.sin(dLon/2)
      [82] var lastLat: Double? = null
      [88] var onSleepTriggered: (() -> Unit)? = null
      [91] var lastLon: Double? = null
      [104] val intent = Intent(context, ConvoyGpsService::class.java)
    CALLS OUT:
      → Log.i
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → closeGpx
      → closeKml
      → context.startForegroundService
      → context.stopService
      → haversineFeet
      → haversineMiles
      → onCreate

  [73] private fun haversineFeet(lat1: Double, lon1: Double, lat2: Double, lon2: Double): Double {
    WORK FIELDS (local computed values):
      [82] var lastLat: Double? = null
      [88] var onSleepTriggered: (() -> Unit)? = null
      [91] var lastLon: Double? = null
      [104] val intent = Intent(context, ConvoyGpsService::class.java)
      [147] val isGpx = ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX"
      [148] val tempFile = if (isGpx) createTempGpxFile() else createTempKmlFile()
    CALLS OUT:
      → Log.i
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → acquireWakeLock
      → closeGpx
      → closeKml
      → context.startForegroundService
      → context.stopService
      → createTempGpxFile
      → createTempKmlFile
      → haversineMiles
      → onCreate
      → onDestroy
      → onStartCommand
      → openGpxWriter

  [103] fun start(context: Context) {
    WORK FIELDS (local computed values):
      [104] val intent = Intent(context, ConvoyGpsService::class.java)
      [147] val isGpx = ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX"
      [148] val tempFile = if (isGpx) createTempGpxFile() else createTempKmlFile()
    CALLS OUT:
      → Log.i
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → acquireWakeLock
      → closeGpx
      → closeKml
      → context.startForegroundService
      → context.stopService
      → createTempGpxFile
      → createTempKmlFile
      → onCreate
      → onDestroy
      → onStartCommand
      → openGpxWriter
      → openKmlWriter

  [108] fun stop(context: Context) {
    WORK FIELDS (local computed values):
      [147] val isGpx = ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX"
      [148] val tempFile = if (isGpx) createTempGpxFile() else createTempKmlFile()
    CALLS OUT:
      → Log.i
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → acquireWakeLock
      → closeGpx
      → closeKml
      → context.stopService
      → createTempGpxFile
      → createTempKmlFile
      → onCreate
      → onDestroy
      → onStartCommand
      → openGpxWriter
      → openKmlWriter
      → pauseTrack

  [114] override fun onCreate() {
    WORK FIELDS (local computed values):
      [147] val isGpx = ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX"
      [148] val tempFile = if (isGpx) createTempGpxFile() else createTempKmlFile()
    CALLS OUT:
      → Log.i
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → acquireWakeLock
      → closeGpx
      → closeKml
      → createTempGpxFile
      → createTempKmlFile
      → onDestroy
      → onStartCommand
      → openGpxWriter
      → openKmlWriter
      → pauseTrack
      → releaseWakeLock
      → resumeTrack

  [120] override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
    WORK FIELDS (local computed values):
      [147] val isGpx = ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX"
      [148] val tempFile = if (isGpx) createTempGpxFile() else createTempKmlFile()
      [199] val file = currentTempFile
    CALLS OUT:
      → Log.i
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → acquireWakeLock
      → closeGpx
      → closeKml
      → createTempGpxFile
      → createTempKmlFile
      → onDestroy
      → openGpxWriter
      → openKmlWriter
      → pauseTrack
      → releaseWakeLock
      → resumeTrack
      → startForeground

  [131] override fun onDestroy() {
    WORK FIELDS (local computed values):
      [147] val isGpx = ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX"
      [148] val tempFile = if (isGpx) createTempGpxFile() else createTempKmlFile()
      [199] val file = currentTempFile
    CALLS OUT:
      → Log.i
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → acquireWakeLock
      → closeGpx
      → closeKml
      → createTempGpxFile
      → createTempKmlFile
      → openGpxWriter
      → openKmlWriter
      → pauseTrack
      → releaseWakeLock
      → resumeTrack
      → startLocationUpdates
      → startTrack

  [145] fun startTrack() {
    WORK FIELDS (local computed values):
      [147] val isGpx = ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX"
      [148] val tempFile = if (isGpx) createTempGpxFile() else createTempKmlFile()
      [199] val file = currentTempFile
      [216] val sdf = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US)
      [217] val safeName = trackName.trim().replace(Regex("[^a-zA-Z0-9_\\- ]"), "").replace(" ", "_")
      [218] val ext = if (ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX") "gpx" else "kml"
      [219] val finalName = "${safeName}_${sdf.format(Date())}.$ext"
      [220] val finalFile = File(tempFile.parent, finalName)
      [221] val success = tempFile.renameTo(finalFile)
    CALLS OUT:
      → Date
      → File
      → Log.i
      → SimpleDateFormat
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → acquireWakeLock
      → closeGpx
      → closeKml
      → createTempGpxFile
      → createTempKmlFile
      → finalizeTrack
      → openGpxWriter
      → openKmlWriter
      → pauseTrack

  [161] fun pauseTrack() {
    WORK FIELDS (local computed values):
      [199] val file = currentTempFile
      [216] val sdf = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US)
      [217] val safeName = trackName.trim().replace(Regex("[^a-zA-Z0-9_\\- ]"), "").replace(" ", "_")
      [218] val ext = if (ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX") "gpx" else "kml"
      [219] val finalName = "${safeName}_${sdf.format(Date())}.$ext"
      [220] val finalFile = File(tempFile.parent, finalName)
      [221] val success = tempFile.renameTo(finalFile)
      [238] var useRadioGps: Boolean = false
    CALLS OUT:
      → Date
      → File
      → Log.e
      → Log.i
      → SimpleDateFormat
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → acquireWakeLock
      → closeGpx
      → closeKml
      → finalizeTrack
      → releaseWakeLock
      → replace
      → resumeTrack
      → startLocationUpdates

  [173] fun resumeTrack() {
    WORK FIELDS (local computed values):
      [199] val file = currentTempFile
      [216] val sdf = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US)
      [217] val safeName = trackName.trim().replace(Regex("[^a-zA-Z0-9_\\- ]"), "").replace(" ", "_")
      [218] val ext = if (ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX") "gpx" else "kml"
      [219] val finalName = "${safeName}_${sdf.format(Date())}.$ext"
      [220] val finalFile = File(tempFile.parent, finalName)
      [221] val success = tempFile.renameTo(finalFile)
      [238] var useRadioGps: Boolean = false
      [253] val lm = getSystemService(Context.LOCATION_SERVICE) as LocationManager
    CALLS OUT:
      → Date
      → File
      → Log.e
      → Log.i
      → SimpleDateFormat
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → acquireWakeLock
      → closeGpx
      → closeKml
      → finalizeTrack
      → getSystemService
      → onGpsUpdate
      → onRadioPosition
      → releaseWakeLock

  [181] fun wakeSleep() {
    WORK FIELDS (local computed values):
      [199] val file = currentTempFile
      [216] val sdf = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US)
      [217] val safeName = trackName.trim().replace(Regex("[^a-zA-Z0-9_\\- ]"), "").replace(" ", "_")
      [218] val ext = if (ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX") "gpx" else "kml"
      [219] val finalName = "${safeName}_${sdf.format(Date())}.$ext"
      [220] val finalFile = File(tempFile.parent, finalName)
      [221] val success = tempFile.renameTo(finalFile)
      [238] var useRadioGps: Boolean = false
      [253] val lm = getSystemService(Context.LOCATION_SERVICE) as LocationManager
    CALLS OUT:
      → Date
      → File
      → Log.e
      → Log.i
      → Log.w
      → SimpleDateFormat
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → allProviders.contains
      → closeGpx
      → closeKml
      → finalizeTrack
      → getSystemService
      → onGpsUpdate
      → onRadioPosition

  [194] fun stopTrack(): File? {
    WORK FIELDS (local computed values):
      [199] val file = currentTempFile
      [216] val sdf = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US)
      [217] val safeName = trackName.trim().replace(Regex("[^a-zA-Z0-9_\\- ]"), "").replace(" ", "_")
      [218] val ext = if (ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX") "gpx" else "kml"
      [219] val finalName = "${safeName}_${sdf.format(Date())}.$ext"
      [220] val finalFile = File(tempFile.parent, finalName)
      [221] val success = tempFile.renameTo(finalFile)
      [238] var useRadioGps: Boolean = false
      [253] val lm = getSystemService(Context.LOCATION_SERVICE) as LocationManager
      [262] val listener = LocationListener { loc ->
    CALLS OUT:
      → Date
      → File
      → Log.e
      → Log.i
      → Log.w
      → SimpleDateFormat
      → TRACK_EXPORT_FORMAT.uppercase
      → allProviders.contains
      → closeGpx
      → closeKml
      → finalizeTrack
      → getSystemService
      → lm.requestLocationUpdates
      → onGpsUpdate
      → onRadioPosition

  [214] fun finalizeTrack(tempFile: File, trackName: String): File? {
    WORK FIELDS (local computed values):
      [216] val sdf = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US)
      [217] val safeName = trackName.trim().replace(Regex("[^a-zA-Z0-9_\\- ]"), "").replace(" ", "_")
      [218] val ext = if (ConvoyConfig.TRACK_EXPORT_FORMAT.uppercase() == "GPX") "gpx" else "kml"
      [219] val finalName = "${safeName}_${sdf.format(Date())}.$ext"
      [220] val finalFile = File(tempFile.parent, finalName)
      [221] val success = tempFile.renameTo(finalFile)
      [238] var useRadioGps: Boolean = false
      [253] val lm = getSystemService(Context.LOCATION_SERVICE) as LocationManager
      [262] val listener = LocationListener { loc ->
      [291] val distFeet = if (lastMoveLat != 0.0) haversineFeet(lastMoveLat, lastMoveLon, lat, lon) else 999.0
    CALLS OUT:
      → Date
      → File
      → Log.e
      → Log.i
      → Log.w
      → SimpleDateFormat
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → allProviders.contains
      → getSystemService
      → haversineFeet
      → lm.requestLocationUpdates
      → onGpsUpdate
      → onRadioPosition
      → removeUpdates

  [245] fun onRadioPosition(lat: Double, lon: Double, alt: Double) {
    WORK FIELDS (local computed values):
      [253] val lm = getSystemService(Context.LOCATION_SERVICE) as LocationManager
      [262] val listener = LocationListener { loc ->
      [291] val distFeet = if (lastMoveLat != 0.0) haversineFeet(lastMoveLat, lastMoveLon, lat, lon) else 999.0
      [304] val prevLat = lastLat
      [305] val prevLon = lastLon
      [323] val dir = File(
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Log.e
      → Log.i
      → Log.w
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → allProviders.contains
      → createTempKmlFile
      → getSystemService
      → haversineFeet
      → haversineMiles
      → invoke
      → lm.requestLocationUpdates
      → onGpsUpdate
      → pauseTrack

  [251] private fun startLocationUpdates() {
    WORK FIELDS (local computed values):
      [253] val lm = getSystemService(Context.LOCATION_SERVICE) as LocationManager
      [262] val listener = LocationListener { loc ->
      [291] val distFeet = if (lastMoveLat != 0.0) haversineFeet(lastMoveLat, lastMoveLon, lat, lon) else 999.0
      [304] val prevLat = lastLat
      [305] val prevLon = lastLon
      [323] val dir = File(
      [327] val sdf = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US)
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → File
      → Log.e
      → Log.i
      → Log.w
      → SimpleDateFormat
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → allProviders.contains
      → createTempKmlFile
      → dir.exists
      → dir.mkdirs
      → getSystemService
      → haversineFeet
      → haversineMiles

  [278] private fun stopLocationUpdates() {
    WORK FIELDS (local computed values):
      [291] val distFeet = if (lastMoveLat != 0.0) haversineFeet(lastMoveLat, lastMoveLon, lat, lon) else 999.0
      [304] val prevLat = lastLat
      [305] val prevLon = lastLon
      [323] val dir = File(
      [327] val sdf = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US)
      [333] val writer = BufferedWriter(FileWriter(file))
    CALLS OUT:
      → BufferedWriter
      → Environment.getExternalStoragePublicDirectory
      → File
      → Log.e
      → Log.i
      → SimpleDateFormat
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → closeKml
      → createTempKmlFile
      → dir.exists
      → dir.mkdirs
      → flush
      → haversineFeet
      → haversineMiles

  [288] private fun onGpsUpdate(lat: Double, lon: Double, alt: Double) {
    WORK FIELDS (local computed values):
      [291] val distFeet = if (lastMoveLat != 0.0) haversineFeet(lastMoveLat, lastMoveLon, lat, lon) else 999.0
      [304] val prevLat = lastLat
      [305] val prevLon = lastLon
      [323] val dir = File(
      [327] val sdf = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US)
      [333] val writer = BufferedWriter(FileWriter(file))
    CALLS OUT:
      → BufferedWriter
      → Environment.getExternalStoragePublicDirectory
      → File
      → Log.e
      → Log.i
      → SimpleDateFormat
      → System.currentTimeMillis
      → TRACK_EXPORT_FORMAT.uppercase
      → close
      → closeKml
      → createTempKmlFile
      → dir.exists
      → dir.mkdirs
      → flush
      → haversineFeet

  [322] private fun createTempKmlFile(): File {
    WORK FIELDS (local computed values):
      [323] val dir = File(
      [327] val sdf = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US)
      [333] val writer = BufferedWriter(FileWriter(file))
      [371] val dir = java.io.File(
      [375] val sdf = java.text.SimpleDateFormat("yyyyMMdd_HHmmss", java.util.Locale.US)
      [381] val writer = java.io.BufferedWriter(java.io.FileWriter(file))
      [395] val ts = java.text.SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss'Z'", java.util.Locale.US).format(java.util.Date())
    CALLS OUT:
      → BufferedWriter
      → Environment.getExternalStoragePublicDirectory
      → File
      → Log.e
      → SimpleDateFormat
      → close
      → closeKml
      → createTempGpxFile
      → dir.exists
      → dir.mkdirs
      → flush
      → io.BufferedWriter
      → io.File
      → io.FileWriter
      → openGpxWriter

  [331] private fun openKmlWriter(file: File) {
    WORK FIELDS (local computed values):
      [333] val writer = BufferedWriter(FileWriter(file))
      [371] val dir = java.io.File(
      [375] val sdf = java.text.SimpleDateFormat("yyyyMMdd_HHmmss", java.util.Locale.US)
      [381] val writer = java.io.BufferedWriter(java.io.FileWriter(file))
      [395] val ts = java.text.SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss'Z'", java.util.Locale.US).format(java.util.Date())
    CALLS OUT:
      → BufferedWriter
      → Environment.getExternalStoragePublicDirectory
      → Log.e
      → close
      → closeGpx
      → closeKml
      → createTempGpxFile
      → dir.exists
      → dir.mkdirs
      → flush
      → io.BufferedWriter
      → io.File
      → io.FileWriter
      → openGpxWriter
      → text.SimpleDateFormat

  [346] private fun writeKmlPoint(lat: Double, lon: Double, alt: Double) {
    WORK FIELDS (local computed values):
      [371] val dir = java.io.File(
      [375] val sdf = java.text.SimpleDateFormat("yyyyMMdd_HHmmss", java.util.Locale.US)
      [381] val writer = java.io.BufferedWriter(java.io.FileWriter(file))
      [395] val ts = java.text.SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss'Z'", java.util.Locale.US).format(java.util.Date())
      [418] val pm = getSystemService(Context.POWER_SERVICE) as PowerManager
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Log.e
      → Log.i
      → acquireWakeLock
      → close
      → closeGpx
      → closeKml
      → createTempGpxFile
      → dir.exists
      → dir.mkdirs
      → flush
      → getSystemService
      → io.BufferedWriter
      → io.File
      → io.FileWriter

  [355] private fun closeKml() {
    WORK FIELDS (local computed values):
      [371] val dir = java.io.File(
      [375] val sdf = java.text.SimpleDateFormat("yyyyMMdd_HHmmss", java.util.Locale.US)
      [381] val writer = java.io.BufferedWriter(java.io.FileWriter(file))
      [395] val ts = java.text.SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss'Z'", java.util.Locale.US).format(java.util.Date())
      [418] val pm = getSystemService(Context.POWER_SERVICE) as PowerManager
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Log.e
      → Log.i
      → acquireWakeLock
      → close
      → closeGpx
      → createTempGpxFile
      → dir.exists
      → dir.mkdirs
      → flush
      → getSystemService
      → io.BufferedWriter
      → io.File
      → io.FileWriter
      → it.acquire

  [370] private fun createTempGpxFile(): java.io.File {
    WORK FIELDS (local computed values):
      [371] val dir = java.io.File(
      [375] val sdf = java.text.SimpleDateFormat("yyyyMMdd_HHmmss", java.util.Locale.US)
      [381] val writer = java.io.BufferedWriter(java.io.FileWriter(file))
      [395] val ts = java.text.SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss'Z'", java.util.Locale.US).format(java.util.Date())
      [418] val pm = getSystemService(Context.POWER_SERVICE) as PowerManager
      [446] val channel = NotificationChannel(
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Log.e
      → Log.i
      → acquireWakeLock
      → close
      → closeGpx
      → dir.exists
      → dir.mkdirs
      → flush
      → getSystemService
      → io.BufferedWriter
      → io.File
      → io.FileWriter
      → it.acquire
      → openGpxWriter

  [379] private fun openGpxWriter(file: java.io.File) {
    WORK FIELDS (local computed values):
      [381] val writer = java.io.BufferedWriter(java.io.FileWriter(file))
      [395] val ts = java.text.SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss'Z'", java.util.Locale.US).format(java.util.Date())
      [418] val pm = getSystemService(Context.POWER_SERVICE) as PowerManager
      [446] val channel = NotificationChannel(
      [454] val nm = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
      [459] val pauseIntent = PendingIntent.getService(
    CALLS OUT:
      → Log.e
      → Log.i
      → acquireWakeLock
      → close
      → closeGpx
      → flush
      → getSystemService
      → io.BufferedWriter
      → io.FileWriter
      → it.acquire
      → pm.newWakeLock
      → release
      → releaseWakeLock
      → setShowBadge
      → text.SimpleDateFormat

  [393] private fun writeGpxPoint(lat: Double, lon: Double, alt: Double) {
    WORK FIELDS (local computed values):
      [395] val ts = java.text.SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss'Z'", java.util.Locale.US).format(java.util.Date())
      [418] val pm = getSystemService(Context.POWER_SERVICE) as PowerManager
      [446] val channel = NotificationChannel(
      [454] val nm = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
      [459] val pauseIntent = PendingIntent.getService(
      [464] val resumeIntent = PendingIntent.getService(
      [469] val endIntent = PendingIntent.getService(
    CALLS OUT:
      → Log.e
      → Log.i
      → acquireWakeLock
      → close
      → closeGpx
      → flush
      → getSystemService
      → it.acquire
      → pm.newWakeLock
      → release
      → releaseWakeLock
      → setShowBadge
      → text.SimpleDateFormat
      → write

  [403] private fun closeGpx() {
    WORK FIELDS (local computed values):
      [418] val pm = getSystemService(Context.POWER_SERVICE) as PowerManager
      [446] val channel = NotificationChannel(
      [454] val nm = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
      [459] val pauseIntent = PendingIntent.getService(
      [464] val resumeIntent = PendingIntent.getService(
      [469] val endIntent = PendingIntent.getService(
      [475] val builder = Notification.Builder(this, CHANNEL_ID)
    CALLS OUT:
      → Log.e
      → Log.i
      → acquireWakeLock
      → builder.setContentText
      → builder.setContentTitle
      → close
      → flush
      → getSystemService
      → it.acquire
      → pm.newWakeLock
      → release
      → releaseWakeLock
      → setOngoing
      → setOnlyAlertOnce
      → setShowBadge

  [416] private fun acquireWakeLock() {
    WORK FIELDS (local computed values):
      [418] val pm = getSystemService(Context.POWER_SERVICE) as PowerManager
      [446] val channel = NotificationChannel(
      [454] val nm = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
      [459] val pauseIntent = PendingIntent.getService(
      [464] val resumeIntent = PendingIntent.getService(
      [469] val endIntent = PendingIntent.getService(
      [475] val builder = Notification.Builder(this, CHANNEL_ID)
    CALLS OUT:
      → Action.Builder
      → Log.e
      → Log.i
      → build
      → builder.addAction
      → builder.setContentText
      → builder.setContentTitle
      → getSystemService
      → it.acquire
      → pm.newWakeLock
      → release
      → releaseWakeLock
      → setOngoing
      → setOnlyAlertOnce
      → setShowBadge

  [431] private fun releaseWakeLock() {
    WORK FIELDS (local computed values):
      [446] val channel = NotificationChannel(
      [454] val nm = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
      [459] val pauseIntent = PendingIntent.getService(
      [464] val resumeIntent = PendingIntent.getService(
      [469] val endIntent = PendingIntent.getService(
      [475] val builder = Notification.Builder(this, CHANNEL_ID)
      [502] val nm = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
    CALLS OUT:
      → Action.Builder
      → Log.e
      → Log.i
      → build
      → builder.addAction
      → builder.build
      → builder.setContentText
      → builder.setContentTitle
      → getSystemService
      → release
      → setOngoing
      → setOnlyAlertOnce
      → setShowBadge
      → setSmallIcon
      → stopForeground

  [445] private fun createNotificationChannel() {
    WORK FIELDS (local computed values):
      [446] val channel = NotificationChannel(
      [454] val nm = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
      [459] val pauseIntent = PendingIntent.getService(
      [464] val resumeIntent = PendingIntent.getService(
      [469] val endIntent = PendingIntent.getService(
      [475] val builder = Notification.Builder(this, CHANNEL_ID)
      [502] val nm = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
    CALLS OUT:
      → Action.Builder
      → build
      → builder.addAction
      → builder.build
      → builder.setContentText
      → builder.setContentTitle
      → getSystemService
      → setOngoing
      → setOnlyAlertOnce
      → setShowBadge
      → setSmallIcon
      → stopForeground
      → stopSelf

  [458] private fun buildNotification(): Notification {
    WORK FIELDS (local computed values):
      [459] val pauseIntent = PendingIntent.getService(
      [464] val resumeIntent = PendingIntent.getService(
      [469] val endIntent = PendingIntent.getService(
      [475] val builder = Notification.Builder(this, CHANNEL_ID)
      [502] val nm = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
    CALLS OUT:
      → Action.Builder
      → build
      → builder.addAction
      → builder.build
      → builder.setContentText
      → builder.setContentTitle
      → getSystemService
      → setOngoing
      → setOnlyAlertOnce
      → setSmallIcon
      → stopForeground
      → stopSelf

  [501] private fun updateNotification() {
    WORK FIELDS (local computed values):
      [502] val nm = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
    CALLS OUT:
      → getSystemService
      → stopForeground
      → stopSelf

========================================
FILE: ConvoyInviteHandler.kt
========================================

  [38] override fun onCreate(savedInstanceState: Bundle?) {
    WORK FIELDS (local computed values):
      [41] val uri = intent?.data
      [51] val token = uri.lastPathSegment
      [67] val downloadResult = ConvoyApiClient.downloadRide(token)
      [73] val json = JSONObject(rideJson)
      [74] val eventConfig = ConvoyEventConfig.fromJson(json)
      [81] val userId = getUserId(this@ConvoyInviteHandler)
      [83] val enrollResult = ConvoyApiClient.enrollRider(
      [96] val mapBoundsNorth = json.optDouble("map_bounds_north", Double.NaN)
      [97] val mapBoundsSouth = json.optDouble("map_bounds_south", Double.NaN)
      [98] val mapBoundsEast  = json.optDouble("map_bounds_east",  Double.NaN)
      [99] val mapBoundsWest  = json.optDouble("map_bounds_west",  Double.NaN)
    CALLS OUT:
      → ConvoyApiClient.downloadRide
      → ConvoyApiClient.enrollRider
      → ConvoyEventConfig.fromJson
      → ConvoyEventStore.save
      → CoroutineScope
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → downloadResult.fold
      → enrollResult.fold
      → finish
      → getUserId
      → mapBoundsEast.isNaN
      → mapBoundsNorth.isNaN

  [65] private suspend fun processInvite(token: String) {
    WORK FIELDS (local computed values):
      [67] val downloadResult = ConvoyApiClient.downloadRide(token)
      [73] val json = JSONObject(rideJson)
      [74] val eventConfig = ConvoyEventConfig.fromJson(json)
      [81] val userId = getUserId(this@ConvoyInviteHandler)
      [83] val enrollResult = ConvoyApiClient.enrollRider(
      [96] val mapBoundsNorth = json.optDouble("map_bounds_north", Double.NaN)
      [97] val mapBoundsSouth = json.optDouble("map_bounds_south", Double.NaN)
      [98] val mapBoundsEast  = json.optDouble("map_bounds_east",  Double.NaN)
      [99] val mapBoundsWest  = json.optDouble("map_bounds_west",  Double.NaN)
    CALLS OUT:
      → ConvoyApiClient.downloadRide
      → ConvoyApiClient.enrollRider
      → ConvoyEventConfig.fromJson
      → ConvoyEventStore.save
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → context.getSharedPreferences
      → downloadResult.fold
      → edit
      → enrollResult.fold
      → finish
      → getUserId
      → mapBoundsEast.isNaN

  [133] fun queueMapDownload(
    WORK FIELDS (local computed values):
      [157] val prefs = context.getSharedPreferences("grouptrack_pending_download", Context.MODE_PRIVATE)
      [177] val north: Double,
      [178] val south: Double,
      [179] val east: Double,
      [180] val west: Double,
      [181] val tileSource: String,
      [182] val zoomMax: Int
    CALLS OUT:
      → Log.i
      → apply
      → clear
      → clearPendingDownload
      → context.getSharedPreferences
      → edit
      → getPendingDownload

  [156] fun getPendingDownload(context: Context): PendingMapDownload? {
    WORK FIELDS (local computed values):
      [157] val prefs = context.getSharedPreferences("grouptrack_pending_download", Context.MODE_PRIVATE)
      [177] val north: Double,
      [178] val south: Double,
      [179] val east: Double,
      [180] val west: Double,
      [181] val tileSource: String,
      [182] val zoomMax: Int
    CALLS OUT:
      → apply
      → clear
      → clearPendingDownload
      → context.getSharedPreferences
      → edit

  [169] fun clearPendingDownload(context: Context) {
    WORK FIELDS (local computed values):
      [177] val north: Double,
      [178] val south: Double,
      [179] val east: Double,
      [180] val west: Double,
      [181] val tileSource: String,
      [182] val zoomMax: Int
    CALLS OUT:
      → apply
      → clear
      → context.getSharedPreferences
      → edit

========================================
FILE: ConvoyInviteSendScreen.kt
========================================

  [26] fun ConvoyInviteSendScreen(
    WORK FIELDS (local computed values):
      [31] val context = LocalContext.current
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → GroupTrackHeader
      → NavBtn3
      → Row
      → Text
      → Toast.makeText
      → background
      → clip
      → height
      → onBack
      → onClick
      → onNavigateToCreateRide

  [84] private fun NavBtn3(icon: String, label: String, onClick: () -> Unit) {
    CALLS OUT:
      → Text
      → onClick
      → padding

========================================
FILE: ConvoyMapBar.kt
========================================

  [30] fun ConvoyMapBar(
    WORK FIELDS (local computed values):
      [78] val isActive = activeSource == label
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → RoundedCornerShape
      → Surface
      → height
      → onNavigate
      → onSourceChange
      → padding

========================================
FILE: ConvoyMapSourceScreen.kt
========================================

  [71] fun ConvoyMapSourceScreen(
    WORK FIELDS (local computed values):
      [74] val allSources = remember { MapSourceManager.getAllSources() }
      [75] val context = LocalContext.current
      [76] val slots = remember { MapSourceManager.getSlotAssignments() }
      [77] var selectedSlot by remember { mutableStateOf<String?>(null) }
      [78] var showApiKeyDialog by remember { mutableStateOf<String?>(null) }
      [81] var refreshSlot by remember { mutableStateOf<String?>(null) }
      [82] var refreshTileCount by remember { mutableStateOf(0) }
      [83] var refreshEnqueued by remember { mutableStateOf(false) }
      [84] val scope = rememberCoroutineScope()
      [87] var satSourceId by remember {
      [90] var topoSourceId by remember {
      [93] var topoPlusSourceId by remember {
    CALLS OUT:
      → ConvoyTileDownloader.scanTilesToKeys
      → Icon
      → IconButton
      → SectionHeader
      → Text
      → applySource
      → currentSourceId
      → fillMaxSize
      → mutableStateOf
      → padding
      → rememberCoroutineScope
      → verticalScroll
      → withContext

  [97] fun currentSourceId(slot: String): String = when (slot) {
    WORK FIELDS (local computed values):
      [113] val checkSlot = slot
      [115] val count = withContext(Dispatchers.IO) {
      [156] val sourceId = currentSourceId(slotKey)
      [157] val source = allSources.find { it.id == sourceId }
    CALLS OUT:
      → ConvoyTileDownloader.scanTilesToKeys
      → HorizontalDivider
      → Icon
      → IconButton
      → SectionHeader
      → Spacer
      → Text
      → applySource
      → currentSourceId
      → fillMaxSize
      → listOf
      → padding
      → verticalScroll
      → withContext

  [104] fun applySource(slot: String, sourceId: String) {
    WORK FIELDS (local computed values):
      [113] val checkSlot = slot
      [115] val count = withContext(Dispatchers.IO) {
      [156] val sourceId = currentSourceId(slotKey)
      [157] val source = allSources.find { it.id == sourceId }
      [182] val hasKey = MapSourceManager.getApiKey(source.id).isNotEmpty()
    CALLS OUT:
      → ConvoyTileDownloader.scanTilesToKeys
      → HorizontalDivider
      → Icon
      → IconButton
      → SectionHeader
      → Spacer
      → Text
      → currentSourceId
      → fillMaxSize
      → isNotEmpty
      → listOf
      → padding
      → verticalScroll
      → withContext

  [322] private fun SectionHeader(title: String) {
    CALLS OUT:
      → Box
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → Text
      → clickable
      → fillMaxWidth
      → padding
      → then

  [332] private fun SlotCard(
    CALLS OUT:
      → Box
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → Text
      → clickable
      → fillMaxWidth
      → padding
      → then

  [389] private fun SourceRow(
    CALLS OUT:
      → Column
      → RoundedCornerShape
      → Spacer
      → Text
      → background
      → border
      → clickable
      → clip
      → fillMaxWidth
      → padding
      → size
      → then

  [452] private fun ApiKeyRow(
    WORK FIELDS (local computed values):
      [496] var keyText by remember { mutableStateOf(currentKey) }
    CALLS OUT:
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → Text
      → TextButton
      → clickable
      → fillMaxWidth
      → keyText.isNotBlank
      → mutableStateOf
      → onSave
      → padding

  [489] private fun ApiKeyDialog(
    WORK FIELDS (local computed values):
      [496] var keyText by remember { mutableStateOf(currentKey) }
    CALLS OUT:
      → Spacer
      → Text
      → TextButton
      → keyText.isNotBlank
      → mutableStateOf
      → onSave

========================================
FILE: ConvoyMapViewerScreen.kt
========================================

  [58] fun ConvoyMapViewerScreen(
    WORK FIELDS (local computed values):
      [65] val context = LocalContext.current
      [66] var webViewRef by remember { mutableStateOf<WebView?>(null) }
      [76] var activeSource by remember { mutableStateOf("SAT") }
      [77] var trailsOn by remember { mutableStateOf(false) }
      [78] var trailsLoaded by remember { mutableStateOf(false) }
      [79] var showTrackPanel by remember { mutableStateOf(false) }
      [80] var showTrackMenu by remember { mutableStateOf(false) }
      [81] var trackFileList by remember { mutableStateOf<List<String>>(emptyList()) }
      [82] var loadedTracks by remember { mutableStateOf<List<Pair<String, String>>>(emptyList()) }
      [83] var trackSearchText by remember { mutableStateOf("") }
      [84] val trackColors = listOf("#39FF14")
      [85] var nextColorIdx by remember { mutableIntStateOf(0) }
    CALLS OUT:
      → DisposableEffect
      → Environment.getExternalStoragePublicDirectory
      → append
      → coroutines.MainScope
      → coroutines.withContext
      → destroy
      → dir.listFiles
      → extension.lowercase
      → hiltViewModel
      → io.File
      → launch
      → listOf
      → mutableStateOf
      → name.startsWith
      → parts.add

  [370] fun onAreaSelected(north: Double, south: Double, east: Double, west: Double) {
    WORK FIELDS (local computed values):
      [383] val radius = 0.002 // ~200 meters in degrees
      [384] val south = lat - radius; val north = lat + radius
      [385] val west = lon - radius; val east = lon + radius
      [389] val names = mutableListOf<String>()
      [392] val trails = SpatialDbManager.queryTrailsByViewport(south, west, north, east, 50)
      [397] val tracks = SpatialDbManager.queryTracksByViewport(south, west, north, east, 50)
      [402] val wpts = SpatialDbManager.queryWaypointsByViewport(south, west, north, east, 50)
      [407] val routes = SpatialDbManager.queryRoutesByViewport(south, west, north, east, 50)
      [411] val html = names.joinToString("<br>")
      [412] val escaped = html.replace("'", "\\'")
      [426] val wv = webViewRef ?: return@post
      [427] val src = ConvoyConfig.ACTIVE_TILE_SOURCE
    CALLS OUT:
      → DownloadBbox
      → Log.e
      → Log.i
      → Looper.getMainLooper
      → SpatialDbManager.init
      → SpatialDbManager.queryRoutesByViewport
      → SpatialDbManager.queryTracksByViewport
      → SpatialDbManager.queryTrailsByViewport
      → SpatialDbManager.queryWaypointsByViewport
      → html.replace
      → names.add
      → names.isNotEmpty
      → onProximityTap
      → onViewportChanged
      → os.Handler

  [379] fun onMapBoundsReady(n: Double, s: Double, e: Double, w: Double) {}
    WORK FIELDS (local computed values):
      [383] val radius = 0.002 // ~200 meters in degrees
      [384] val south = lat - radius; val north = lat + radius
      [385] val west = lon - radius; val east = lon + radius
      [389] val names = mutableListOf<String>()
      [392] val trails = SpatialDbManager.queryTrailsByViewport(south, west, north, east, 50)
      [397] val tracks = SpatialDbManager.queryTracksByViewport(south, west, north, east, 50)
      [402] val wpts = SpatialDbManager.queryWaypointsByViewport(south, west, north, east, 50)
      [407] val routes = SpatialDbManager.queryRoutesByViewport(south, west, north, east, 50)
      [411] val html = names.joinToString("<br>")
      [412] val escaped = html.replace("'", "\\'")
      [426] val wv = webViewRef ?: return@post
      [427] val src = ConvoyConfig.ACTIVE_TILE_SOURCE
    CALLS OUT:
      → Log.e
      → Looper.getMainLooper
      → SpatialDbManager.buildTrailGeoJson
      → SpatialDbManager.init
      → SpatialDbManager.queryRoutesByViewport
      → SpatialDbManager.queryTracksByViewport
      → SpatialDbManager.queryTrailsByViewport
      → SpatialDbManager.queryWaypointsByViewport
      → html.replace
      → names.add
      → names.isNotEmpty
      → onProximityTap
      → onViewportChanged
      → os.Handler
      → setOverlayLayers

  [381] fun onProximityTap(lat: Double, lon: Double) {
    WORK FIELDS (local computed values):
      [383] val radius = 0.002 // ~200 meters in degrees
      [384] val south = lat - radius; val north = lat + radius
      [385] val west = lon - radius; val east = lon + radius
      [389] val names = mutableListOf<String>()
      [392] val trails = SpatialDbManager.queryTrailsByViewport(south, west, north, east, 50)
      [397] val tracks = SpatialDbManager.queryTracksByViewport(south, west, north, east, 50)
      [402] val wpts = SpatialDbManager.queryWaypointsByViewport(south, west, north, east, 50)
      [407] val routes = SpatialDbManager.queryRoutesByViewport(south, west, north, east, 50)
      [411] val html = names.joinToString("<br>")
      [412] val escaped = html.replace("'", "\\'")
      [426] val wv = webViewRef ?: return@post
      [427] val src = ConvoyConfig.ACTIVE_TILE_SOURCE
    CALLS OUT:
      → Log.e
      → Looper.getMainLooper
      → SpatialDbManager.buildTrailGeoJson
      → SpatialDbManager.init
      → SpatialDbManager.queryRoutesByViewport
      → SpatialDbManager.queryTracksByViewport
      → SpatialDbManager.queryTrailsByViewport
      → SpatialDbManager.queryWaypointsByViewport
      → html.replace
      → names.add
      → names.isNotEmpty
      → onViewportChanged
      → os.Handler
      → setOverlayLayers
      → setTileUrl

  [424] fun onMapReady(n: Double, s: Double, e: Double, w: Double) {
    WORK FIELDS (local computed values):
      [426] val wv = webViewRef ?: return@post
      [427] val src = ConvoyConfig.ACTIVE_TILE_SOURCE
      [428] val url = MapSourceManager.getSlotSources().find { it.first == src }?.third
      [431] val ovJson = MapSourceManager.getOverlayJson(src)
      [441] val z = zoom.toInt()
      [442] val limit = if (z < 14) 500 else 2000
      [445] val capTrailState = ConvoyConfig.trailDisplayState
      [446] val capTrackState = ConvoyConfig.trackDisplayState
      [447] val capWaypointState = ConvoyConfig.waypointDisplayState
      [448] val capRouteState = ConvoyConfig.routeDisplayState
      [449] val capTrailIds = ConvoyConfig.trailChecked
      [450] val capTrackIds = ConvoyConfig.trackChecked
    CALLS OUT:
      → Log.e
      → Looper.getMainLooper
      → SpatialDbManager.buildRouteGeoJson
      → SpatialDbManager.buildTrackGeoJson
      → SpatialDbManager.buildTrailGeoJson
      → SpatialDbManager.buildWaypointGeoJson
      → SpatialDbManager.init
      → SpatialDbManager.queryRoutesByViewport
      → SpatialDbManager.queryTracksByViewport
      → SpatialDbManager.queryTrailsByViewport
      → SpatialDbManager.queryWaypointsByViewport
      → onViewportChanged
      → os.Handler
      → routeResults.isNotEmpty
      → setOverlayLayers

  [438] fun onViewportChanged(north: Double, south: Double, east: Double, west: Double, zoom: Double) {
    WORK FIELDS (local computed values):
      [441] val z = zoom.toInt()
      [442] val limit = if (z < 14) 500 else 2000
      [445] val capTrailState = ConvoyConfig.trailDisplayState
      [446] val capTrackState = ConvoyConfig.trackDisplayState
      [447] val capWaypointState = ConvoyConfig.waypointDisplayState
      [448] val capRouteState = ConvoyConfig.routeDisplayState
      [449] val capTrailIds = ConvoyConfig.trailChecked
      [450] val capTrackIds = ConvoyConfig.trackChecked
      [451] val capWaypointIds = ConvoyConfig.waypointChecked
      [452] val capRouteIds = ConvoyConfig.routeChecked
      [456] val trailsRaw = if (z >= 8 && capTrailState != DS_OFF) SpatialDbManager.queryTrailsByViewport(south, west, north, east, limit) else emptyList()
      [457] val trails = if (capTrailState == DS_SELECTED)
    CALLS OUT:
      → Log.e
      → Looper.getMainLooper
      → SpatialDbManager.buildRouteGeoJson
      → SpatialDbManager.buildTrackGeoJson
      → SpatialDbManager.buildTrailGeoJson
      → SpatialDbManager.buildWaypointGeoJson
      → SpatialDbManager.init
      → SpatialDbManager.queryRoutesByViewport
      → SpatialDbManager.queryTracksByViewport
      → SpatialDbManager.queryTrailsByViewport
      → SpatialDbManager.queryWaypointsByViewport
      → WebViewClient
      → file.exists
      → io.File
      → io.FileInputStream

  [510] override fun shouldInterceptRequest(view: WebView?, request: android.webkit.WebResourceRequest?): android.webkit.WebResourceResponse? {
    WORK FIELDS (local computed values):
      [511] val url = request?.url?.toString() ?: return super.shouldInterceptRequest(view, request)
      [514] val parts = url.split("/tile/").lastOrNull()?.split("/")
      [516] val file = java.io.File(ConvoyConfig.TILE_DIR, "SAT_LABELS_TRANSPORT/${parts[0]}/${parts[2]}/${parts[1]}.png")
      [523] val parts = url.split("/tile/").lastOrNull()?.split("/")
      [525] val file = java.io.File(ConvoyConfig.TILE_DIR, "SAT_LABELS_PLACES/${parts[0]}/${parts[2]}/${parts[1]}.png")
      [548] val initOverlayJson = MapSourceManager.getOverlayJson("SAT")
      [558] val lm = context.getSystemService(android.content.Context.LOCATION_SERVICE) as android.location.LocationManager
      [560] val loc = lm.getLastKnownLocation(android.location.LocationManager.GPS_PROVIDER)
      [579] val sourceDir = java.io.File(ConvoyConfig.TILE_DIR, pmActiveSource)
      [580] var maxZ = 0
      [583] val z = zDir.name.toIntOrNull()
    CALLS OUT:
      → Log.e
      → Log.i
      → Log.w
      → Looper.getMainLooper
      → SpatialDbManager.init
      → SpatialDbManager.syncTracksFromFiles
      → Suppress
      → context.getSystemService
      → coroutines.MainScope
      → coroutines.withContext
      → file.exists
      → io.File
      → io.FileInputStream
      → lastOrNull
      → lm.getLastKnownLocation

  [533] override fun onPageFinished(view: WebView?, url: String?) {
    WORK FIELDS (local computed values):
      [548] val initOverlayJson = MapSourceManager.getOverlayJson("SAT")
      [558] val lm = context.getSystemService(android.content.Context.LOCATION_SERVICE) as android.location.LocationManager
      [560] val loc = lm.getLastKnownLocation(android.location.LocationManager.GPS_PROVIDER)
      [579] val sourceDir = java.io.File(ConvoyConfig.TILE_DIR, pmActiveSource)
      [580] var maxZ = 0
      [583] val z = zDir.name.toIntOrNull()
    CALLS OUT:
      → Log.e
      → Log.i
      → Log.w
      → Looper.getMainLooper
      → SpatialDbManager.init
      → SpatialDbManager.syncTracksFromFiles
      → Suppress
      → context.getSystemService
      → coroutines.MainScope
      → coroutines.withContext
      → io.File
      → lm.getLastKnownLocation
      → loadUrl
      → os.Handler
      → setOverlayLayers

  [1219] private fun scanTrackDir(context: android.content.Context): List<String> {
    WORK FIELDS (local computed values):
      [1220] val dir = java.io.File(
      [1225] val files = dir.listFiles()
      [1227] val ext = f.extension.lowercase()
      [1248] val result = kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) {
      [1249] val dir = java.io.File(
      [1254] val file = java.io.File(dir, fileName)
      [1256] val text = file.readText()
      [1257] val coords = if (fileName.lowercase().endsWith(".gpx")) parseGpx(text) else parseKml(text)
      [1259] val json = coords.joinToString(",", "[", "]") { "[${it.first},${it.second}]" }
      [1263] val safe = fileName.replace("'", "\\'")
      [1274] val coords = mutableListOf<Pair<Double, Double>>()
      [1275] val pattern = Regex("""<coordinates>([\s\S]*?)</coordinates>""")
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Log.d
      → Log.e
      → Pair
      → Regex
      → coords.add
      → coords.isEmpty
      → coroutines.MainScope
      → coroutines.withContext
      → dir.exists
      → dir.listFiles
      → endsWith
      → extension.lowercase
      → file.exists
      → file.readText

  [1239] private fun loadTrackOnMap(
    WORK FIELDS (local computed values):
      [1248] val result = kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.IO) {
      [1249] val dir = java.io.File(
      [1254] val file = java.io.File(dir, fileName)
      [1256] val text = file.readText()
      [1257] val coords = if (fileName.lowercase().endsWith(".gpx")) parseGpx(text) else parseKml(text)
      [1259] val json = coords.joinToString(",", "[", "]") { "[${it.first},${it.second}]" }
      [1263] val safe = fileName.replace("'", "\\'")
      [1274] val coords = mutableListOf<Pair<Double, Double>>()
      [1275] val pattern = Regex("""<coordinates>([\s\S]*?)</coordinates>""")
      [1278] val parts = line.trim().split(",")
      [1280] val lon = parts[0].toDoubleOrNull()
      [1281] val lat = parts[1].toDoubleOrNull()
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → LegendItem
      → Log.d
      → Log.e
      → Pair
      → Regex
      → Row
      → coords.add
      → coords.isEmpty
      → coroutines.MainScope
      → coroutines.withContext
      → drawCircle
      → drawLine
      → endsWith
      → file.exists

  [1273] private fun parseKml(text: String): List<Pair<Double, Double>> {
    WORK FIELDS (local computed values):
      [1274] val coords = mutableListOf<Pair<Double, Double>>()
      [1275] val pattern = Regex("""<coordinates>([\s\S]*?)</coordinates>""")
      [1278] val parts = line.trim().split(",")
      [1280] val lon = parts[0].toDoubleOrNull()
      [1281] val lat = parts[1].toDoubleOrNull()
      [1292] val coords = mutableListOf<Pair<Double, Double>>()
      [1293] val pattern = Regex("""<trkpt\s+lat="([^"]+)"\s+lon="([^"]+)"""")
      [1295] val lat = match.groupValues[1].toDoubleOrNull()
      [1296] val lon = match.groupValues[2].toDoubleOrNull()
      [1308] val y = size.height / 2
      [1312] val d = size.width / 4
    CALLS OUT:
      → LegendItem
      → Pair
      → Regex
      → Row
      → Spacer
      → Text
      → coords.add
      → drawCircle
      → drawLine
      → foundation.Canvas
      → geometry.Offset
      → line.trim
      → lines
      → parseGpx
      → pattern.findAll

  [1291] private fun parseGpx(text: String): List<Pair<Double, Double>> {
    WORK FIELDS (local computed values):
      [1292] val coords = mutableListOf<Pair<Double, Double>>()
      [1293] val pattern = Regex("""<trkpt\s+lat="([^"]+)"\s+lon="([^"]+)"""")
      [1295] val lat = match.groupValues[1].toDoubleOrNull()
      [1296] val lon = match.groupValues[2].toDoubleOrNull()
      [1308] val y = size.height / 2
      [1312] val d = size.width / 4
    CALLS OUT:
      → LegendItem
      → Pair
      → Regex
      → Row
      → Spacer
      → Text
      → coords.add
      → drawCircle
      → drawLine
      → foundation.Canvas
      → geometry.Offset
      → pattern.findAll

  [1305] private fun LegendItem(color: Color, label: String, style: String = "line") {
    WORK FIELDS (local computed values):
      [1308] val y = size.height / 2
      [1312] val d = size.width / 4
    CALLS OUT:
      → Row
      → Spacer
      → Text
      → drawCircle
      → drawLine
      → foundation.Canvas
      → geometry.Offset

========================================
FILE: ConvoyMarkerRenderer.kt
========================================

========================================
FILE: ConvoyMasterApply.kt
========================================

  [33] suspend fun archiveCurrentRadio(
    WORK FIELDS (local computed values):
      [38] val fmt = DateTimeFormatter.ofPattern("yyyyMMdd_HHmmss")
      [39] val ts  = LocalDateTime.now().format(fmt)
      [40] val archiveFile = java.io.File(
      [56] val bytes = inputStream.readBytes()
      [85] val nodeId = "!%08x".format(nodeNum)
      [88] val archivePath = archiveCurrentRadio(context, nodeId, convoyViewModel)
      [93] val profile = loadMasterProfile(context)
    CALLS OUT:
      → ADAPTER.decode
      → DateTimeFormatter.ofPattern
      → Exception
      → LocalDateTime.now
      → Log.i
      → archiveCurrentRadio
      → assets.open
      → convoyViewModel.exportProfileToFile
      → convoyViewModel.installProfileToRadio
      → inputStream.readBytes
      → installMasterToRadio
      → io.File
      → loadMasterProfile
      → mkdirs

  [54] fun loadMasterProfile(context: Context): Result<DeviceProfile> = runCatching {
    WORK FIELDS (local computed values):
      [56] val bytes = inputStream.readBytes()
      [85] val nodeId = "!%08x".format(nodeNum)
      [88] val archivePath = archiveCurrentRadio(context, nodeId, convoyViewModel)
      [93] val profile = loadMasterProfile(context)
    CALLS OUT:
      → ADAPTER.decode
      → Exception
      → Log.i
      → archiveCurrentRadio
      → assets.open
      → convoyViewModel.installProfileToRadio
      → inputStream.readBytes
      → installMasterToRadio
      → loadMasterProfile

  [66] fun installMasterToRadio(
    WORK FIELDS (local computed values):
      [85] val nodeId = "!%08x".format(nodeNum)
      [88] val archivePath = archiveCurrentRadio(context, nodeId, convoyViewModel)
      [93] val profile = loadMasterProfile(context)
    CALLS OUT:
      → Exception
      → Log.i
      → archiveCurrentRadio
      → convoyViewModel.installProfileToRadio
      → installMasterToRadio
      → loadMasterProfile

  [80] suspend fun applyMasterConfig(
    WORK FIELDS (local computed values):
      [85] val nodeId = "!%08x".format(nodeNum)
      [88] val archivePath = archiveCurrentRadio(context, nodeId, convoyViewModel)
      [93] val profile = loadMasterProfile(context)
    CALLS OUT:
      → Exception
      → Log.i
      → archiveCurrentRadio
      → installMasterToRadio
      → loadMasterProfile

========================================
FILE: ConvoyMasterCaptureMaintScreen.kt
========================================

  [13] fun ConvoyMasterCaptureMaintScreen(
    CALLS OUT:
      → hiltViewModel

========================================
FILE: ConvoyMasterCaptureScreen.kt
========================================

  [80] private fun freqMhzToChannelNum(freqMhz: Float): Int =
    WORK FIELDS (local computed values):
      [98] val context = LocalContext.current
      [99] val scope   = rememberCoroutineScope()
      [101] var statusMsg       by remember { mutableStateOf("") }
      [102] var statusOk        by remember { mutableStateOf(true) }
      [103] var isCapturing     by remember { mutableStateOf(false) }
      [104] var captureLog      by remember { mutableStateOf("") }
      [105] var captureComplete by remember { mutableStateOf(false) }
      [108] var loraFreqMhzInput by remember { mutableStateOf("") }
      [110] val nodeInfo        by viewModel.myNodeInfo.collectAsState()
      [111] val localConfig     by channelViewModel.localConfig.collectAsStateWithLifecycle()
      [112] val channels        by channelViewModel.channels.collectAsStateWithLifecycle()
      [113] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
    CALLS OUT:
      → Box
      → Color
      → Spacer
      → background
      → channelNumToFreqMhz
      → coerceIn
      → fillMaxSize
      → height
      → hiltViewModel
      → mutableStateOf
      → myNodeInfo.collectAsState
      → onBack
      → padding
      → rememberCoroutineScope
      → verticalScroll

  [85] private fun channelNumToFreqMhz(channelNum: Int): Float =
    WORK FIELDS (local computed values):
      [98] val context = LocalContext.current
      [99] val scope   = rememberCoroutineScope()
      [101] var statusMsg       by remember { mutableStateOf("") }
      [102] var statusOk        by remember { mutableStateOf(true) }
      [103] var isCapturing     by remember { mutableStateOf(false) }
      [104] var captureLog      by remember { mutableStateOf("") }
      [105] var captureComplete by remember { mutableStateOf(false) }
      [108] var loraFreqMhzInput by remember { mutableStateOf("") }
      [110] val nodeInfo        by viewModel.myNodeInfo.collectAsState()
      [111] val localConfig     by channelViewModel.localConfig.collectAsStateWithLifecycle()
      [112] val channels        by channelViewModel.channels.collectAsStateWithLifecycle()
      [113] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
    CALLS OUT:
      → Box
      → Color
      → RoundedCornerShape
      → Spacer
      → background
      → fillMaxSize
      → height
      → hiltViewModel
      → mutableStateOf
      → myNodeInfo.collectAsState
      → onBack
      → padding
      → rememberCoroutineScope
      → verticalScroll

  [91] fun ConvoyMasterCaptureScreen(
    WORK FIELDS (local computed values):
      [98] val context = LocalContext.current
      [99] val scope   = rememberCoroutineScope()
      [101] var statusMsg       by remember { mutableStateOf("") }
      [102] var statusOk        by remember { mutableStateOf(true) }
      [103] var isCapturing     by remember { mutableStateOf(false) }
      [104] var captureLog      by remember { mutableStateOf("") }
      [105] var captureComplete by remember { mutableStateOf(false) }
      [108] var loraFreqMhzInput by remember { mutableStateOf("") }
      [110] val nodeInfo        by viewModel.myNodeInfo.collectAsState()
      [111] val localConfig     by channelViewModel.localConfig.collectAsStateWithLifecycle()
      [112] val channels        by channelViewModel.channels.collectAsStateWithLifecycle()
      [113] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
    CALLS OUT:
      → Box
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → background
      → fillMaxSize
      → height
      → hiltViewModel
      → mutableStateOf
      → myNodeInfo.collectAsState
      → onBack
      → padding
      → rememberCoroutineScope
      → verticalScroll

========================================
FILE: ConvoyMasterConfig.kt
========================================

  [88] fun toJson(): JSONObject = JSONObject().apply {
    CALLS OUT:
      → fromJson
      → put

  [155] fun fromJson(obj: JSONObject): ConvoyMasterConfig = ConvoyMasterConfig(
    WORK FIELDS (local computed values):
      [195] telemetryEnvInterval   = obj.optInt("telemetryEnvInterval", 0),
      [217] val deviceFile = File(context.filesDir, DEVICE_FILE)
      [222] val json = JSONObject(deviceFile.readText())
      [223] val deviceVersion = json.optInt("configVersion", 0)
    CALLS OUT:
      → File
      → JSONObject
      → Log.i
      → Log.w
      → deviceFile.exists
      → fromJson
      → load
      → restoreFromAsset

  [216] fun load(context: Context): ConvoyMasterConfig? {
    WORK FIELDS (local computed values):
      [217] val deviceFile = File(context.filesDir, DEVICE_FILE)
      [222] val json = JSONObject(deviceFile.readText())
      [223] val deviceVersion = json.optInt("configVersion", 0)
      [247] val json = context.assets.open(ASSET_FILE)
    CALLS OUT:
      → File
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → assets.open
      → bufferedReader
      → close
      → deviceFile.exists
      → exists
      → fromJson
      → it.readText
      → it.writeText
      → mkdirs
      → restoreFromAsset

  [245] private fun restoreFromAsset(context: Context): ConvoyMasterConfig? {
    WORK FIELDS (local computed values):
      [247] val json = context.assets.open(ASSET_FILE)
    CALLS OUT:
      → File
      → Log.e
      → Log.i
      → assets.open
      → bufferedReader
      → close
      → exists
      → fromJson
      → it.readText
      → it.writeText
      → mkdirs

  [264] fun exists(context: Context): Boolean {
    CALLS OUT:
      → File
      → assets.open
      → close
      → exists

========================================
FILE: ConvoyMasterSuccessScreen.kt
========================================

  [29] fun ConvoyMasterSuccessScreen(
    WORK FIELDS (local computed values):
      [33] val context = LocalContext.current
      [34] val master  = remember { ConvoyMasterConfig.load(context) }
    CALLS OUT:
      → Box
      → Color
      → ConvoyMasterConfig.load
      → DataCard
      → RoundedCornerShape
      → Spacer
      → background
      → fillMaxSize
      → fillMaxWidth
      → height
      → onCaptureNew
      → padding
      → verticalScroll

  [136] private fun DataCard(label: String, value: String) {
    CALLS OUT:
      → Color
      → RoundedCornerShape
      → fillMaxWidth
      → padding

========================================
FILE: ConvoyMyOrganizersScreen.kt
========================================

  [32] fun ConvoyMyOrganizersScreen(
    WORK FIELDS (local computed values):
      [35] val context     = LocalContext.current
      [36] val scope       = rememberCoroutineScope()
      [37] val scrollState = rememberScrollState()
      [40] data class OrganizerRow(val id: String, val name: String, val location: String)
      [41] val allOrganizers = remember {
      [53] val followedIds = remember {
      [59] var activeTab by remember { mutableStateOf("ALL") }
      [60] val tabs = listOf("FOLLOWING", "NOT FOLLOWING", "ALL")
      [62] val displayList = remember(activeTab, followedIds.value) {
      [93] val isActive = tab == activeTab
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → OrganizerRow
      → Row
      → Text
      → background
      → clip
      → listOf
      → material3.HorizontalDivider
      → mutableStateOf
      → onBack
      → padding
      → remember

========================================
FILE: ConvoyMyRidesScreen.kt
========================================

  [30] fun ConvoyMyRidesScreen(
    WORK FIELDS (local computed values):
      [36] val context = LocalContext.current
      [37] val tabs = listOf("ALL", "ENROLLED", "ORGANIZED", "COMPLETED")
      [38] var selectedTab by remember { mutableStateOf(0) }
      [40] val rideCount = ConvoyDevSeeder.getRideCount(context)
      [41] val allRides = (0 until rideCount).map { i ->
      [52] val filteredRides = when (selectedTab) {
      [84] var isFollowing by remember { mutableStateOf(false) }
      [93] val status = ride["status"] ?: ""
      [94] val color = when (status) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → ConvoyDevSeeder.getRideCount
      → ConvoyDevSeeder.getRideDate
      → ConvoyDevSeeder.getRideEmail
      → ConvoyDevSeeder.getRideName
      → ConvoyDevSeeder.getRideOrganizer
      → ConvoyDevSeeder.getRideStatus
      → ConvoyDevSeeder.getRideTime
      → GroupTrackHeader
      → LazyColumn
      → PaddingValues
      → Row

  [142] private fun BottomNavBtn(icon: String, label: String, onClick: () -> Unit) {
    CALLS OUT:
      → Text
      → onClick
      → padding

========================================
FILE: ConvoyNavArgs.kt
========================================

========================================
FILE: ConvoyNode.kt
========================================

========================================
FILE: ConvoyNodeFilter.kt
========================================

  [28] fun filter(
    WORK FIELDS (local computed values):
      [34] val todayMidnight  = todayMidnightEpochSeconds()
      [35] val admissionCutoff = nowEpochSeconds - (admissionWindowHours * 3600L)
    CALLS OUT:
      → LocalDate.now
      → System.currentTimeMillis
      → atStartOfDay
      → toEpochSecond
      → todayMidnightEpochSeconds

  [50] fun todayMidnightEpochSeconds(): Long =
    CALLS OUT:
      → LocalDate.now
      → atStartOfDay
      → toEpochSecond

========================================
FILE: ConvoyPendingRidesScreen.kt
========================================

  [31] fun ConvoyOrganizerPendingRides(
    WORK FIELDS (local computed values):
      [34] val context = LocalContext.current
      [37] val pendingRides = remember {
      [38] val count = ConvoyDevSeeder.getRideCount(context)
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → ConvoyDevSeeder.getRideCount
      → ConvoyDevSeeder.getRideDate
      → ConvoyDevSeeder.getRideField
      → ConvoyDevSeeder.getRideName
      → ConvoyDevSeeder.getRideStatus
      → background
      → clip
      → padding
      → pendingRides.isEmpty

========================================
FILE: ConvoyPrivacyScreen.kt
========================================

  [18] fun ConvoyPrivacyScreen(
    WORK FIELDS (local computed values):
      [22] val context = LocalContext.current
      [23] val scrollState = rememberScrollState()
    CALLS OUT:
      → Arrangement.spacedBy
      → Column
      → GroupTrackButton
      → GroupTrackHeader
      → PrivacySection
      → Spacer
      → Text
      → background
      → onAccept
      → padding
      → rememberScrollState
      → verticalScroll
      → weight

  [45] private fun PrivacySection(title: String, body: String) {
    CALLS OUT:
      → Text

========================================
FILE: ConvoyProfileBuilder.kt
========================================

  [34] fun buildProfile(config: ConvoyMasterConfig): DeviceProfile {
    WORK FIELDS (local computed values):
      [37] val loraRegion = try {
      [44] val modemPreset = try {
      [51] val loraConfig = Config.LoRaConfig(
      [65] val nodeRole = try {
      [72] val deviceConfig = Config.DeviceConfig(
      [78] val gpsMode = try {
      [85] val positionConfig = Config.PositionConfig(
      [86] gps_update_interval                  = config.gpsUpdateSecs,
      [98] val displayUnits = Config.DisplayConfig.DisplayUnits.fromValue(config.displayUnits)
      [101] val displayConfig = Config.DisplayConfig(
      [108] val localConfig = LocalConfig(
    CALLS OUT:
      → Config.DeviceConfig
      → Config.DisplayConfig
      → Config.LoRaConfig
      → Config.PositionConfig
      → DisplayUnits.fromValue
      → Log.w

  [201] fun buildBytes(config: ConvoyMasterConfig): ByteArray = buildProfile(config).encode()
    WORK FIELDS (local computed values):
      [212] val outFile = File(context.filesDir, "convoy_profiles/$fileName")
      [228] val masterConfig = ConvoyMasterConfig.load(context) ?: run {
      [235] val generatedBytes = try {
      [244] val masterBytes = try {
      [253] val generated = DeviceProfile.ADAPTER.decode(generatedBytes)
      [254] val master    = DeviceProfile.ADAPTER.decode(masterBytes)
      [256] var pass = true
    CALLS OUT:
      → ADAPTER.decode
      → ConvoyMasterConfig.load
      → File
      → Log.e
      → Log.i
      → assets.open
      → buildBytes
      → check
      → mkdirs
      → outFile.writeBytes
      → readBytes
      → testAgainstMasterCfg

  [207] fun buildAndSave(
    WORK FIELDS (local computed values):
      [212] val outFile = File(context.filesDir, "convoy_profiles/$fileName")
      [228] val masterConfig = ConvoyMasterConfig.load(context) ?: run {
      [235] val generatedBytes = try {
      [244] val masterBytes = try {
      [253] val generated = DeviceProfile.ADAPTER.decode(generatedBytes)
      [254] val master    = DeviceProfile.ADAPTER.decode(masterBytes)
      [256] var pass = true
    CALLS OUT:
      → ADAPTER.decode
      → ConvoyMasterConfig.load
      → File
      → Log.e
      → Log.i
      → assets.open
      → buildBytes
      → check
      → mkdirs
      → outFile.writeBytes
      → readBytes
      → testAgainstMasterCfg

  [224] fun testAgainstMasterCfg(context: Context): Boolean {
    WORK FIELDS (local computed values):
      [228] val masterConfig = ConvoyMasterConfig.load(context) ?: run {
      [235] val generatedBytes = try {
      [244] val masterBytes = try {
      [253] val generated = DeviceProfile.ADAPTER.decode(generatedBytes)
      [254] val master    = DeviceProfile.ADAPTER.decode(masterBytes)
      [256] var pass = true
    CALLS OUT:
      → ADAPTER.decode
      → ConvoyMasterConfig.load
      → Log.e
      → Log.i
      → assets.open
      → buildBytes
      → buildProfile
      → check
      → readBytes

  [258] fun <T> check(name: String, gen: T, mst: T) {
    WORK FIELDS (local computed values):
      [306] val config = ConvoyMasterConfig(
    CALLS OUT:
      → Log.e
      → Log.i
      → buildProfile
      → check

  [304] fun buildProfile(wconfig: WorkingConfig): DeviceProfile {
    WORK FIELDS (local computed values):
      [306] val config = ConvoyMasterConfig(
      [346] telemetryEnvInterval   = wconfig.telemetryEnvInterval,
    CALLS OUT:
      → buildProfile

========================================
FILE: ConvoyProfileScreen.kt
========================================

  [32] fun ConvoyProfileScreen(
    WORK FIELDS (local computed values):
      [40] val context = LocalContext.current
      [41] val scrollState = rememberScrollState()
      [44] var firstName by remember { mutableStateOf(ConvoySessionManager.getFirstName(context)) }
      [45] var lastName by remember { mutableStateOf(context.getSharedPreferences("grouptrack_user", android.content.Context.MODE_PRIVATE).getString("last_name", "") ?: "") }
      [46] var email by remember { mutableStateOf(ConvoySessionManager.getEmail(context)) }
      [47] var cell by remember { mutableStateOf("") }
      [48] var addressLine1 by remember { mutableStateOf("") }
      [49] var city by remember { mutableStateOf("") }
      [50] var state by remember { mutableStateOf("") }
      [51] var zipCode by remember { mutableStateOf(ConvoySessionManager.getZipCode(context)) }
      [52] var radiusMiles by remember { mutableStateOf(ConvoySessionManager.getSearchRadius(context).toString()) }
      [53] var emailOptIn by remember { mutableStateOf(true) }
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → ConvoySessionManager.isOrganizer
      → ConvoySessionManager.isSubscribed
      → ConvoySessionManager.setSearchRadius
      → ConvoySessionManager.setZipCode
      → GroupTrackHeader
      → ProfileField
      → ProfileSectionLabel
      → Row
      → StatusBadge
      → background
      → fillMaxWidth

  [238] private fun ProfileSectionLabel(label: String) {
    CALLS OUT:
      → Color
      → OutlinedTextFieldDefaults.colors
      → StatusBadge
      → Text
      → background
      → clip
      → padding
      → placeholder.isNotEmpty

  [248] private fun StatusBadge(label: String, color: Color) {
    CALLS OUT:
      → Color
      → OutlinedTextFieldDefaults.colors
      → Text
      → background
      → clip
      → padding
      → placeholder.isNotEmpty

  [263] private fun ProfileField(
    CALLS OUT:
      → Color
      → OutlinedTextFieldDefaults.colors
      → Text
      → placeholder.isNotEmpty

========================================
FILE: ConvoyQueuesPanel.kt
========================================

  [24] fun ConvoyQueuesPanel(onDismiss: () -> Unit = {}, modifier: Modifier = Modifier) {
    WORK FIELDS (local computed values):
      [25] var expanded by remember { mutableStateOf(false) }
      [26] val mono = FontFamily.Monospace
    CALLS OUT:
      → AnimatedVisibility
      → Arrangement.spacedBy
      → Color
      → Column
      → QRow
      → RoundedCornerShape
      → Row
      → Surface
      → Text
      → mutableStateOf
      → padding
      → shrinkVertically

  [54] private fun QRow(label: String, count: Int, color: Color) {
    CALLS OUT:
      → Row
      → Text
      → padding

========================================
FILE: ConvoyRadioConfigScreens.kt
========================================

  [33] fun ConnectionState.toRadioReadiness(): RadioReadiness = when (this) {
    WORK FIELDS (local computed values):
      [42] val label = when (readiness) {
      [48] val color = when (readiness) {
      [54] val bg = when (readiness) {
      [75] val enabled = isConnected && !isProcessing
    CALLS OUT:
      → Color
      → ConvoyCancelButton
      → ConvoyRadioStatusBar
      → RoundedCornerShape
      → Surface
      → Text
      → clickable
      → onClick

  [41] fun ConvoyRadioStatusBar(readiness: RadioReadiness) {
    WORK FIELDS (local computed values):
      [42] val label = when (readiness) {
      [48] val color = when (readiness) {
      [54] val bg = when (readiness) {
      [75] val enabled = isConnected && !isProcessing
    CALLS OUT:
      → Color
      → ConvoyCancelButton
      → ConvoyConfigRow
      → RoundedCornerShape
      → Row
      → Surface
      → Text
      → clickable
      → onClick
      → padding

  [69] fun ConvoyProceedButton(
    WORK FIELDS (local computed values):
      [75] val enabled = isConnected && !isProcessing
    CALLS OUT:
      → Color
      → ConvoyCancelButton
      → ConvoyConfigRow
      → ConvoyConfigSection
      → HorizontalDivider
      → RoundedCornerShape
      → Row
      → Surface
      → Text
      → clickable
      → content
      → onClick
      → padding

  [105] fun ConvoyCancelButton(onClick: () -> Unit) {
    CALLS OUT:
      → Box
      → Color
      → Column
      → ConvoyConfigRow
      → ConvoyConfigSection
      → ConvoyRadioStatusBar
      → HorizontalDivider
      → RoundedCornerShape
      → Row
      → Spacer
      → Surface
      → Text
      → background
      → content
      → height

  [119] fun ConvoyConfigRow(label: String, value: String, highlight: Boolean = false) {
    WORK FIELDS (local computed values):
      [189] val context         = LocalContext.current
      [190] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [191] val myNodeInfo      by uiViewModel.myNodeInfo.collectAsStateWithLifecycle()
      [192] val localConfig     by channelViewModel.localConfig.collectAsStateWithLifecycle()
      [193] val channels        by channelViewModel.channels.collectAsStateWithLifecycle()
      [194] val readiness       = connectionState.toRadioReadiness()
      [195] val isConnected     = readiness == RadioReadiness.CONNECTED
      [197] var archiveDone   by remember { mutableStateOf(false) }
      [198] var archiveFailed by remember { mutableStateOf(false) }
      [199] var isRunning     by remember { mutableStateOf(false) }
    CALLS OUT:
      → Box
      → Color
      → Column
      → ConvoyConfigSection
      → ConvoyRadioStatusBar
      → HorizontalDivider
      → RoundedCornerShape
      → Row
      → Spacer
      → Surface
      → Text
      → background
      → connectionState.toRadioReadiness
      → content
      → height

  [133] fun ConvoyConfigSection(title: String, content: @Composable () -> Unit) {
    WORK FIELDS (local computed values):
      [189] val context         = LocalContext.current
      [190] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [191] val myNodeInfo      by uiViewModel.myNodeInfo.collectAsStateWithLifecycle()
      [192] val localConfig     by channelViewModel.localConfig.collectAsStateWithLifecycle()
      [193] val channels        by channelViewModel.channels.collectAsStateWithLifecycle()
      [194] val readiness       = connectionState.toRadioReadiness()
      [195] val isConnected     = readiness == RadioReadiness.CONNECTED
      [197] var archiveDone   by remember { mutableStateOf(false) }
      [198] var archiveFailed by remember { mutableStateOf(false) }
      [199] var isRunning     by remember { mutableStateOf(false) }
      [200] var logLines      by remember { mutableStateOf<List<Pair<String, Boolean>>>(emptyList()) }
      [204] val channelsReady = channels.settings.isNotEmpty()
    CALLS OUT:
      → Box
      → Color
      → Column
      → ConvoyRadioStatusBar
      → HorizontalDivider
      → LaunchedEffect
      → Pair
      → RoundedCornerShape
      → Row
      → Spacer
      → Surface
      → Text
      → addLog
      → background
      → connectionState.toRadioReadiness

  [150] fun ConvoyConfigHeader(
    WORK FIELDS (local computed values):
      [189] val context         = LocalContext.current
      [190] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [191] val myNodeInfo      by uiViewModel.myNodeInfo.collectAsStateWithLifecycle()
      [192] val localConfig     by channelViewModel.localConfig.collectAsStateWithLifecycle()
      [193] val channels        by channelViewModel.channels.collectAsStateWithLifecycle()
      [194] val readiness       = connectionState.toRadioReadiness()
      [195] val isConnected     = readiness == RadioReadiness.CONNECTED
      [197] var archiveDone   by remember { mutableStateOf(false) }
      [198] var archiveFailed by remember { mutableStateOf(false) }
      [199] var isRunning     by remember { mutableStateOf(false) }
      [200] var logLines      by remember { mutableStateOf<List<Pair<String, Boolean>>>(emptyList()) }
      [204] val channelsReady = channels.settings.isNotEmpty()
    CALLS OUT:
      → Box
      → Color
      → Column
      → ConvoyRadioManager.buildSnapshot
      → ConvoyRadioStatusBar
      → LaunchedEffect
      → Pair
      → Row
      → Spacer
      → Text
      → addLog
      → background
      → connectionState.toRadioReadiness
      → height
      → hiltViewModel

  [182] fun ConvoyWriteArchiveScreen(
    WORK FIELDS (local computed values):
      [189] val context         = LocalContext.current
      [190] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [191] val myNodeInfo      by uiViewModel.myNodeInfo.collectAsStateWithLifecycle()
      [192] val localConfig     by channelViewModel.localConfig.collectAsStateWithLifecycle()
      [193] val channels        by channelViewModel.channels.collectAsStateWithLifecycle()
      [194] val readiness       = connectionState.toRadioReadiness()
      [195] val isConnected     = readiness == RadioReadiness.CONNECTED
      [197] var archiveDone   by remember { mutableStateOf(false) }
      [198] var archiveFailed by remember { mutableStateOf(false) }
      [199] var isRunning     by remember { mutableStateOf(false) }
      [200] var logLines      by remember { mutableStateOf<List<Pair<String, Boolean>>>(emptyList()) }
      [204] val channelsReady = channels.settings.isNotEmpty()
    CALLS OUT:
      → Box
      → Column
      → ConvoyRadioManager.buildSnapshot
      → ConvoyRadioManager.saveBackup
      → LaunchedEffect
      → Pair
      → Row
      → Spacer
      → Text
      → addLog
      → background
      → connectionState.toRadioReadiness
      → convoyViewModel.setWorkingConfig
      → hiltViewModel
      → mutableStateOf

  [202] fun addLog(msg: String, ok: Boolean = true) { logLines = logLines + Pair(msg, ok) }
    WORK FIELDS (local computed values):
      [204] val channelsReady = channels.settings.isNotEmpty()
      [205] val configReady   = localConfig.lora != null
      [212] val ni = myNodeInfo
      [227] val nodeLongName = convoyViewModel.ourNodeInfo.value?.user?.long_name ?: ""
      [228] val snapshot = ConvoyRadioManager.buildSnapshot(
      [235] val filePath = ConvoyRadioManager.saveBackup(context, snapshot, "pre_write")
    CALLS OUT:
      → Box
      → Color
      → Column
      → ConvoyRadioManager.buildSnapshot
      → ConvoyRadioManager.saveBackup
      → ConvoyRadioStatusBar
      → LaunchedEffect
      → RoundedCornerShape
      → Row
      → Spacer
      → Surface
      → Text
      → addLog
      → background
      → convoyViewModel.setWorkingConfig

  [340] fun ConvoyDeviceConfigScreen(
    WORK FIELDS (local computed values):
      [347] val context = LocalContext.current
      [348] val scope = rememberCoroutineScope()
      [349] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [350] val readiness   = connectionState.toRadioReadiness()
      [351] val isConnected = readiness == RadioReadiness.CONNECTED
      [352] var isProcessing     by remember { mutableStateOf(false) }
      [353] var waitingReconnect by remember { mutableStateOf(false) }
      [354] var statusMsg        by remember { mutableStateOf("") }
      [355] var statusOk         by remember { mutableStateOf(true) }
      [356] var wasConnected     by remember { mutableStateOf(isConnected) }
      [358] var countdown        by remember { mutableStateOf(0) }
      [359] var reconnectFailed  by remember { mutableStateOf(false) }
    CALLS OUT:
      → Box
      → Color
      → Column
      → ConvoyConfigHeader
      → ConvoyConfigRow
      → ConvoyConfigSection
      → HorizontalDivider
      → LaunchedEffect
      → RoundedCornerShape
      → Spacer
      → Surface
      → Text
      → background
      → connectionState.toRadioReadiness
      → coroutines.delay

  [470] fun ConvoyLoRaConfigScreen(
    WORK FIELDS (local computed values):
      [477] val context = LocalContext.current
      [478] val scope = rememberCoroutineScope()
      [479] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [480] val readiness   = connectionState.toRadioReadiness()
      [481] val isConnected = readiness == RadioReadiness.CONNECTED
      [482] var isProcessing     by remember { mutableStateOf(false) }
      [483] var waitingReconnect by remember { mutableStateOf(false) }
      [484] var statusMsg        by remember { mutableStateOf("") }
      [485] var statusOk         by remember { mutableStateOf(true) }
      [486] var wasConnected     by remember { mutableStateOf(isConnected) }
      [488] var countdown        by remember { mutableStateOf(0) }
      [489] var reconnectFailed  by remember { mutableStateOf(false) }
    CALLS OUT:
      → Box
      → Color
      → Column
      → ConvoyConfigHeader
      → ConvoyConfigRow
      → ConvoyConfigSection
      → HorizontalDivider
      → LaunchedEffect
      → RoundedCornerShape
      → Spacer
      → Surface
      → background
      → connectionState.toRadioReadiness
      → coroutines.delay
      → hiltViewModel

  [611] fun ConvoyPositionConfigScreen(
    WORK FIELDS (local computed values):
      [618] val scope = rememberCoroutineScope()
      [619] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [620] val readiness   = connectionState.toRadioReadiness()
      [621] val isConnected = readiness == RadioReadiness.CONNECTED
      [622] var isProcessing by remember { mutableStateOf(false) }
      [623] var statusMsg    by remember { mutableStateOf("") }
      [624] var statusOk     by remember { mutableStateOf(true) }
      [625] val broadcastSecs = workingConfig.positionBroadcastSecs
      [626] val gpsUpdateSecs = workingConfig.gpsUpdateSecs
      [627] val smartEnabled  = workingConfig.smartPositionEnabled
      [628] val smartMinSecs  = workingConfig.smartMinIntervalSecs
      [629] val smartMinDist  = workingConfig.smartMinDistanceMeters
    CALLS OUT:
      → Box
      → Color
      → Column
      → Config.PositionConfig
      → ConvoyConfigHeader
      → ConvoyConfigRow
      → ConvoyConfigSection
      → HorizontalDivider
      → RoundedCornerShape
      → Spacer
      → Surface
      → background
      → channelViewModel.setConfig
      → connectionState.toRadioReadiness
      → coroutines.delay

  [703] fun ConvoyChannelConfigScreen(
    WORK FIELDS (local computed values):
      [710] val context = LocalContext.current
      [711] val scope = rememberCoroutineScope()
      [712] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [713] val readiness   = connectionState.toRadioReadiness()
      [714] val isConnected = readiness == RadioReadiness.CONNECTED
      [715] var isProcessing     by remember { mutableStateOf(false) }
      [716] var waitingReconnect by remember { mutableStateOf(false) }
      [717] var statusMsg        by remember { mutableStateOf("") }
      [718] var statusOk         by remember { mutableStateOf(true) }
      [719] var wasConnected     by remember { mutableStateOf(isConnected) }
      [720] var writeComplete    by remember { mutableStateOf(false) }
      [722] var countdown        by remember { mutableStateOf(0) }
    CALLS OUT:
      → Box
      → Color
      → Column
      → ConvoyConfigHeader
      → ConvoyConfigRow
      → ConvoyConfigSection
      → HorizontalDivider
      → LaunchedEffect
      → RoundedCornerShape
      → Spacer
      → Surface
      → background
      → connectionState.toRadioReadiness
      → coroutines.delay
      → hiltViewModel

========================================
FILE: ConvoyRadioManager.kt
========================================

  [62] fun toJson(): JSONObject = JSONObject().apply {
    WORK FIELDS (local computed values):
      [106] val fmt = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss")
      [108] val primaryChannel  = channelSet?.settings?.firstOrNull()
      [109] val channelName     = primaryChannel?.name ?: ""
      [110] val pskBase64       = primaryChannel?.psk?.let { psk ->
      [113] val lora            = localConfig?.lora
    CALLS OUT:
      → DateTimeFormatter.ofPattern
      → LocalDateTime.now
      → firstOrNull
      → it.encode
      → psk.toByteArray
      → put
      → saveBackup

  [92] fun buildSnapshot(
    WORK FIELDS (local computed values):
      [106] val fmt = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss")
      [108] val primaryChannel  = channelSet?.settings?.firstOrNull()
      [109] val channelName     = primaryChannel?.name ?: ""
      [110] val pskBase64       = primaryChannel?.psk?.let { psk ->
      [113] val lora            = localConfig?.lora
      [143] val fmt      = DateTimeFormatter.ofPattern("yyyyMMdd_HHmmss")
      [144] val ts       = LocalDateTime.now().format(fmt)
      [145] val safeName = label.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [146] val dir      = java.io.File(context.filesDir, "$BACKUPS_DIR/${snapshot.hardwareId}").also { it.mkdirs() }
      [147] val file     = java.io.File(dir, "${safeName}_${ts}.json")
    CALLS OUT:
      → DateTimeFormatter.ofPattern
      → LocalDateTime.now
      → Log.i
      → Regex
      → canWrite
      → file.writeText
      → firstOrNull
      → hardwareId.isNotBlank
      → io.File
      → it.exists
      → it.mkdirs
      → label.replace
      → listBackups
      → psk.toByteArray
      → saveBackup

  [142] fun saveBackup(context: android.content.Context, snapshot: RadioSnapshot, label: String = "pre_convoy"): String {
    WORK FIELDS (local computed values):
      [143] val fmt      = DateTimeFormatter.ofPattern("yyyyMMdd_HHmmss")
      [144] val ts       = LocalDateTime.now().format(fmt)
      [145] val safeName = label.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [146] val dir      = java.io.File(context.filesDir, "$BACKUPS_DIR/${snapshot.hardwareId}").also { it.mkdirs() }
      [147] val file     = java.io.File(dir, "${safeName}_${ts}.json")
    CALLS OUT:
      → DateTimeFormatter.ofPattern
      → LocalDateTime.now
      → Log.i
      → Regex
      → canWrite
      → file.writeText
      → hardwareId.isNotBlank
      → io.File
      → it.exists
      → it.mkdirs
      → label.replace
      → listBackups
      → snapshot.toJson

  [154] fun listBackups(context: android.content.Context, hardwareId: String): List<java.io.File> {
    CALLS OUT:
      → canWrite
      → hardwareId.isNotBlank
      → io.File
      → it.exists

  [162] fun canWrite(snapshot: RadioSnapshot): Boolean =
    CALLS OUT:
      → hardwareId.isNotBlank

========================================
FILE: ConvoyRadioWriter.kt
========================================

  [60] fun withLog(msg: String) = copy(log = log + msg)
    WORK FIELDS (local computed values):
      [93] val archivePath = ConvoyRadioManager.saveBackup(context, snapshot, "pre_write")
      [129] val pskBytes = okio.ByteString.of(*Base64.decode(workingConfig.channelPsk, Base64.NO_WRAP))
      [130] val chSettings = ChannelSettings(name = workingConfig.channelName, psk = pskBytes)
    CALLS OUT:
      → Base64.decode
      → Config
      → ConvoyRadioManager.saveBackup
      → Log.e
      → MutableStateFlow
      → WriteState
      → _state.asStateFlow
      → channelViewModel.setChannels
      → channelViewModel.setConfig
      → delay
      → emit
      → proceedToLoRa
      → reset

  [69] fun reset() {
    WORK FIELDS (local computed values):
      [93] val archivePath = ConvoyRadioManager.saveBackup(context, snapshot, "pre_write")
      [129] val pskBytes = okio.ByteString.of(*Base64.decode(workingConfig.channelPsk, Base64.NO_WRAP))
      [130] val chSettings = ChannelSettings(name = workingConfig.channelName, psk = pskBytes)
    CALLS OUT:
      → Base64.decode
      → Config
      → ConvoyRadioManager.saveBackup
      → Log.e
      → WriteState
      → channelViewModel.setChannels
      → channelViewModel.setConfig
      → delay
      → emit
      → proceedToLoRa
      → withLog

  [83] suspend fun execute(
    WORK FIELDS (local computed values):
      [93] val archivePath = ConvoyRadioManager.saveBackup(context, snapshot, "pre_write")
      [129] val pskBytes = okio.ByteString.of(*Base64.decode(workingConfig.channelPsk, Base64.NO_WRAP))
      [130] val chSettings = ChannelSettings(name = workingConfig.channelName, psk = pskBytes)
    CALLS OUT:
      → Base64.decode
      → Config
      → ConvoyRadioManager.saveBackup
      → Log.e
      → channelViewModel.setChannels
      → channelViewModel.setConfig
      → delay
      → emit
      → proceedToChannel
      → proceedToLoRa
      → updateBleStatus
      → withLog

  [147] fun proceedToLoRa() {
    WORK FIELDS (local computed values):
      [164] val current = _state.value
      [165] val wasConnected = current.bleConnected
      [187] val nodeId: String,
      [189] val longName: String,
      [190] val shortName: String,
      [191] val nodeRole: String,
      [192] val isManaged: Boolean,
      [193] val serialEnabled: Boolean,
      [195] val loraRegion: String,
      [196] val loraModemPreset: String,
      [197] val loraBandwidth: Int,
      [198] val loraSpreadFactor: Int,
    CALLS OUT:
      → Log.i
      → current.copy
      → emit
      → proceedToChannel
      → updateBleStatus
      → withLog

  [155] fun proceedToChannel() {
    WORK FIELDS (local computed values):
      [164] val current = _state.value
      [165] val wasConnected = current.bleConnected
      [187] val nodeId: String,
      [189] val longName: String,
      [190] val shortName: String,
      [191] val nodeRole: String,
      [192] val isManaged: Boolean,
      [193] val serialEnabled: Boolean,
      [195] val loraRegion: String,
      [196] val loraModemPreset: String,
      [197] val loraBandwidth: Int,
      [198] val loraSpreadFactor: Int,
    CALLS OUT:
      → Log.i
      → current.copy
      → emit
      → updateBleStatus
      → withLog

  [163] fun updateBleStatus(connected: Boolean) {
    WORK FIELDS (local computed values):
      [164] val current = _state.value
      [165] val wasConnected = current.bleConnected
      [187] val nodeId: String,
      [189] val longName: String,
      [190] val shortName: String,
      [191] val nodeRole: String,
      [192] val isManaged: Boolean,
      [193] val serialEnabled: Boolean,
      [195] val loraRegion: String,
      [196] val loraModemPreset: String,
      [197] val loraBandwidth: Int,
      [198] val loraSpreadFactor: Int,
    CALLS OUT:
      → Log.i
      → current.copy
      → emit
      → withLog

  [176] private fun emit(step: WriteStep, msg: String) {
    WORK FIELDS (local computed values):
      [187] val nodeId: String,
      [189] val longName: String,
      [190] val shortName: String,
      [191] val nodeRole: String,
      [192] val isManaged: Boolean,
      [193] val serialEnabled: Boolean,
      [195] val loraRegion: String,
      [196] val loraModemPreset: String,
      [197] val loraBandwidth: Int,
      [198] val loraSpreadFactor: Int,
      [199] val loraCodingRate: Int,
      [200] val loraHopLimit: Int,
    CALLS OUT:
      → Log.i
      → withLog

========================================
FILE: ConvoyReconnectWaitScreen.kt
========================================

  [43] fun ConvoyReconnectWaitScreen(
    WORK FIELDS (local computed values):
      [49] val scope           = rememberCoroutineScope()
      [50] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [51] val rawConnected    = connectionState.toString().contains("Connected", ignoreCase = true)
      [53] var countdown  by remember { mutableStateOf(60) }
      [54] var phase      by remember { mutableStateOf("WAITING") }
      [55] var statusMsg  by remember { mutableStateOf("Waiting for radio reboot after binary install...") }
      [56] var logLines   by remember { mutableStateOf(listOf<String>()) }
      [58] val savedDeviceAddress = remember { uiViewModel.getDeviceAddress() ?: "" }
    CALLS OUT:
      → LaunchedEffect
      → Log.i
      → addLog
      → contains
      → delay
      → hiltViewModel
      → mutableStateOf
      → onProceed
      → rememberCoroutineScope
      → uiViewModel.getDeviceAddress
      → uiViewModel.setDeviceAddress

  [60] fun addLog(msg: String) {
    CALLS OUT:
      → BackHandler
      → Box
      → LaunchedEffect
      → Log.i
      → addLog
      → background
      → delay
      → fillMaxSize
      → onProceed
      → padding
      → uiViewModel.setDeviceAddress

========================================
FILE: ConvoyRideDetailScreen.kt
========================================

  [34] fun ConvoyRideDetailScreen(
    WORK FIELDS (local computed values):
      [46] val context      = LocalContext.current
      [47] val scrollState  = rememberScrollState()
      [48] val currentUserId = remember { ConvoySessionManager.getUserId(context) ?: "" }
      [51] val rideName     = "Sunday Desert Run — Gooseberry Mesa"
      [52] val visibility   = "PRIVATE"
      [53] var rideStatus   by remember { mutableStateOf("open") }
      [54] val city         = "Springdale, UT"
      [55] val trailhead    = "Gooseberry Trailhead"
      [56] var rideDate     by remember { mutableStateOf("April 13, 2026") }
      [57] var arriveTime   by remember { mutableStateOf("7:30 AM") }
      [58] var rolloutTime  by remember { mutableStateOf("8:00 AM") }
      [59] var description  by remember { mutableStateOf("Moderate terrain, UTV recommended. Bring water and snacks. Meet at staging area.") }
    CALLS OUT:
      → ConvoyEnrollmentQueue.followOrganizer
      → ConvoyEnrollmentQueue.unfollowOrganizer
      → ConvoySessionManager.getUserId
      → EnrolleeData
      → apply
      → contains
      → context.getSharedPreferences
      → context.startActivity
      → current.add
      → current.remove
      → launchEmail
      → mutableStateOf
      → orgId.isEmpty
      → prefs.edit
      → rememberScrollState

  [92] fun launchEmail(address: String) {
    WORK FIELDS (local computed values):
      [93] val intent = Intent(Intent.ACTION_SENDTO, Uri.parse("mailto:$address"))
      [99] val prefs = context.getSharedPreferences("grouptrack_follows", android.content.Context.MODE_PRIVATE)
      [100] val current = (prefs.getStringSet("followed_ids", emptySet()) ?: emptySet()).toMutableSet()
      [112] val accepted = listOf(
      [119] val maybe = listOf(
      [123] val declined = listOf(
      [126] val invited = listOf(
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → ConvoyEnrollmentQueue.followOrganizer
      → ConvoyEnrollmentQueue.unfollowOrganizer
      → EnrolleeData
      → RoundedCornerShape
      → Row
      → Text
      → apply
      → background
      → clip
      → context.getSharedPreferences
      → context.startActivity
      → current.add

  [97] fun toggleFollow() {
    WORK FIELDS (local computed values):
      [99] val prefs = context.getSharedPreferences("grouptrack_follows", android.content.Context.MODE_PRIVATE)
      [100] val current = (prefs.getStringSet("followed_ids", emptySet()) ?: emptySet()).toMutableSet()
      [112] val accepted = listOf(
      [119] val maybe = listOf(
      [123] val declined = listOf(
      [126] val invited = listOf(
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → ConvoyEnrollmentQueue.followOrganizer
      → ConvoyEnrollmentQueue.unfollowOrganizer
      → DetailRideStatusBadge
      → DetailVisibilityBadge
      → EnrolleeData
      → RoundedCornerShape
      → Row
      → Text
      → apply
      → background
      → clip
      → context.getSharedPreferences

  [422] private fun EnrollmentTwistie(
    WORK FIELDS (local computed values):
      [468] val name  = row.name
      [469] val email = row.email
      [470] val date  = row.date
      [493] val textColor = when {
      [498] val bgColor = when {
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → Text
      → activeColor.copy
      → background
      → clip
      → content
      → copy
      → onEmailTap
      → onToggle
      → padding

  [463] private fun EnrolleeRow(
    WORK FIELDS (local computed values):
      [468] val name  = row.name
      [469] val email = row.email
      [470] val date  = row.date
      [493] val textColor = when {
      [498] val bgColor = when {
      [517] val color = if (visibility.uppercase() == "PUBLIC") Color(0xFF4AB8E8) else Color(0xFFF59E0B)
      [528] val (color, label) = when (status.lowercase()) {
    CALLS OUT:
      → Box
      → Color
      → Column
      → DetailRideStatusBadge
      → DetailVisibilityBadge
      → RoundedCornerShape
      → Text
      → activeColor.copy
      → background
      → copy
      → onClick
      → onEmailTap
      → padding
      → status.lowercase
      → then

  [489] private fun DetailActionButton(
    WORK FIELDS (local computed values):
      [493] val textColor = when {
      [498] val bgColor = when {
      [517] val color = if (visibility.uppercase() == "PUBLIC") Color(0xFF4AB8E8) else Color(0xFFF59E0B)
      [528] val (color, label) = when (status.lowercase()) {
    CALLS OUT:
      → Box
      → Color
      → DetailRideStatusBadge
      → DetailVisibilityBadge
      → RoundedCornerShape
      → Text
      → activeColor.copy
      → background
      → copy
      → onClick
      → padding
      → status.lowercase
      → then
      → visibility.uppercase

  [516] private fun DetailVisibilityBadge(visibility: String) {
    WORK FIELDS (local computed values):
      [517] val color = if (visibility.uppercase() == "PUBLIC") Color(0xFF4AB8E8) else Color(0xFFF59E0B)
      [528] val (color, label) = when (status.lowercase()) {
    CALLS OUT:
      → Box
      → Color
      → DetailRideStatusBadge
      → Text
      → background
      → padding
      → status.lowercase
      → visibility.uppercase

  [527] private fun DetailRideStatusBadge(status: String) {
    WORK FIELDS (local computed values):
      [528] val (color, label) = when (status.lowercase()) {
    CALLS OUT:
      → Box
      → Color
      → background
      → padding
      → status.lowercase

========================================
FILE: ConvoyRidesScreen.kt
========================================

  [28] fun ConvoyRidesScreen(
    WORK FIELDS (local computed values):
      [38] val scrollState = rememberScrollState()
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → background
      → material3.HorizontalDivider
      → onNavigateToCompletedRides
      → padding
      → rememberScrollState
      → verticalScroll
      → weight

  [139] private fun RidesMenuItem(
    WORK FIELDS (local computed values):
      [145] val isActive = status == "ACTIVE"
    CALLS OUT:
      → Box
      → Color
      → Column
      → background
      → clip
      → copy
      → onClick
      → padding
      → then

========================================
FILE: ConvoyRouteCreateScreen.kt
========================================

  [18] fun ConvoyRouteCreateScreen(onNavigateBack: () -> Unit = {}) {
    CALLS OUT:
      → Box
      → Column
      → Text

========================================
FILE: ConvoyScreen.kt
========================================

  [103] fun ConvoyScreen(
    WORK FIELDS (local computed values):
      [112] val channelViewModel: ChannelViewModel = hiltViewModel()
      [113] val uiViewModel: UIViewModel = hiltViewModel()
      [114] val convoyState by viewModel.convoyState.collectAsStateWithLifecycle()
      [115] val hudMode by viewModel.hudMode.collectAsStateWithLifecycle()
      [116] val selectedNode by viewModel.selectedNode.collectAsStateWithLifecycle()
      [117] val trackActive by viewModel.trackActive.collectAsStateWithLifecycle()
      [118] val trackLeadOnly by viewModel.trackLeadOnly.collectAsStateWithLifecycle()
      [119] val offTrackIds by viewModel.offTrackIds.collectAsStateWithLifecycle()
      [120] val simulationMode by viewModel.simulationMode.collectAsStateWithLifecycle()
      [121] val showLeadTrack by viewModel.showLeadTrack.collectAsStateWithLifecycle()
      [122] var recordingState by viewModel.recordingState
      [123] var showLocationPermissionDialog by remember { mutableStateOf(false) }
    CALLS OUT:
      → ActivityResultContracts.RequestPermission
      → DisposableEffect
      → Environment.isExternalStorageManager
      → SpatialDbManager.init
      → Toast.makeText
      → hiltViewModel
      → mutableStateOf
      → show

  [476] fun onViewportChanged(north: Double, south: Double, east: Double, west: Double, zoom: Double) {
    WORK FIELDS (local computed values):
      [478] val z = zoom.toInt()
      [479] val limit = if (z < 14) 500 else 2000
      [480] val capTrailState = ConvoyConfig.trailDisplayState
      [481] val capTrackState = ConvoyConfig.trackDisplayState
      [482] val capWaypointState = ConvoyConfig.waypointDisplayState
      [483] val capRouteState = ConvoyConfig.routeDisplayState
      [484] val capTrailIds = ConvoyConfig.trailChecked
      [485] val capTrackIds = ConvoyConfig.trackChecked
      [486] val capWaypointIds = ConvoyConfig.waypointChecked
      [487] val capRouteIds = ConvoyConfig.routeChecked
      [492] val trailsRaw = SpatialDbManager.queryTrailsByViewport(south, west, north, east, limit)
      [493] val trails = if (capTrailState == DS_SELECTED && capTrailIds != null) trailsRaw.filter { it["trail_id"] in capTrailIds } else trailsRaw
    CALLS OUT:
      → ConvoyTileCalculator.quickEstimate
      → ConvoyViewModel.PendingDownload
      → Log.e
      → Log.i
      → Looper.getMainLooper
      → SpatialDbManager.buildRouteGeoJson
      → SpatialDbManager.buildTrackGeoJson
      → SpatialDbManager.buildTrailGeoJson
      → SpatialDbManager.buildWaypointGeoJson
      → SpatialDbManager.init
      → SpatialDbManager.queryRoutesByViewport
      → SpatialDbManager.queryTracksByViewport
      → SpatialDbManager.queryTrailsByViewport
      → SpatialDbManager.queryWaypointsByViewport
      → onAreaSelected

  [521] fun onMarkerTapped(nodeId: String) {
    WORK FIELDS (local computed values):
      [522] val node = viewModel.convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }
      [529] val estimate = ConvoyTileCalculator.quickEstimate(north, south, east, west)
      [531] val pending = ConvoyViewModel.PendingDownload(
      [559] val ctx = view?.context ?: return
      [560] val cm = ctx?.getSystemService(android.content.Context.CONNECTIVITY_SERVICE) as? android.net.ConnectivityManager
      [561] val hasInternet = cm?.activeNetwork?.let { net ->
      [564] val tileUrl = if (hasInternet) {
      [579] val overlayJson = MapSourceManager.getOverlayJson(ConvoyConfig.ACTIVE_TILE_SOURCE)
      [586] val lm = ctx.getSystemService(android.content.Context.LOCATION_SERVICE) as android.location.LocationManager
      [587] val loc = lm.getLastKnownLocation(android.location.LocationManager.GPS_PROVIDER)
      [600] val url = request?.url?.toString() ?: return super.shouldInterceptRequest(view, request)
    CALLS OUT:
      → ConvoyTileCalculator.quickEstimate
      → ConvoyViewModel.PendingDownload
      → Log.d
      → Log.i
      → Log.w
      → Looper.getMainLooper
      → cm.getNetworkCapabilities
      → ctx.getSystemService
      → getSystemService
      → hasCapability
      → lm.getLastKnownLocation
      → onAreaSelected
      → onPageFinished
      → os.Handler
      → setLayerType

  [527] fun onAreaSelected(north: Double, south: Double, east: Double, west: Double) {
    WORK FIELDS (local computed values):
      [529] val estimate = ConvoyTileCalculator.quickEstimate(north, south, east, west)
      [531] val pending = ConvoyViewModel.PendingDownload(
      [559] val ctx = view?.context ?: return
      [560] val cm = ctx?.getSystemService(android.content.Context.CONNECTIVITY_SERVICE) as? android.net.ConnectivityManager
      [561] val hasInternet = cm?.activeNetwork?.let { net ->
      [564] val tileUrl = if (hasInternet) {
      [579] val overlayJson = MapSourceManager.getOverlayJson(ConvoyConfig.ACTIVE_TILE_SOURCE)
      [586] val lm = ctx.getSystemService(android.content.Context.LOCATION_SERVICE) as android.location.LocationManager
      [587] val loc = lm.getLastKnownLocation(android.location.LocationManager.GPS_PROVIDER)
      [600] val url = request?.url?.toString() ?: return super.shouldInterceptRequest(view, request)
      [602] val tilePath = url.removePrefix("convoy://tiles/")
      [603] val file = java.io.File(ConvoyConfig.TILE_DIR, tilePath)
    CALLS OUT:
      → ConvoyTileCalculator.quickEstimate
      → ConvoyViewModel.PendingDownload
      → Log.d
      → Log.i
      → Log.w
      → Looper.getMainLooper
      → cm.getNetworkCapabilities
      → ctx.getSystemService
      → file.exists
      → file.inputStream
      → getSystemService
      → hasCapability
      → io.File
      → lm.getLastKnownLocation
      → onPageFinished

  [557] override fun onPageFinished(view: android.webkit.WebView?, url: String?) {
    WORK FIELDS (local computed values):
      [559] val ctx = view?.context ?: return
      [560] val cm = ctx?.getSystemService(android.content.Context.CONNECTIVITY_SERVICE) as? android.net.ConnectivityManager
      [561] val hasInternet = cm?.activeNetwork?.let { net ->
      [564] val tileUrl = if (hasInternet) {
      [579] val overlayJson = MapSourceManager.getOverlayJson(ConvoyConfig.ACTIVE_TILE_SOURCE)
      [586] val lm = ctx.getSystemService(android.content.Context.LOCATION_SERVICE) as android.location.LocationManager
      [587] val loc = lm.getLastKnownLocation(android.location.LocationManager.GPS_PROVIDER)
      [600] val url = request?.url?.toString() ?: return super.shouldInterceptRequest(view, request)
      [602] val tilePath = url.removePrefix("convoy://tiles/")
      [603] val file = java.io.File(ConvoyConfig.TILE_DIR, tilePath)
      [610] val parts = url.split("/tile/").lastOrNull()?.split("/")
      [612] val file = java.io.File(ConvoyConfig.TILE_DIR, "SAT_LABELS_TRANSPORT/${parts[0]}/${parts[2]}/${parts[1]}.png")
    CALLS OUT:
      → ConvoyConfig.migrateTiles
      → Log.d
      → Log.w
      → Looper.getMainLooper
      → cm.getNetworkCapabilities
      → ctx.getSystemService
      → file.exists
      → file.inputStream
      → getSystemService
      → hasCapability
      → io.File
      → io.FileInputStream
      → lastOrNull
      → lm.getLastKnownLocation
      → msg.lineNumber

  [599] override fun shouldInterceptRequest(view: android.webkit.WebView?, request: android.webkit.WebResourceRequest?): android.webkit.WebResourceResponse? {
    WORK FIELDS (local computed values):
      [600] val url = request?.url?.toString() ?: return super.shouldInterceptRequest(view, request)
      [602] val tilePath = url.removePrefix("convoy://tiles/")
      [603] val file = java.io.File(ConvoyConfig.TILE_DIR, tilePath)
      [610] val parts = url.split("/tile/").lastOrNull()?.split("/")
      [612] val file = java.io.File(ConvoyConfig.TILE_DIR, "SAT_LABELS_TRANSPORT/${parts[0]}/${parts[2]}/${parts[1]}.png")
      [619] val parts = url.split("/tile/").lastOrNull()?.split("/")
      [621] val file = java.io.File(ConvoyConfig.TILE_DIR, "SAT_LABELS_PLACES/${parts[0]}/${parts[2]}/${parts[1]}.png")
      [642] val node = viewModel.convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }
      [649] val z = zoom.toInt()
      [650] val limit = if (z < 14) 500 else 2000
      [651] val capTrailState = ConvoyConfig.trailDisplayState
      [652] val capTrackState = ConvoyConfig.trackDisplayState
    CALLS OUT:
      → ConvoyConfig.migrateTiles
      → Log.d
      → Looper.getMainLooper
      → SpatialDbManager.buildTrackGeoJson
      → SpatialDbManager.buildTrailGeoJson
      → SpatialDbManager.buildWaypointGeoJson
      → SpatialDbManager.init
      → SpatialDbManager.queryTracksByViewport
      → SpatialDbManager.queryTrailsByViewport
      → SpatialDbManager.queryWaypointsByViewport
      → file.exists
      → file.inputStream
      → io.File
      → io.FileInputStream
      → lastOrNull

  [631] override fun onConsoleMessage(msg: android.webkit.ConsoleMessage): Boolean {
    WORK FIELDS (local computed values):
      [642] val node = viewModel.convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }
      [649] val z = zoom.toInt()
      [650] val limit = if (z < 14) 500 else 2000
      [651] val capTrailState = ConvoyConfig.trailDisplayState
      [652] val capTrackState = ConvoyConfig.trackDisplayState
      [653] val capWaypointState = ConvoyConfig.waypointDisplayState
      [654] val capRouteState = ConvoyConfig.routeDisplayState
      [658] val capTrailIds = ConvoyConfig.trailChecked
      [659] val capTrackIds = ConvoyConfig.trackChecked
      [660] val capWaypointIds = ConvoyConfig.waypointChecked
      [661] val capRouteIds = ConvoyConfig.routeChecked
      [663] val trails = SpatialDbManager.queryTrailsByViewport(south, west, north, east, limit)
    CALLS OUT:
      → ConvoyConfig.migrateTiles
      → ConvoyTileCalculator.quickEstimate
      → ConvoyViewModel.PendingDownload
      → Log.d
      → Log.e
      → Log.i
      → Looper.getMainLooper
      → SpatialDbManager.buildRouteGeoJson
      → SpatialDbManager.buildTrackGeoJson
      → SpatialDbManager.buildTrailGeoJson
      → SpatialDbManager.buildWaypointGeoJson
      → SpatialDbManager.init
      → SpatialDbManager.queryRoutesByViewport
      → SpatialDbManager.queryTracksByViewport
      → SpatialDbManager.queryTrailsByViewport

  [641] fun onMarkerTapped(nodeId: String) {
    WORK FIELDS (local computed values):
      [642] val node = viewModel.convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }
      [649] val z = zoom.toInt()
      [650] val limit = if (z < 14) 500 else 2000
      [651] val capTrailState = ConvoyConfig.trailDisplayState
      [652] val capTrackState = ConvoyConfig.trackDisplayState
      [653] val capWaypointState = ConvoyConfig.waypointDisplayState
      [654] val capRouteState = ConvoyConfig.routeDisplayState
      [658] val capTrailIds = ConvoyConfig.trailChecked
      [659] val capTrackIds = ConvoyConfig.trackChecked
      [660] val capWaypointIds = ConvoyConfig.waypointChecked
      [661] val capRouteIds = ConvoyConfig.routeChecked
      [663] val trails = SpatialDbManager.queryTrailsByViewport(south, west, north, east, limit)
    CALLS OUT:
      → ConvoyTileCalculator.quickEstimate
      → ConvoyViewModel.PendingDownload
      → Log.e
      → Log.i
      → Looper.getMainLooper
      → SpatialDbManager.buildRouteGeoJson
      → SpatialDbManager.buildTrackGeoJson
      → SpatialDbManager.buildTrailGeoJson
      → SpatialDbManager.buildWaypointGeoJson
      → SpatialDbManager.init
      → SpatialDbManager.queryRoutesByViewport
      → SpatialDbManager.queryTracksByViewport
      → SpatialDbManager.queryTrailsByViewport
      → SpatialDbManager.queryWaypointsByViewport
      → io.File

  [647] fun onViewportChanged(north: Double, south: Double, east: Double, west: Double, zoom: Double) {
    WORK FIELDS (local computed values):
      [649] val z = zoom.toInt()
      [650] val limit = if (z < 14) 500 else 2000
      [651] val capTrailState = ConvoyConfig.trailDisplayState
      [652] val capTrackState = ConvoyConfig.trackDisplayState
      [653] val capWaypointState = ConvoyConfig.waypointDisplayState
      [654] val capRouteState = ConvoyConfig.routeDisplayState
      [658] val capTrailIds = ConvoyConfig.trailChecked
      [659] val capTrackIds = ConvoyConfig.trackChecked
      [660] val capWaypointIds = ConvoyConfig.waypointChecked
      [661] val capRouteIds = ConvoyConfig.routeChecked
      [663] val trails = SpatialDbManager.queryTrailsByViewport(south, west, north, east, limit)
      [664] val json = SpatialDbManager.buildTrailGeoJson(trails)
    CALLS OUT:
      → ConvoyTileCalculator.quickEstimate
      → ConvoyViewModel.PendingDownload
      → Log.e
      → Log.i
      → Looper.getMainLooper
      → SpatialDbManager.buildRouteGeoJson
      → SpatialDbManager.buildTrackGeoJson
      → SpatialDbManager.buildTrailGeoJson
      → SpatialDbManager.buildWaypointGeoJson
      → SpatialDbManager.init
      → SpatialDbManager.queryRoutesByViewport
      → SpatialDbManager.queryTracksByViewport
      → SpatialDbManager.queryTrailsByViewport
      → SpatialDbManager.queryWaypointsByViewport
      → io.File

  [697] fun onAreaSelected(north: Double, south: Double, east: Double, west: Double) {
    WORK FIELDS (local computed values):
      [699] val estimate = ConvoyTileCalculator.quickEstimate(north, south, east, west)
      [701] val pending = ConvoyViewModel.PendingDownload(
      [719] val wv = webViewRef.value ?: return@post
      [721] val tilesDir = java.io.File(ConvoyConfig.TILE_DIR, "SAT/18")
      [723] val bounds = mutableListOf<String>()
      [725] val z = 18
      [726] val n = 1 shl z
      [729] var xMin = Long.MAX_VALUE; var xMax = Long.MIN_VALUE
      [730] var yMin = Long.MAX_VALUE; var yMax = Long.MIN_VALUE
      [732] val x = xDir.name.toLongOrNull() ?: return@forEach
      [734] val y = yFile.name.removeSuffix(".png").toLongOrNull() ?: return@forEach
      [740] val tileN = Math.toDegrees(Math.atan(Math.sinh(Math.PI * (1.0 - 2.0 * yMin / n))))
    CALLS OUT:
      → ConvoyTileCalculator.quickEstimate
      → ConvoyViewModel.PendingDownload
      → Log.i
      → Looper.getMainLooper
      → Math.atan
      → Math.sinh
      → Math.toDegrees
      → bounds.add
      → findNearestMarker
      → io.File
      → name.removeSuffix
      → nodeId.isNotEmpty
      → os.Handler
      → showDownloadedAreas
      → start

  [717] fun onMapBoundsReady(north: Double, south: Double, east: Double, west: Double) {
    WORK FIELDS (local computed values):
      [719] val wv = webViewRef.value ?: return@post
      [721] val tilesDir = java.io.File(ConvoyConfig.TILE_DIR, "SAT/18")
      [723] val bounds = mutableListOf<String>()
      [725] val z = 18
      [726] val n = 1 shl z
      [729] var xMin = Long.MAX_VALUE; var xMax = Long.MIN_VALUE
      [730] var yMin = Long.MAX_VALUE; var yMax = Long.MIN_VALUE
      [732] val x = xDir.name.toLongOrNull() ?: return@forEach
      [734] val y = yFile.name.removeSuffix(".png").toLongOrNull() ?: return@forEach
      [740] val tileN = Math.toDegrees(Math.atan(Math.sinh(Math.PI * (1.0 - 2.0 * yMin / n))))
      [741] val tileS = Math.toDegrees(Math.atan(Math.sinh(Math.PI * (1.0 - 2.0 * (yMax + 1) / n))))
      [742] val tileW = xMin.toDouble() / n * 360.0 - 180.0
    CALLS OUT:
      → LaunchedEffect
      → Log.i
      → Looper.getMainLooper
      → Math.atan
      → Math.sinh
      → Math.toDegrees
      → bounds.add
      → coroutines.delay
      → findNearestMarker
      → io.File
      → name.removeSuffix
      → nodeId.isNotEmpty
      → os.Handler
      → showDownloadedAreas
      → start

  [1458] fun GroupHud(
    WORK FIELDS (local computed values):
      [1471] val chColor = when {
      [1477] // Vertical interval slider — flush against HudCard
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Row
      → SliderDefaults.colors
      → Text
      → copy
      → geometry.Offset
      → graphics.Shadow
      → height
      → material3.Slider
      → offset
      → shape.RoundedCornerShape
      → text.TextStyle
      → width

  [1561] fun MyCartHud(
    WORK FIELDS (local computed values):
      [1567] val myCart = state.nodes.firstOrNull { it.isMyCart }
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → HudStat
      → Row
      → Spacer
      → Text
      → geometry.Offset
      → graphics.Shadow
      → text.TextStyle

  [1624] fun NodeDetailHud(
    WORK FIELDS (local computed values):
      [1691] val infiniteTransition = rememberInfiniteTransition(label = "pill_blink")
      [1692] val alpha by infiniteTransition.animateFloat(
    CALLS OUT:
      → Color
      → HudStat
      → RoundedCornerShape
      → Row
      → Spacer
      → Text
      → geometry.Offset
      → graphics.Shadow
      → infiniteRepeatable
      → layout.Box
      → onDismiss
      → onExpand
      → onRemove
      → padding
      → rememberInfiniteTransition

  [1685] fun CollapsedPill(
    WORK FIELDS (local computed values):
      [1691] val infiniteTransition = rememberInfiniteTransition(label = "pill_blink")
      [1692] val alpha by infiniteTransition.animateFloat(
      [1725] val infiniteTransition = rememberInfiniteTransition(label = "banner_blink")
      [1726] val alpha by infiniteTransition.animateFloat(
      [1731] val nameStr = if (lostNames.isNotEmpty()) lostNames.joinToString(", ") else ""
    CALLS OUT:
      → Color
      → Column
      → ContactLostBanner
      → HudCard
      → RoundedCornerShape
      → Spacer
      → Text
      → alpha
      → infiniteRepeatable
      → lostNames.isNotEmpty
      → nameStr.isNotEmpty
      → onExpand
      → padding
      → rememberInfiniteTransition
      → wrapContentWidth

  [1724] fun ContactLostBanner(lostCount: Int, lostNames: List<String> = emptyList(), modifier: Modifier = Modifier) {
    WORK FIELDS (local computed values):
      [1725] val infiniteTransition = rememberInfiniteTransition(label = "banner_blink")
      [1726] val alpha by infiniteTransition.animateFloat(
      [1731] val nameStr = if (lostNames.isNotEmpty()) lostNames.joinToString(", ") else ""
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → HudCard
      → HudModeRow
      → HudStat
      → RoundedCornerShape
      → Text
      → alpha
      → geometry.Offset
      → graphics.Shadow
      → infiniteRepeatable
      → lostNames.isNotEmpty
      → nameStr.isNotEmpty
      → onModeChange

  [1760] fun HudCard(content: @Composable ColumnScope.() -> Unit) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → HudModeRow
      → HudStat
      → RoundedCornerShape
      → Text
      → geometry.Offset
      → graphics.Shadow
      → onModeChange
      → padding
      → text.TextStyle
      → wrapContentWidth

  [1770] fun HudStat(label: String, value: String, valueColor: Color = Color(0xFF111111)) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → HudModeRow
      → RoundedCornerShape
      → Text
      → geometry.Offset
      → graphics.Shadow
      → onModeChange
      → text.TextStyle

  [1782] fun HudModeRow(current: HudMode, onModeChange: (HudMode) -> Unit, onNavigateToSettings: () -> Unit = {}) {
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → RoundedCornerShape
      → onModeChange

  [1856] fun CartPickerPanel(
    CALLS OUT:
      → Color
      → Column
      → RoundedCornerShape
      → background
      → fillMaxWidth
      → nodes.isEmpty
      → onDismiss
      → onSelect
      → padding

  [1932] fun ConvoyButtonBar(
    WORK FIELDS (local computed values):
      [1958] val isActive = hudMode == mode
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Triple
      → action
      → background
      → fillMaxHeight
      → fillMaxWidth
      → geometry.Offset
      → height
      → onModeChange
      → onSelectCart
      → weight

  [2047] fun convoyListTracks(context: android.content.Context): List<String> {
    WORK FIELDS (local computed values):
      [2048] val dir = java.io.File(
      [2056] val ext = f.extension.lowercase()
      [2072] val dir = java.io.File(
      [2077] val file = java.io.File(dir, fileName)
      [2079] val text = file.readText()
      [2080] val coords = if (fileName.lowercase().endsWith(".gpx")) convoyParseGpx(text) else convoyParseKml(text)
      [2095] val dir = java.io.File(
      [2100] val file = java.io.File(dir, fileName)
      [2102] val text = file.readText()
      [2103] val coords = if (fileName.lowercase().endsWith(".gpx")) convoyParseGpx(text) else convoyParseKml(text)
      [2105] val json = coords.joinToString(",", "[", "]") { p -> "[${p.first},${p.second}]" }
      [2106] val safe = fileName.replace("'", "\\'")
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Log.e
      → Pair
      → Regex
      → convoyParseGpx
      → convoyParseKml
      → coords.add
      → coords.isEmpty
      → dir.exists
      → dir.listFiles
      → endsWith
      → extension.lowercase
      → file.exists
      → file.readText
      → fileName.lowercase

  [2067] fun convoyLoadTrackData(
    WORK FIELDS (local computed values):
      [2072] val dir = java.io.File(
      [2077] val file = java.io.File(dir, fileName)
      [2079] val text = file.readText()
      [2080] val coords = if (fileName.lowercase().endsWith(".gpx")) convoyParseGpx(text) else convoyParseKml(text)
      [2095] val dir = java.io.File(
      [2100] val file = java.io.File(dir, fileName)
      [2102] val text = file.readText()
      [2103] val coords = if (fileName.lowercase().endsWith(".gpx")) convoyParseGpx(text) else convoyParseKml(text)
      [2105] val json = coords.joinToString(",", "[", "]") { p -> "[${p.first},${p.second}]" }
      [2106] val safe = fileName.replace("'", "\\'")
      [2114] val coords = mutableListOf<Pair<Double, Double>>()
      [2115] val pattern = Regex("""<coordinates>([\s\S]*?)</coordinates>""")
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Log.e
      → Pair
      → Regex
      → convoyParseGpx
      → convoyParseKml
      → coords.add
      → coords.isEmpty
      → endsWith
      → file.exists
      → file.readText
      → fileName.lowercase
      → fileName.replace
      → io.File
      → line.trim

  [2088] fun convoyLoadTrack(
    WORK FIELDS (local computed values):
      [2095] val dir = java.io.File(
      [2100] val file = java.io.File(dir, fileName)
      [2102] val text = file.readText()
      [2103] val coords = if (fileName.lowercase().endsWith(".gpx")) convoyParseGpx(text) else convoyParseKml(text)
      [2105] val json = coords.joinToString(",", "[", "]") { p -> "[${p.first},${p.second}]" }
      [2106] val safe = fileName.replace("'", "\\'")
      [2114] val coords = mutableListOf<Pair<Double, Double>>()
      [2115] val pattern = Regex("""<coordinates>([\s\S]*?)</coordinates>""")
      [2118] val parts = line.trim().split(",")
      [2120] val lon = parts[0].toDoubleOrNull()
      [2121] val lat = parts[1].toDoubleOrNull()
      [2132] val coords = mutableListOf<Pair<Double, Double>>()
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Log.e
      → Pair
      → Regex
      → convoyParseGpx
      → convoyParseKml
      → coords.add
      → coords.isEmpty
      → endsWith
      → file.exists
      → file.readText
      → fileName.lowercase
      → fileName.replace
      → io.File
      → line.trim

  [2113] fun convoyParseKml(text: String): List<Pair<Double, Double>> {
    WORK FIELDS (local computed values):
      [2114] val coords = mutableListOf<Pair<Double, Double>>()
      [2115] val pattern = Regex("""<coordinates>([\s\S]*?)</coordinates>""")
      [2118] val parts = line.trim().split(",")
      [2120] val lon = parts[0].toDoubleOrNull()
      [2121] val lat = parts[1].toDoubleOrNull()
      [2132] val coords = mutableListOf<Pair<Double, Double>>()
      [2133] val pattern = Regex("""<trkpt\s+lat="([^"]+)"\s+lon="([^"]+)"""")
      [2135] val lat = match.groupValues[1].toDoubleOrNull()
      [2136] val lon = match.groupValues[2].toDoubleOrNull()
    CALLS OUT:
      → Pair
      → Regex
      → convoyParseGpx
      → coords.add
      → line.trim
      → lines
      → pattern.findAll
      → split
      → trim

  [2131] fun convoyParseGpx(text: String): List<Pair<Double, Double>> {
    WORK FIELDS (local computed values):
      [2132] val coords = mutableListOf<Pair<Double, Double>>()
      [2133] val pattern = Regex("""<trkpt\s+lat="([^"]+)"\s+lon="([^"]+)"""")
      [2135] val lat = match.groupValues[1].toDoubleOrNull()
      [2136] val lon = match.groupValues[2].toDoubleOrNull()
    CALLS OUT:
      → Pair
      → Regex
      → coords.add
      → pattern.findAll

========================================
FILE: ConvoySearchByAreaScreen.kt
========================================

  [39] fun ConvoySearchByAreaScreen(
    CALLS OUT:
      → Arrangement.spacedBy
      → Box
      → Color
      → Column
      → Row
      → Spacer
      → Text
      → background
      → clip
      → material3.HorizontalDivider
      → onBack
      → padding
      → weight

========================================
FILE: ConvoyServicesChooser.kt
========================================

  [31] fun ConvoyServicesChooser(
    WORK FIELDS (local computed values):
      [37] val sheetState = rememberModalBottomSheetState(skipPartiallyExpanded = true)
      [38] val signedIn = ConvoySessionManager.isSignedIn(context)
      [39] val subscribed = ConvoySessionManager.isSubscribed(context)
    CALLS OUT:
      → Brush.horizontalGradient
      → Color
      → ConvoySessionManager.isSignedIn
      → ConvoySessionManager.isSubscribed
      → RoundedCornerShape
      → Row
      → Spacer
      → clip
      → fillMaxWidth
      → height
      → listOf
      → onOnline
      → padding
      → rememberModalBottomSheetState

========================================
FILE: ConvoySessionManager.kt
========================================

  [14] fun getUserId(ctx: Context): String? =
    WORK FIELDS (local computed values):
      [22] val exp = prefs(ctx).getLong("subscription_expires_at", 0L)
    CALLS OUT:
      → System.currentTimeMillis
      → apply
      → edit
      → getCity
      → getEmail
      → getFirstName
      → getState
      → getUserId
      → getZipCode
      → isOrganizer
      → isSignedIn
      → isSubscribed
      → isSubscriptionExpired
      → prefs
      → privacyAccepted

  [17] fun isSignedIn(ctx: Context): Boolean =
    WORK FIELDS (local computed values):
      [22] val exp = prefs(ctx).getLong("subscription_expires_at", 0L)
    CALLS OUT:
      → System.currentTimeMillis
      → apply
      → edit
      → getCity
      → getEmail
      → getFirstName
      → getSearchRadius
      → getState
      → getUserId
      → getZipCode
      → isOrganizer
      → isSignedIn
      → isSubscribed
      → isSubscriptionExpired
      → prefs

  [20] fun isSubscribed(ctx: Context): Boolean {
    WORK FIELDS (local computed values):
      [22] val exp = prefs(ctx).getLong("subscription_expires_at", 0L)
    CALLS OUT:
      → Log.i
      → System.currentTimeMillis
      → acceptPrivacy
      → acceptTerms
      → apply
      → edit
      → getCity
      → getEmail
      → getFirstName
      → getSearchRadius
      → getState
      → getZipCode
      → isOrganizer
      → isSignedIn
      → isSubscribed

  [26] fun isSubscriptionExpired(ctx: Context): Boolean =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → edit
      → getCity
      → getEmail
      → getFirstName
      → getSearchRadius
      → getState
      → getZipCode
      → isOrganizer
      → isSignedIn

  [29] fun termsAccepted(ctx: Context): Boolean =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → edit
      → getCity
      → getEmail
      → getFirstName
      → getSearchRadius
      → getState
      → getZipCode
      → isOrganizer
      → isSignedIn

  [32] fun privacyAccepted(ctx: Context): Boolean =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → edit
      → getCity
      → getEmail
      → getFirstName
      → getSearchRadius
      → getState
      → getZipCode
      → isOrganizer
      → isSignedIn

  [35] fun isOrganizer(ctx: Context): Boolean =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → edit
      → getCity
      → getEmail
      → getFirstName
      → getSearchRadius
      → getState
      → getZipCode
      → isSignedIn
      → isSubscribed

  [38] fun getFirstName(ctx: Context): String =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → getCity
      → getEmail
      → getSearchRadius
      → getState
      → getZipCode
      → isSignedIn
      → isSubscribed

  [41] fun getEmail(ctx: Context): String =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → getCity
      → getSearchRadius
      → getState
      → getZipCode
      → isSignedIn
      → isSubscribed
      → prefs

  [44] fun getZipCode(ctx: Context): String =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → getCity
      → getSearchRadius
      → getState
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted

  [46] fun getCity(ctx: Context): String =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → getSearchRadius
      → getState
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute

  [48] fun setCity(ctx: Context, city: String) =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → getSearchRadius
      → getState
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute

  [50] fun getState(ctx: Context): String =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → getSearchRadius
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → setOrganizer

  [52] fun setState(ctx: Context, state: String) =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → getSearchRadius
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → setOrganizer

  [58] fun getSearchRadius(ctx: Context): Int =
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → setOrganizer
      → setSearchRadius

  [61] fun setZipCode(ctx: Context, zip: String) {
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → setOrganizer
      → setSearchRadius

  [65] fun setSearchRadius(ctx: Context, miles: Int) {
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → setOrganizer
      → setSubscriptionExpiry

  [69] fun saveUser(ctx: Context, userId: String, googleId: String,
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → acceptTerms
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → setOrganizer
      → setSubscriptionExpiry

  [82] fun acceptTerms(ctx: Context) {
    CALLS OUT:
      → Log.i
      → acceptPrivacy
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → setOrganizer
      → setSubscriptionExpiry
      → termsAccepted

  [86] fun acceptPrivacy(ctx: Context) {
    CALLS OUT:
      → Log.i
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → setOrganizer
      → setSubscriptionExpiry
      → termsAccepted

  [90] fun setOrganizer(ctx: Context, value: Boolean) {
    CALLS OUT:
      → Log.i
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → setSubscriptionExpiry
      → termsAccepted

  [94] fun setSubscriptionExpiry(ctx: Context, expiresAtMs: Long) {
    CALLS OUT:
      → Log.i
      → apply
      → clear
      → clearSession
      → ctx.getSharedPreferences
      → edit
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → termsAccepted

  [98] fun clearSession(ctx: Context) {
    CALLS OUT:
      → Log.i
      → apply
      → clear
      → ctx.getSharedPreferences
      → edit
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → resolveLaunchRoute
      → termsAccepted

  [107] fun resolveLaunchRoute(ctx: Context, hasInternet: Boolean): LaunchRoute {
    CALLS OUT:
      → ctx.getSharedPreferences
      → isSignedIn
      → isSubscribed
      → prefs
      → privacyAccepted
      → termsAccepted

  [115] private fun prefs(ctx: Context) =
    CALLS OUT:
      → ctx.getSharedPreferences

========================================
FILE: ConvoySettingsPanel.kt
========================================

  [54] private fun sha256(input: String): String {
    WORK FIELDS (local computed values):
      [55] val bytes = MessageDigest.getInstance("SHA-256").digest(input.toByteArray())
      [65] var password by remember { mutableStateOf("") }
      [66] val keyboard = LocalSoftwareKeyboardController.current
    CALLS OUT:
      → Color
      → MessageDigest.getInstance
      → RoundedCornerShape
      → Spacer
      → Text
      → background
      → digest
      → fillMaxSize
      → hide
      → imePadding
      → mutableStateOf
      → onAuthenticated
      → onDismiss
      → padding
      → sha256

  [61] fun ConvoySettingsGate(
    WORK FIELDS (local computed values):
      [65] var password by remember { mutableStateOf("") }
      [66] val keyboard = LocalSoftwareKeyboardController.current
    CALLS OUT:
      → Color
      → RoundedCornerShape
      → Spacer
      → Text
      → background
      → fillMaxSize
      → hide
      → imePadding
      → mutableStateOf
      → onAuthenticated
      → onDismiss
      → padding
      → sha256
      → tryAuth

  [67] fun tryAuth() { keyboard?.hide(); if (sha256(password) == PASSWORD_HASH) onAuthenticated() else onDismiss() }
    CALLS OUT:
      → Color
      → RoundedCornerShape
      → Spacer
      → Text
      → background
      → fillMaxSize
      → imePadding
      → onDismiss
      → padding
      → tryAuth

  [144] fun ConvoySettingsPanelScreen(
    CALLS OUT:
      → Box
      → Color
      → Column
      → PanelSection
      → RoundedCornerShape
      → Spacer
      → Surface
      → Text
      → background
      → fillMaxSize
      → height
      → imePadding
      → onBack
      → onNavigateToArchiveRestore
      → padding

  [256] private fun PanelSection(text: String) {
    CALLS OUT:
      → Color

========================================
FILE: ConvoySettingsRepository.kt
========================================

  [66] suspend fun setSignalDropMinutes(value: Float) {
    WORK FIELDS (local computed values):
      [90] val entry = "$nodeId|$callsign|${LocalDate.now().toEpochDay()}"
      [92] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [99] val today = LocalDate.now().toEpochDay().toString()
      [101] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [110] val today = LocalDate.now().toEpochDay().toString()
      [112] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
    CALLS OUT:
      → LocalDate.now
      → it.endsWith
      → it.startsWith
      → purgeStaleRemovedCarts
      → reinstateCart
      → removeCart
      → setAdmissionWindowHours
      → setLeadLockFeet
      → setOffTrackMiles
      → setSignalLostMinutes
      → toEpochDay

  [71] suspend fun setSignalLostMinutes(value: Float) {
    WORK FIELDS (local computed values):
      [90] val entry = "$nodeId|$callsign|${LocalDate.now().toEpochDay()}"
      [92] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [99] val today = LocalDate.now().toEpochDay().toString()
      [101] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [110] val today = LocalDate.now().toEpochDay().toString()
      [112] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
    CALLS OUT:
      → LocalDate.now
      → it.endsWith
      → it.startsWith
      → purgeStaleRemovedCarts
      → reinstateCart
      → removeCart
      → setAdmissionWindowHours
      → setLeadLockFeet
      → setOffTrackMiles
      → toEpochDay

  [76] suspend fun setOffTrackMiles(value: Float) {
    WORK FIELDS (local computed values):
      [90] val entry = "$nodeId|$callsign|${LocalDate.now().toEpochDay()}"
      [92] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [99] val today = LocalDate.now().toEpochDay().toString()
      [101] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [110] val today = LocalDate.now().toEpochDay().toString()
      [112] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
    CALLS OUT:
      → LocalDate.now
      → it.endsWith
      → it.startsWith
      → purgeStaleRemovedCarts
      → reinstateCart
      → removeCart
      → setAdmissionWindowHours
      → setLeadLockFeet
      → toEpochDay

  [81] suspend fun setAdmissionWindowHours(value: Int) {
    WORK FIELDS (local computed values):
      [90] val entry = "$nodeId|$callsign|${LocalDate.now().toEpochDay()}"
      [92] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [99] val today = LocalDate.now().toEpochDay().toString()
      [101] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [110] val today = LocalDate.now().toEpochDay().toString()
      [112] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
    CALLS OUT:
      → LocalDate.now
      → it.endsWith
      → it.startsWith
      → purgeStaleRemovedCarts
      → reinstateCart
      → removeCart
      → setLeadLockFeet
      → toEpochDay

  [84] suspend fun setLeadLockFeet(value: Float) {
    WORK FIELDS (local computed values):
      [90] val entry = "$nodeId|$callsign|${LocalDate.now().toEpochDay()}"
      [92] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [99] val today = LocalDate.now().toEpochDay().toString()
      [101] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [110] val today = LocalDate.now().toEpochDay().toString()
      [112] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
    CALLS OUT:
      → LocalDate.now
      → it.endsWith
      → it.startsWith
      → purgeStaleRemovedCarts
      → reinstateCart
      → removeCart
      → toEpochDay

  [89] suspend fun removeCart(nodeId: String, callsign: String) {
    WORK FIELDS (local computed values):
      [90] val entry = "$nodeId|$callsign|${LocalDate.now().toEpochDay()}"
      [92] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [99] val today = LocalDate.now().toEpochDay().toString()
      [101] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [110] val today = LocalDate.now().toEpochDay().toString()
      [112] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
    CALLS OUT:
      → LocalDate.now
      → it.endsWith
      → it.startsWith
      → purgeStaleRemovedCarts
      → reinstateCart
      → toEpochDay

  [98] suspend fun reinstateCart(nodeId: String) {
    WORK FIELDS (local computed values):
      [99] val today = LocalDate.now().toEpochDay().toString()
      [101] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
      [110] val today = LocalDate.now().toEpochDay().toString()
      [112] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
    CALLS OUT:
      → LocalDate.now
      → it.endsWith
      → it.startsWith
      → purgeStaleRemovedCarts
      → toEpochDay

  [109] suspend fun purgeStaleRemovedCarts() {
    WORK FIELDS (local computed values):
      [110] val today = LocalDate.now().toEpochDay().toString()
      [112] val current = prefs[KEY_REMOVED_CARTS] ?: emptySet()
    CALLS OUT:
      → LocalDate.now
      → it.endsWith
      → toEpochDay

========================================
FILE: ConvoySettingsScreen.kt
========================================

  [52] fun ConvoySettingsScreen(
    WORK FIELDS (local computed values):
      [58] val uiState by viewModel.uiState.collectAsStateWithLifecycle()
      [59] val trackLeadOnly by convoyViewModel.trackLeadOnly.collectAsStateWithLifecycle()
      [60] val snackbarHostState = remember { SnackbarHostState() }
    CALLS OUT:
      → HorizontalDivider
      → IconButton
      → LaunchedEffect
      → SectionLabel
      → SnackbarHost
      → SnackbarHostState
      → Spacer
      → Text
      → fillMaxSize
      → hiltViewModel
      → padding
      → snackbarHostState.showSnackbar
      → verticalScroll
      → viewModel.onUserMessageShown

  [269] private fun SectionLabel(title: String) {
    WORK FIELDS (local computed values):
      [289] var localValue = value
    CALLS OUT:
      → Text
      → fillMaxWidth
      → onValueChangeFinished
      → padding

  [280] private fun AlertSlider(
    WORK FIELDS (local computed values):
      [289] var localValue = value
    CALLS OUT:
      → Icon
      → Text
      → TextButton
      → fillMaxWidth
      → onValueChangeFinished
      → padding

  [328] private fun RemovedCartRow(
    CALLS OUT:
      → Icon
      → Text
      → TextButton

========================================
FILE: ConvoySettingsViewModel.kt
========================================

  [38] private fun observeSettings() {
    WORK FIELDS (local computed values):
      [78] val leadLockFeet = repository.leadLockFeet
    CALLS OUT:
      → it.copy
      → onAdmissionWindowChanged
      → onOffTrackChanged
      → onReinstateCart
      → onSignalDropChanged
      → onSignalLostChanged
      → onUserMessageShown
      → repository.reinstateCart
      → repository.setAdmissionWindowHours
      → repository.setLeadLockFeet
      → repository.setOffTrackMiles
      → repository.setSignalDropMinutes
      → repository.setSignalLostMinutes
      → setLeadLockFeet

  [63] fun onSignalDropChanged(value: Float) {
    WORK FIELDS (local computed values):
      [78] val leadLockFeet = repository.leadLockFeet
    CALLS OUT:
      → it.copy
      → onAdmissionWindowChanged
      → onOffTrackChanged
      → onReinstateCart
      → onSignalLostChanged
      → onUserMessageShown
      → repository.reinstateCart
      → repository.setAdmissionWindowHours
      → repository.setLeadLockFeet
      → repository.setOffTrackMiles
      → repository.setSignalDropMinutes
      → repository.setSignalLostMinutes
      → setLeadLockFeet

  [67] fun onSignalLostChanged(value: Float) {
    WORK FIELDS (local computed values):
      [78] val leadLockFeet = repository.leadLockFeet
    CALLS OUT:
      → it.copy
      → onAdmissionWindowChanged
      → onOffTrackChanged
      → onReinstateCart
      → onUserMessageShown
      → repository.reinstateCart
      → repository.setAdmissionWindowHours
      → repository.setLeadLockFeet
      → repository.setOffTrackMiles
      → repository.setSignalLostMinutes
      → setLeadLockFeet

  [71] fun onOffTrackChanged(value: Float) {
    WORK FIELDS (local computed values):
      [78] val leadLockFeet = repository.leadLockFeet
    CALLS OUT:
      → it.copy
      → onAdmissionWindowChanged
      → onReinstateCart
      → onUserMessageShown
      → repository.reinstateCart
      → repository.setAdmissionWindowHours
      → repository.setLeadLockFeet
      → repository.setOffTrackMiles
      → setLeadLockFeet

  [75] fun setLeadLockFeet(value: Float) {
    WORK FIELDS (local computed values):
      [78] val leadLockFeet = repository.leadLockFeet
    CALLS OUT:
      → it.copy
      → onAdmissionWindowChanged
      → onReinstateCart
      → onUserMessageShown
      → repository.reinstateCart
      → repository.setAdmissionWindowHours
      → repository.setLeadLockFeet

  [79] fun onAdmissionWindowChanged(value: Int) {
    CALLS OUT:
      → it.copy
      → onReinstateCart
      → onUserMessageShown
      → repository.reinstateCart
      → repository.setAdmissionWindowHours

  [83] fun onReinstateCart(nodeId: String, callsign: String) {
    CALLS OUT:
      → it.copy
      → onUserMessageShown
      → repository.reinstateCart

  [90] fun onUserMessageShown() {
    CALLS OUT:
      → it.copy

========================================
FILE: ConvoySharePromptDialog.kt
========================================

  [26] fun ConvoySharePromptDialog(
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → RoundedCornerShape
      → Row
      → Text
      → TextButton

========================================
FILE: ConvoySignInScreen.kt
========================================

  [66] fun saveUserToPrefs(context: Context, userId: String, googleId: String,
    WORK FIELDS (local computed values):
      [95] val expiresAt = if (subscribed) System.currentTimeMillis() + (365L * 24 * 60 * 60 * 1000) else 0L
      [113] val context = LocalContext.current
      [114] val scope = rememberCoroutineScope()
      [116] var isLoading by remember { mutableStateOf(false) }
      [117] var errorMessage by remember { mutableStateOf("") }
      [120] val gso = GoogleSignInOptions.Builder(GoogleSignInOptions.DEFAULT_SIGN_IN)
      [126] val googleSignInClient = remember { GoogleSignIn.getClient(context, gso) }
      [129] val signInLauncher = rememberLauncherForActivityResult(
      [133] val task = GoogleSignIn.getSignedInAccountFromIntent(result.data)
      [135] val account: GoogleSignInAccount = task.getResult(ApiException::class.java)
      [136] val googleId   = account.id ?: ""
      [137] val email      = account.email ?: ""
    CALLS OUT:
      → ActivityResultContracts.StartActivityForResult
      → GoogleSignIn.getClient
      → GoogleSignInOptions.Builder
      → Log.i
      → System.currentTimeMillis
      → apply
      → build
      → context.getSharedPreferences
      → edit
      → email.isEmpty
      → getDevSubscribed
      → getUserId
      → googleId.isEmpty
      → isSignedIn
      → isSubscribed

  [79] fun getUserId(context: Context): String? =
    WORK FIELDS (local computed values):
      [95] val expiresAt = if (subscribed) System.currentTimeMillis() + (365L * 24 * 60 * 60 * 1000) else 0L
      [113] val context = LocalContext.current
      [114] val scope = rememberCoroutineScope()
      [116] var isLoading by remember { mutableStateOf(false) }
      [117] var errorMessage by remember { mutableStateOf("") }
      [120] val gso = GoogleSignInOptions.Builder(GoogleSignInOptions.DEFAULT_SIGN_IN)
      [126] val googleSignInClient = remember { GoogleSignIn.getClient(context, gso) }
      [129] val signInLauncher = rememberLauncherForActivityResult(
      [133] val task = GoogleSignIn.getSignedInAccountFromIntent(result.data)
      [135] val account: GoogleSignInAccount = task.getResult(ApiException::class.java)
      [136] val googleId   = account.id ?: ""
      [137] val email      = account.email ?: ""
    CALLS OUT:
      → ActivityResultContracts.StartActivityForResult
      → ConvoyApiClient.registerUser
      → GoogleSignIn.getClient
      → GoogleSignInOptions.Builder
      → Log.i
      → System.currentTimeMillis
      → apply
      → build
      → context.getSharedPreferences
      → edit
      → email.isEmpty
      → getDevSubscribed
      → getUserId
      → googleId.isEmpty
      → isSignedIn

  [83] fun isSignedIn(context: Context): Boolean = getUserId(context) != null
    WORK FIELDS (local computed values):
      [95] val expiresAt = if (subscribed) System.currentTimeMillis() + (365L * 24 * 60 * 60 * 1000) else 0L
      [113] val context = LocalContext.current
      [114] val scope = rememberCoroutineScope()
      [116] var isLoading by remember { mutableStateOf(false) }
      [117] var errorMessage by remember { mutableStateOf("") }
      [120] val gso = GoogleSignInOptions.Builder(GoogleSignInOptions.DEFAULT_SIGN_IN)
      [126] val googleSignInClient = remember { GoogleSignIn.getClient(context, gso) }
      [129] val signInLauncher = rememberLauncherForActivityResult(
      [133] val task = GoogleSignIn.getSignedInAccountFromIntent(result.data)
      [135] val account: GoogleSignInAccount = task.getResult(ApiException::class.java)
      [136] val googleId   = account.id ?: ""
      [137] val email      = account.email ?: ""
    CALLS OUT:
      → ActivityResultContracts.StartActivityForResult
      → ConvoyApiClient.registerUser
      → GoogleSignIn.getClient
      → GoogleSignInOptions.Builder
      → Log.e
      → Log.i
      → System.currentTimeMillis
      → apply
      → build
      → context.getSharedPreferences
      → edit
      → email.isEmpty
      → getDevSubscribed
      → googleId.isEmpty
      → isSubscribed

  [90] fun setDevSubscribed(context: Context, subscribed: Boolean) {
    WORK FIELDS (local computed values):
      [95] val expiresAt = if (subscribed) System.currentTimeMillis() + (365L * 24 * 60 * 60 * 1000) else 0L
      [113] val context = LocalContext.current
      [114] val scope = rememberCoroutineScope()
      [116] var isLoading by remember { mutableStateOf(false) }
      [117] var errorMessage by remember { mutableStateOf("") }
      [120] val gso = GoogleSignInOptions.Builder(GoogleSignInOptions.DEFAULT_SIGN_IN)
      [126] val googleSignInClient = remember { GoogleSignIn.getClient(context, gso) }
      [129] val signInLauncher = rememberLauncherForActivityResult(
      [133] val task = GoogleSignIn.getSignedInAccountFromIntent(result.data)
      [135] val account: GoogleSignInAccount = task.getResult(ApiException::class.java)
      [136] val googleId   = account.id ?: ""
      [137] val email      = account.email ?: ""
    CALLS OUT:
      → ActivityResultContracts.StartActivityForResult
      → ConvoyApiClient.registerUser
      → GoogleSignIn.getClient
      → GoogleSignInOptions.Builder
      → Log.e
      → Log.i
      → System.currentTimeMillis
      → apply
      → build
      → context.getSharedPreferences
      → edit
      → email.isEmpty
      → getDevSubscribed
      → googleId.isEmpty
      → isSubscribed

  [102] fun getDevSubscribed(context: Context): Boolean =
    WORK FIELDS (local computed values):
      [113] val context = LocalContext.current
      [114] val scope = rememberCoroutineScope()
      [116] var isLoading by remember { mutableStateOf(false) }
      [117] var errorMessage by remember { mutableStateOf("") }
      [120] val gso = GoogleSignInOptions.Builder(GoogleSignInOptions.DEFAULT_SIGN_IN)
      [126] val googleSignInClient = remember { GoogleSignIn.getClient(context, gso) }
      [129] val signInLauncher = rememberLauncherForActivityResult(
      [133] val task = GoogleSignIn.getSignedInAccountFromIntent(result.data)
      [135] val account: GoogleSignInAccount = task.getResult(ApiException::class.java)
      [136] val googleId   = account.id ?: ""
      [137] val email      = account.email ?: ""
      [138] val firstName  = account.givenName ?: ""
    CALLS OUT:
      → ActivityResultContracts.StartActivityForResult
      → ConvoyApiClient.registerUser
      → GoogleSignIn.getClient
      → GoogleSignInOptions.Builder
      → Log.e
      → build
      → context.getSharedPreferences
      → email.isEmpty
      → googleId.isEmpty
      → mutableStateOf
      → onSignInComplete
      → rememberCoroutineScope
      → requestEmail
      → requestId
      → requestProfile

  [109] fun ConvoySignInScreen(
    WORK FIELDS (local computed values):
      [113] val context = LocalContext.current
      [114] val scope = rememberCoroutineScope()
      [116] var isLoading by remember { mutableStateOf(false) }
      [117] var errorMessage by remember { mutableStateOf("") }
      [120] val gso = GoogleSignInOptions.Builder(GoogleSignInOptions.DEFAULT_SIGN_IN)
      [126] val googleSignInClient = remember { GoogleSignIn.getClient(context, gso) }
      [129] val signInLauncher = rememberLauncherForActivityResult(
      [133] val task = GoogleSignIn.getSignedInAccountFromIntent(result.data)
      [135] val account: GoogleSignInAccount = task.getResult(ApiException::class.java)
      [136] val googleId   = account.id ?: ""
      [137] val email      = account.email ?: ""
      [138] val firstName  = account.givenName ?: ""
    CALLS OUT:
      → ActivityResultContracts.StartActivityForResult
      → ConvoyApiClient.registerUser
      → GoogleSignIn.getClient
      → GoogleSignInOptions.Builder
      → GroupTrackHeader
      → Log.e
      → background
      → build
      → email.isEmpty
      → fillMaxSize
      → googleId.isEmpty
      → mutableStateOf
      → onSignInComplete
      → rememberCoroutineScope
      → requestEmail

========================================
FILE: ConvoySimulation.kt
========================================

  [50] fun start() {
    WORK FIELDS (local computed values):
      [62] val (baseLat, baseLon) = BASE_POSITIONS[index]
      [65] val driftLat = tickCount * 0.000005
      [66] val driftLon = tickCount * -0.000006
      [67] val lat = baseLat + driftLat
      [68] val lon = baseLon + driftLon
      [71] val lastSeenMs = when (callsign) {
      [78] val speed = when {
      [86] val battery = when (callsign) {
      [114] val nodes = tick(nowMs)
    CALLS OUT:
      → ConvoyEngine.compute
      → Instant.ofEpochMilli
      → System.currentTimeMillis
      → computeState
      → reset
      → tick

  [59] fun tick(nowMs: Long = System.currentTimeMillis()): List<ConvoyNode> {
    WORK FIELDS (local computed values):
      [62] val (baseLat, baseLon) = BASE_POSITIONS[index]
      [65] val driftLat = tickCount * 0.000005
      [66] val driftLon = tickCount * -0.000006
      [67] val lat = baseLat + driftLat
      [68] val lon = baseLon + driftLon
      [71] val lastSeenMs = when (callsign) {
      [78] val speed = when {
      [86] val battery = when (callsign) {
      [114] val nodes = tick(nowMs)
    CALLS OUT:
      → ConvoyEngine.compute
      → Instant.ofEpochMilli
      → System.currentTimeMillis
      → computeState
      → reset
      → tick

  [113] fun computeState(nowMs: Long = System.currentTimeMillis()): ConvoyEngine.ConvoyState {
    WORK FIELDS (local computed values):
      [114] val nodes = tick(nowMs)
    CALLS OUT:
      → ConvoyEngine.compute
      → System.currentTimeMillis
      → reset
      → tick

  [119] fun reset() {
    CALLS OUT:
      → System.currentTimeMillis

========================================
FILE: ConvoySubMenu.kt
========================================

  [46] fun ConvoySubMenu(
    WORK FIELDS (local computed values):
      [68] var expanded by remember { mutableStateOf<String?>(null) }
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → RoundedCornerShape
      → expandVertically
      → fillMaxWidth
      → onCreateEventRide
      → onDismiss
      → onNavigateToCreateEvent
      → padding
      → shrinkVertically

  [243] private fun AccordionHeader(
    CALLS OUT:
      → Color
      → Column
      → RoundedCornerShape
      → Row
      → Spacer
      → Text
      → clickable
      → fillMaxWidth
      → onClick
      → padding
      → sub.isNotEmpty

  [285] private fun SubMenuItem(
    CALLS OUT:
      → Color
      → Column
      → RoundedCornerShape
      → Spacer
      → clickable
      → fillMaxWidth
      → onClick
      → padding
      → sub.isNotEmpty

========================================
FILE: ConvoySubscriptionScreen.kt
========================================

  [129] fun ConvoySubscriptionScreen(
    WORK FIELDS (local computed values):
      [133] val context = LocalContext.current
      [134] val scope = rememberCoroutineScope()
      [135] val pagerState = rememberPagerState(pageCount = { slides.size })
      [136] val isLastSlide = pagerState.currentPage == slides.size - 1
      [193] val isActive = index == pagerState.currentPage
      [194] val color by animateColorAsState(
    CALLS OUT:
      → Brush.verticalGradient
      → ValueSlidePanel
      → background
      → clip
      → fillMaxSize
      → fillMaxWidth
      → listOf
      → padding
      → pagerState.animateScrollToPage
      → rememberCoroutineScope
      → rememberPagerState
      → size
      → weight

  [313] fun ValueSlidePanel(slide: ValueSlide) {
    WORK FIELDS (local computed values):
      [386] val prefs = context.getSharedPreferences("grouptrack_user", android.content.Context.MODE_PRIVATE)
      [387] val expiresAt = prefs.getLong("subscription_expires_at", 0L)
    CALLS OUT:
      → Arrangement.spacedBy
      → Spacer
      → System.currentTimeMillis
      → context.getSharedPreferences
      → fillMaxSize
      → isSubscribed
      → padding

  [385] fun isSubscribed(context: android.content.Context): Boolean {
    WORK FIELDS (local computed values):
      [386] val prefs = context.getSharedPreferences("grouptrack_user", android.content.Context.MODE_PRIVATE)
      [387] val expiresAt = prefs.getLong("subscription_expires_at", 0L)
    CALLS OUT:
      → System.currentTimeMillis
      → apply
      → cacheSubscription
      → context.getSharedPreferences
      → edit

  [395] fun cacheSubscription(context: android.content.Context, expiresAtMs: Long) {
    CALLS OUT:
      → apply
      → context.getSharedPreferences
      → edit

========================================
FILE: ConvoyTermsScreen.kt
========================================

  [18] fun ConvoyTermsScreen(onAccept: () -> Unit, onDecline: () -> Unit) {
    WORK FIELDS (local computed values):
      [19] val context = LocalContext.current
      [20] val scrollState = rememberScrollState()
    CALLS OUT:
      → Arrangement.spacedBy
      → Column
      → GroupTrackButton
      → GroupTrackHeader
      → Spacer
      → TermsSection
      → Text
      → background
      → onAccept
      → padding
      → rememberScrollState
      → verticalScroll
      → weight

  [42] private fun TermsSection(title: String, body: String) {
    CALLS OUT:
      → Text

========================================
FILE: ConvoyTileCalculator.kt
========================================

  [39] fun lon2tile(lon: Double, z: Int): Int =
    WORK FIELDS (local computed values):
      [47] val rad = Math.toRadians(lat)
      [78] val effectiveZMax = if (sourceName.isNotEmpty()) minOf(zMax, maxZoomForSource(sourceName)) else zMax
      [79] val tiles = mutableListOf<TileKey>()
      [81] val xMin = lon2tile(west, z)
      [82] val xMax = lon2tile(east, z)
      [83] val yMin = lat2tile(north, z)  // north = smaller y value
      [84] val yMax = lat2tile(south, z)  // south = larger y value
    CALLS OUT:
      → Math.toRadians
      → TileKey
      → cos
      → estimateSizeMB
      → floor
      → isWithinCeiling
      → lat2tile
      → ln
      → lon2tile
      → maxZoomForSource
      → minOf
      → sourceName.isNotEmpty
      → tiles.add

  [46] fun lat2tile(lat: Double, z: Int): Int {
    WORK FIELDS (local computed values):
      [47] val rad = Math.toRadians(lat)
      [78] val effectiveZMax = if (sourceName.isNotEmpty()) minOf(zMax, maxZoomForSource(sourceName)) else zMax
      [79] val tiles = mutableListOf<TileKey>()
      [81] val xMin = lon2tile(west, z)
      [82] val xMax = lon2tile(east, z)
      [83] val yMin = lat2tile(north, z)  // north = smaller y value
      [84] val yMax = lat2tile(south, z)  // south = larger y value
      [121] var totalTiles = 0
      [123] val xCount = lon2tile(east, z) - lon2tile(west, z) + 1
      [124] val yCount = lat2tile(south, z) - lat2tile(north, z) + 1
    CALLS OUT:
      → Math.toRadians
      → TileKey
      → cos
      → estimateSizeMB
      → isWithinCeiling
      → lat2tile
      → ln
      → lon2tile
      → maxZoomForSource
      → minOf
      → sourceName.isNotEmpty
      → tiles.add

  [66] fun maxZoomForSource(sourceName: String): Int =
    WORK FIELDS (local computed values):
      [78] val effectiveZMax = if (sourceName.isNotEmpty()) minOf(zMax, maxZoomForSource(sourceName)) else zMax
      [79] val tiles = mutableListOf<TileKey>()
      [81] val xMin = lon2tile(west, z)
      [82] val xMax = lon2tile(east, z)
      [83] val yMin = lat2tile(north, z)  // north = smaller y value
      [84] val yMax = lat2tile(south, z)  // south = larger y value
      [121] var totalTiles = 0
      [123] val xCount = lon2tile(east, z) - lon2tile(west, z) + 1
      [124] val yCount = lat2tile(south, z) - lat2tile(north, z) + 1
      [127] val sizeMB = totalTiles * AVG_TILE_KB / 1024f
      [144] data class TileKey(val z: Int, val x: Int, val y: Int) {
    CALLS OUT:
      → TileKey
      → estimateSizeMB
      → isWithinCeiling
      → lat2tile
      → lon2tile
      → minOf
      → sourceName.isNotEmpty
      → tiles.add

  [69] fun calculateTiles(
    WORK FIELDS (local computed values):
      [78] val effectiveZMax = if (sourceName.isNotEmpty()) minOf(zMax, maxZoomForSource(sourceName)) else zMax
      [79] val tiles = mutableListOf<TileKey>()
      [81] val xMin = lon2tile(west, z)
      [82] val xMax = lon2tile(east, z)
      [83] val yMin = lat2tile(north, z)  // north = smaller y value
      [84] val yMax = lat2tile(south, z)  // south = larger y value
      [121] var totalTiles = 0
      [123] val xCount = lon2tile(east, z) - lon2tile(west, z) + 1
      [124] val yCount = lat2tile(south, z) - lat2tile(north, z) + 1
      [127] val sizeMB = totalTiles * AVG_TILE_KB / 1024f
      [144] data class TileKey(val z: Int, val x: Int, val y: Int) {
    CALLS OUT:
      → TileKey
      → estimateSizeMB
      → isWithinCeiling
      → lat2tile
      → lon2tile
      → minOf
      → sourceName.isNotEmpty
      → tiles.add

  [100] fun estimateSizeMB(tiles: List<TileKey>): Float =
    WORK FIELDS (local computed values):
      [121] var totalTiles = 0
      [123] val xCount = lon2tile(east, z) - lon2tile(west, z) + 1
      [124] val yCount = lat2tile(south, z) - lat2tile(north, z) + 1
      [127] val sizeMB = totalTiles * AVG_TILE_KB / 1024f
      [144] data class TileKey(val z: Int, val x: Int, val y: Int) {
      [150] val parts = s.split("/")
      [160] val tileCount: Int,
      [161] val estimatedMB: Float,
      [162] val withinCeiling: Boolean,
      [163] val zoomMin: Int,
      [164] val zoomMax: Int
      [166] val displaySize: String get() = "%.1f MB".format(estimatedMB)
    CALLS OUT:
      → TileKey
      → estimateSizeMB
      → get
      → isWithinCeiling
      → lat2tile
      → lon2tile
      → s.split

  [106] fun isWithinCeiling(tiles: List<TileKey>): Boolean =
    WORK FIELDS (local computed values):
      [121] var totalTiles = 0
      [123] val xCount = lon2tile(east, z) - lon2tile(west, z) + 1
      [124] val yCount = lat2tile(south, z) - lat2tile(north, z) + 1
      [127] val sizeMB = totalTiles * AVG_TILE_KB / 1024f
      [144] data class TileKey(val z: Int, val x: Int, val y: Int) {
      [150] val parts = s.split("/")
      [160] val tileCount: Int,
      [161] val estimatedMB: Float,
      [162] val withinCeiling: Boolean,
      [163] val zoomMin: Int,
      [164] val zoomMax: Int
      [166] val displaySize: String get() = "%.1f MB".format(estimatedMB)
    CALLS OUT:
      → TileKey
      → estimateSizeMB
      → get
      → lat2tile
      → lon2tile
      → s.split

  [113] fun quickEstimate(
    WORK FIELDS (local computed values):
      [121] var totalTiles = 0
      [123] val xCount = lon2tile(east, z) - lon2tile(west, z) + 1
      [124] val yCount = lat2tile(south, z) - lat2tile(north, z) + 1
      [127] val sizeMB = totalTiles * AVG_TILE_KB / 1024f
      [144] data class TileKey(val z: Int, val x: Int, val y: Int) {
      [150] val parts = s.split("/")
      [160] val tileCount: Int,
      [161] val estimatedMB: Float,
      [162] val withinCeiling: Boolean,
      [163] val zoomMin: Int,
      [164] val zoomMax: Int
      [166] val displaySize: String get() = "%.1f MB".format(estimatedMB)
    CALLS OUT:
      → TileKey
      → get
      → lat2tile
      → lon2tile
      → s.split

  [146] override fun toString(): String = "$z/$x/$y"
    WORK FIELDS (local computed values):
      [150] val parts = s.split("/")
      [160] val tileCount: Int,
      [161] val estimatedMB: Float,
      [162] val withinCeiling: Boolean,
      [163] val zoomMin: Int,
      [164] val zoomMax: Int
      [166] val displaySize: String get() = "%.1f MB".format(estimatedMB)
      [167] val displayCount: String get() = "%,d tiles".format(tileCount)
      [168] val summary: String get() = "$displayCount — $displaySize (z$zoomMin–z$zoomMax)"
    CALLS OUT:
      → TileKey
      → get
      → s.split

  [149] fun fromString(s: String): TileKey {
    WORK FIELDS (local computed values):
      [150] val parts = s.split("/")
      [160] val tileCount: Int,
      [161] val estimatedMB: Float,
      [162] val withinCeiling: Boolean,
      [163] val zoomMin: Int,
      [164] val zoomMax: Int
      [166] val displaySize: String get() = "%.1f MB".format(estimatedMB)
      [167] val displayCount: String get() = "%,d tiles".format(tileCount)
      [168] val summary: String get() = "$displayCount — $displaySize (z$zoomMin–z$zoomMax)"
    CALLS OUT:
      → TileKey
      → get
      → s.split

========================================
FILE: ConvoyTileDownloader.kt
========================================

  [48] fun buildTileUrl(tile: TileKey, sourceUrl: String): String {
    WORK FIELDS (local computed values):
      [58] val dir = File(ConvoyConfig.TILE_DIR, "${sourceName}/${tile.z}/${tile.x}")
      [68] val success = withContext(Dispatchers.IO) {
      [69] val request = Request.Builder()
      [73] val response = client.newCall(request).execute()
      [107] var downloaded = 0
      [108] var failed = 0
      [109] val total = tiles.size
      [119] val dest = tilePath(context, sourceName, tile)
      [128] val url = buildTileUrl(tile, sourceUrl)
    CALLS OUT:
      → File
      → Log.e
      → Log.w
      → Request.Builder
      → Result.failure
      → build
      → buildTileUrl
      → bytes
      → client.newCall
      → coroutines.CancellationException
      → coroutines.delay
      → dest.exists
      → dest.length
      → dest.writeBytes
      → dir.mkdirs

  [57] fun tilePath(context: Context, sourceName: String, tile: TileKey): File {
    WORK FIELDS (local computed values):
      [58] val dir = File(ConvoyConfig.TILE_DIR, "${sourceName}/${tile.z}/${tile.x}")
      [68] val success = withContext(Dispatchers.IO) {
      [69] val request = Request.Builder()
      [73] val response = client.newCall(request).execute()
      [107] var downloaded = 0
      [108] var failed = 0
      [109] val total = tiles.size
      [119] val dest = tilePath(context, sourceName, tile)
      [128] val url = buildTileUrl(tile, sourceUrl)
      [130] val success = downloadTile(url, dest)
    CALLS OUT:
      → File
      → Log.e
      → Log.i
      → Log.w
      → Request.Builder
      → Result.failure
      → build
      → buildTileUrl
      → bytes
      → client.newCall
      → coroutines.CancellationException
      → coroutines.delay
      → dest.exists
      → dest.length
      → dest.writeBytes

  [64] suspend fun downloadTile(url: String, dest: File): Boolean {
    WORK FIELDS (local computed values):
      [68] val success = withContext(Dispatchers.IO) {
      [69] val request = Request.Builder()
      [73] val response = client.newCall(request).execute()
      [107] var downloaded = 0
      [108] var failed = 0
      [109] val total = tiles.size
      [119] val dest = tilePath(context, sourceName, tile)
      [128] val url = buildTileUrl(tile, sourceUrl)
      [130] val success = downloadTile(url, dest)
      [140] val totalMB = (downloaded * AVG_TILE_BYTES) / (1024f * 1024f)
    CALLS OUT:
      → DownloadSummary
      → Log.e
      → Log.i
      → Log.w
      → Request.Builder
      → Result.failure
      → Result.success
      → build
      → buildTileUrl
      → bytes
      → client.newCall
      → coroutines.CancellationException
      → coroutines.delay
      → dest.exists
      → dest.length

  [98] suspend fun downloadTiles(
    WORK FIELDS (local computed values):
      [107] var downloaded = 0
      [108] var failed = 0
      [109] val total = tiles.size
      [119] val dest = tilePath(context, sourceName, tile)
      [128] val url = buildTileUrl(tile, sourceUrl)
      [130] val success = downloadTile(url, dest)
      [140] val totalMB = (downloaded * AVG_TILE_BYTES) / (1024f * 1024f)
      [150] val dir = File(ConvoyConfig.TILE_DIR, slotName)
      [152] val keys = mutableListOf<TileKey>()
      [155] val z = zDir.name.toIntOrNull() ?: return@forEach
      [157] val x = xDir.name.toIntOrNull() ?: return@forEach
      [159] val y = yFile.nameWithoutExtension.toIntOrNull() ?: return@forEach
    CALLS OUT:
      → DownloadSummary
      → File
      → Log.i
      → Result.failure
      → Result.success
      → TileKey
      → buildTileUrl
      → coroutines.CancellationException
      → dest.exists
      → dest.length
      → dir.exists
      → dir.listFiles
      → downloadTile
      → keys.add
      → keys.isEmpty

  [149] fun scanTilesToKeys(slotName: String): List<TileKey> {
    WORK FIELDS (local computed values):
      [150] val dir = File(ConvoyConfig.TILE_DIR, slotName)
      [152] val keys = mutableListOf<TileKey>()
      [155] val z = zDir.name.toIntOrNull() ?: return@forEach
      [157] val x = xDir.name.toIntOrNull() ?: return@forEach
      [159] val y = yFile.nameWithoutExtension.toIntOrNull() ?: return@forEach
      [170] val keys = scanTilesToKeys(slotName)
      [172] val zoomGroups = keys.groupBy { it.z }
      [173] val bestZ = zoomGroups.maxByOrNull { it.value.size }?.key ?: return null
      [174] val tilesAtZ = zoomGroups[bestZ] ?: return null
      [175] val minX = tilesAtZ.minOf { it.x }
      [176] val maxX = tilesAtZ.maxOf { it.x }
      [177] val minY = tilesAtZ.minOf { it.y }
    CALLS OUT:
      → File
      → Log.i
      → Math.atan
      → Math.sinh
      → Math.toDegrees
      → Pair
      → TileKey
      → cells.add
      → deleteSource
      → dir.deleteRecursively
      → dir.exists
      → dir.listFiles
      → dir.walkTopDown
      → doubleArrayOf
      → gridCells

  [169] fun tileBoundsLatLon(slotName: String): DoubleArray? {
    WORK FIELDS (local computed values):
      [170] val keys = scanTilesToKeys(slotName)
      [172] val zoomGroups = keys.groupBy { it.z }
      [173] val bestZ = zoomGroups.maxByOrNull { it.value.size }?.key ?: return null
      [174] val tilesAtZ = zoomGroups[bestZ] ?: return null
      [175] val minX = tilesAtZ.minOf { it.x }
      [176] val maxX = tilesAtZ.maxOf { it.x }
      [177] val minY = tilesAtZ.minOf { it.y }
      [178] val maxY = tilesAtZ.maxOf { it.y }
      [179] val n = 1 shl bestZ
      [180] val north = Math.toDegrees(Math.atan(Math.sinh(Math.PI * (1.0 - 2.0 * minY / n))))
      [181] val south = Math.toDegrees(Math.atan(Math.sinh(Math.PI * (1.0 - 2.0 * (maxY + 1) / n))))
      [182] val west = minX.toDouble() / n * 360.0 - 180.0
    CALLS OUT:
      → File
      → Log.i
      → Math.atan
      → Math.sinh
      → Math.toDegrees
      → Pair
      → cells.add
      → deleteSource
      → dir.deleteRecursively
      → dir.exists
      → dir.walkTopDown
      → doubleArrayOf
      → gridCells
      → it.length
      → keys.isEmpty

  [189] fun gridCells(north: Double, south: Double, east: Double, west: Double, gridMiles: Double = 12.0): List<DoubleArray> {
    WORK FIELDS (local computed values):
      [190] val step = gridMiles / 69.0
      [191] val cells = mutableListOf<DoubleArray>()
      [192] var lat = south
      [194] val cellN = minOf(lat + step, north)
      [195] var lon = west
      [197] val cellE = minOf(lon + step, east)
      [209] val dir = File(ConvoyConfig.TILE_DIR, sourceName)
      [215] val dir = File(ConvoyConfig.TILE_DIR, sourceName)
      [217] var count = 0
      [218] var bytes = 0L
    CALLS OUT:
      → File
      → Log.i
      → Pair
      → cells.add
      → deleteSource
      → dir.deleteRecursively
      → dir.exists
      → dir.walkTopDown
      → doubleArrayOf
      → it.length
      → minOf
      → sourceInfo

  [208] fun deleteSource(context: Context, sourceName: String): Boolean {
    WORK FIELDS (local computed values):
      [209] val dir = File(ConvoyConfig.TILE_DIR, sourceName)
      [215] val dir = File(ConvoyConfig.TILE_DIR, sourceName)
      [217] var count = 0
      [218] var bytes = 0L
    CALLS OUT:
      → File
      → Pair
      → dir.deleteRecursively
      → dir.exists
      → dir.walkTopDown
      → it.length
      → sourceInfo

  [214] fun sourceInfo(context: Context, sourceName: String): Pair<Int, Float> {
    WORK FIELDS (local computed values):
      [215] val dir = File(ConvoyConfig.TILE_DIR, sourceName)
      [217] var count = 0
      [218] var bytes = 0L
    CALLS OUT:
      → File
      → Pair
      → dir.exists
      → dir.walkTopDown
      → it.length

========================================
FILE: ConvoyTrackDialogs.kt
========================================

  [23] fun TrackActionDialog(
    WORK FIELDS (local computed values):
      [71] var text by remember(file) { mutableStateOf(file.nameWithoutExtension) }
      [72] val ext = file.extension
      [97] val newName = text.trim()
    CALLS OUT:
      → ActionRow
      → Color
      → RoundedCornerShape
      → SolidColor
      → Spacer
      → Text
      → TextButton
      → TextStyle
      → background
      → clickable
      → fillMaxWidth
      → mutableStateOf
      → newName.isNotEmpty
      → onConfirm
      → onDelete

  [52] private fun ActionRow(label: String, onClick: () -> Unit) {
    WORK FIELDS (local computed values):
      [71] var text by remember(file) { mutableStateOf(file.nameWithoutExtension) }
      [72] val ext = file.extension
      [97] val newName = text.trim()
    CALLS OUT:
      → Color
      → RoundedCornerShape
      → SolidColor
      → Spacer
      → Text
      → TextButton
      → TextStyle
      → background
      → clickable
      → fillMaxWidth
      → mutableStateOf
      → newName.isNotEmpty
      → onConfirm
      → padding
      → remember

  [65] fun RenameTrackDialog(
    WORK FIELDS (local computed values):
      [71] var text by remember(file) { mutableStateOf(file.nameWithoutExtension) }
      [72] val ext = file.extension
      [97] val newName = text.trim()
    CALLS OUT:
      → Color
      → RoundedCornerShape
      → SolidColor
      → Spacer
      → Text
      → TextButton
      → TextStyle
      → background
      → mutableStateOf
      → newName.isNotEmpty
      → onConfirm
      → padding
      → remember
      → text.trim

  [109] fun DeleteTrackDialog(
    CALLS OUT:
      → Text
      → TextButton

========================================
FILE: ConvoyTrackExportSheet.kt
========================================

  [39] fun ConvoyTrackExportSheet(onDismiss: () -> Unit, onNavigateToTrackImport: () -> Unit = {}) {
    WORK FIELDS (local computed values):
      [40] val context = LocalContext.current
      [41] val scope = rememberCoroutineScope()
      [43] var allTracks by remember { mutableStateOf<List<File>>(emptyList()) }
      [44] var searchText by remember { mutableStateOf("") }
      [45] var sortMode by remember { mutableStateOf(SortMode.DATE_DESC) }
      [46] var filterMode by remember { mutableStateOf(FilterMode.ALL) }
      [47] var refreshTick by remember { mutableStateOf(0) }
      [49] var actionTarget by remember { mutableStateOf<File?>(null) }
      [50] var renameTarget by remember { mutableStateOf<File?>(null) }
      [51] var deleteTarget by remember { mutableStateOf<File?>(null) }
      [52] var statusMsg by remember { mutableStateOf<String?>(null) }
      [58] val visibleTracks = remember(allTracks, searchText, sortMode, filterMode, refreshTick) {
    CALLS OUT:
      → Color
      → ConvoyTrackOps.fixDatesForFiles
      → ConvoyTrackOps.isInProgress
      → ConvoyTrackOps.listTracks
      → LaunchedEffect
      → RoundedCornerShape
      → Row
      → SimpleDateFormat
      → Spacer
      → Text
      → TextButton
      → background
      → fillMaxSize
      → mutableStateOf
      → name.contains

  [285] private fun FilterTab(label: String, selected: Boolean, onClick: () -> Unit) {
    WORK FIELDS (local computed values):
      [306] val isInProgress = ConvoyTrackOps.isInProgress(file)
    CALLS OUT:
      → Color
      → Column
      → ConvoyTrackOps.isInProgress
      → Date
      → RoundedCornerShape
      → Spacer
      → Text
      → clickable
      → file.length
      → padding

  [301] private fun TrackRow(
    WORK FIELDS (local computed values):
      [306] val isInProgress = ConvoyTrackOps.isInProgress(file)
    CALLS OUT:
      → Color
      → Column
      → ConvoyTrackOps.isInProgress
      → Date
      → RoundedCornerShape
      → Spacer
      → Text
      → clickable
      → file.length
      → padding

========================================
FILE: ConvoyTrackImportScreen.kt
========================================

  [63] fun ConvoyTrackImportScreen(onDismiss: () -> Unit) {
    WORK FIELDS (local computed values):
      [66] var files by remember { mutableStateOf<List<File>>(emptyList()) }
      [67] var selected by remember { mutableStateOf<Set<String>>(emptySet()) }
      [68] var scanning by remember { mutableStateOf(true) }
      [71] var showProgress by remember { mutableStateOf(false) }
      [72] var progressCurrent by remember { mutableStateOf(0) }
      [73] var progressTotal by remember { mutableStateOf(0) }
      [74] var progressName by remember { mutableStateOf("") }
      [77] var showRecap by remember { mutableStateOf(false) }
      [78] var recapImported by remember { mutableStateOf<List<String>>(emptyList()) }
      [79] var recapSkipped by remember { mutableStateOf<List<String>>(emptyList()) }
      [80] var recapFailed by remember { mutableStateOf<List<String>>(emptyList()) }
      [81] var recapDatesCorrected by remember { mutableStateOf(0) }
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → LaunchedEffect
      → dlDir.listFiles
      → doImport
      → errors.isNotEmpty
      → extension.lowercase
      → failed.add
      → failed.addAll
      → imported.addAll
      → listOf
      → mutableStateOf
      → rememberCoroutineScope
      → sel.isEmpty
      → sel.withIndex
      → selected.contains

  [107] fun doImport() {
    WORK FIELDS (local computed values):
      [108] val sel = files.filter { selected.contains(it.name) }
      [115] val imported = mutableListOf<String>()
      [116] val skipped = mutableListOf<String>()
      [117] val failed = mutableListOf<String>()
      [118] var datesCorrected = 0
      [119] var wptTotal = 0
      [120] var rteTotal = 0
      [126] val summary = ConvoyTrackOps.importGpxAllArtifacts(f, context)
    CALLS OUT:
      → background
      → coroutines.withContext
      → errors.isNotEmpty
      → f.delete
      → failed.add
      → failed.addAll
      → fillMaxSize
      → fillMaxWidth
      → imported.addAll
      → onDismiss
      → sel.isEmpty
      → sel.withIndex
      → selected.contains

  [416] private fun ImportProgressDialog(current: Int, total: Int, currentName: String) {
    CALLS OUT:
      → CircularProgressIndicator
      → Color
      → RoundedCornerShape
      → Spacer
      → TextButton
      → onDismiss

  [461] private fun ImportRecapDialog(
    CALLS OUT:
      → Color
      → TextButton
      → failed.isNotEmpty
      → imported.isNotEmpty
      → onDismiss
      → skipped.isNotEmpty

  [620] private fun formatFileSize(bytes: Long): String {
    CALLS OUT:
      → SimpleDateFormat

  [628] private fun formatFileDate(millis: Long): String {
    CALLS OUT:
      → SimpleDateFormat

========================================
FILE: ConvoyTrackOps.kt
========================================

  [18] fun tracksDir(): File = File(
    WORK FIELDS (local computed values):
      [28] val dir = tracksDir()
      [31] val name = f.name
      [32] val ext = f.extension.lowercase()
      [52] val ext = file.extension
      [53] val target = File(file.parentFile, "$newBaseName.$ext")
      [70] val target = File(downloadsDir(), file.name)
      [84] val uri = FileProvider.getUriForFile(
      [89] val intent = Intent(Intent.ACTION_SEND).apply {
      [94] val chooser = Intent.createChooser(intent, "Share track").apply {
      [117] val sourceName: String,
      [118] val createdFiles: List<String>
      [121] val sourceName: String,
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → File
      → FileProvider.getUriForFile
      → FixDatesResult
      → GpxRoute
      → GpxWaypoint
      → ImportResult
      → ImportResult.Failed
      → ImportResult.PartialSuccess
      → ImportResult.Success
      → Instant.parse
      → LINESTRING
      → Log.e
      → Log.i
      → Log.w

  [23] fun downloadsDir(): File =
    WORK FIELDS (local computed values):
      [28] val dir = tracksDir()
      [31] val name = f.name
      [32] val ext = f.extension.lowercase()
      [52] val ext = file.extension
      [53] val target = File(file.parentFile, "$newBaseName.$ext")
      [70] val target = File(downloadsDir(), file.name)
      [84] val uri = FileProvider.getUriForFile(
      [89] val intent = Intent(Intent.ACTION_SEND).apply {
      [94] val chooser = Intent.createChooser(intent, "Share track").apply {
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → File
      → FileProvider.getUriForFile
      → Log.e
      → RenameResult
      → addFlags
      → context.startActivity
      → copyToDownloads
      → deleteTrack
      → dir.exists
      → dir.listFiles
      → extension.lowercase
      → file.delete
      → file.inputStream
      → file.renameTo

  [27] suspend fun listTracks(): List<File> = withContext(Dispatchers.IO) {
    WORK FIELDS (local computed values):
      [28] val dir = tracksDir()
      [31] val name = f.name
      [32] val ext = f.extension.lowercase()
      [52] val ext = file.extension
      [53] val target = File(file.parentFile, "$newBaseName.$ext")
      [70] val target = File(downloadsDir(), file.name)
      [84] val uri = FileProvider.getUriForFile(
      [89] val intent = Intent(Intent.ACTION_SEND).apply {
      [94] val chooser = Intent.createChooser(intent, "Share track").apply {
    CALLS OUT:
      → File
      → FileProvider.getUriForFile
      → Log.e
      → RenameResult
      → addFlags
      → context.startActivity
      → copyToDownloads
      → deleteTrack
      → dir.exists
      → dir.listFiles
      → extension.lowercase
      → file.delete
      → file.inputStream
      → file.renameTo
      → input.copyTo

  [40] fun isInProgress(file: File): Boolean =
    WORK FIELDS (local computed values):
      [52] val ext = file.extension
      [53] val target = File(file.parentFile, "$newBaseName.$ext")
      [70] val target = File(downloadsDir(), file.name)
      [84] val uri = FileProvider.getUriForFile(
      [89] val intent = Intent(Intent.ACTION_SEND).apply {
      [94] val chooser = Intent.createChooser(intent, "Share track").apply {
      [117] val sourceName: String,
      [118] val createdFiles: List<String>
    CALLS OUT:
      → File
      → FileProvider.getUriForFile
      → ImportResult
      → Log.e
      → RenameResult
      → addFlags
      → context.startActivity
      → copyToDownloads
      → deleteTrack
      → file.delete
      → file.inputStream
      → file.renameTo
      → input.copyTo
      → name.startsWith
      → putExtra

  [50] suspend fun renameTrack(file: File, newBaseName: String): RenameResult =
    WORK FIELDS (local computed values):
      [52] val ext = file.extension
      [53] val target = File(file.parentFile, "$newBaseName.$ext")
      [70] val target = File(downloadsDir(), file.name)
      [84] val uri = FileProvider.getUriForFile(
      [89] val intent = Intent(Intent.ACTION_SEND).apply {
      [94] val chooser = Intent.createChooser(intent, "Share track").apply {
      [117] val sourceName: String,
      [118] val createdFiles: List<String>
      [121] val sourceName: String,
      [122] val createdFiles: List<String>,
      [123] val skippedFiles: List<String>
      [126] val sourceName: String,
    CALLS OUT:
      → File
      → FileProvider.getUriForFile
      → ImportResult
      → Log.e
      → addFlags
      → context.startActivity
      → copyToDownloads
      → deleteTrack
      → file.delete
      → file.inputStream
      → file.renameTo
      → input.copyTo
      → putExtra
      → shareTrack
      → target.exists

  [63] suspend fun deleteTrack(file: File): Boolean = withContext(Dispatchers.IO) {
    WORK FIELDS (local computed values):
      [70] val target = File(downloadsDir(), file.name)
      [84] val uri = FileProvider.getUriForFile(
      [89] val intent = Intent(Intent.ACTION_SEND).apply {
      [94] val chooser = Intent.createChooser(intent, "Share track").apply {
      [117] val sourceName: String,
      [118] val createdFiles: List<String>
      [121] val sourceName: String,
      [122] val createdFiles: List<String>,
      [123] val skippedFiles: List<String>
      [126] val sourceName: String,
      [127] val reason: String
      [135] val updated: Int,
    CALLS OUT:
      → File
      → FileProvider.getUriForFile
      → ImportResult
      → Log.e
      → addFlags
      → context.startActivity
      → copyToDownloads
      → file.delete
      → file.inputStream
      → input.copyTo
      → putExtra
      → shareTrack
      → target.outputStream
      → withContext

  [68] suspend fun copyToDownloads(file: File): Boolean = withContext(Dispatchers.IO) {
    WORK FIELDS (local computed values):
      [70] val target = File(downloadsDir(), file.name)
      [84] val uri = FileProvider.getUriForFile(
      [89] val intent = Intent(Intent.ACTION_SEND).apply {
      [94] val chooser = Intent.createChooser(intent, "Share track").apply {
      [117] val sourceName: String,
      [118] val createdFiles: List<String>
      [121] val sourceName: String,
      [122] val createdFiles: List<String>,
      [123] val skippedFiles: List<String>
      [126] val sourceName: String,
      [127] val reason: String
      [135] val updated: Int,
    CALLS OUT:
      → File
      → FileProvider.getUriForFile
      → ImportResult
      → Log.e
      → Regex
      → addFlags
      → context.startActivity
      → extractEarliestTime
      → file.inputStream
      → input.copyTo
      → putExtra
      → shareTrack
      → target.outputStream

  [82] fun shareTrack(context: Context, file: File) {
    WORK FIELDS (local computed values):
      [84] val uri = FileProvider.getUriForFile(
      [89] val intent = Intent(Intent.ACTION_SEND).apply {
      [94] val chooser = Intent.createChooser(intent, "Share track").apply {
      [117] val sourceName: String,
      [118] val createdFiles: List<String>
      [121] val sourceName: String,
      [122] val createdFiles: List<String>,
      [123] val skippedFiles: List<String>
      [126] val sourceName: String,
      [127] val reason: String
      [135] val updated: Int,
      [136] val unchanged: Int,
    CALLS OUT:
      → FileProvider.getUriForFile
      → ImportResult
      → Instant.parse
      → Log.e
      → OffsetDateTime.parse
      → Regex
      → addFlags
      → context.startActivity
      → extractEarliestTime
      → odt.toInstant
      → pattern.findAll
      → putExtra
      → toEpochMilli
      → trim

  [104] fun formatSize(bytes: Long): String = when {
    WORK FIELDS (local computed values):
      [117] val sourceName: String,
      [118] val createdFiles: List<String>
      [121] val sourceName: String,
      [122] val createdFiles: List<String>,
      [123] val skippedFiles: List<String>
      [126] val sourceName: String,
      [127] val reason: String
      [135] val updated: Int,
      [136] val unchanged: Int,
      [137] val failed: Int
      [145] val patterns = listOf(
      [149] var earliest: Long? = null
    CALLS OUT:
      → ImportResult
      → Instant.parse
      → OffsetDateTime.parse
      → Regex
      → extractEarliestTime
      → odt.toInstant
      → pattern.findAll
      → replace
      → sanitizeFilename
      → toEpochMilli
      → trim

  [144] fun extractEarliestTime(content: String): Long? {
    WORK FIELDS (local computed values):
      [145] val patterns = listOf(
      [149] var earliest: Long? = null
      [153] val epochMs = java.time.Instant.parse(match.groupValues[1].trim()).toEpochMilli()
      [160] val odt = java.time.OffsetDateTime.parse(match.groupValues[1].trim())
      [161] val epochMs = odt.toInstant().toEpochMilli()
      [196] val sourceName = sourceFile.name
      [202] var text = sourceFile.readText()
      [205] val ext = sourceFile.extension.lowercase()
      [206] val isGpx = ext == "gpx"
      [207] val isKml = ext == "kml"
      [212] val dir = tracksDir()
      [215] val created = mutableListOf<String>()
    CALLS OUT:
      → ImportResult.Failed
      → Instant.parse
      → OffsetDateTime.parse
      → Regex
      → dir.exists
      → dir.mkdirs
      → extension.lowercase
      → odt.toInstant
      → pattern.findAll
      → replace
      → sanitizeFilename
      → sourceFile.exists
      → sourceFile.readText
      → text.contains
      → text.replace

  [177] private fun sanitizeFilename(name: String): String {
    WORK FIELDS (local computed values):
      [196] val sourceName = sourceFile.name
      [202] var text = sourceFile.readText()
      [205] val ext = sourceFile.extension.lowercase()
      [206] val isGpx = ext == "gpx"
      [207] val isKml = ext == "kml"
      [212] val dir = tracksDir()
      [215] val created = mutableListOf<String>()
      [216] val skipped = mutableListOf<String>()
      [220] val trkPattern = Regex("""<trk>([\s\S]*?)</trk>""")
      [221] val tracks = trkPattern.findAll(text).toList()
      [224] val namePattern = Regex("<name>([^<]*)</name>")
      [226] val trkContent = trk.groupValues[1]
    CALLS OUT:
      → File
      → ImportResult.Failed
      → ImportResult.PartialSuccess
      → ImportResult.Success
      → Regex
      → created.add
      → created.isEmpty
      → dest.exists
      → dest.writeText
      → dir.exists
      → dir.mkdirs
      → extension.lowercase
      → extractEarliestTime
      → get
      → invoke

  [192] suspend fun importTrackFile(
    WORK FIELDS (local computed values):
      [196] val sourceName = sourceFile.name
      [202] var text = sourceFile.readText()
      [205] val ext = sourceFile.extension.lowercase()
      [206] val isGpx = ext == "gpx"
      [207] val isKml = ext == "kml"
      [212] val dir = tracksDir()
      [215] val created = mutableListOf<String>()
      [216] val skipped = mutableListOf<String>()
      [220] val trkPattern = Regex("""<trk>([\s\S]*?)</trk>""")
      [221] val tracks = trkPattern.findAll(text).toList()
      [224] val namePattern = Regex("<name>([^<]*)</name>")
      [226] val trkContent = trk.groupValues[1]
    CALLS OUT:
      → File
      → ImportResult.Failed
      → ImportResult.PartialSuccess
      → ImportResult.Success
      → Regex
      → contains
      → created.add
      → created.isEmpty
      → dest.exists
      → dest.writeText
      → dir.exists
      → dir.mkdirs
      → extension.lowercase
      → extractEarliestTime
      → get

  [321] suspend fun fixDateFromContent(file: File): Boolean = withContext(Dispatchers.IO) {
    WORK FIELDS (local computed values):
      [324] val content = file.readText()
      [325] val earliest = extractEarliestTime(content) ?: return@withContext false
      [342] var updated = 0
      [343] var unchanged = 0
      [344] var failed = 0
      [362] val name: String,
      [363] val lat: Double,
      [364] val lon: Double,
      [365] val type: String,
      [366] val description: String = ""
      [370] val name: String,
      [371] val points: List<Pair<Double, Double>>  // (lon, lat) pairs
    CALLS OUT:
      → FixDatesResult
      → Log.e
      → Regex
      → extractEarliestTime
      → file.exists
      → file.readText
      → files.withIndex
      → fixDateFromContent
      → get
      → invoke
      → namePattern.find
      → parseGpxWaypoints
      → trim
      → withContext
      → wptPattern.findAll

  [338] suspend fun fixDatesForFiles(
    WORK FIELDS (local computed values):
      [342] var updated = 0
      [343] var unchanged = 0
      [344] var failed = 0
      [362] val name: String,
      [363] val lat: Double,
      [364] val lon: Double,
      [365] val type: String,
      [366] val description: String = ""
      [370] val name: String,
      [371] val points: List<Pair<Double, Double>>  // (lon, lat) pairs
      [375] val sourceName: String,
      [376] val trackCount: Int,
    CALLS OUT:
      → FixDatesResult
      → GpxWaypoint
      → Regex
      → descPattern.find
      → files.withIndex
      → fixDateFromContent
      → get
      → invoke
      → listOf
      → lowercase
      → mapWaypointType
      → namePattern.find
      → parseGpxWaypoints
      → rawType.lowercase
      → results.add

  [389] fun parseGpxWaypoints(gpxText: String): List<GpxWaypoint> {
    WORK FIELDS (local computed values):
      [390] val results = mutableListOf<GpxWaypoint>()
      [391] val wptPattern = Regex("""<wpt\s+lat="([^"]+)"\s+lon="([^"]+)"[^>]*>([\s\S]*?)</wpt>""")
      [392] val namePattern = Regex("""<name>([^<]*)</name>""")
      [393] val typePattern = Regex("""<type>([^<]*)</type>""")
      [394] val descPattern = Regex("""<desc>([^<]*)</desc>""")
      [397] val lat = match.groupValues[1].toDoubleOrNull() ?: continue
      [398] val lon = match.groupValues[2].toDoubleOrNull() ?: continue
      [399] val inner = match.groupValues[3]
      [401] val name = namePattern.find(inner)?.groupValues?.get(1)?.trim() ?: "Unnamed Waypoint"
      [402] val rawType = typePattern.find(inner)?.groupValues?.get(1)?.trim()?.lowercase() ?: "other"
      [403] val desc = descPattern.find(inner)?.groupValues?.get(1)?.trim() ?: ""
      [406] val mappedType = mapWaypointType(rawType)
    CALLS OUT:
      → GpxRoute
      → GpxWaypoint
      → Pair
      → Regex
      → descPattern.find
      → get
      → listOf
      → lowercase
      → mapWaypointType
      → namePattern.find
      → parseGpxRoutes
      → points.add
      → rawType.lowercase
      → results.add
      → rtePattern.findAll

  [415] private fun mapWaypointType(rawType: String): String {
    WORK FIELDS (local computed values):
      [416] val lower = rawType.lowercase().trim()
      [442] val results = mutableListOf<GpxRoute>()
      [443] val rtePattern = Regex("""<rte>([\s\S]*?)</rte>""")
      [444] val namePattern = Regex("""<name>([^<]*)</name>""")
      [445] val rteptPattern = Regex("""<rtept\s+lat="([^"]+)"\s+lon="([^"]+)"""")
      [448] val inner = match.groupValues[1]
      [449] val name = namePattern.find(inner)?.groupValues?.get(1)?.trim() ?: "Unnamed Route"
      [450] val points = mutableListOf<Pair<Double, Double>>()
      [453] val lat = ptMatch.groupValues[1].toDoubleOrNull() ?: continue
      [454] val lon = ptMatch.groupValues[2].toDoubleOrNull() ?: continue
      [483] val sourceName = sourceFile.name
      [484] val errors = mutableListOf<String>()
    CALLS OUT:
      → GpxRoute
      → Pair
      → Regex
      → coroutines.withContext
      → get
      → listOf
      → namePattern.find
      → parseGpxRoutes
      → points.add
      → rawType.lowercase
      → results.add
      → rtePattern.findAll
      → rteptPattern.findAll
      → sourceFile.exists
      → sourceFile.readText

  [441] fun parseGpxRoutes(gpxText: String): List<GpxRoute> {
    WORK FIELDS (local computed values):
      [442] val results = mutableListOf<GpxRoute>()
      [443] val rtePattern = Regex("""<rte>([\s\S]*?)</rte>""")
      [444] val namePattern = Regex("""<name>([^<]*)</name>""")
      [445] val rteptPattern = Regex("""<rtept\s+lat="([^"]+)"\s+lon="([^"]+)"""")
      [448] val inner = match.groupValues[1]
      [449] val name = namePattern.find(inner)?.groupValues?.get(1)?.trim() ?: "Unnamed Route"
      [450] val points = mutableListOf<Pair<Double, Double>>()
      [453] val lat = ptMatch.groupValues[1].toDoubleOrNull() ?: continue
      [454] val lon = ptMatch.groupValues[2].toDoubleOrNull() ?: continue
      [483] val sourceName = sourceFile.name
      [484] val errors = mutableListOf<String>()
      [485] val trackFiles = mutableListOf<String>()
    CALLS OUT:
      → GpxRoute
      → Pair
      → Regex
      → SpatialDbManager.init
      → coroutines.withContext
      → dir.exists
      → dir.mkdirs
      → extension.lowercase
      → get
      → listOf
      → namePattern.find
      → points.add
      → replace
      → results.add
      → rtePattern.findAll

  [479] suspend fun importGpxAllArtifacts(
    WORK FIELDS (local computed values):
      [483] val sourceName = sourceFile.name
      [484] val errors = mutableListOf<String>()
      [485] val trackFiles = mutableListOf<String>()
      [486] var waypointCount = 0
      [487] var routeCount = 0
      [494] var text = sourceFile.readText()
      [503] val dir = tracksDir()
      [507] val trkPattern = Regex("""<trk>([\s\S]*?)</trk>""")
      [508] val namePattern = Regex("""<name>([^<]*)</name>""")
      [509] val tracks = trkPattern.findAll(text).toList()
      [513] val trkContent = trk.groupValues[1]
      [514] val rawName = namePattern.find(trkContent)?.groupValues?.get(1)?.trim()
    CALLS OUT:
      → File
      → LINESTRING
      → Regex
      → SpatialDbManager.init
      → SpatialDbManager.insertTrackToDb
      → SpatialDbManager.insertWaypoint
      → SpatialDbManager.parseGpxTrackPoints
      → coords.isNotEmpty
      → coroutines.withContext
      → dest.exists
      → dest.writeText
      → dir.exists
      → dir.mkdirs
      → errors.add
      → extension.lowercase

  [606] fun shareGpx(context: android.content.Context, name: String, gpxContent: String) {
    WORK FIELDS (local computed values):
      [608] val tempFile = java.io.File(context.cacheDir, "${name.replace(" ", "_")}.gpx")
      [610] val uri = androidx.core.content.FileProvider.getUriForFile(
      [612] val intent = android.content.Intent(android.content.Intent.ACTION_SEND).apply {
      [630] val downloadsDir = android.os.Environment.getExternalStoragePublicDirectory(
      [632] val file = java.io.File(downloadsDir, "${name.replace(" ", "_")}.gpx")
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → FileProvider.getUriForFile
      → Log.e
      → Log.i
      → Toast.makeText
      → addFlags
      → context.startActivity
      → exportGpxToDownloads
      → file.writeText
      → io.File
      → name.replace
      → putExtra
      → show
      → tempFile.writeText

  [628] fun exportGpxToDownloads(name: String, gpxContent: String): Boolean {
    WORK FIELDS (local computed values):
      [630] val downloadsDir = android.os.Environment.getExternalStoragePublicDirectory(
      [632] val file = java.io.File(downloadsDir, "${name.replace(" ", "_")}.gpx")
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Log.e
      → Log.i
      → file.writeText
      → io.File
      → name.replace

========================================
FILE: ConvoyTrackSaveOps.kt
========================================

  [17] fun parseGpxMetadata(filePath: String) {
    CALLS OUT:
      → Log.d
      → insertTrackRow
      → stub
      → triggerTileFollow

  [22] fun insertTrackRow(name: String, filePath: String) {
    CALLS OUT:
      → Log.d
      → stub
      → triggerTileFollow

  [27] fun triggerTileFollow(trackId: String) {
    CALLS OUT:
      → Log.d
      → stub

  [32] fun stub() { /* Pass 1 scaffold */ }

========================================
FILE: ConvoyTracksScreen.kt
========================================

  [15] fun ConvoyTracksScreen(onBack: () -> Unit) {
    CALLS OUT:
      → Box
      → Column
      → GroupTrackHeader
      → Text
      → background
      → onBack
      → padding
      → weight

========================================
FILE: ConvoyTrailSourceScreen.kt
========================================

  [64] fun ConvoyTrailSourceScreen(onNavigateBack: () -> Unit = {}) {
    WORK FIELDS (local computed values):
      [65] val context = LocalContext.current
      [66] val scope = rememberCoroutineScope()
      [68] var step by remember { mutableStateOf(ImportStep.A1_SOURCE_SELECT) }
      [69] var sources by remember { mutableStateOf(listOf<CatalogSource>()) }
      [70] var selectedSourceId by remember { mutableStateOf<String?>(null) }
      [71] var importResult by remember { mutableStateOf<String?>(null) }
      [72] var importTrails by remember { mutableStateOf(true) }
      [73] var importRunning by remember { mutableStateOf(false) }
      [74] val importProgress by TrailImporter.progress.collectAsState()
      [75] var pendingBbox by remember { mutableStateOf<org.json.JSONObject?>(null) }
      [76] var selectedSourceIds by remember { mutableStateOf(setOf<String>()) }
      [79] val pending = TrailImporter.readPendingArea()
    CALLS OUT:
      → Arrangement.spacedBy
      → Icon
      → LaunchedEffect
      → SectionLabel
      → Text
      → TopAppBarDefaults.topAppBarColors
      → TrailImporter.readPendingArea
      → fillMaxSize
      → loadSourceCatalog
      → mutableStateOf
      → onNavigateBack
      → padding
      → progress.collectAsState
      → rememberCoroutineScope
      → verticalScroll

  [537] private fun SectionLabel(text: String) {
    WORK FIELDS (local computed values):
      [610] val json = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [611] val root = JSONObject(json)
      [612] val arr = root.getJSONArray("sources")
      [614] val s = arr.getJSONObject(i)
      [615] val b = s.optJSONObject("boundary")
      [616] val scan = s.optJSONObject("scan")
    CALLS OUT:
      → Color
      → Column
      → JSONObject
      → MethodCard
      → RadioButtonDefaults.colors
      → RoundedCornerShape
      → Row
      → SourceSelectCard
      → Spacer
      → Text
      → ValidationRow
      → arr.getJSONObject
      → arr.length
      → assets.open
      → bufferedReader

  [542] private fun MethodCard(title: String, desc: String, selected: Boolean, onClick: () -> Unit) {
    WORK FIELDS (local computed values):
      [610] val json = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [611] val root = JSONObject(json)
      [612] val arr = root.getJSONArray("sources")
      [614] val s = arr.getJSONObject(i)
      [615] val b = s.optJSONObject("boundary")
      [616] val scan = s.optJSONObject("scan")
    CALLS OUT:
      → Color
      → Column
      → JSONObject
      → RadioButtonDefaults.colors
      → RoundedCornerShape
      → Row
      → SourceSelectCard
      → Spacer
      → Text
      → ValidationRow
      → arr.getJSONObject
      → arr.length
      → assets.open
      → bufferedReader
      → foundation.BorderStroke

  [558] private fun SourceSelectCard(source: CatalogSource, isSelected: Boolean, isImported: Boolean = false, onClick: () -> Unit) {
    WORK FIELDS (local computed values):
      [610] val json = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [611] val root = JSONObject(json)
      [612] val arr = root.getJSONArray("sources")
      [614] val s = arr.getJSONObject(i)
      [615] val b = s.optJSONObject("boundary")
      [616] val scan = s.optJSONObject("scan")
    CALLS OUT:
      → Color
      → Column
      → JSONObject
      → Log.e
      → RadioButtonDefaults.colors
      → RoundedCornerShape
      → Row
      → Text
      → ValidationRow
      → arr.getJSONObject
      → arr.length
      → assets.open
      → bufferedReader
      → foundation.BorderStroke
      → green.copy

  [598] private fun ValidationRow(label: String, value: String) {
    WORK FIELDS (local computed values):
      [610] val json = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [611] val root = JSONObject(json)
      [612] val arr = root.getJSONArray("sources")
      [614] val s = arr.getJSONObject(i)
      [615] val b = s.optJSONObject("boundary")
      [616] val scan = s.optJSONObject("scan")
    CALLS OUT:
      → JSONObject
      → Log.e
      → Row
      → Text
      → arr.getJSONObject
      → arr.length
      → assets.open
      → bufferedReader
      → it.readText
      → loadSourceCatalog
      → padding
      → root.getJSONArray
      → s.optJSONObject

  [608] private fun loadSourceCatalog(context: Context): List<CatalogSource> {
    WORK FIELDS (local computed values):
      [610] val json = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [611] val root = JSONObject(json)
      [612] val arr = root.getJSONArray("sources")
      [614] val s = arr.getJSONObject(i)
      [615] val b = s.optJSONObject("boundary")
      [616] val scan = s.optJSONObject("scan")
    CALLS OUT:
      → JSONObject
      → Log.e
      → arr.getJSONObject
      → arr.length
      → assets.open
      → bufferedReader
      → it.readText
      → root.getJSONArray
      → s.optJSONObject

========================================
FILE: ConvoyTransferRideScreen.kt
========================================

  [30] fun ConvoyTransferRideScreen(
    WORK FIELDS (local computed values):
      [33] val context = LocalContext.current
      [34] val today   = LocalDate.now()
      [35] val fmt     = DateTimeFormatter.ISO_LOCAL_DATE
      [37] val allRides = remember {
      [48] var searchQuery  by remember { mutableStateOf("") }
      [49] var selectedRide by remember { mutableStateOf<ConvoyEventConfig?>(null) }
      [50] var statusMsg    by remember { mutableStateOf("") }
      [52] val filteredRides = remember(searchQuery, allRides) {
      [62] val safeName = ride.eventName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [63] val safeDate = ride.eventDate.replace("/", "-").replace(" ", "_")
      [64] val fileName = "convoy_ride_${safeName}_${safeDate}.convoy"
      [65] val outFile  = File(context.cacheDir, fileName)
    CALLS OUT:
      → Box
      → Column
      → ConvoyEventStore.loadAll
      → File
      → FileProvider.getUriForFile
      → LocalDate.now
      → LocalDate.parse
      → Log.e
      → Log.i
      → Regex
      → Row
      → Spacer
      → Text
      → addFlags
      → background

  [60] fun sendRideEmail(ride: ConvoyEventConfig) {
    WORK FIELDS (local computed values):
      [62] val safeName = ride.eventName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [63] val safeDate = ride.eventDate.replace("/", "-").replace(" ", "_")
      [64] val fileName = "convoy_ride_${safeName}_${safeDate}.convoy"
      [65] val outFile  = File(context.cacheDir, fileName)
      [66] val rideJson = ride.toJson().apply { put("convoyDocType", "convoy_ride") }
      [69] val uri = FileProvider.getUriForFile(
      [73] val intent = Intent(Intent.ACTION_SEND).apply {
      [139] val isSelected = selectedRide?.eventId == ride.eventId
      [140] val rideDate   = try { LocalDate.parse(ride.eventDate, fmt) }
    CALLS OUT:
      → Box
      → Color
      → Column
      → File
      → FileProvider.getUriForFile
      → LazyColumn
      → LocalDate.parse
      → Log.e
      → Log.i
      → Regex
      → Row
      → Spacer
      → Text
      → addFlags
      → allRides.isEmpty

========================================
FILE: ConvoyUser.kt
========================================

  [42] fun toJson(): JSONObject = JSONObject().apply {
    WORK FIELDS (local computed values):
      [57] val devices = mutableListOf<String>()
      [58] val arr = obj.optJSONArray("knownDevices")
      [84] val today = LocalDate.now()
      [85] val fmt   = DateTimeFormatter.ISO_LOCAL_DATE
      [119] val file = java.io.File(context.filesDir, FILE_NAME)
      [122] val arr = JSONArray(file.readText())
    CALLS OUT:
      → JSONArray
      → LocalDate.now
      → UUID.randomUUID
      → arr.length
      → cellPhone.trim
      → devices.add
      → email.trim
      → file.exists
      → firstName.trim
      → fromJson
      → io.File
      → lastName.trim
      → loadAll
      → obj.optJSONArray
      → put

  [56] fun fromJson(obj: JSONObject): ConvoyUser {
    WORK FIELDS (local computed values):
      [57] val devices = mutableListOf<String>()
      [58] val arr = obj.optJSONArray("knownDevices")
      [84] val today = LocalDate.now()
      [85] val fmt   = DateTimeFormatter.ISO_LOCAL_DATE
      [119] val file = java.io.File(context.filesDir, FILE_NAME)
      [122] val arr = JSONArray(file.readText())
      [131] val arr = JSONArray()
    CALLS OUT:
      → ConvoyUser.fromJson
      → JSONArray
      → LocalDate.now
      → UUID.randomUUID
      → arr.getJSONObject
      → arr.length
      → arr.put
      → cellPhone.trim
      → devices.add
      → email.trim
      → file.exists
      → firstName.trim
      → io.File
      → it.toJson
      → lastName.trim

  [76] fun create(
    WORK FIELDS (local computed values):
      [84] val today = LocalDate.now()
      [85] val fmt   = DateTimeFormatter.ISO_LOCAL_DATE
      [119] val file = java.io.File(context.filesDir, FILE_NAME)
      [122] val arr = JSONArray(file.readText())
      [131] val arr = JSONArray()
      [138] val users = loadAll(context).toMutableList()
      [139] val idx = users.indexOfFirst { it.userId == user.userId }
      [146] val prefs  = context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)
      [147] val active = prefs.getString(ACTIVE_KEY, null) ?: return null
    CALLS OUT:
      → ConvoyUser.fromJson
      → JSONArray
      → LocalDate.now
      → UUID.randomUUID
      → apply
      → arr.getJSONObject
      → arr.length
      → arr.put
      → cellPhone.trim
      → context.getSharedPreferences
      → edit
      → email.trim
      → file.exists
      → firstName.trim
      → getActiveUser

  [118] fun loadAll(context: Context): List<ConvoyUser> {
    WORK FIELDS (local computed values):
      [119] val file = java.io.File(context.filesDir, FILE_NAME)
      [122] val arr = JSONArray(file.readText())
      [131] val arr = JSONArray()
      [138] val users = loadAll(context).toMutableList()
      [139] val idx = users.indexOfFirst { it.userId == user.userId }
      [146] val prefs  = context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)
      [147] val active = prefs.getString(ACTIVE_KEY, null) ?: return null
      [162] val user = getActiveUser(context) ?: return
      [172] val activeUser = getActiveUser(context) ?: return null
    CALLS OUT:
      → ConvoyUser.fromJson
      → JSONArray
      → addDeviceToActiveUser
      → apply
      → arr.getJSONObject
      → arr.length
      → arr.put
      → context.getSharedPreferences
      → edit
      → file.exists
      → getActiveUser
      → io.File
      → isEnrolled
      → it.toJson
      → knownDevices.add

  [130] fun saveAll(context: Context, users: List<ConvoyUser>) {
    WORK FIELDS (local computed values):
      [131] val arr = JSONArray()
      [138] val users = loadAll(context).toMutableList()
      [139] val idx = users.indexOfFirst { it.userId == user.userId }
      [146] val prefs  = context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)
      [147] val active = prefs.getString(ACTIVE_KEY, null) ?: return null
      [162] val user = getActiveUser(context) ?: return
      [172] val activeUser = getActiveUser(context) ?: return null
    CALLS OUT:
      → JSONArray
      → addDeviceToActiveUser
      → apply
      → arr.put
      → context.getSharedPreferences
      → edit
      → getActiveUser
      → io.File
      → isEnrolled
      → it.toJson
      → knownDevices.add
      → knownDevices.contains
      → loadAll
      → resolvePairing
      → save

  [137] fun save(context: Context, user: ConvoyUser) {
    WORK FIELDS (local computed values):
      [138] val users = loadAll(context).toMutableList()
      [139] val idx = users.indexOfFirst { it.userId == user.userId }
      [146] val prefs  = context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)
      [147] val active = prefs.getString(ACTIVE_KEY, null) ?: return null
      [162] val user = getActiveUser(context) ?: return
      [172] val activeUser = getActiveUser(context) ?: return null
    CALLS OUT:
      → addDeviceToActiveUser
      → apply
      → context.getSharedPreferences
      → edit
      → getActiveUser
      → isEnrolled
      → knownDevices.add
      → knownDevices.contains
      → loadAll
      → resolvePairing
      → save
      → saveAll
      → setActiveUser
      → users.add

  [145] fun getActiveUser(context: Context): ConvoyUser? {
    WORK FIELDS (local computed values):
      [146] val prefs  = context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)
      [147] val active = prefs.getString(ACTIVE_KEY, null) ?: return null
      [162] val user = getActiveUser(context) ?: return
      [172] val activeUser = getActiveUser(context) ?: return null
    CALLS OUT:
      → addDeviceToActiveUser
      → apply
      → context.getSharedPreferences
      → edit
      → getActiveUser
      → isEnrolled
      → knownDevices.add
      → knownDevices.contains
      → loadAll
      → resolvePairing
      → save
      → setActiveUser

  [152] fun setActiveUser(context: Context, userId: String) {
    WORK FIELDS (local computed values):
      [162] val user = getActiveUser(context) ?: return
      [172] val activeUser = getActiveUser(context) ?: return null
    CALLS OUT:
      → addDeviceToActiveUser
      → apply
      → context.getSharedPreferences
      → edit
      → getActiveUser
      → isEnrolled
      → knownDevices.add
      → knownDevices.contains
      → resolvePairing
      → save

  [158] fun isEnrolled(context: Context): Boolean = getActiveUser(context) != null
    WORK FIELDS (local computed values):
      [162] val user = getActiveUser(context) ?: return
      [172] val activeUser = getActiveUser(context) ?: return null
    CALLS OUT:
      → addDeviceToActiveUser
      → getActiveUser
      → knownDevices.add
      → knownDevices.contains
      → resolvePairing
      → save

  [161] fun addDeviceToActiveUser(context: Context, hardwareId: String) {
    WORK FIELDS (local computed values):
      [162] val user = getActiveUser(context) ?: return
      [172] val activeUser = getActiveUser(context) ?: return null
    CALLS OUT:
      → getActiveUser
      → knownDevices.add
      → knownDevices.contains
      → resolvePairing
      → save

  [171] fun resolvePairing(context: Context, hardwareId: String): ConvoyUser? {
    WORK FIELDS (local computed values):
      [172] val activeUser = getActiveUser(context) ?: return null
    CALLS OUT:
      → getActiveUser
      → knownDevices.add
      → knownDevices.contains
      → save

========================================
FILE: ConvoyVerifyConfigScreen.kt
========================================

  [34] fun ConvoyVerifyConfigScreen(
    WORK FIELDS (local computed values):
      [41] val connectionState by uiViewModel.connectionState.collectAsStateWithLifecycle()
      [42] val isConnected = connectionState.toString().contains("Connected", ignoreCase = true)
      [43] val localConfig by channelViewModel.localConfig.collectAsStateWithLifecycle()
      [44] val channels    by channelViewModel.channels.collectAsStateWithLifecycle()
      [46] var verified  by remember { mutableStateOf(false) }
      [47] var results   by remember { mutableStateOf<List<VerifyResult>>(emptyList()) }
      [48] var allPassed by remember { mutableStateOf(false) }
      [55] val list = mutableListOf<VerifyResult>()
      [60] val actRole = localConfig.device?.role?.name ?: "—"
      [64] val actManaged = localConfig.device?.is_managed?.toString() ?: "—"
      [68] val actSerial = localConfig.device?.serial_enabled?.toString() ?: "—"
      [73] val actRegion = localConfig.lora?.region?.name ?: "—"
    CALLS OUT:
      → LaunchedEffect
      → contains
      → hiltViewModel
      → list.add
      → mutableStateOf
      → settings.firstOrNull
      → vr

  [50] fun vr(field: String, group: String, expected: String, actual: String, pass: Boolean) =
    WORK FIELDS (local computed values):
      [55] val list = mutableListOf<VerifyResult>()
      [60] val actRole = localConfig.device?.role?.name ?: "—"
      [64] val actManaged = localConfig.device?.is_managed?.toString() ?: "—"
      [68] val actSerial = localConfig.device?.serial_enabled?.toString() ?: "—"
      [73] val actRegion = localConfig.lora?.region?.name ?: "—"
      [77] val actPreset = localConfig.lora?.modem_preset?.name ?: "—"
      [81] val actBandwidth = localConfig.lora?.bandwidth?.toString() ?: "—"
      [85] val actSpread = localConfig.lora?.spread_factor?.toString() ?: "—"
      [89] val actCoding = localConfig.lora?.coding_rate?.toString() ?: "—"
      [93] val actHop = localConfig.lora?.hop_limit?.toString() ?: "—"
      [97] val actTxEnabled = localConfig.lora?.tx_enabled?.toString() ?: "—"
      [101] val actTxPower = localConfig.lora?.tx_power?.toString() ?: "—"
    CALLS OUT:
      → LaunchedEffect
      → channelPsk.isNotBlank
      → list.add
      → settings.firstOrNull
      → vr

========================================
FILE: ConvoyViewModel.kt
========================================

  [69] fun clearImportBanner() { _pendingImportBanner.value = null }
    WORK FIELDS (local computed values):
      [114] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager ?: return
      [133] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager
    CALLS OUT:
      → Log.e
      → Looper.getMainLooper
      → MutableStateFlow
      → System.currentTimeMillis
      → _autoPan.asStateFlow
      → _isLocalTiles.asStateFlow
      → _isOfflineMode.asStateFlow
      → _leadLocked.asStateFlow
      → _mapTypeLabel.asStateFlow
      → _offTrackIds.asStateFlow
      → _selectedNode.asStateFlow
      → _trackActive.asStateFlow
      → _trackLeadOnly.asStateFlow
      → appContext.getSystemService
      → lastKnownPosition.clear

  [96] fun setOfflineMode(offline: Boolean) { _isOfflineMode.value = offline }
    WORK FIELDS (local computed values):
      [114] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager ?: return
      [133] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager
      [157] val myNum = _myNodeInfo.value?.myNodeNum
      [159] val nodes = readLiveNodes(System.currentTimeMillis())
    CALLS OUT:
      → Log.e
      → Looper.getMainLooper
      → MutableStateFlow
      → System.currentTimeMillis
      → _autoPan.asStateFlow
      → _isLocalTiles.asStateFlow
      → _mapTypeLabel.asStateFlow
      → appContext.getSystemService
      → convoyLog
      → lastKnownPosition.clear
      → lastNodePositions.clear
      → lm.requestLocationUpdates
      → nodeLastComputedSpeed.clear
      → nodeSpeedWindowStart.clear
      → nodes.isEmpty

  [99] fun setMapTypeLabel(label: String) { _mapTypeLabel.value = label }
    WORK FIELDS (local computed values):
      [114] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager ?: return
      [133] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager
      [157] val myNum = _myNodeInfo.value?.myNodeNum
      [159] val nodes = readLiveNodes(System.currentTimeMillis())
    CALLS OUT:
      → Log.e
      → Looper.getMainLooper
      → MutableStateFlow
      → System.currentTimeMillis
      → _autoPan.asStateFlow
      → _isLocalTiles.asStateFlow
      → appContext.getSystemService
      → convoyLog
      → lastKnownPosition.clear
      → lastNodePositions.clear
      → lm.requestLocationUpdates
      → nodeLastComputedSpeed.clear
      → nodeSpeedWindowStart.clear
      → nodes.isEmpty
      → readLiveNodes

  [102] fun setLocalTiles(local: Boolean) { _isLocalTiles.value = local }
    WORK FIELDS (local computed values):
      [114] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager ?: return
      [133] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager
      [157] val myNum = _myNodeInfo.value?.myNodeNum
      [159] val nodes = readLiveNodes(System.currentTimeMillis())
    CALLS OUT:
      → Log.e
      → Looper.getMainLooper
      → MutableStateFlow
      → System.currentTimeMillis
      → _autoPan.asStateFlow
      → appContext.getSystemService
      → convoyLog
      → lastKnownPosition.clear
      → lastNodePositions.clear
      → lm.requestLocationUpdates
      → nodeDistanceAccum.clear
      → nodeLastComputedSpeed.clear
      → nodeLastLat.clear
      → nodeSpeedWindowStart.clear
      → nodes.isEmpty

  [105] fun setAutoPan(pan: Boolean) { _autoPan.value = pan }
    WORK FIELDS (local computed values):
      [114] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager ?: return
      [133] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager
      [157] val myNum = _myNodeInfo.value?.myNodeNum
      [159] val nodes = readLiveNodes(System.currentTimeMillis())
    CALLS OUT:
      → Log.e
      → Looper.getMainLooper
      → System.currentTimeMillis
      → appContext.getSystemService
      → convoyLog
      → lastKnownPosition.clear
      → lastNodePositions.clear
      → lm.requestLocationUpdates
      → nodeDistanceAccum.clear
      → nodeLastComputedSpeed.clear
      → nodeLastLat.clear
      → nodeLastLon.clear
      → nodeSpeedWindowStart.clear
      → nodes.isEmpty
      → readLiveNodes

  [111] private fun startPhoneGps() {
    WORK FIELDS (local computed values):
      [114] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager ?: return
      [133] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager
      [157] val myNum = _myNodeInfo.value?.myNodeNum
      [159] val nodes = readLiveNodes(System.currentTimeMillis())
    CALLS OUT:
      → Log.e
      → Looper.getMainLooper
      → System.currentTimeMillis
      → appContext.getSystemService
      → convoyLog
      → lastKnownPosition.clear
      → lastNodePositions.clear
      → lm.requestLocationUpdates
      → nodeDistanceAccum.clear
      → nodeLastComputedSpeed.clear
      → nodeLastLat.clear
      → nodeLastLon.clear
      → nodeSpeedWindowStart.clear
      → nodes.isEmpty
      → readLiveNodes

  [130] private fun stopPhoneGps() {
    WORK FIELDS (local computed values):
      [133] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager
      [157] val myNum = _myNodeInfo.value?.myNodeNum
      [159] val nodes = readLiveNodes(System.currentTimeMillis())
      [199] val name = _convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }?.callsign ?: nodeId
    CALLS OUT:
      → MutableStateFlow
      → System.currentTimeMillis
      → _radioInactive.asStateFlow
      → appContext.getSystemService
      → convoyLog
      → lastKnownPosition.clear
      → lastNodePositions.clear
      → nodeDistanceAccum.clear
      → nodeLastComputedSpeed.clear
      → nodeLastLat.clear
      → nodeLastLon.clear
      → nodeSpeedWindowStart.clear
      → nodes.isEmpty
      → readLiveNodes
      → removeUpdates

  [140] fun startGroupTrack() {
    WORK FIELDS (local computed values):
      [157] val myNum = _myNodeInfo.value?.myNodeNum
      [159] val nodes = readLiveNodes(System.currentTimeMillis())
      [199] val name = _convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }?.callsign ?: nodeId
    CALLS OUT:
      → MutableStateFlow
      → System.currentTimeMillis
      → _radioInactive.asStateFlow
      → convoyLog
      → lastKnownPosition.clear
      → lastNodePositions.clear
      → nodeDistanceAccum.clear
      → nodeLastComputedSpeed.clear
      → nodeLastLat.clear
      → nodeLastLon.clear
      → nodeSpeedWindowStart.clear
      → nodes.isEmpty
      → readLiveNodes
      → reconnectRadio
      → setLeadCart

  [176] fun stopGroupTrack() {
    WORK FIELDS (local computed values):
      [199] val name = _convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }?.callsign ?: nodeId
      [222] var recordingState = androidx.compose.runtime.mutableStateOf(com.geeksville.mesh.convoy.RecordingState.IDLE)
      [223] var pendingTrackName = androidx.compose.runtime.mutableStateOf("")
      [224] var showRecMenu = androidx.compose.runtime.mutableStateOf(false)
      [225] var pendingEnrollmentEmail = androidx.compose.runtime.mutableStateOf("")
      [226] var hasSeenNodes = androidx.compose.runtime.mutableStateOf(false)
      [232] val deviceProfileFlow = radioConfigRepository.deviceProfileFlow
      [235] val profile = radioConfigRepository.deviceProfileFlow.first()
      [250] val currentUser = nodeRepository.ourNodeInfo.value?.user
    CALLS OUT:
      → Log.i
      → MutableStateFlow
      → System.currentTimeMillis
      → _radioInactive.asStateFlow
      → _workingConfig.asStateFlow
      → convoyLog
      → deviceProfileFlow.first
      → exportProfileToFile
      → exportProfileUseCase
      → file.inputStream
      → file.outputStream
      → importProfileFromFile
      → importProfileUseCase
      → installProfileToRadio
      → installProfileUseCase

  [194] fun setLeadCart(nodeId: String?) {
    WORK FIELDS (local computed values):
      [199] val name = _convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }?.callsign ?: nodeId
      [222] var recordingState = androidx.compose.runtime.mutableStateOf(com.geeksville.mesh.convoy.RecordingState.IDLE)
      [223] var pendingTrackName = androidx.compose.runtime.mutableStateOf("")
      [224] var showRecMenu = androidx.compose.runtime.mutableStateOf(false)
      [225] var pendingEnrollmentEmail = androidx.compose.runtime.mutableStateOf("")
      [226] var hasSeenNodes = androidx.compose.runtime.mutableStateOf(false)
      [232] val deviceProfileFlow = radioConfigRepository.deviceProfileFlow
      [235] val profile = radioConfigRepository.deviceProfileFlow.first()
      [250] val currentUser = nodeRepository.ourNodeInfo.value?.user
      [266] val dir = java.io.File(context.filesDir, "convoy_backups/$nodeId")
      [268] val ts = java.time.LocalDateTime.now()
      [273] val dir = java.io.File(context.filesDir, "convoy_rides")
    CALLS OUT:
      → LocalDateTime.now
      → Log.e
      → Log.i
      → MutableStateFlow
      → System.currentTimeMillis
      → _radioInactive.asStateFlow
      → _workingConfig.asStateFlow
      → archiveCfg
      → convoyLog
      → deviceProfileFlow.first
      → dir.mkdirs
      → exportProfileToFile
      → exportProfileUseCase
      → file.inputStream
      → file.outputStream

  [213] fun reconnectRadio() {
    WORK FIELDS (local computed values):
      [222] var recordingState = androidx.compose.runtime.mutableStateOf(com.geeksville.mesh.convoy.RecordingState.IDLE)
      [223] var pendingTrackName = androidx.compose.runtime.mutableStateOf("")
      [224] var showRecMenu = androidx.compose.runtime.mutableStateOf(false)
      [225] var pendingEnrollmentEmail = androidx.compose.runtime.mutableStateOf("")
      [226] var hasSeenNodes = androidx.compose.runtime.mutableStateOf(false)
      [232] val deviceProfileFlow = radioConfigRepository.deviceProfileFlow
      [235] val profile = radioConfigRepository.deviceProfileFlow.first()
      [250] val currentUser = nodeRepository.ourNodeInfo.value?.user
      [266] val dir = java.io.File(context.filesDir, "convoy_backups/$nodeId")
      [268] val ts = java.time.LocalDateTime.now()
      [273] val dir = java.io.File(context.filesDir, "convoy_rides")
      [275] val safeName = rideName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
    CALLS OUT:
      → LocalDateTime.now
      → Log.e
      → Log.i
      → MutableStateFlow
      → Regex
      → System.currentTimeMillis
      → _simulationMode.asStateFlow
      → _workingConfig.asStateFlow
      → archiveCfg
      → assetsMasterCfg
      → clearWorkingConfig
      → deviceProfileFlow.first
      → dir.mkdirs
      → exportProfileToFile
      → exportProfileUseCase

  [218] fun toggleLeadOnly() {
    WORK FIELDS (local computed values):
      [222] var recordingState = androidx.compose.runtime.mutableStateOf(com.geeksville.mesh.convoy.RecordingState.IDLE)
      [223] var pendingTrackName = androidx.compose.runtime.mutableStateOf("")
      [224] var showRecMenu = androidx.compose.runtime.mutableStateOf(false)
      [225] var pendingEnrollmentEmail = androidx.compose.runtime.mutableStateOf("")
      [226] var hasSeenNodes = androidx.compose.runtime.mutableStateOf(false)
      [232] val deviceProfileFlow = radioConfigRepository.deviceProfileFlow
      [235] val profile = radioConfigRepository.deviceProfileFlow.first()
      [250] val currentUser = nodeRepository.ourNodeInfo.value?.user
      [266] val dir = java.io.File(context.filesDir, "convoy_backups/$nodeId")
      [268] val ts = java.time.LocalDateTime.now()
      [273] val dir = java.io.File(context.filesDir, "convoy_rides")
      [275] val safeName = rideName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
    CALLS OUT:
      → LocalDateTime.now
      → Log.e
      → Log.i
      → MutableStateFlow
      → Regex
      → _myCartId.asStateFlow
      → _showLeadTrack.asStateFlow
      → _simulationMode.asStateFlow
      → _workingConfig.asStateFlow
      → archiveCfg
      → assetsMasterCfg
      → clearWorkingConfig
      → deviceProfileFlow.first
      → dir.mkdirs
      → exportProfileToFile

  [230] fun setWorkingConfig(config: WorkingConfig) { _workingConfig.value = config }
    WORK FIELDS (local computed values):
      [232] val deviceProfileFlow = radioConfigRepository.deviceProfileFlow
      [235] val profile = radioConfigRepository.deviceProfileFlow.first()
      [250] val currentUser = nodeRepository.ourNodeInfo.value?.user
      [266] val dir = java.io.File(context.filesDir, "convoy_backups/$nodeId")
      [268] val ts = java.time.LocalDateTime.now()
      [273] val dir = java.io.File(context.filesDir, "convoy_rides")
      [275] val safeName = rideName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [284] var persistentWebView: android.webkit.WebView? = null
      [291] val num = nodeRepository.myNodeInfo.value?.myNodeNum
    CALLS OUT:
      → LocalDateTime.now
      → Log.e
      → Log.i
      → MutableStateFlow
      → Regex
      → _gpsTrailSegments.asStateFlow
      → _leadTrackSegments.asStateFlow
      → _myCartId.asStateFlow
      → _routeTrailSegments.asStateFlow
      → _showLeadTrack.asStateFlow
      → _simulationMode.asStateFlow
      → archiveCfg
      → assetsMasterCfg
      → clearWorkingConfig
      → deviceProfileFlow.first

  [234] suspend fun exportProfileToFile(context: android.content.Context, file: java.io.File): Result<Unit> = runCatching {
    WORK FIELDS (local computed values):
      [235] val profile = radioConfigRepository.deviceProfileFlow.first()
      [250] val currentUser = nodeRepository.ourNodeInfo.value?.user
      [266] val dir = java.io.File(context.filesDir, "convoy_backups/$nodeId")
      [268] val ts = java.time.LocalDateTime.now()
      [273] val dir = java.io.File(context.filesDir, "convoy_rides")
      [275] val safeName = rideName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [284] var persistentWebView: android.webkit.WebView? = null
      [291] val num = nodeRepository.myNodeInfo.value?.myNodeNum
    CALLS OUT:
      → LocalDateTime.now
      → Log.e
      → Log.i
      → MutableStateFlow
      → Regex
      → _gpsTrailSegments.asStateFlow
      → _leadTrackSegments.asStateFlow
      → _myCartId.asStateFlow
      → _routeTrailSegments.asStateFlow
      → _showLeadTrack.asStateFlow
      → _simulationMode.asStateFlow
      → archiveCfg
      → assetsMasterCfg
      → clearWorkingConfig
      → deviceProfileFlow.first

  [243] fun importProfileFromFile(file: java.io.File): Result<DeviceProfile> = runCatching {
    WORK FIELDS (local computed values):
      [250] val currentUser = nodeRepository.ourNodeInfo.value?.user
      [266] val dir = java.io.File(context.filesDir, "convoy_backups/$nodeId")
      [268] val ts = java.time.LocalDateTime.now()
      [273] val dir = java.io.File(context.filesDir, "convoy_rides")
      [275] val safeName = rideName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [284] var persistentWebView: android.webkit.WebView? = null
      [291] val num = nodeRepository.myNodeInfo.value?.myNodeNum
    CALLS OUT:
      → LocalDateTime.now
      → Log.e
      → Log.i
      → MutableStateFlow
      → Regex
      → _gpsTrailSegments.asStateFlow
      → _leadTrackSegments.asStateFlow
      → _myCartId.asStateFlow
      → _offTrackNodes.asStateFlow
      → _routeRecording.asStateFlow
      → _routeTrailSegments.asStateFlow
      → _showLeadTrack.asStateFlow
      → _simulationMode.asStateFlow
      → archiveCfg
      → assetsMasterCfg

  [249] fun installProfileToRadio(destNum: Int, profile: DeviceProfile) {
    WORK FIELDS (local computed values):
      [250] val currentUser = nodeRepository.ourNodeInfo.value?.user
      [266] val dir = java.io.File(context.filesDir, "convoy_backups/$nodeId")
      [268] val ts = java.time.LocalDateTime.now()
      [273] val dir = java.io.File(context.filesDir, "convoy_rides")
      [275] val safeName = rideName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [284] var persistentWebView: android.webkit.WebView? = null
      [291] val num = nodeRepository.myNodeInfo.value?.myNodeNum
    CALLS OUT:
      → LocalDateTime.now
      → Log.e
      → Log.i
      → MutableStateFlow
      → Regex
      → _gpsTrailSegments.asStateFlow
      → _leadTrackSegments.asStateFlow
      → _myCartId.asStateFlow
      → _offTrackNodes.asStateFlow
      → _routeRecording.asStateFlow
      → _routeTrailSegments.asStateFlow
      → _showLeadTrack.asStateFlow
      → _simulationMode.asStateFlow
      → archiveCfg
      → assetsMasterCfg

  [263] fun masterCfg(context: android.content.Context) =
    WORK FIELDS (local computed values):
      [266] val dir = java.io.File(context.filesDir, "convoy_backups/$nodeId")
      [268] val ts = java.time.LocalDateTime.now()
      [273] val dir = java.io.File(context.filesDir, "convoy_rides")
      [275] val safeName = rideName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [284] var persistentWebView: android.webkit.WebView? = null
      [291] val num = nodeRepository.myNodeInfo.value?.myNodeNum
      [331] val num = info?.myNodeNum
      [342] val importDir = java.io.File(appContext.filesDir, "convoy_import").also { it.mkdirs() }
    CALLS OUT:
      → LocalDateTime.now
      → MutableStateFlow
      → Regex
      → _gpsTrailSegments.asStateFlow
      → _leadTrackSegments.asStateFlow
      → _myCartId.asStateFlow
      → _offTrackNodes.asStateFlow
      → _routeRecording.asStateFlow
      → _routeTrailSegments.asStateFlow
      → _showLeadTrack.asStateFlow
      → _simulationMode.asStateFlow
      → archiveCfg
      → assetsMasterCfg
      → clearWorkingConfig
      → dir.mkdirs

  [265] fun archiveCfg(context: android.content.Context, nodeId: String, label: String): java.io.File {
    WORK FIELDS (local computed values):
      [266] val dir = java.io.File(context.filesDir, "convoy_backups/$nodeId")
      [268] val ts = java.time.LocalDateTime.now()
      [273] val dir = java.io.File(context.filesDir, "convoy_rides")
      [275] val safeName = rideName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [284] var persistentWebView: android.webkit.WebView? = null
      [291] val num = nodeRepository.myNodeInfo.value?.myNodeNum
      [331] val num = info?.myNodeNum
      [342] val importDir = java.io.File(appContext.filesDir, "convoy_import").also { it.mkdirs() }
    CALLS OUT:
      → LocalDateTime.now
      → MutableStateFlow
      → Regex
      → _gpsTrailSegments.asStateFlow
      → _leadTrackSegments.asStateFlow
      → _myCartId.asStateFlow
      → _offTrackNodes.asStateFlow
      → _routeRecording.asStateFlow
      → _routeTrailSegments.asStateFlow
      → _showLeadTrack.asStateFlow
      → _simulationMode.asStateFlow
      → assetsMasterCfg
      → clearWorkingConfig
      → dir.mkdirs
      → importDir.exists

  [272] fun rideCfg(context: android.content.Context, rideName: String, rideDate: String): java.io.File {
    WORK FIELDS (local computed values):
      [273] val dir = java.io.File(context.filesDir, "convoy_rides")
      [275] val safeName = rideName.replace(Regex("[^a-zA-Z0-9_-]"), "_")
      [284] var persistentWebView: android.webkit.WebView? = null
      [291] val num = nodeRepository.myNodeInfo.value?.myNodeNum
      [331] val num = info?.myNodeNum
      [342] val importDir = java.io.File(appContext.filesDir, "convoy_import").also { it.mkdirs() }
      [346] val files = importDir.listFiles { f -> f.extension == "convoy" || f.extension == "json" } ?: return
      [347] var importCount = 0
      [348] var lastImportedName = ""
      [351] val json = org.json.JSONObject(file.readText())
      [352] val docType = json.optString("convoyDocType", "convoy_ride")
    CALLS OUT:
      → MutableStateFlow
      → Regex
      → _gpsTrailSegments.asStateFlow
      → _leadTrackSegments.asStateFlow
      → _myCartId.asStateFlow
      → _offTrackNodes.asStateFlow
      → _routeRecording.asStateFlow
      → _routeTrailSegments.asStateFlow
      → _showLeadTrack.asStateFlow
      → _simulationMode.asStateFlow
      → assetsMasterCfg
      → clearWorkingConfig
      → dir.mkdirs
      → file.readText
      → importDir.exists

  [278] fun assetsMasterCfg(context: android.content.Context): java.io.File =
    WORK FIELDS (local computed values):
      [284] var persistentWebView: android.webkit.WebView? = null
      [291] val num = nodeRepository.myNodeInfo.value?.myNodeNum
      [331] val num = info?.myNodeNum
      [342] val importDir = java.io.File(appContext.filesDir, "convoy_import").also { it.mkdirs() }
      [346] val files = importDir.listFiles { f -> f.extension == "convoy" || f.extension == "json" } ?: return
      [347] var importCount = 0
      [348] var lastImportedName = ""
      [351] val json = org.json.JSONObject(file.readText())
      [352] val docType = json.optString("convoyDocType", "convoy_ride")
      [355] val event = ConvoyEventConfig.fromJson(json)
    CALLS OUT:
      → ConvoyEventConfig.fromJson
      → ConvoyEventStore.save
      → MutableStateFlow
      → _gpsTrailSegments.asStateFlow
      → _leadTrackSegments.asStateFlow
      → _myCartId.asStateFlow
      → _offTrackNodes.asStateFlow
      → _routeRecording.asStateFlow
      → _routeTrailSegments.asStateFlow
      → _showLeadTrack.asStateFlow
      → _simulationMode.asStateFlow
      → clearWorkingConfig
      → file.readText
      → importDir.exists
      → io.File

  [282] fun clearWorkingConfig() { _workingConfig.value = null }
    WORK FIELDS (local computed values):
      [284] var persistentWebView: android.webkit.WebView? = null
      [291] val num = nodeRepository.myNodeInfo.value?.myNodeNum
      [331] val num = info?.myNodeNum
      [342] val importDir = java.io.File(appContext.filesDir, "convoy_import").also { it.mkdirs() }
      [346] val files = importDir.listFiles { f -> f.extension == "convoy" || f.extension == "json" } ?: return
      [347] var importCount = 0
      [348] var lastImportedName = ""
      [351] val json = org.json.JSONObject(file.readText())
      [352] val docType = json.optString("convoyDocType", "convoy_ride")
      [355] val event = ConvoyEventConfig.fromJson(json)
    CALLS OUT:
      → ConvoyEventConfig.fromJson
      → ConvoyEventStore.save
      → Log.w
      → MutableStateFlow
      → _gpsTrailSegments.asStateFlow
      → _leadTrackSegments.asStateFlow
      → _myCartId.asStateFlow
      → _offTrackNodes.asStateFlow
      → _routeRecording.asStateFlow
      → _routeTrailSegments.asStateFlow
      → _showLeadTrack.asStateFlow
      → _simulationMode.asStateFlow
      → file.delete
      → file.readText
      → importDir.exists

  [290] private fun resolveMyCartId(): String {
    WORK FIELDS (local computed values):
      [291] val num = nodeRepository.myNodeInfo.value?.myNodeNum
      [331] val num = info?.myNodeNum
      [342] val importDir = java.io.File(appContext.filesDir, "convoy_import").also { it.mkdirs() }
      [346] val files = importDir.listFiles { f -> f.extension == "convoy" || f.extension == "json" } ?: return
      [347] var importCount = 0
      [348] var lastImportedName = ""
      [351] val json = org.json.JSONObject(file.readText())
      [352] val docType = json.optString("convoyDocType", "convoy_ride")
      [355] val event = ConvoyEventConfig.fromJson(json)
      [369] val msg = if (importCount == 1) "NEW RIDE IMPORTED: $lastImportedName" else "$importCount NEW RIDES IMPORTED"
    CALLS OUT:
      → ConvoyEventConfig.fromJson
      → ConvoyEventStore.save
      → Log.e
      → Log.w
      → MutableStateFlow
      → _gpsTrailSegments.asStateFlow
      → _leadTrackSegments.asStateFlow
      → _myCartId.asStateFlow
      → _offTrackNodes.asStateFlow
      → _routeRecording.asStateFlow
      → _routeTrailSegments.asStateFlow
      → _showLeadTrack.asStateFlow
      → file.delete
      → file.readText
      → importDir.exists

  [340] suspend fun scanImportDirectory() {
    WORK FIELDS (local computed values):
      [342] val importDir = java.io.File(appContext.filesDir, "convoy_import").also { it.mkdirs() }
      [346] val files = importDir.listFiles { f -> f.extension == "convoy" || f.extension == "json" } ?: return
      [347] var importCount = 0
      [348] var lastImportedName = ""
      [351] val json = org.json.JSONObject(file.readText())
      [352] val docType = json.optString("convoyDocType", "convoy_ride")
      [355] val event = ConvoyEventConfig.fromJson(json)
      [369] val msg = if (importCount == 1) "NEW RIDE IMPORTED: $lastImportedName" else "$importCount NEW RIDES IMPORTED"
      [391] gps_update_interval = secs,
      [405] val current = _convoyState.value
    CALLS OUT:
      → Config.PositionConfig
      → ConvoyEventConfig.fromJson
      → ConvoyEventStore.save
      → ConvoySimulation.start
      → Log.e
      → Log.w
      → MutableStateFlow
      → _distanceMiles.asStateFlow
      → channelViewModel.setConfig
      → current.copy
      → dismissNodeHud
      → file.delete
      → file.readText
      → importDir.exists
      → io.File

  [377] fun setHudMode(mode: HudMode) { _hudMode.value = mode }
    WORK FIELDS (local computed values):
      [391] gps_update_interval = secs,
      [405] val current = _convoyState.value
      [429] val conn = object : android.content.ServiceConnection {
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
    CALLS OUT:
      → Config.PositionConfig
      → ConvoyEngine.LeadTrackSegment
      → ConvoySimulation.start
      → Log.e
      → MutableStateFlow
      → _avgChannelUtil.asStateFlow
      → _distanceMiles.asStateFlow
      → bindGpsService
      → channelViewModel.setConfig
      → context.bindService
      → current.copy
      → dismissNodeHud
      → getService
      → onBound
      → onMarkerTapped

  [378] fun onMarkerTapped(node: ConvoyNode) { _selectedNode.value = node; _hudMode.value = HudMode.NODE }
    WORK FIELDS (local computed values):
      [391] gps_update_interval = secs,
      [405] val current = _convoyState.value
      [429] val conn = object : android.content.ServiceConnection {
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
    CALLS OUT:
      → Config.PositionConfig
      → ConvoyEngine.LeadTrackSegment
      → ConvoyGpsService.start
      → ConvoySimulation.start
      → Log.e
      → MutableStateFlow
      → _avgChannelUtil.asStateFlow
      → _distanceMiles.asStateFlow
      → bindGpsService
      → channelViewModel.setConfig
      → context.bindService
      → current.copy
      → dismissNodeHud
      → getService
      → onBound

  [379] fun dismissNodeHud() { _selectedNode.value = null; _hudMode.value = HudMode.GROUP }
    WORK FIELDS (local computed values):
      [391] gps_update_interval = secs,
      [405] val current = _convoyState.value
      [429] val conn = object : android.content.ServiceConnection {
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
    CALLS OUT:
      → Config.PositionConfig
      → ConvoyEngine.LeadTrackSegment
      → ConvoyGpsService.start
      → ConvoySimulation.start
      → Log.e
      → MutableStateFlow
      → _avgChannelUtil.asStateFlow
      → _distanceMiles.asStateFlow
      → bindGpsService
      → channelViewModel.setConfig
      → context.bindService
      → current.copy
      → getService
      → onBound
      → onServiceConnected

  [384] fun setGpsInterval(secs: Int, channelViewModel: com.geeksville.mesh.ui.sharing.ChannelViewModel) {
    WORK FIELDS (local computed values):
      [391] gps_update_interval = secs,
      [405] val current = _convoyState.value
      [429] val conn = object : android.content.ServiceConnection {
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
    CALLS OUT:
      → Config.PositionConfig
      → ConvoyEngine.LeadTrackSegment
      → ConvoyGpsService.start
      → ConvoySimulation.start
      → Log.e
      → MutableStateFlow
      → _avgChannelUtil.asStateFlow
      → _distanceMiles.asStateFlow
      → bindGpsService
      → channelViewModel.setConfig
      → context.bindService
      → current.copy
      → getService
      → onBound
      → onServiceConnected

  [402] fun removeNode(nodeId: String) {
    WORK FIELDS (local computed values):
      [405] val current = _convoyState.value
      [429] val conn = object : android.content.ServiceConnection {
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
      [476] val temp = pendingTempFile ?: return
    CALLS OUT:
      → ConvoyEngine.LeadTrackSegment
      → ConvoyGpsService.start
      → ConvoySimulation.start
      → Log.d
      → MutableStateFlow
      → _avgChannelUtil.asStateFlow
      → _distanceMiles.asStateFlow
      → bindGpsService
      → context.bindService
      → current.copy
      → deleteTempTrack
      → getService
      → onBound
      → onServiceConnected
      → onServiceDisconnected

  [409] fun setSimulationMode(enabled: Boolean) {
    WORK FIELDS (local computed values):
      [429] val conn = object : android.content.ServiceConnection {
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
    CALLS OUT:
      → ConvoyEngine.LeadTrackSegment
      → ConvoyGpsService.start
      → ConvoySimulation.start
      → Log.d
      → MutableStateFlow
      → _avgChannelUtil.asStateFlow
      → _distanceMiles.asStateFlow
      → bindGpsService
      → context.bindService
      → context.unbindService
      → deleteTempTrack
      → finalizeTrack
      → getService
      → onBound
      → onServiceConnected

  [414] fun setMyCartId(nodeId: String) { _myCartId.value = nodeId }
    WORK FIELDS (local computed values):
      [429] val conn = object : android.content.ServiceConnection {
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
    CALLS OUT:
      → ConvoyEngine.LeadTrackSegment
      → ConvoyGpsService.start
      → Log.d
      → MutableStateFlow
      → _avgChannelUtil.asStateFlow
      → _distanceMiles.asStateFlow
      → bindGpsService
      → context.bindService
      → context.unbindService
      → deleteTempTrack
      → finalizeTrack
      → getService
      → io.File
      → onBound
      → onServiceConnected

  [415] fun toggleLeadTrack() { _showLeadTrack.value = !_showLeadTrack.value }
    WORK FIELDS (local computed values):
      [429] val conn = object : android.content.ServiceConnection {
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
    CALLS OUT:
      → ConvoyEngine.LeadTrackSegment
      → ConvoyGpsService.start
      → Log.d
      → MutableStateFlow
      → System.currentTimeMillis
      → _avgChannelUtil.asStateFlow
      → _distanceMiles.asStateFlow
      → bindGpsService
      → context.bindService
      → context.unbindService
      → deleteTempTrack
      → finalizeTrack
      → getService
      → io.File
      → it.writeText

  [416] fun setShowLeadTrack(visible: Boolean) { _showLeadTrack.value = visible }
    WORK FIELDS (local computed values):
      [429] val conn = object : android.content.ServiceConnection {
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
    CALLS OUT:
      → ConvoyEngine.LeadTrackSegment
      → ConvoyGpsService.start
      → Log.d
      → MutableStateFlow
      → System.currentTimeMillis
      → _avgChannelUtil.asStateFlow
      → _distanceMiles.asStateFlow
      → bindGpsService
      → context.bindService
      → context.unbindService
      → deleteTempTrack
      → finalizeTrack
      → getService
      → io.File
      → it.writeText

  [428] private fun bindGpsService(context: android.content.Context, onBound: (ConvoyGpsService) -> Unit) {
    WORK FIELDS (local computed values):
      [429] val conn = object : android.content.ServiceConnection {
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
    CALLS OUT:
      → ConvoyEngine.LeadTrackSegment
      → ConvoyGpsService.start
      → Log.d
      → System.currentTimeMillis
      → bindGpsService
      → cancel
      → context.bindService
      → context.unbindService
      → convoyLog
      → convoyLogFile.appendText
      → delay
      → deleteTempTrack
      → finalizeTrack
      → getService
      → io.File

  [430] override fun onServiceConnected(name: android.content.ComponentName, service: android.os.IBinder) {
    WORK FIELDS (local computed values):
      [431] val svc = (service as ConvoyGpsService.LocalBinder).getService()
      [435] val prevLat = lastGpsLat
      [436] val prevLon = lastGpsLon
      [438] val seg = ConvoyEngine.LeadTrackSegment(
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
    CALLS OUT:
      → ConvoyEngine.LeadTrackSegment
      → ConvoyGpsService.start
      → Log.d
      → System.currentTimeMillis
      → bindGpsService
      → cancel
      → context.bindService
      → context.unbindService
      → convoyLog
      → convoyLogFile.appendText
      → delay
      → deleteTempTrack
      → finalizeTrack
      → getService
      → io.File

  [450] override fun onServiceDisconnected(name: android.content.ComponentName) { gpsService = null }
    WORK FIELDS (local computed values):
      [453] val intent = android.content.Intent(context, ConvoyGpsService::class.java)
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
    CALLS OUT:
      → ConvoyGpsService.start
      → Log.d
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → bindGpsService
      → cancel
      → context.bindService
      → context.unbindService
      → convoyLog
      → convoyLogFile.appendText
      → delay
      → deleteTempTrack
      → finalizeTrack

  [457] fun startRecording(context: android.content.Context) {
    WORK FIELDS (local computed values):
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
    CALLS OUT:
      → ConvoyGpsService.start
      → Log.d
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → bindGpsService
      → cancel
      → context.unbindService
      → convoyLog
      → convoyLogFile.appendText
      → delay
      → deleteTempTrack

  [461] fun pauseRecording() { gpsService?.pauseTrack(); _routeRecording.value = false }
    WORK FIELDS (local computed values):
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → Log.d
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → bindGpsService
      → cancel
      → context.unbindService
      → convoyLog
      → convoyLogFile.appendText
      → delay
      → deleteTempTrack

  [462] fun wakeFromSleep(context: android.content.Context) {
    WORK FIELDS (local computed values):
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → Log.d
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → bindGpsService
      → cancel
      → context.unbindService
      → convoyLog
      → convoyLogFile.appendText
      → delay
      → deleteTempTrack

  [466] fun resumeRecording(context: android.content.Context) {
    WORK FIELDS (local computed values):
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
      [543] val nowMs = System.currentTimeMillis()
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → ConvoySimulation.tick
      → Log.d
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → bindGpsService
      → cancel
      → context.unbindService
      → convoyLog
      → convoyLogFile.appendText
      → delay

  [471] fun stopRecording() {
    WORK FIELDS (local computed values):
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
      [543] val nowMs = System.currentTimeMillis()
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → ConvoySimulation.tick
      → Log.d
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → cancel
      → context.unbindService
      → convoyLog
      → convoyLogFile.appendText
      → delay
      → deleteTempTrack

  [475] fun deleteTempTrack() {
    WORK FIELDS (local computed values):
      [476] val temp = pendingTempFile ?: return
      [485] val temp = pendingTempFile ?: return
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
      [543] val nowMs = System.currentTimeMillis()
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → ConvoyEngine.compute
      → ConvoySimulation.tick
      → Log.d
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → cancel
      → context.unbindService
      → convoyLog
      → convoyLogFile.appendText
      → delay

  [484] fun finalizeTrack(name: String, context: android.content.Context) {
    WORK FIELDS (local computed values):
      [485] val temp = pendingTempFile ?: return
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
      [543] val nowMs = System.currentTimeMillis()
      [544] val nodes: List<ConvoyNode> = if (_simulationMode.value) {
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → ConvoyEngine.compute
      → ConvoySimulation.tick
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → cancel
      → context.unbindService
      → convoyLog
      → convoyLogFile.appendText
      → delay
      → finalizeTrack

  [498] private fun convoyLog(msg: String) {
    WORK FIELDS (local computed values):
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
      [543] val nowMs = System.currentTimeMillis()
      [544] val nodes: List<ConvoyNode> = if (_simulationMode.value) {
      [553] val tailNodeId: String? = null
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → ConvoyEngine.compute
      → ConvoySimulation.tick
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → cancel
      → convoyLogFile.appendText
      → delay
      → haversineMiles
      → id.takeLast
      → onRadioPosition

  [505] private fun startTick() {
    WORK FIELDS (local computed values):
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
      [543] val nowMs = System.currentTimeMillis()
      [544] val nodes: List<ConvoyNode> = if (_simulationMode.value) {
      [553] val tailNodeId: String? = null
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → ConvoyEngine.compute
      → ConvoySimulation.tick
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → cancel
      → convoyLog
      → delay
      → haversineMiles
      → id.takeLast
      → onRadioPosition

  [512] private fun pointToSegmentDistanceMiles(
    WORK FIELDS (local computed values):
      [515] val dx = bLon - aLon; val dy = bLat - aLat
      [517] val dlat = pLat - aLat; val dlon = pLon - aLon
      [520] val t = ((pLon - aLon) * dx + (pLat - aLat) * dy) / (dx * dx + dy * dy)
      [521] val clampedT = t.coerceIn(0.0, 1.0)
      [522] val nearLat = aLat + clampedT * dy; val nearLon = aLon + clampedT * dx
      [523] val dlat = pLat - nearLat; val dlon = pLon - nearLon
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
      [543] val nowMs = System.currentTimeMillis()
      [544] val nodes: List<ConvoyNode> = if (_simulationMode.value) {
      [553] val tailNodeId: String? = null
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → ConvoyEngine.compute
      → ConvoySimulation.tick
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → convoyLog
      → haversineMiles
      → id.takeLast
      → math.cos
      → math.sin
      → onRadioPosition

  [527] private fun haversineMiles(lat1: Double, lon1: Double, lat2: Double, lon2: Double): Float {
    WORK FIELDS (local computed values):
      [528] val R = 3958.8
      [529] val dLat = Math.toRadians(lat2 - lat1); val dLon = Math.toRadians(lon2 - lon1)
      [530] val a = Math.sin(dLat/2) * Math.sin(dLat/2) +
      [543] val nowMs = System.currentTimeMillis()
      [544] val nodes: List<ConvoyNode> = if (_simulationMode.value) {
      [553] val tailNodeId: String? = null
      [555] val state = ConvoyEngine.compute(
      [567] val myCart = state.nodes.firstOrNull { it.isMyCart }
      [574] val leadOut = state.lead?.callsign ?: "NONE"
      [575] val trackFrom = currentLeadNodeId ?: "NONE"
      [576] val accumStr = nodeDistanceAccum.entries.joinToString(" ") { (id, d) ->
      [577] val name = nodes.firstOrNull { it.nodeId == id }?.callsign ?: id.takeLast(4)
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → ConvoyEngine.compute
      → ConvoySimulation.tick
      → Math.atan2
      → Math.cos
      → Math.sin
      → Math.sqrt
      → Math.toRadians
      → System.currentTimeMillis
      → convoyLog
      → id.takeLast
      → math.cos
      → math.sin
      → onRadioPosition
      → readLiveNodes

  [536] private fun tick() { try {
    WORK FIELDS (local computed values):
      [543] val nowMs = System.currentTimeMillis()
      [544] val nodes: List<ConvoyNode> = if (_simulationMode.value) {
      [553] val tailNodeId: String? = null
      [555] val state = ConvoyEngine.compute(
      [567] val myCart = state.nodes.firstOrNull { it.isMyCart }
      [574] val leadOut = state.lead?.callsign ?: "NONE"
      [575] val trackFrom = currentLeadNodeId ?: "NONE"
      [576] val accumStr = nodeDistanceAccum.entries.joinToString(" ") { (id, d) ->
      [577] val name = nodes.firstOrNull { it.nodeId == id }?.callsign ?: id.takeLast(4)
      [589] val resolvedPositions = mutableMapOf<String, Pair<Double, Double>>()
      [590] val headingRad = Math.toRadians(state.convoyHeading.toDouble())
      [591] val hdx = kotlin.math.sin(headingRad)
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → ConvoyEngine.compute
      → ConvoySimulation.tick
      → Math.toRadians
      → System.currentTimeMillis
      → convoyLog
      → id.takeLast
      → math.cos
      → math.sin
      → onRadioPosition
      → readLiveNodes
      → resolveMyCartId
      → setLeadCart
      → startGroupTrack

  [783] private fun getPhoneLocation(): android.location.Location? {
    WORK FIELDS (local computed values):
      [785] val svcLat = gpsService?.lastLat
      [786] val svcLon = gpsService?.lastLon
      [788] val loc = android.location.Location("gps")
      [794] val live = livePhoneLocation
      [800] val lm = appContext.getSystemService(android.content.Context.LOCATION_SERVICE) as? android.location.LocationManager
      [806] val nodeMap = try { nodeRepository.nodeDBbyNum.value } catch (e: Exception) { emptyMap() }
      [815] val loc = getPhoneLocation()
      [816] val lat = loc?.latitude ?: 0.0
      [817] val lon = loc?.longitude ?: 0.0
      [818] val alt = ((loc?.altitude ?: 0.0) * 3.28084).toInt()
      [819] val spd = (loc?.speed ?: 0f) * 2.23694f
      [820] val hdg = loc?.bearing ?: 0f
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → Pair
      → appContext.getSystemService
      → convoyLog
      → getLastKnownLocation
      → getPhoneLocation
      → location.Location
      → readLiveNodes
      → startGroupTrack
      → startPhoneGps

  [805] private fun readLiveNodes(nowMs: Long): List<ConvoyNode> {
    WORK FIELDS (local computed values):
      [806] val nodeMap = try { nodeRepository.nodeDBbyNum.value } catch (e: Exception) { emptyMap() }
      [815] val loc = getPhoneLocation()
      [816] val lat = loc?.latitude ?: 0.0
      [817] val lon = loc?.longitude ?: 0.0
      [818] val alt = ((loc?.altitude ?: 0.0) * 3.28084).toInt()
      [819] val spd = (loc?.speed ?: 0f) * 2.23694f
      [820] val hdg = loc?.bearing ?: 0f
      [834] val allNodes = nodeMap.values.mapNotNull { node ->
      [835] val user = node.user
      [836] val pos = node.position
      [837] val callsign = user.long_name.ifBlank { user.short_name }.ifBlank { "!${node.num}" }
      [838] val nodeId = "!%08x".format(node.num)
    CALLS OUT:
      → ContextCompat.checkSelfPermission
      → Pair
      → System.currentTimeMillis
      → convoyLog
      → getPhoneLocation
      → startGroupTrack
      → startPhoneGps

  [952] fun setPendingDownload(pending: PendingDownload) { _pendingDownload.value = pending }
    WORK FIELDS (local computed values):
      [959] val entry = DownloadQueueManager.enqueue(
      [971] val entry = entries.find { it.id == entryId } ?: return@collect
      [978] val avgBytes = 15_360L
      [979] val mb = (entry.downloadedTiles * avgBytes) / (1024f * 1024f)
    CALLS OUT:
      → DownloadQueueManager.cancel
      → DownloadQueueManager.enqueue
      → DownloadState.Complete
      → DownloadState.Downloading
      → DownloadState.Error
      → DownloadSummary
      → System.currentTimeMillis
      → cancel
      → cancelDownload
      → clearPendingDownload
      → coroutines.delay
      → observeQueueEntry
      → onCleared
      → startDownload
      → super.onCleared

  [953] fun clearPendingDownload() { _pendingDownload.value = null }
    WORK FIELDS (local computed values):
      [959] val entry = DownloadQueueManager.enqueue(
      [971] val entry = entries.find { it.id == entryId } ?: return@collect
      [978] val avgBytes = 15_360L
      [979] val mb = (entry.downloadedTiles * avgBytes) / (1024f * 1024f)
    CALLS OUT:
      → DownloadQueueManager.cancel
      → DownloadQueueManager.enqueue
      → DownloadState.Complete
      → DownloadState.Downloading
      → DownloadState.Error
      → DownloadSummary
      → System.currentTimeMillis
      → cancel
      → cancelDownload
      → clearPendingDownload
      → coroutines.delay
      → observeQueueEntry
      → onCleared
      → startDownload
      → super.onCleared

  [955] fun startDownload(context: android.content.Context, pending: PendingDownload) {
    WORK FIELDS (local computed values):
      [959] val entry = DownloadQueueManager.enqueue(
      [971] val entry = entries.find { it.id == entryId } ?: return@collect
      [978] val avgBytes = 15_360L
      [979] val mb = (entry.downloadedTiles * avgBytes) / (1024f * 1024f)
    CALLS OUT:
      → DownloadQueueManager.cancel
      → DownloadQueueManager.enqueue
      → DownloadState.Complete
      → DownloadState.Downloading
      → DownloadState.Error
      → DownloadSummary
      → System.currentTimeMillis
      → cancel
      → cancelDownload
      → clearPendingDownload
      → coroutines.delay
      → observeQueueEntry
      → onCleared
      → super.onCleared

  [968] private fun observeQueueEntry(entryId: String) {
    WORK FIELDS (local computed values):
      [971] val entry = entries.find { it.id == entryId } ?: return@collect
      [978] val avgBytes = 15_360L
      [979] val mb = (entry.downloadedTiles * avgBytes) / (1024f * 1024f)
    CALLS OUT:
      → DownloadQueueManager.cancel
      → DownloadState.Complete
      → DownloadState.Downloading
      → DownloadState.Error
      → DownloadSummary
      → cancel
      → cancelDownload
      → coroutines.delay
      → onCleared
      → super.onCleared

  [992] fun cancelDownload() {
    CALLS OUT:
      → DownloadQueueManager.cancel
      → cancel
      → coroutines.delay
      → onCleared
      → super.onCleared

  [1001] override fun onCleared() {
    CALLS OUT:
      → cancel
      → super.onCleared

========================================
FILE: ConvoyWaypointCreateDialog.kt
========================================

  [19] fun ConvoyWaypointCreateDialog(
    CALLS OUT:
      → Arrangement.spacedBy
      → Color
      → Column
      → RoundedCornerShape
      → Surface
      → Text

========================================
FILE: GroupTrackTheme.kt
========================================

  [63] fun GroupTrackLogoText(
    WORK FIELDS (local computed values):
      [90] val context = androidx.compose.ui.platform.LocalContext.current
      [91] var showDevMenu by androidx.compose.runtime.remember { androidx.compose.runtime.mutableStateOf(false) }
    CALLS OUT:
      → Arrangement.spacedBy
      → Column
      → HorizontalDivider
      → OptIn
      → Spacer
      → append
      → background
      → clickable
      → fillMaxWidth
      → foundation.Image
      → padding
      → res.painterResource
      → runtime.mutableStateOf
      → subtitle.isNotEmpty
      → subtitle.uppercase

  [86] fun GroupTrackHeader(
    WORK FIELDS (local computed values):
      [90] val context = androidx.compose.ui.platform.LocalContext.current
      [91] var showDevMenu by androidx.compose.runtime.remember { androidx.compose.runtime.mutableStateOf(false) }
      [163] val uid = ConvoySessionManager.getUserId(context) ?: "none"
      [164] val zip = ConvoySessionManager.getZipCode(context)
      [165] val sub = ConvoySessionManager.isSubscribed(context)
      [166] val org = ConvoySessionManager.isOrganizer(context)
    CALLS OUT:
      → Arrangement.spacedBy
      → Column
      → ConvoyDevSeeder.clear
      → ConvoyDevSeeder.seed
      → ConvoySessionManager.clearSession
      → ConvoySessionManager.getUserId
      → ConvoySessionManager.getZipCode
      → ConvoySessionManager.isOrganizer
      → ConvoySessionManager.isSubscribed
      → DevMenuButton
      → HorizontalDivider
      → Spacer
      → Toast.makeText
      → background
      → clickable

  [181] private fun DevMenuButton(text: String, color: androidx.compose.ui.graphics.Color = GroupTrackColors.SkyBlue, onClick: () -> Unit) {
    WORK FIELDS (local computed values):
      [256] var showRidesMenu   by remember { mutableStateOf(false) }
      [257] var showRadioMenu   by remember { mutableStateOf(false) }
      [258] var showProfileMenu by remember { mutableStateOf(false) }
      [259] var showMapMenu     by remember { mutableStateOf(false) }
    CALLS OUT:
      → ButtonDefaults.buttonColors
      → GroupTrackTagline
      → Text
      → background
      → closeAll
      → material3.Button
      → mutableStateOf
      → onClick
      → padding

  [195] fun GroupTrackButton(
    WORK FIELDS (local computed values):
      [256] var showRidesMenu   by remember { mutableStateOf(false) }
      [257] var showRadioMenu   by remember { mutableStateOf(false) }
      [258] var showProfileMenu by remember { mutableStateOf(false) }
      [259] var showMapMenu     by remember { mutableStateOf(false) }
    CALLS OUT:
      → ButtonDefaults.buttonColors
      → GroupTrackTagline
      → background
      → closeAll
      → fillMaxWidth
      → layout.Column
      → layout.Row
      → material3.Button
      → mutableStateOf
      → navigationBarsPadding
      → onHome
      → padding

  [224] fun GroupTrackTagline(modifier: Modifier = Modifier) {
    WORK FIELDS (local computed values):
      [256] var showRidesMenu   by remember { mutableStateOf(false) }
      [257] var showRadioMenu   by remember { mutableStateOf(false) }
      [258] var showProfileMenu by remember { mutableStateOf(false) }
      [259] var showMapMenu     by remember { mutableStateOf(false) }
    CALLS OUT:
      → GtMenuItem
      → GtMenuLabel
      → background
      → closeAll
      → fillMaxWidth
      → layout.Column
      → layout.Row
      → material3.DropdownMenu
      → mutableStateOf
      → navigationBarsPadding
      → onCreateRide
      → onHome
      → onNavigateToCompletedRides
      → onNavigateToSearchByArea
      → padding

  [240] fun GroupTrackBottomNav(
    WORK FIELDS (local computed values):
      [256] var showRidesMenu   by remember { mutableStateOf(false) }
      [257] var showRadioMenu   by remember { mutableStateOf(false) }
      [258] var showProfileMenu by remember { mutableStateOf(false) }
      [259] var showMapMenu     by remember { mutableStateOf(false) }
    CALLS OUT:
      → GtMenuItem
      → GtMenuLabel
      → background
      → closeAll
      → fillMaxWidth
      → layout.Column
      → layout.Row
      → material3.DropdownMenu
      → mutableStateOf
      → navigationBarsPadding
      → onCreateRide
      → onHome
      → onNavigateToCompletedRides
      → onNavigateToSearchByArea
      → padding

  [260] fun closeAll() { showRidesMenu = false; showRadioMenu = false; showProfileMenu = false; showMapMenu = false }
    CALLS OUT:
      → GtMenuItem
      → GtMenuLabel
      → background
      → closeAll
      → fillMaxWidth
      → layout.Column
      → layout.Row
      → material3.DropdownMenu
      → navigationBarsPadding
      → onCreateRide
      → onHome
      → onMyOrganizers
      → onMyProfile
      → onNavigateToCompletedRides
      → onNavigateToSearchByArea

  [364] private fun GtNavBtn(icon: String, label: String, isActive: Boolean, onClick: () -> Unit) {
    CALLS OUT:
      → GtMenuItem
      → GtMenuLabel
      → MenuDefaults.itemColors
      → background
      → graphics.Color
      → layout.Box
      → layout.Column
      → layout.Row
      → material3.DropdownMenuItem
      → material3.HorizontalDivider
      → material3.Text
      → onClick
      → padding
      → shape.RoundedCornerShape

  [383] private fun GtMenuLabel(title: String) {
    CALLS OUT:
      → GtMenuItem
      → MenuDefaults.itemColors
      → background
      → graphics.Color
      → layout.Box
      → layout.Row
      → material3.DropdownMenuItem
      → material3.HorizontalDivider
      → material3.Text
      → onClick
      → padding
      → shape.RoundedCornerShape

  [410] private fun GtMenuItem(label: String, active: Boolean, onClick: () -> Unit) {
    CALLS OUT:
      → MenuDefaults.itemColors
      → background
      → graphics.Color
      → layout.Box
      → layout.Row
      → material3.DropdownMenuItem
      → material3.Text
      → onClick
      → padding
      → shape.RoundedCornerShape

========================================
FILE: MapSourceManager.kt
========================================

  [58] private fun externalDir(): File {
    WORK FIELDS (local computed values):
      [59] val dir = File(android.os.Environment.getExternalStoragePublicDirectory(
      [76] val json = context.assets.open("map_sources.json")
      [79] val root = JSONObject(json)
      [80] val sourcesArray = root.getJSONArray("sources")
      [81] val parsed = mutableListOf<TileSource>()
      [83] val s = sourcesArray.getJSONObject(i)
      [84] val layersArray = s.getJSONArray("layers")
      [85] val layers = mutableListOf<TileLayer>()
      [87] val l = layersArray.getJSONObject(j)
      [108] val slotsArray = root.getJSONArray("default_slots")
      [109] val slots = mutableListOf<SlotConfig>()
      [111] val sl = slotsArray.getJSONObject(i)
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → File
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → TileLayer
      → apiKeysFile
      → assets.open
      → bufferedReader
      → dir.exists
      → dir.mkdirs
      → externalJsonFile
      → init
      → it.readText

  [66] private fun externalJsonFile(): File = File(externalDir(), "map_sources.json")
    WORK FIELDS (local computed values):
      [76] val json = context.assets.open("map_sources.json")
      [79] val root = JSONObject(json)
      [80] val sourcesArray = root.getJSONArray("sources")
      [81] val parsed = mutableListOf<TileSource>()
      [83] val s = sourcesArray.getJSONObject(i)
      [84] val layersArray = s.getJSONArray("layers")
      [85] val layers = mutableListOf<TileLayer>()
      [87] val l = layersArray.getJSONObject(j)
      [108] val slotsArray = root.getJSONArray("default_slots")
      [109] val slots = mutableListOf<SlotConfig>()
      [111] val sl = slotsArray.getJSONObject(i)
    CALLS OUT:
      → File
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → SlotConfig
      → TileLayer
      → apiKeysFile
      → assets.open
      → bufferedReader
      → init
      → it.readText
      → layers.add
      → layersArray.getJSONObject
      → layersArray.length

  [67] private fun apiKeysFile(): File = File(externalDir(), "api_keys.json")
    WORK FIELDS (local computed values):
      [76] val json = context.assets.open("map_sources.json")
      [79] val root = JSONObject(json)
      [80] val sourcesArray = root.getJSONArray("sources")
      [81] val parsed = mutableListOf<TileSource>()
      [83] val s = sourcesArray.getJSONObject(i)
      [84] val layersArray = s.getJSONArray("layers")
      [85] val layers = mutableListOf<TileLayer>()
      [87] val l = layersArray.getJSONObject(j)
      [108] val slotsArray = root.getJSONArray("default_slots")
      [109] val slots = mutableListOf<SlotConfig>()
      [111] val sl = slotsArray.getJSONObject(i)
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → SlotConfig
      → TileLayer
      → assets.open
      → bufferedReader
      → init
      → it.readText
      → layers.add
      → layersArray.getJSONObject
      → layersArray.length
      → listOf
      → loadApiKeys

  [71] fun init(context: Context) {
    WORK FIELDS (local computed values):
      [76] val json = context.assets.open("map_sources.json")
      [79] val root = JSONObject(json)
      [80] val sourcesArray = root.getJSONArray("sources")
      [81] val parsed = mutableListOf<TileSource>()
      [83] val s = sourcesArray.getJSONObject(i)
      [84] val layersArray = s.getJSONArray("layers")
      [85] val layers = mutableListOf<TileLayer>()
      [87] val l = layersArray.getJSONObject(j)
      [108] val slotsArray = root.getJSONArray("default_slots")
      [109] val slots = mutableListOf<SlotConfig>()
      [111] val sl = slotsArray.getJSONObject(i)
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → SlotConfig
      → TileLayer
      → assets.open
      → bufferedReader
      → getSlotSources
      → it.readText
      → layers.add
      → layersArray.getJSONObject
      → layersArray.length
      → listOf
      → loadApiKeys

  [128] private fun loadFallback() {
    WORK FIELDS (local computed values):
      [172] val slot = defaultSlots.find { it.legacyKey == legacyKey }
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
    CALLS OUT:
      → Log.w
      → Pair
      → SlotConfig
      → TileLayer
      → Triple
      → ensureInit
      → getActiveLocalUrl
      → getActiveOnlineUrl
      → getAllOverlayLayers
      → getDownloadSources
      → getLocalUrl
      → getOnlineUrl
      → getOverlayJson
      → getOverlayLayers
      → getSlotSources

  [151] fun getSlotSources(): List<Triple<String, String, String>> {
    WORK FIELDS (local computed values):
      [172] val slot = defaultSlots.find { it.legacyKey == legacyKey }
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
    CALLS OUT:
      → Log.i
      → Pair
      → Triple
      → ensureInit
      → getActiveLocalUrl
      → getActiveOnlineUrl
      → getAllOverlayLayers
      → getDownloadSources
      → getLocalUrl
      → getOnlineUrl
      → getOverlayJson
      → getOverlayLayers
      → getSourceByKey
      → layers.isEmpty
      → loadApiKeys

  [161] fun getDownloadSources(): List<Pair<String, List<TileLayer>>> {
    WORK FIELDS (local computed values):
      [172] val slot = defaultSlots.find { it.legacyKey == legacyKey }
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → Pair
      → apiKeysFile
      → ensureInit
      → file.exists
      → getActiveLocalUrl
      → getActiveOnlineUrl
      → getAllOverlayLayers
      → getLocalUrl
      → getOnlineUrl
      → getOverlayJson
      → getOverlayLayers
      → getSourceByKey

  [170] fun getSourceByKey(legacyKey: String): TileSource? {
    WORK FIELDS (local computed values):
      [172] val slot = defaultSlots.find { it.legacyKey == legacyKey }
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → apiKeysFile
      → ensureInit
      → file.exists
      → getActiveLocalUrl
      → getActiveOnlineUrl
      → getAllOverlayLayers
      → getLocalUrl
      → getOnlineUrl
      → getOverlayJson
      → getOverlayLayers
      → getSourceByKey
      → json.keys

  [176] fun getOnlineUrl(legacyKey: String): String =
    WORK FIELDS (local computed values):
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → apiKeysFile
      → ensureInit
      → file.exists
      → getActiveLocalUrl
      → getActiveOnlineUrl
      → getAllOverlayLayers
      → getApiKey
      → getLocalUrl
      → getOnlineUrl
      → getOverlayJson
      → getOverlayLayers
      → getSourceByKey

  [179] fun getLocalUrl(legacyKey: String): String =
    WORK FIELDS (local computed values):
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → apiKeysFile
      → ensureInit
      → file.exists
      → getActiveLocalUrl
      → getActiveOnlineUrl
      → getAllOverlayLayers
      → getApiKey
      → getLocalUrl
      → getOnlineUrl
      → getOverlayJson
      → getOverlayLayers
      → getSourceByKey

  [182] fun getOverlayLayers(legacyKey: String): List<TileLayer> =
    WORK FIELDS (local computed values):
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
      [260] val source = sources.find { it.id == sourceId } ?: return false
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → apiKeys.containsKey
      → apiKeysFile
      → ensureInit
      → file.exists
      → getActiveLocalUrl
      → getActiveOnlineUrl
      → getAllOverlayLayers
      → getApiKey
      → getLocalUrl
      → getOnlineUrl
      → getOverlayJson
      → getOverlayLayers

  [185] fun getAllOverlayLayers(): List<TileLayer> {
    WORK FIELDS (local computed values):
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
      [260] val source = sources.find { it.id == sourceId } ?: return false
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → apiKeys.containsKey
      → apiKeysFile
      → ensureInit
      → file.exists
      → getActiveLocalUrl
      → getActiveOnlineUrl
      → getAllSources
      → getApiKey
      → getLocalUrl
      → getOnlineUrl
      → getOverlayJson
      → getOverlayLayers

  [192] fun setActive(legacyKey: String) {
    WORK FIELDS (local computed values):
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
      [260] val source = sources.find { it.id == sourceId } ?: return false
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → apiKeys.containsKey
      → apiKeysFile
      → ensureInit
      → file.exists
      → getActiveLocalUrl
      → getActiveOnlineUrl
      → getAllSources
      → getApiKey
      → getLocalUrl
      → getOnlineUrl
      → getOverlayJson
      → getOverlayLayers

  [196] fun getActiveOnlineUrl(): String = getOnlineUrl(activeSourceKey)
    WORK FIELDS (local computed values):
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
      [260] val source = sources.find { it.id == sourceId } ?: return false
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → apiKeys.containsKey
      → apiKeysFile
      → ensureInit
      → file.exists
      → getActiveLocalUrl
      → getAllSources
      → getApiKey
      → getLocalUrl
      → getOverlayJson
      → getOverlayLayers
      → getSlotAssignments
      → isSourceAvailable

  [197] fun getActiveLocalUrl(): String = getLocalUrl(activeSourceKey)
    WORK FIELDS (local computed values):
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
      [260] val source = sources.find { it.id == sourceId } ?: return false
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → apiKeys.containsKey
      → apiKeysFile
      → ensureInit
      → file.exists
      → getAllSources
      → getApiKey
      → getOverlayJson
      → getOverlayLayers
      → getSlotAssignments
      → isSourceAvailable
      → json.keys
      → json.put

  [200] fun getOverlayJson(legacyKey: String): String {
    WORK FIELDS (local computed values):
      [201] val layers = getOverlayLayers(legacyKey)
      [203] val sb = StringBuilder("[")
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
      [260] val source = sources.find { it.id == sourceId } ?: return false
      [278] val key = apiKeys[sourceId] ?: ""
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → apiKeys.containsKey
      → apiKeysFile
      → ensureInit
      → file.exists
      → getAllSources
      → getApiKey
      → getOverlayLayers
      → getSlotAssignments
      → isSourceAvailable
      → json.keys
      → json.put
      → layers.isEmpty

  [213] fun updateSlotSource(legacyKey: String, newSourceId: String) {
    WORK FIELDS (local computed values):
      [215] val slotIndex = defaultSlots.indexOfFirst { it.legacyKey == legacyKey }
      [217] val oldSlot = defaultSlots[slotIndex]
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
      [260] val source = sources.find { it.id == sourceId } ?: return false
      [278] val key = apiKeys[sourceId] ?: ""
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → apiKeys.containsKey
      → apiKeysFile
      → ensureInit
      → file.exists
      → getAllSources
      → getApiKey
      → getSlotAssignments
      → isSourceAvailable
      → json.keys
      → json.put
      → loadApiKeys

  [226] private fun saveExternalJson() {
    WORK FIELDS (local computed values):
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
      [260] val source = sources.find { it.id == sourceId } ?: return false
      [278] val key = apiKeys[sourceId] ?: ""
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → apiKeys.containsKey
      → apiKeysFile
      → ensureInit
      → file.exists
      → getAllSources
      → getApiKey
      → getSlotAssignments
      → isSourceAvailable
      → json.keys
      → json.put
      → loadApiKeys

  [231] private fun loadApiKeys() {
    WORK FIELDS (local computed values):
      [233] val file = apiKeysFile()
      [235] val json = JSONObject(file.readText(Charsets.UTF_8))
      [246] val json = JSONObject()
      [260] val source = sources.find { it.id == sourceId } ?: return false
      [278] val key = apiKeys[sourceId] ?: ""
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → apiKeys.containsKey
      → apiKeysFile
      → ensureInit
      → file.exists
      → getAllSources
      → getApiKey
      → getSlotAssignments
      → isSourceAvailable
      → json.keys
      → json.put
      → loadFallback

  [243] fun saveApiKey(sourceId: String, key: String) {
    WORK FIELDS (local computed values):
      [246] val json = JSONObject()
      [260] val source = sources.find { it.id == sourceId } ?: return false
      [278] val key = apiKeys[sourceId] ?: ""
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → apiKeys.containsKey
      → apiKeysFile
      → ensureInit
      → getAllSources
      → getApiKey
      → getSlotAssignments
      → isSourceAvailable
      → json.put
      → loadFallback
      → resolveUrl
      → urlTemplate.replace

  [256] fun getApiKey(sourceId: String): String = apiKeys[sourceId] ?: ""
    WORK FIELDS (local computed values):
      [260] val source = sources.find { it.id == sourceId } ?: return false
      [278] val key = apiKeys[sourceId] ?: ""
    CALLS OUT:
      → Log.w
      → apiKeys.containsKey
      → ensureInit
      → getAllSources
      → getSlotAssignments
      → isSourceAvailable
      → loadFallback
      → resolveUrl
      → urlTemplate.replace

  [259] fun isSourceAvailable(sourceId: String): Boolean {
    WORK FIELDS (local computed values):
      [260] val source = sources.find { it.id == sourceId } ?: return false
      [278] val key = apiKeys[sourceId] ?: ""
    CALLS OUT:
      → Log.w
      → apiKeys.containsKey
      → ensureInit
      → getAllSources
      → getSlotAssignments
      → loadFallback
      → resolveUrl
      → urlTemplate.replace

  [265] fun getAllSources(): List<TileSource> {
    WORK FIELDS (local computed values):
      [278] val key = apiKeys[sourceId] ?: ""
    CALLS OUT:
      → Log.w
      → ensureInit
      → getSlotAssignments
      → loadFallback
      → resolveUrl
      → urlTemplate.replace

  [271] fun getSlotAssignments(): List<SlotConfig> {
    WORK FIELDS (local computed values):
      [278] val key = apiKeys[sourceId] ?: ""
    CALLS OUT:
      → Log.w
      → ensureInit
      → loadFallback
      → resolveUrl
      → urlTemplate.replace

  [277] fun resolveUrl(urlTemplate: String, sourceId: String): String {
    WORK FIELDS (local computed values):
      [278] val key = apiKeys[sourceId] ?: ""
    CALLS OUT:
      → Log.w
      → ensureInit
      → loadFallback
      → urlTemplate.replace

  [282] private fun ensureInit() {
    CALLS OUT:
      → Log.w
      → loadFallback

========================================
FILE: RouteManager.kt
========================================

  [10] fun stub() { /* Pass 1 scaffold -- functions in Pass 2 */ }

========================================
FILE: SpatialDbManager.kt
========================================

  [43] private fun dbDir(): File {
    WORK FIELDS (local computed values):
      [44] val dir = File(
      [49] val noMedia = File(dir, ".nomedia")
      [60] val dir = dbDir()
      [63] val spatialFile = File(dir, SPATIAL_DB)
      [122] val extFile = File(dir, EXTENSION_DB)
      [135] val tileDir = File(dir, "maps/tiles")
      [137] val tileMapsNoMedia = File(dir, "maps/.nomedia")
      [141] val tileNoMedia = File(File(dir, "maps/tiles"), ".nomedia")
      [162] val trailCount = countRows(spatialDb!!, "trails")
      [163] val trackCount = countRows(spatialDb!!, "tracks")
      [164] val wptCount = countRows(spatialDb!!, "waypoints")
      [165] val routeCount = countRows(spatialDb!!, "routes")
    CALLS OUT:
      → COUNT
      → Environment.getExternalStoragePublicDirectory
      → File
      → IllegalStateException
      → Instant.now
      → LINESTRING
      → Log.e
      → Log.i
      → Log.w
      → MAX
      → MIN
      → MULTILINESTRING
      → POINT
      → Pair
      → Regex

  [57] fun init(context: Context) {
    WORK FIELDS (local computed values):
      [60] val dir = dbDir()
      [63] val spatialFile = File(dir, SPATIAL_DB)
      [122] val extFile = File(dir, EXTENSION_DB)
      [135] val tileDir = File(dir, "maps/tiles")
      [137] val tileMapsNoMedia = File(dir, "maps/.nomedia")
    CALLS OUT:
      → File
      → Log.i
      → Log.w
      → SQLiteDatabase.openOrCreateDatabase
      → dbDir
      → execSQL
      → hasTable
      → it.moveToFirst
      → rawQuery
      → runSchemaFromAsset
      → tileDir.exists

  [173] private fun hasTable(db: SQLiteDatabase, tableName: String): Boolean {
    WORK FIELDS (local computed values):
      [175] val cursor = db.rawQuery(
      [179] val exists = cursor.moveToFirst()
      [189] val raw = context.assets.open(assetName).bufferedReader().use { it.readText() }
      [191] val sql = raw.lines()
      [223] val db = spatialDb ?: return emptyList()
      [224] val results = mutableListOf<Map<String, String?>>()
      [225] val cursor = db.rawQuery(
      [230] val wkt = cursor.getString(2)
      [245] val inner: String
      [246] val isMulti: Boolean
    CALLS OUT:
      → LINESTRING
      → Log.w
      → MULTILINESTRING
      → arrayOf
      → assets.open
      → bufferedReader
      → cursor.close
      → cursor.moveToFirst
      → cursor.moveToNext
      → db.beginTransaction
      → db.endTransaction
      → db.execSQL
      → db.rawQuery
      → db.setTransactionSuccessful
      → getExtensionDb

  [188] private fun runSchemaFromAsset(context: Context, db: SQLiteDatabase, assetName: String) {
    WORK FIELDS (local computed values):
      [189] val raw = context.assets.open(assetName).bufferedReader().use { it.readText() }
      [191] val sql = raw.lines()
      [223] val db = spatialDb ?: return emptyList()
      [224] val results = mutableListOf<Map<String, String?>>()
      [225] val cursor = db.rawQuery(
      [230] val wkt = cursor.getString(2)
      [245] val inner: String
      [246] val isMulti: Boolean
      [255] val coords = inner.split(",").map { it.trim().split(" ") }
      [258] val lines = inner.split("),(").map { it.trim('(', ')') }
      [259] val jsonLines = lines.map { line ->
      [260] val coords = line.split(",").map { it.trim().split(" ") }
    CALLS OUT:
      → LINESTRING
      → Log.w
      → MULTILINESTRING
      → arrayOf
      → assets.open
      → bufferedReader
      → buildTrailGeoJson
      → cursor.close
      → cursor.moveToNext
      → db.beginTransaction
      → db.endTransaction
      → db.execSQL
      → db.rawQuery
      → db.setTransactionSuccessful
      → getExtensionDb

  [213] fun getSpatialDb(): SQLiteDatabase? = spatialDb
    WORK FIELDS (local computed values):
      [223] val db = spatialDb ?: return emptyList()
      [224] val results = mutableListOf<Map<String, String?>>()
      [225] val cursor = db.rawQuery(
      [230] val wkt = cursor.getString(2)
      [245] val inner: String
      [246] val isMulti: Boolean
      [255] val coords = inner.split(",").map { it.trim().split(" ") }
      [258] val lines = inner.split("),(").map { it.trim('(', ')') }
      [259] val jsonLines = lines.map { line ->
      [260] val coords = line.split(",").map { it.trim().split(" ") }
      [269] val sb = StringBuilder()
      [271] var first = true
    CALLS OUT:
      → LINESTRING
      → MULTILINESTRING
      → arrayOf
      → buildTrailGeoJson
      → cursor.close
      → cursor.moveToNext
      → db.rawQuery
      → getExtensionDb
      → inner.split
      → it.trim
      → line.split
      → queryTrailsByViewport
      → removeSuffix
      → replace
      → results.add

  [216] fun getExtensionDb(): SQLiteDatabase? = extensionDb
    WORK FIELDS (local computed values):
      [223] val db = spatialDb ?: return emptyList()
      [224] val results = mutableListOf<Map<String, String?>>()
      [225] val cursor = db.rawQuery(
      [230] val wkt = cursor.getString(2)
      [245] val inner: String
      [246] val isMulti: Boolean
      [255] val coords = inner.split(",").map { it.trim().split(" ") }
      [258] val lines = inner.split("),(").map { it.trim('(', ')') }
      [259] val jsonLines = lines.map { line ->
      [260] val coords = line.split(",").map { it.trim().split(" ") }
      [269] val sb = StringBuilder()
      [271] var first = true
    CALLS OUT:
      → LINESTRING
      → MULTILINESTRING
      → arrayOf
      → buildTrailGeoJson
      → cursor.close
      → cursor.moveToNext
      → db.rawQuery
      → inner.split
      → it.trim
      → line.split
      → queryTrailsByViewport
      → removeSuffix
      → replace
      → results.add
      → s

  [222] fun queryTrailsByViewport(south: Double, west: Double, north: Double, east: Double, limit: Int = 500): List<Map<String, String?>> {
    WORK FIELDS (local computed values):
      [223] val db = spatialDb ?: return emptyList()
      [224] val results = mutableListOf<Map<String, String?>>()
      [225] val cursor = db.rawQuery(
      [230] val wkt = cursor.getString(2)
      [245] val inner: String
      [246] val isMulti: Boolean
      [255] val coords = inner.split(",").map { it.trim().split(" ") }
      [258] val lines = inner.split("),(").map { it.trim('(', ')') }
      [259] val jsonLines = lines.map { line ->
      [260] val coords = line.split(",").map { it.trim().split(" ") }
      [269] val sb = StringBuilder()
      [271] var first = true
    CALLS OUT:
      → LINESTRING
      → MULTILINESTRING
      → arrayOf
      → buildTrailGeoJson
      → cursor.close
      → cursor.moveToNext
      → db.rawQuery
      → inner.split
      → it.trim
      → line.split
      → queryTracksByViewport
      → removeSuffix
      → replace
      → results.add
      → s

  [244] fun wktToGeoJsonCoords(wkt: String): String {
    WORK FIELDS (local computed values):
      [245] val inner: String
      [246] val isMulti: Boolean
      [255] val coords = inner.split(",").map { it.trim().split(" ") }
      [258] val lines = inner.split("),(").map { it.trim('(', ')') }
      [259] val jsonLines = lines.map { line ->
      [260] val coords = line.split(",").map { it.trim().split(" ") }
      [269] val sb = StringBuilder()
      [271] var first = true
      [273] val wkt = trail["geometry"] ?: continue
      [274] val coords = wktToGeoJsonCoords(wkt)
      [278] val geoType = if (wkt.startsWith("MULTI")) "MultiLineString" else "LineString"
      [300] val db = spatialDb ?: return emptyList()
    CALLS OUT:
      → LINESTRING
      → Log.e
      → Log.i
      → MULTILINESTRING
      → arrayOf
      → buildTrailGeoJson
      → db.rawQuery
      → inner.split
      → it.moveToNext
      → it.trim
      → line.split
      → queryTracksByViewport
      → removeSuffix
      → replace
      → results.add

  [268] fun buildTrailGeoJson(trails: List<Map<String, String?>>): String {
    WORK FIELDS (local computed values):
      [269] val sb = StringBuilder()
      [271] var first = true
      [273] val wkt = trail["geometry"] ?: continue
      [274] val coords = wktToGeoJsonCoords(wkt)
      [278] val geoType = if (wkt.startsWith("MULTI")) "MultiLineString" else "LineString"
      [300] val db = spatialDb ?: return emptyList()
      [301] val results = mutableListOf<Map<String, String?>>()
      [303] val cursor = db.rawQuery(
      [309] val wkt = it.getString(2) ?: continue
      [326] val sb = StringBuilder()
      [330] val geom = track["geometry"] ?: return@forEachIndexed
      [331] val name = (track["name"] ?: "Unnamed Track").replace("\"", "\\\"")
    CALLS OUT:
      → LINESTRING
      → Log.e
      → Log.i
      → arrayOf
      → buildTrackGeoJson
      → coordStr.split
      → db.rawQuery
      → geom.removePrefix
      → io.File
      → it.moveToNext
      → pair.trim
      → queryTracksByViewport
      → removeSuffix
      → replace
      → results.add

  [279] fun s(k: String): String = (trail[k] ?: "").replace("\\", "\\\\").replace("\"", "\\\\\"")
    WORK FIELDS (local computed values):
      [300] val db = spatialDb ?: return emptyList()
      [301] val results = mutableListOf<Map<String, String?>>()
      [303] val cursor = db.rawQuery(
      [309] val wkt = it.getString(2) ?: continue
      [326] val sb = StringBuilder()
      [330] val geom = track["geometry"] ?: return@forEachIndexed
      [331] val name = (track["name"] ?: "Unnamed Track").replace("\"", "\\\"")
      [332] val coordStr = geom.removePrefix("LINESTRING(").removeSuffix(")")
      [333] val coords = coordStr.split(",").joinToString(",") { pair ->
      [334] val parts = pair.trim().split(" ")
      [337] val cc = (track["carto_code"] ?: "").replace("\"", "\\\"")
      [346] val db = spatialDb ?: return
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → LINESTRING
      → Log.e
      → Log.i
      → arrayOf
      → buildTrackGeoJson
      → coordStr.split
      → db.rawQuery
      → endsWith
      → geom.removePrefix
      → io.File
      → it.moveToNext
      → name.lowercase
      → pair.trim
      → queryTracksByViewport

  [299] fun queryTracksByViewport(south: Double, west: Double, north: Double, east: Double, limit: Int = 500): List<Map<String, String?>> {
    WORK FIELDS (local computed values):
      [300] val db = spatialDb ?: return emptyList()
      [301] val results = mutableListOf<Map<String, String?>>()
      [303] val cursor = db.rawQuery(
      [309] val wkt = it.getString(2) ?: continue
      [326] val sb = StringBuilder()
      [330] val geom = track["geometry"] ?: return@forEachIndexed
      [331] val name = (track["name"] ?: "Unnamed Track").replace("\"", "\\\"")
      [332] val coordStr = geom.removePrefix("LINESTRING(").removeSuffix(")")
      [333] val coords = coordStr.split(",").joinToString(",") { pair ->
      [334] val parts = pair.trim().split(" ")
      [337] val cc = (track["carto_code"] ?: "").replace("\"", "\\\"")
      [346] val db = spatialDb ?: return
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → LINESTRING
      → Log.e
      → Log.i
      → arrayOf
      → buildTrackGeoJson
      → coordStr.split
      → coords.isEmpty
      → db.rawQuery
      → endsWith
      → existing.add
      → existing.contains
      → file.readText
      → geom.removePrefix
      → io.File

  [325] fun buildTrackGeoJson(tracks: List<Map<String, String?>>): String {
    WORK FIELDS (local computed values):
      [326] val sb = StringBuilder()
      [330] val geom = track["geometry"] ?: return@forEachIndexed
      [331] val name = (track["name"] ?: "Unnamed Track").replace("\"", "\\\"")
      [332] val coordStr = geom.removePrefix("LINESTRING(").removeSuffix(")")
      [333] val coords = coordStr.split(",").joinToString(",") { pair ->
      [334] val parts = pair.trim().split(" ")
      [337] val cc = (track["carto_code"] ?: "").replace("\"", "\\\"")
      [346] val db = spatialDb ?: return
      [348] val tracksDir = java.io.File(
      [356] val gpxFiles = tracksDir.listFiles { f -> f.name.lowercase().endsWith(".gpx") } ?: return
      [359] val existing = mutableSetOf<String>()
      [360] val cursor = db.rawQuery("SELECT name FROM tracks", null)
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → LINESTRING
      → Log.e
      → Log.i
      → Regex
      → UUID.randomUUID
      → arrayOf
      → coordStr.split
      → coords.isEmpty
      → db.execSQL
      → db.rawQuery
      → endsWith
      → existing.add
      → existing.contains
      → file.readText

  [345] fun syncTracksFromFiles(context: android.content.Context) {
    WORK FIELDS (local computed values):
      [346] val db = spatialDb ?: return
      [348] val tracksDir = java.io.File(
      [356] val gpxFiles = tracksDir.listFiles { f -> f.name.lowercase().endsWith(".gpx") } ?: return
      [359] val existing = mutableSetOf<String>()
      [360] val cursor = db.rawQuery("SELECT name FROM tracks", null)
      [363] var inserted = 0
      [365] val name = file.nameWithoutExtension
      [368] val text = file.readText()
      [369] val coords = parseGpxTrackPoints(text)
      [372] var minLat = Double.MAX_VALUE; var maxLat = -Double.MAX_VALUE
      [373] var minLon = Double.MAX_VALUE; var maxLon = -Double.MAX_VALUE
      [379] val wkt = "LINESTRING(" + coords.joinToString(",") { "${it.first} ${it.second}" } + ")"
    CALLS OUT:
      → COUNT
      → Environment.getExternalStoragePublicDirectory
      → LINESTRING
      → Log.e
      → Log.i
      → Pair
      → Regex
      → UUID.randomUUID
      → arrayOf
      → coords.add
      → coords.isEmpty
      → countRows
      → cursor.close
      → cursor.moveToFirst
      → db.execSQL

  [400] fun parseGpxTrackPoints(gpxText: String): List<Pair<Double, Double>> {
    WORK FIELDS (local computed values):
      [401] val coords = mutableListOf<Pair<Double, Double>>()
      [402] val regex = Regex("""lat="([^"]+)"\s+lon="([^"]+)"""")
      [404] val lat = match.groupValues[1].toDoubleOrNull() ?: continue
      [405] val lon = match.groupValues[2].toDoubleOrNull() ?: continue
      [413] val cursor = db.rawQuery("SELECT COUNT(*) FROM $table", null)
      [415] val count = cursor.getInt(0)
      [424] val db = spatialDb ?: return emptyList()
      [425] val results = mutableListOf<Map<String, String?>>()
      [429] val cursor = db.rawQuery(
      [435] val wkt = it.getString(2) ?: continue
      [453] val sb = StringBuilder()
      [457] val geom = wpt["geometry"] ?: return@forEachIndexed
    CALLS OUT:
      → COUNT
      → Log.e
      → Log.i
      → POINT
      → Pair
      → Regex
      → arrayOf
      → buildWaypointGeoJson
      → coords.add
      → countRows
      → cursor.close
      → cursor.moveToFirst
      → db.rawQuery
      → find
      → it.moveToNext

  [412] private fun countRows(db: SQLiteDatabase, table: String): Int {
    WORK FIELDS (local computed values):
      [413] val cursor = db.rawQuery("SELECT COUNT(*) FROM $table", null)
      [415] val count = cursor.getInt(0)
      [424] val db = spatialDb ?: return emptyList()
      [425] val results = mutableListOf<Map<String, String?>>()
      [429] val cursor = db.rawQuery(
      [435] val wkt = it.getString(2) ?: continue
      [453] val sb = StringBuilder()
      [457] val geom = wpt["geometry"] ?: return@forEachIndexed
      [458] val name = (wpt["name"] ?: "Unnamed").replace("\"", "\\\"")
      [459] val wptType = (wpt["type"] ?: "other").replace("\"", "\\\"")
      [461] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(geom)
      [463] val lon = match.groupValues[1]
    CALLS OUT:
      → COUNT
      → Log.e
      → Log.i
      → POINT
      → Regex
      → arrayOf
      → buildWaypointGeoJson
      → cursor.close
      → cursor.moveToFirst
      → db.rawQuery
      → find
      → it.moveToNext
      → queryRoutesByViewport
      → queryWaypointsByViewport
      → replace

  [423] fun queryWaypointsByViewport(south: Double, west: Double, north: Double, east: Double, limit: Int = 500): List<Map<String, String?>> {
    WORK FIELDS (local computed values):
      [424] val db = spatialDb ?: return emptyList()
      [425] val results = mutableListOf<Map<String, String?>>()
      [429] val cursor = db.rawQuery(
      [435] val wkt = it.getString(2) ?: continue
      [453] val sb = StringBuilder()
      [457] val geom = wpt["geometry"] ?: return@forEachIndexed
      [458] val name = (wpt["name"] ?: "Unnamed").replace("\"", "\\\"")
      [459] val wptType = (wpt["type"] ?: "other").replace("\"", "\\\"")
      [461] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(geom)
      [463] val lon = match.groupValues[1]
      [464] val lat = match.groupValues[2]
      [475] val db = spatialDb ?: return emptyList()
    CALLS OUT:
      → Log.e
      → Log.i
      → POINT
      → Regex
      → arrayOf
      → buildRouteGeoJson
      → buildWaypointGeoJson
      → db.rawQuery
      → find
      → it.moveToNext
      → queryRoutesByViewport
      → replace
      → results.add
      → sb.append

  [452] fun buildWaypointGeoJson(waypoints: List<Map<String, String?>>): String {
    WORK FIELDS (local computed values):
      [453] val sb = StringBuilder()
      [457] val geom = wpt["geometry"] ?: return@forEachIndexed
      [458] val name = (wpt["name"] ?: "Unnamed").replace("\"", "\\\"")
      [459] val wptType = (wpt["type"] ?: "other").replace("\"", "\\\"")
      [461] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(geom)
      [463] val lon = match.groupValues[1]
      [464] val lat = match.groupValues[2]
      [475] val db = spatialDb ?: return emptyList()
      [476] val results = mutableListOf<Map<String, String?>>()
      [478] val cursor = db.rawQuery(
      [484] val wkt = it.getString(2) ?: continue
      [501] val sb = StringBuilder()
    CALLS OUT:
      → LINESTRING
      → Log.e
      → Log.i
      → POINT
      → Regex
      → arrayOf
      → buildRouteGeoJson
      → coordStr.split
      → db.rawQuery
      → find
      → geom.removePrefix
      → it.moveToNext
      → pair.trim
      → queryRoutesByViewport
      → queryTracksOnlyByViewport

  [474] fun queryRoutesByViewport(south: Double, west: Double, north: Double, east: Double, limit: Int = 500): List<Map<String, String?>> {
    WORK FIELDS (local computed values):
      [475] val db = spatialDb ?: return emptyList()
      [476] val results = mutableListOf<Map<String, String?>>()
      [478] val cursor = db.rawQuery(
      [484] val wkt = it.getString(2) ?: continue
      [501] val sb = StringBuilder()
      [505] val geom = route["geometry"] ?: return@forEachIndexed
      [506] val name = (route["name"] ?: "Unnamed Route").replace("\"", "\\\"")
      [507] val coordStr = geom.removePrefix("LINESTRING(").removeSuffix(")")
      [508] val coords = coordStr.split(",").joinToString(",") { pair ->
      [509] val parts = pair.trim().split(" ")
      [520] val db = spatialDb ?: return emptyList()
      [521] val results = mutableListOf<Map<String, String?>>()
    CALLS OUT:
      → LINESTRING
      → Log.e
      → Log.i
      → arrayOf
      → buildRouteGeoJson
      → coordStr.split
      → db.rawQuery
      → geom.removePrefix
      → insertWaypointWithId
      → it.moveToNext
      → now
      → pair.trim
      → queryTracksOnlyByViewport
      → removeSuffix
      → replace

  [500] fun buildRouteGeoJson(routes: List<Map<String, String?>>): String {
    WORK FIELDS (local computed values):
      [501] val sb = StringBuilder()
      [505] val geom = route["geometry"] ?: return@forEachIndexed
      [506] val name = (route["name"] ?: "Unnamed Route").replace("\"", "\\\"")
      [507] val coordStr = geom.removePrefix("LINESTRING(").removeSuffix(")")
      [508] val coords = coordStr.split(",").joinToString(",") { pair ->
      [509] val parts = pair.trim().split(" ")
      [520] val db = spatialDb ?: return emptyList()
      [521] val results = mutableListOf<Map<String, String?>>()
      [523] val cursor = db.rawQuery(
      [529] val wkt = it.getString(2) ?: continue
      [553] val db = spatialDb ?: return false
      [554] val ts = now()
    CALLS OUT:
      → IllegalStateException
      → LINESTRING
      → Log.e
      → Log.i
      → arrayOf
      → coordStr.split
      → db.execSQL
      → db.rawQuery
      → geom.removePrefix
      → insertWaypoint
      → insertWaypointWithId
      → it.moveToNext
      → newId
      → now
      → pair.trim

  [519] fun queryTracksOnlyByViewport(south: Double, west: Double, north: Double, east: Double, limit: Int = 500): List<Map<String, String?>> {
    WORK FIELDS (local computed values):
      [520] val db = spatialDb ?: return emptyList()
      [521] val results = mutableListOf<Map<String, String?>>()
      [523] val cursor = db.rawQuery(
      [529] val wkt = it.getString(2) ?: continue
      [553] val db = spatialDb ?: return false
      [554] val ts = now()
      [555] val geometry = pointWkt(lat, lon)
      [568] val db = spatialDb ?: throw IllegalStateException("SpatialDbManager not initialized")
      [569] val id = newId()
      [570] val ts = now()
      [571] val geometry = pointWkt(lat, lon)
    CALLS OUT:
      → IllegalStateException
      → Log.e
      → Log.i
      → arrayOf
      → db.execSQL
      → db.rawQuery
      → insertWaypoint
      → insertWaypointWithId
      → it.moveToNext
      → newId
      → now
      → pointWkt
      → results.add
      → type.lowercase

  [552] fun insertWaypointWithId(waypointId: String, name: String, lat: Double, lon: Double, type: String): Boolean {
    WORK FIELDS (local computed values):
      [553] val db = spatialDb ?: return false
      [554] val ts = now()
      [555] val geometry = pointWkt(lat, lon)
      [568] val db = spatialDb ?: throw IllegalStateException("SpatialDbManager not initialized")
      [569] val id = newId()
      [570] val ts = now()
      [571] val geometry = pointWkt(lat, lon)
      [601] val db = spatialDb ?: throw IllegalStateException("SpatialDbManager not initialized")
      [602] val id = newId()
      [603] val ts = now()
      [622] val db = spatialDb ?: throw IllegalStateException("SpatialDbManager not initialized")
      [623] val id = newId()
    CALLS OUT:
      → IllegalStateException
      → Log.e
      → Log.i
      → arrayOf
      → db.execSQL
      → insertRoute
      → insertTrackToDb
      → insertWaypoint
      → newId
      → now
      → pointWkt
      → type.lowercase

  [567] fun insertWaypoint(name: String, lat: Double, lon: Double, type: String = "other"): String {
    WORK FIELDS (local computed values):
      [568] val db = spatialDb ?: throw IllegalStateException("SpatialDbManager not initialized")
      [569] val id = newId()
      [570] val ts = now()
      [571] val geometry = pointWkt(lat, lon)
      [601] val db = spatialDb ?: throw IllegalStateException("SpatialDbManager not initialized")
      [602] val id = newId()
      [603] val ts = now()
      [622] val db = spatialDb ?: throw IllegalStateException("SpatialDbManager not initialized")
      [623] val id = newId()
      [624] val ts = now()
      [625] val bbox = "$minLat,$minLon,$maxLat,$maxLon"
      [642] val db = spatialDb ?: return null
    CALLS OUT:
      → IllegalStateException
      → Log.e
      → Log.i
      → MAX
      → MIN
      → arrayOf
      → db.execSQL
      → db.rawQuery
      → insertRoute
      → insertTrackToDb
      → newId
      → now
      → pointWkt
      → type.lowercase

  [600] fun insertRoute(name: String, geometryWkt: String, minLat: Double, maxLat: Double, minLon: Double, maxLon: Double): String {
    WORK FIELDS (local computed values):
      [601] val db = spatialDb ?: throw IllegalStateException("SpatialDbManager not initialized")
      [602] val id = newId()
      [603] val ts = now()
      [622] val db = spatialDb ?: throw IllegalStateException("SpatialDbManager not initialized")
      [623] val id = newId()
      [624] val ts = now()
      [625] val bbox = "$minLat,$minLon,$maxLat,$maxLon"
      [642] val db = spatialDb ?: return null
      [643] val latCol = if (table == "trails") "min_lat" else "min_lat"
      [645] val cursor = db.rawQuery(
      [664] val db = spatialDb ?: return emptyList()
      [665] val results = mutableListOf<Map<String, String?>>()
    CALLS OUT:
      → IllegalStateException
      → Log.e
      → Log.i
      → MAX
      → MIN
      → arrayOf
      → db.execSQL
      → db.rawQuery
      → doubleArrayOf
      → insertTrackToDb
      → it.isNull
      → it.moveToFirst
      → it.moveToNext
      → newId
      → now

  [621] fun insertTrackToDb(name: String, geometryWkt: String, minLat: Double, maxLat: Double, minLon: Double, maxLon: Double): String {
    WORK FIELDS (local computed values):
      [622] val db = spatialDb ?: throw IllegalStateException("SpatialDbManager not initialized")
      [623] val id = newId()
      [624] val ts = now()
      [625] val bbox = "$minLat,$minLon,$maxLat,$maxLon"
      [642] val db = spatialDb ?: return null
      [643] val latCol = if (table == "trails") "min_lat" else "min_lat"
      [645] val cursor = db.rawQuery(
      [664] val db = spatialDb ?: return emptyList()
      [665] val results = mutableListOf<Map<String, String?>>()
      [667] val idCol = when (table) {
      [674] val typeCol = if (table == "waypoints") ", type" else ""
      [675] val typeFilter = if (table == "tracks") " AND (type='TRACK' OR type IS NULL)" else ""
    CALLS OUT:
      → IllegalStateException
      → Log.e
      → Log.i
      → MAX
      → MIN
      → arrayOf
      → db.execSQL
      → db.rawQuery
      → doubleArrayOf
      → it.isNull
      → it.moveToFirst
      → it.moveToNext
      → newId
      → now
      → renameWaypoint

  [641] fun getArtifactBounds(table: String): DoubleArray? {
    WORK FIELDS (local computed values):
      [642] val db = spatialDb ?: return null
      [643] val latCol = if (table == "trails") "min_lat" else "min_lat"
      [645] val cursor = db.rawQuery(
      [664] val db = spatialDb ?: return emptyList()
      [665] val results = mutableListOf<Map<String, String?>>()
      [667] val idCol = when (table) {
      [674] val typeCol = if (table == "waypoints") ", type" else ""
      [675] val typeFilter = if (table == "tracks") " AND (type='TRACK' OR type IS NULL)" else ""
      [676] val sql = "SELECT $idCol, name$typeCol FROM $table WHERE min_lat IS NOT NULL AND max_lat >= ? AND min_lat <= ? AND max_lon >= ? AND min_lon <= ?$typeFilter ORDER BY name LIMIT 200"
      [677] val cursor = db.rawQuery(sql, arrayOf(south.toString(), north.toString(), west.toString(), east.toString()))
      [680] val map = mutableMapOf<String, String?>()
    CALLS OUT:
      → Log.e
      → MAX
      → MIN
      → arrayOf
      → changeWaypointType
      → db.rawQuery
      → deleteRoute
      → deleteWaypoint
      → doubleArrayOf
      → execSQL
      → it.isNull
      → it.moveToFirst
      → it.moveToNext
      → newType.lowercase
      → now

  [663] fun queryArtifactList(table: String, south: Double, west: Double, north: Double, east: Double): List<Map<String, String?>> {
    WORK FIELDS (local computed values):
      [664] val db = spatialDb ?: return emptyList()
      [665] val results = mutableListOf<Map<String, String?>>()
      [667] val idCol = when (table) {
      [674] val typeCol = if (table == "waypoints") ", type" else ""
      [675] val typeFilter = if (table == "tracks") " AND (type='TRACK' OR type IS NULL)" else ""
      [676] val sql = "SELECT $idCol, name$typeCol FROM $table WHERE min_lat IS NOT NULL AND max_lat >= ? AND min_lat <= ? AND max_lon >= ? AND min_lon <= ?$typeFilter ORDER BY name LIMIT 200"
      [677] val cursor = db.rawQuery(sql, arrayOf(south.toString(), north.toString(), west.toString(), east.toString()))
      [680] val map = mutableMapOf<String, String?>()
    CALLS OUT:
      → Log.e
      → arrayOf
      → changeWaypointType
      → db.rawQuery
      → deleteRoute
      → deleteTrackFromDb
      → deleteWaypoint
      → execSQL
      → it.moveToNext
      → newType.lowercase
      → now
      → renameRoute
      → renameTrackInDb
      → renameWaypoint
      → results.add

  [698] fun renameWaypoint(id: String, newName: String) {
    WORK FIELDS (local computed values):
      [749] val db = spatialDb ?: return null
      [751] val cursor = db.rawQuery(
      [756] val name = c.getString(0) ?: "Unnamed"
      [757] val type = c.getString(1) ?: ""
      [758] val geom = c.getString(2) ?: return null
      [759] val desc = if (c.columnCount > 3) (c.getString(3) ?: "") else ""
      [760] val coords = geom.removePrefix("POINT(").removeSuffix(")").trim().split(" ")
      [762] val lon = coords[0]
      [763] val lat = coords[1]
      [764] val sb = StringBuilder()
    CALLS OUT:
      → Log.e
      → POINT
      → Pair
      → append
      → arrayOf
      → buildWaypointGpxById
      → c.moveToFirst
      → changeWaypointType
      → db.rawQuery
      → deleteRoute
      → deleteTrackFromDb
      → deleteWaypoint
      → desc.isNotEmpty
      → execSQL
      → geom.removePrefix

  [704] fun deleteWaypoint(id: String) {
    WORK FIELDS (local computed values):
      [749] val db = spatialDb ?: return null
      [751] val cursor = db.rawQuery(
      [756] val name = c.getString(0) ?: "Unnamed"
      [757] val type = c.getString(1) ?: ""
      [758] val geom = c.getString(2) ?: return null
      [759] val desc = if (c.columnCount > 3) (c.getString(3) ?: "") else ""
      [760] val coords = geom.removePrefix("POINT(").removeSuffix(")").trim().split(" ")
      [762] val lon = coords[0]
      [763] val lat = coords[1]
      [764] val sb = StringBuilder()
      [782] val db = spatialDb ?: return null
      [784] val cursor = db.rawQuery(
    CALLS OUT:
      → Log.e
      → POINT
      → Pair
      → append
      → arrayOf
      → buildRouteGpxById
      → buildWaypointGpxById
      → c.moveToFirst
      → changeWaypointType
      → db.rawQuery
      → deleteRoute
      → deleteTrackFromDb
      → desc.isNotEmpty
      → execSQL
      → geom.removePrefix

  [709] fun changeWaypointType(id: String, newType: String) {
    WORK FIELDS (local computed values):
      [749] val db = spatialDb ?: return null
      [751] val cursor = db.rawQuery(
      [756] val name = c.getString(0) ?: "Unnamed"
      [757] val type = c.getString(1) ?: ""
      [758] val geom = c.getString(2) ?: return null
      [759] val desc = if (c.columnCount > 3) (c.getString(3) ?: "") else ""
      [760] val coords = geom.removePrefix("POINT(").removeSuffix(")").trim().split(" ")
      [762] val lon = coords[0]
      [763] val lat = coords[1]
      [764] val sb = StringBuilder()
      [782] val db = spatialDb ?: return null
      [784] val cursor = db.rawQuery(
    CALLS OUT:
      → Log.e
      → POINT
      → Pair
      → append
      → arrayOf
      → buildRouteGpxById
      → buildWaypointGpxById
      → c.moveToFirst
      → db.rawQuery
      → deleteRoute
      → deleteTrackFromDb
      → desc.isNotEmpty
      → execSQL
      → geom.removePrefix
      → newType.lowercase

  [715] fun renameRoute(id: String, newName: String) {
    WORK FIELDS (local computed values):
      [749] val db = spatialDb ?: return null
      [751] val cursor = db.rawQuery(
      [756] val name = c.getString(0) ?: "Unnamed"
      [757] val type = c.getString(1) ?: ""
      [758] val geom = c.getString(2) ?: return null
      [759] val desc = if (c.columnCount > 3) (c.getString(3) ?: "") else ""
      [760] val coords = geom.removePrefix("POINT(").removeSuffix(")").trim().split(" ")
      [762] val lon = coords[0]
      [763] val lat = coords[1]
      [764] val sb = StringBuilder()
      [782] val db = spatialDb ?: return null
      [784] val cursor = db.rawQuery(
    CALLS OUT:
      → LINESTRING
      → Log.e
      → POINT
      → Pair
      → append
      → arrayOf
      → buildRouteGpxById
      → buildWaypointGpxById
      → c.moveToFirst
      → coordStr.split
      → db.rawQuery
      → deleteRoute
      → deleteTrackFromDb
      → desc.isNotEmpty
      → execSQL

  [721] fun deleteRoute(id: String) {
    WORK FIELDS (local computed values):
      [749] val db = spatialDb ?: return null
      [751] val cursor = db.rawQuery(
      [756] val name = c.getString(0) ?: "Unnamed"
      [757] val type = c.getString(1) ?: ""
      [758] val geom = c.getString(2) ?: return null
      [759] val desc = if (c.columnCount > 3) (c.getString(3) ?: "") else ""
      [760] val coords = geom.removePrefix("POINT(").removeSuffix(")").trim().split(" ")
      [762] val lon = coords[0]
      [763] val lat = coords[1]
      [764] val sb = StringBuilder()
      [782] val db = spatialDb ?: return null
      [784] val cursor = db.rawQuery(
    CALLS OUT:
      → LINESTRING
      → Log.e
      → POINT
      → Pair
      → append
      → arrayOf
      → buildRouteGpxById
      → buildWaypointGpxById
      → c.moveToFirst
      → coordStr.split
      → db.rawQuery
      → deleteTrackFromDb
      → desc.isNotEmpty
      → execSQL
      → geom.removePrefix

  [726] fun renameTrackInDb(id: String, newName: String) {
    WORK FIELDS (local computed values):
      [749] val db = spatialDb ?: return null
      [751] val cursor = db.rawQuery(
      [756] val name = c.getString(0) ?: "Unnamed"
      [757] val type = c.getString(1) ?: ""
      [758] val geom = c.getString(2) ?: return null
      [759] val desc = if (c.columnCount > 3) (c.getString(3) ?: "") else ""
      [760] val coords = geom.removePrefix("POINT(").removeSuffix(")").trim().split(" ")
      [762] val lon = coords[0]
      [763] val lat = coords[1]
      [764] val sb = StringBuilder()
      [782] val db = spatialDb ?: return null
      [784] val cursor = db.rawQuery(
    CALLS OUT:
      → LINESTRING
      → Log.e
      → POINT
      → Pair
      → append
      → arrayOf
      → buildRouteGpxById
      → buildWaypointGpxById
      → c.moveToFirst
      → coordStr.split
      → db.rawQuery
      → deleteTrackFromDb
      → desc.isNotEmpty
      → execSQL
      → geom.removePrefix

  [732] fun deleteTrackFromDb(id: String) {
    WORK FIELDS (local computed values):
      [749] val db = spatialDb ?: return null
      [751] val cursor = db.rawQuery(
      [756] val name = c.getString(0) ?: "Unnamed"
      [757] val type = c.getString(1) ?: ""
      [758] val geom = c.getString(2) ?: return null
      [759] val desc = if (c.columnCount > 3) (c.getString(3) ?: "") else ""
      [760] val coords = geom.removePrefix("POINT(").removeSuffix(")").trim().split(" ")
      [762] val lon = coords[0]
      [763] val lat = coords[1]
      [764] val sb = StringBuilder()
      [782] val db = spatialDb ?: return null
      [784] val cursor = db.rawQuery(
    CALLS OUT:
      → LINESTRING
      → Log.e
      → POINT
      → Pair
      → append
      → arrayOf
      → buildRouteGpxById
      → buildWaypointGpxById
      → c.moveToFirst
      → coordStr.split
      → db.rawQuery
      → desc.isNotEmpty
      → execSQL
      → geom.removePrefix
      → pt.trim

  [744] private fun xmlEscape(s: String): String =
    WORK FIELDS (local computed values):
      [749] val db = spatialDb ?: return null
      [751] val cursor = db.rawQuery(
      [756] val name = c.getString(0) ?: "Unnamed"
      [757] val type = c.getString(1) ?: ""
      [758] val geom = c.getString(2) ?: return null
      [759] val desc = if (c.columnCount > 3) (c.getString(3) ?: "") else ""
      [760] val coords = geom.removePrefix("POINT(").removeSuffix(")").trim().split(" ")
      [762] val lon = coords[0]
      [763] val lat = coords[1]
      [764] val sb = StringBuilder()
      [782] val db = spatialDb ?: return null
      [784] val cursor = db.rawQuery(
    CALLS OUT:
      → LINESTRING
      → Log.e
      → POINT
      → Pair
      → append
      → arrayOf
      → buildRouteGpxById
      → buildTrailGpxById
      → buildWaypointGpxById
      → c.moveToFirst
      → coordStr.split
      → db.rawQuery
      → desc.isNotEmpty
      → geom.removePrefix
      → pt.trim

  [748] fun buildWaypointGpxById(waypointId: String): Pair<String, String>? {
    WORK FIELDS (local computed values):
      [749] val db = spatialDb ?: return null
      [751] val cursor = db.rawQuery(
      [756] val name = c.getString(0) ?: "Unnamed"
      [757] val type = c.getString(1) ?: ""
      [758] val geom = c.getString(2) ?: return null
      [759] val desc = if (c.columnCount > 3) (c.getString(3) ?: "") else ""
      [760] val coords = geom.removePrefix("POINT(").removeSuffix(")").trim().split(" ")
      [762] val lon = coords[0]
      [763] val lat = coords[1]
      [764] val sb = StringBuilder()
      [782] val db = spatialDb ?: return null
      [784] val cursor = db.rawQuery(
    CALLS OUT:
      → LINESTRING
      → Log.e
      → POINT
      → Pair
      → append
      → arrayOf
      → buildRouteGpxById
      → buildTrailGpxById
      → c.moveToFirst
      → coordStr.split
      → db.rawQuery
      → desc.isNotEmpty
      → geom.removePrefix
      → pt.trim
      → removeSuffix

  [781] fun buildRouteGpxById(routeId: String): Pair<String, String>? {
    WORK FIELDS (local computed values):
      [782] val db = spatialDb ?: return null
      [784] val cursor = db.rawQuery(
      [789] val name = c.getString(0) ?: "Unnamed"
      [790] val geom = c.getString(1) ?: return null
      [791] val desc = if (c.columnCount > 2) (c.getString(2) ?: "") else ""
      [792] val coordStr = geom.removePrefix("LINESTRING(").removeSuffix(")")
      [793] val points = coordStr.split(",").mapNotNull { pt ->
      [794] val parts = pt.trim().split(" ")
      [797] val sb = StringBuilder()
      [802] var idx = 1
      [821] val db = spatialDb ?: return null
      [823] val cursor = db.rawQuery(
    CALLS OUT:
      → LINESTRING
      → Log.e
      → Log.i
      → Pair
      → append
      → arrayOf
      → buildTrailGpxById
      → c.moveToFirst
      → close
      → coordStr.split
      → db.rawQuery
      → desc.isNotEmpty
      → geom.removePrefix
      → pt.trim
      → removeSuffix

  [820] fun buildTrailGpxById(trailId: String): Pair<String, String>? {
    WORK FIELDS (local computed values):
      [821] val db = spatialDb ?: return null
      [823] val cursor = db.rawQuery(
      [828] val name = c.getString(0) ?: "Unnamed"
      [829] val geom = c.getString(1) ?: return null
      [830] val coordStr = geom.removePrefix("LINESTRING(").removeSuffix(")")
      [831] val points = coordStr.split(",").mapNotNull { pt ->
      [832] val parts = pt.trim().split(" ")
      [835] val sb = StringBuilder()
      [892] val coords = points.joinToString(", ") { "${it.second} ${it.first}" }
    CALLS OUT:
      → Instant.now
      → LINESTRING
      → Log.e
      → Log.i
      → POINT
      → POLYGON
      → Pair
      → UUID.randomUUID
      → append
      → arrayOf
      → bboxWkt
      → c.moveToFirst
      → check
      → close
      → coordStr.split

  [855] fun close() {
    WORK FIELDS (local computed values):
      [892] val coords = points.joinToString(", ") { "${it.second} ${it.first}" }
      [902] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(wkt) ?: return null
      [903] val lng = match.groupValues[1].toDoubleOrNull() ?: return null
      [904] val lat = match.groupValues[2].toDoubleOrNull() ?: return null
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
    CALLS OUT:
      → Instant.now
      → LINESTRING
      → Log.i
      → POINT
      → POLYGON
      → Pair
      → Regex
      → UUID.randomUUID
      → bboxWkt
      → check
      → close
      → coord.trim
      → doubleArrayOf
      → extension
      → find

  [866] fun spatial(): SQLiteDatabase {
    WORK FIELDS (local computed values):
      [892] val coords = points.joinToString(", ") { "${it.second} ${it.first}" }
      [902] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(wkt) ?: return null
      [903] val lng = match.groupValues[1].toDoubleOrNull() ?: return null
      [904] val lat = match.groupValues[2].toDoubleOrNull() ?: return null
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
    CALLS OUT:
      → Instant.now
      → LINESTRING
      → POINT
      → POLYGON
      → Pair
      → Regex
      → UUID.randomUUID
      → bboxWkt
      → check
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extension
      → extensionSchemaVersion

  [871] fun extension(): SQLiteDatabase {
    WORK FIELDS (local computed values):
      [892] val coords = points.joinToString(", ") { "${it.second} ${it.first}" }
      [902] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(wkt) ?: return null
      [903] val lng = match.groupValues[1].toDoubleOrNull() ?: return null
      [904] val lat = match.groupValues[2].toDoubleOrNull() ?: return null
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
    CALLS OUT:
      → Instant.now
      → LINESTRING
      → POINT
      → POLYGON
      → Pair
      → Regex
      → UUID.randomUUID
      → bboxWkt
      → check
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extensionSchemaVersion
      → find

  [876] fun isReady(): Boolean = initialized
    WORK FIELDS (local computed values):
      [892] val coords = points.joinToString(", ") { "${it.second} ${it.first}" }
      [902] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(wkt) ?: return null
      [903] val lng = match.groupValues[1].toDoubleOrNull() ?: return null
      [904] val lat = match.groupValues[2].toDoubleOrNull() ?: return null
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
    CALLS OUT:
      → Instant.now
      → LINESTRING
      → POINT
      → POLYGON
      → Pair
      → Regex
      → UUID.randomUUID
      → bboxWkt
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extensionSchemaVersion
      → find
      → lats.min

  [879] fun newId(): String = UUID.randomUUID().toString()
    WORK FIELDS (local computed values):
      [892] val coords = points.joinToString(", ") { "${it.second} ${it.first}" }
      [902] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(wkt) ?: return null
      [903] val lng = match.groupValues[1].toDoubleOrNull() ?: return null
      [904] val lat = match.groupValues[2].toDoubleOrNull() ?: return null
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
    CALLS OUT:
      → Instant.now
      → LINESTRING
      → POINT
      → POLYGON
      → Pair
      → Regex
      → bboxWkt
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extensionSchemaVersion
      → find
      → lats.min
      → lineWkt

  [882] fun now(): String = Instant.now().toString()
    WORK FIELDS (local computed values):
      [892] val coords = points.joinToString(", ") { "${it.second} ${it.first}" }
      [902] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(wkt) ?: return null
      [903] val lng = match.groupValues[1].toDoubleOrNull() ?: return null
      [904] val lat = match.groupValues[2].toDoubleOrNull() ?: return null
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
    CALLS OUT:
      → LINESTRING
      → POINT
      → POLYGON
      → Pair
      → Regex
      → bboxWkt
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extensionSchemaVersion
      → find
      → lats.min
      → lineWkt
      → lngs.max

  [887] fun pointWkt(lat: Double, lng: Double): String =
    WORK FIELDS (local computed values):
      [892] val coords = points.joinToString(", ") { "${it.second} ${it.first}" }
      [902] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(wkt) ?: return null
      [903] val lng = match.groupValues[1].toDoubleOrNull() ?: return null
      [904] val lat = match.groupValues[2].toDoubleOrNull() ?: return null
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
    CALLS OUT:
      → LINESTRING
      → POINT
      → POLYGON
      → Pair
      → Regex
      → bboxWkt
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extensionSchemaVersion
      → find
      → lats.min
      → lineWkt
      → lngs.max

  [891] fun lineWkt(points: List<Pair<Double, Double>>): String {
    WORK FIELDS (local computed values):
      [892] val coords = points.joinToString(", ") { "${it.second} ${it.first}" }
      [902] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(wkt) ?: return null
      [903] val lng = match.groupValues[1].toDoubleOrNull() ?: return null
      [904] val lat = match.groupValues[2].toDoubleOrNull() ?: return null
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
    CALLS OUT:
      → LINESTRING
      → POLYGON
      → Pair
      → Regex
      → bboxWkt
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extensionSchemaVersion
      → find
      → lats.min
      → lngs.max
      → lngs.min
      → parseBbox

  [897] fun bboxWkt(north: Double, south: Double, east: Double, west: Double): String =
    WORK FIELDS (local computed values):
      [902] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(wkt) ?: return null
      [903] val lng = match.groupValues[1].toDoubleOrNull() ?: return null
      [904] val lat = match.groupValues[2].toDoubleOrNull() ?: return null
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
      [930] val lats = points.map { it.second }
    CALLS OUT:
      → POLYGON
      → Pair
      → Regex
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extensionSchemaVersion
      → find
      → lats.min
      → lngs.max
      → lngs.min
      → parseBbox
      → parseLine
      → parsePoint

  [901] fun parsePoint(wkt: String): Pair<Double, Double>? {
    WORK FIELDS (local computed values):
      [902] val match = Regex("POINT\\(([\\d.\\-]+) ([\\d.\\-]+)\\)").find(wkt) ?: return null
      [903] val lng = match.groupValues[1].toDoubleOrNull() ?: return null
      [904] val lat = match.groupValues[2].toDoubleOrNull() ?: return null
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
      [930] val lats = points.map { it.second }
    CALLS OUT:
      → Pair
      → Regex
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extensionSchemaVersion
      → find
      → lats.min
      → lngs.max
      → lngs.min
      → parseBbox
      → parseLine
      → rawQuery
      → spatialSchemaVersion

  [909] fun parseLine(wkt: String): List<Pair<Double, Double>> {
    WORK FIELDS (local computed values):
      [910] val match = Regex("LINESTRING\\((.+)\\)").find(wkt) ?: return emptyList()
      [912] val parts = coord.trim().split(" ")
      [914] val lng = parts[0].toDoubleOrNull()
      [915] val lat = parts[1].toDoubleOrNull()
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
      [930] val lats = points.map { it.second }
      [938] val cursor = spatialDb!!.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [940] val v = cursor.getInt(0)
      [949] val cursor = extensionDb!!.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
    CALLS OUT:
      → Pair
      → Regex
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extensionSchemaVersion
      → find
      → lats.min
      → lngs.max
      → lngs.min
      → parseBbox
      → rawQuery
      → spatialSchemaVersion
      → split

  [922] fun parseBbox(wkt: String): DoubleArray? {
    WORK FIELDS (local computed values):
      [923] val match = Regex("POLYGON\\(\\((.+)\\)\\)").find(wkt) ?: return null
      [924] val points = match.groupValues[1].split(",").mapNotNull { coord ->
      [925] val parts = coord.trim().split(" ")
      [929] val lngs = points.map { it.first }
      [930] val lats = points.map { it.second }
      [938] val cursor = spatialDb!!.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [940] val v = cursor.getInt(0)
      [949] val cursor = extensionDb!!.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [951] val v = cursor.getInt(0)
    CALLS OUT:
      → Pair
      → Regex
      → coord.trim
      → cursor.close
      → cursor.moveToFirst
      → doubleArrayOf
      → extensionSchemaVersion
      → find
      → lats.min
      → lngs.max
      → lngs.min
      → rawQuery
      → spatialSchemaVersion
      → split

  [935] fun spatialSchemaVersion(): Int {
    WORK FIELDS (local computed values):
      [938] val cursor = spatialDb!!.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [940] val v = cursor.getInt(0)
      [949] val cursor = extensionDb!!.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [951] val v = cursor.getInt(0)
    CALLS OUT:
      → cursor.close
      → cursor.moveToFirst
      → extensionSchemaVersion
      → rawQuery

  [946] fun extensionSchemaVersion(): Int {
    WORK FIELDS (local computed values):
      [949] val cursor = extensionDb!!.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [951] val v = cursor.getInt(0)
    CALLS OUT:
      → cursor.close
      → cursor.moveToFirst
      → rawQuery

========================================
FILE: TrackManager.kt
========================================

  [10] fun stub() { /* Pass 1 scaffold -- functions in Pass 2 */ }

========================================
FILE: TrailImporter.kt
========================================

  [60] private fun emitProgress(p: ImportProgress) { _progress.value = p }
    WORK FIELDS (local computed values):
      [83] val catalog = loadCatalog(context)
      [85] val source = catalog.firstOrNull { it.id == sourceId }
      [90] val sDb = SpatialDbManager.getSpatialDb()
      [91] val eDb = SpatialDbManager.getExtensionDb()
      [95] var inserted = 0; var skipped = 0; var errors = 0; var fetched = 0; var rejected = 0
      [96] var offset = 0; var hasMore = true
      [99] val url = buildUrl(source, south, west, north, east, offset)
      [106] val json = httpGet(url)
      [109] val root = try { JSONObject(json) } catch (_: Exception) { errors++; break }
      [115] val features = root.optJSONArray("features") ?: JSONArray()
      [118] val pageCount = features.length()
      [123] val feat = features.getJSONObject(i)
    CALLS OUT:
      → ImportResult
      → JSONArray
      → JSONObject
      → Log.e
      → Log.i
      → SpatialDbManager.getExtensionDb
      → SpatialDbManager.getSpatialDb
      → SpatialDbManager.init
      → applyMigrationIfNeeded
      → buildUrl
      → coords.length
      → coords.optJSONArray
      → eDb.beginTransaction
      → feat.optJSONObject
      → features.getJSONObject

  [63] suspend fun importFullSource(context: Context, sourceId: String): ImportResult {
    WORK FIELDS (local computed values):
      [83] val catalog = loadCatalog(context)
      [85] val source = catalog.firstOrNull { it.id == sourceId }
      [90] val sDb = SpatialDbManager.getSpatialDb()
      [91] val eDb = SpatialDbManager.getExtensionDb()
      [95] var inserted = 0; var skipped = 0; var errors = 0; var fetched = 0; var rejected = 0
      [96] var offset = 0; var hasMore = true
      [99] val url = buildUrl(source, south, west, north, east, offset)
      [106] val json = httpGet(url)
      [109] val root = try { JSONObject(json) } catch (_: Exception) { errors++; break }
      [115] val features = root.optJSONArray("features") ?: JSONArray()
      [118] val pageCount = features.length()
      [123] val feat = features.getJSONObject(i)
    CALLS OUT:
      → ImportResult
      → JSONArray
      → JSONObject
      → Log.e
      → Log.i
      → SpatialDbManager.getExtensionDb
      → SpatialDbManager.getSpatialDb
      → SpatialDbManager.init
      → applyMigrationIfNeeded
      → buildUrl
      → coords.length
      → coords.optJSONArray
      → eDb.beginTransaction
      → feat.optJSONObject
      → features.getJSONObject

  [68] suspend fun importByArea(
    WORK FIELDS (local computed values):
      [83] val catalog = loadCatalog(context)
      [85] val source = catalog.firstOrNull { it.id == sourceId }
      [90] val sDb = SpatialDbManager.getSpatialDb()
      [91] val eDb = SpatialDbManager.getExtensionDb()
      [95] var inserted = 0; var skipped = 0; var errors = 0; var fetched = 0; var rejected = 0
      [96] var offset = 0; var hasMore = true
      [99] val url = buildUrl(source, south, west, north, east, offset)
      [106] val json = httpGet(url)
      [109] val root = try { JSONObject(json) } catch (_: Exception) { errors++; break }
      [115] val features = root.optJSONArray("features") ?: JSONArray()
      [118] val pageCount = features.length()
      [123] val feat = features.getJSONObject(i)
    CALLS OUT:
      → ImportResult
      → JSONArray
      → JSONObject
      → Log.e
      → Log.i
      → SpatialDbManager.getExtensionDb
      → SpatialDbManager.getSpatialDb
      → SpatialDbManager.init
      → applyMigrationIfNeeded
      → buildUrl
      → coords.length
      → coords.optJSONArray
      → eDb.beginTransaction
      → feat.optJSONObject
      → features.getJSONObject

  [79] private suspend fun importFromSource(
    WORK FIELDS (local computed values):
      [83] val catalog = loadCatalog(context)
      [85] val source = catalog.firstOrNull { it.id == sourceId }
      [90] val sDb = SpatialDbManager.getSpatialDb()
      [91] val eDb = SpatialDbManager.getExtensionDb()
      [95] var inserted = 0; var skipped = 0; var errors = 0; var fetched = 0; var rejected = 0
      [96] var offset = 0; var hasMore = true
      [99] val url = buildUrl(source, south, west, north, east, offset)
      [106] val json = httpGet(url)
      [109] val root = try { JSONObject(json) } catch (_: Exception) { errors++; break }
      [115] val features = root.optJSONArray("features") ?: JSONArray()
      [118] val pageCount = features.length()
      [123] val feat = features.getJSONObject(i)
    CALLS OUT:
      → ImportResult
      → JSONArray
      → JSONObject
      → Log.e
      → Log.i
      → SpatialDbManager.getExtensionDb
      → SpatialDbManager.getSpatialDb
      → SpatialDbManager.init
      → applyMigrationIfNeeded
      → buildUrl
      → coords.length
      → coords.optJSONArray
      → eDb.beginTransaction
      → eDb.endTransaction
      → eDb.setTransactionSuccessful

  [175] private fun buildUrl(src: Src, s: Double?, w: Double?, n: Double?, e: Double?, offset: Int): String {
    WORK FIELDS (local computed values):
      [176] val sb = StringBuilder(src.queryUrl)
      [190] val conn = URL(urlStr).openConnection() as HttpURLConnection
      [202] val props = feature.optJSONObject("properties") ?: return "error"
      [203] val geom = feature.optJSONObject("geometry") ?: return "error"
      [204] val uid = props.optString(src.fieldId, "").ifEmpty { return "error" }
      [207] val cur = eDb.rawQuery(
      [211] val exists = cur.moveToFirst(); cur.close()
      [214] val name = props.optString(src.fieldName, "").ifEmpty { null }
      [215] val wkt = geojsonGeomToWkt(geom)
      [216] val bbox = computeBbox(geom)
      [217] val tid = UUID.randomUUID().toString()
      [218] val now = Instant.now().toString()
    CALLS OUT:
      → Instant.now
      → LINESTRING
      → Log.e
      → MULTILINESTRING
      → POINT
      → URL
      → UUID.randomUUID
      → arrayOf
      → computeBbox
      → conn.setRequestProperty
      → coordRingToWkt
      → coords.length
      → cur.close
      → cur.moveToFirst
      → eDb.execSQL

  [188] private fun httpGet(urlStr: String): String? {
    WORK FIELDS (local computed values):
      [190] val conn = URL(urlStr).openConnection() as HttpURLConnection
      [202] val props = feature.optJSONObject("properties") ?: return "error"
      [203] val geom = feature.optJSONObject("geometry") ?: return "error"
      [204] val uid = props.optString(src.fieldId, "").ifEmpty { return "error" }
      [207] val cur = eDb.rawQuery(
      [211] val exists = cur.moveToFirst(); cur.close()
      [214] val name = props.optString(src.fieldName, "").ifEmpty { null }
      [215] val wkt = geojsonGeomToWkt(geom)
      [216] val bbox = computeBbox(geom)
      [217] val tid = UUID.randomUUID().toString()
      [218] val now = Instant.now().toString()
      [225] val motorized = props.optString(src.fieldExtras.getOrDefault("motorized", ""), "")
    CALLS OUT:
      → Instant.now
      → JSONArray
      → LINESTRING
      → Log.e
      → MULTILINESTRING
      → POINT
      → URL
      → UUID.randomUUID
      → arr.getJSONArray
      → arr.length
      → arrayOf
      → computeBbox
      → conn.setRequestProperty
      → coordRingToWkt
      → coords.length

  [201] private fun insertFeature(sDb: SQLiteDatabase, eDb: SQLiteDatabase, feature: JSONObject, src: Src): String {
    WORK FIELDS (local computed values):
      [202] val props = feature.optJSONObject("properties") ?: return "error"
      [203] val geom = feature.optJSONObject("geometry") ?: return "error"
      [204] val uid = props.optString(src.fieldId, "").ifEmpty { return "error" }
      [207] val cur = eDb.rawQuery(
      [211] val exists = cur.moveToFirst(); cur.close()
      [214] val name = props.optString(src.fieldName, "").ifEmpty { null }
      [215] val wkt = geojsonGeomToWkt(geom)
      [216] val bbox = computeBbox(geom)
      [217] val tid = UUID.randomUUID().toString()
      [218] val now = Instant.now().toString()
      [225] val motorized = props.optString(src.fieldExtras.getOrDefault("motorized", ""), "")
      [226] val surface = props.optString(src.fieldExtras.getOrDefault("surface", ""), "")
    CALLS OUT:
      → Instant.now
      → JSONArray
      → LINESTRING
      → MULTILINESTRING
      → POINT
      → Pair
      → UUID.randomUUID
      → arr.getJSONArray
      → arr.length
      → arr.opt
      → arrayOf
      → computeBbox
      → coordRingToWkt
      → coords.length
      → cur.close

  [241] fun geojsonGeomToWkt(geom: JSONObject): String {
    WORK FIELDS (local computed values):
      [242] val type = geom.optString("type")
      [243] val coords = geom.optJSONArray("coordinates") ?: return ""
      [247] val lines = (0 until coords.length()).map { i ->
      [259] val c = arr.getJSONArray(i)
      [265] val pts = mutableListOf<Pair<Double, Double>>()
      [268] var minLat = Double.MAX_VALUE; var maxLat = -Double.MAX_VALUE
      [269] var minLon = Double.MAX_VALUE; var maxLon = -Double.MAX_VALUE
      [296] val dir = File(
      [302] val isFullSource = (south == null && west == null && north == null && east == null)
      [303] val json = JSONObject().apply {
      [312] val catJson = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [313] val sources = JSONObject(catJson).getJSONArray("sources")
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → JSONArray
      → JSONObject
      → LINESTRING
      → MULTILINESTRING
      → POINT
      → Pair
      → arr.getJSONArray
      → arr.length
      → arr.opt
      → arr.optJSONArray
      → assets.open
      → bufferedReader
      → computeBbox
      → coordRingToWkt

  [257] private fun coordRingToWkt(arr: JSONArray): String {
    WORK FIELDS (local computed values):
      [259] val c = arr.getJSONArray(i)
      [265] val pts = mutableListOf<Pair<Double, Double>>()
      [268] var minLat = Double.MAX_VALUE; var maxLat = -Double.MAX_VALUE
      [269] var minLon = Double.MAX_VALUE; var maxLon = -Double.MAX_VALUE
      [296] val dir = File(
      [302] val isFullSource = (south == null && west == null && north == null && east == null)
      [303] val json = JSONObject().apply {
      [312] val catJson = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [313] val sources = JSONObject(catJson).getJSONArray("sources")
      [315] val s = sources.getJSONObject(j)
      [317] val b = s.optJSONObject("boundary")
      [332] val filename = if (isFullSource)
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → JSONArray
      → JSONObject
      → LocalDateTime.now
      → Pair
      → arr.getJSONArray
      → arr.length
      → arr.opt
      → arr.optJSONArray
      → assets.open
      → bufferedReader
      → computeBbox
      → dir.exists
      → dir.mkdirs
      → doubleArrayOf

  [264] fun computeBbox(geom: JSONObject): DoubleArray {
    WORK FIELDS (local computed values):
      [265] val pts = mutableListOf<Pair<Double, Double>>()
      [268] var minLat = Double.MAX_VALUE; var maxLat = -Double.MAX_VALUE
      [269] var minLon = Double.MAX_VALUE; var maxLon = -Double.MAX_VALUE
      [296] val dir = File(
      [302] val isFullSource = (south == null && west == null && north == null && east == null)
      [303] val json = JSONObject().apply {
      [312] val catJson = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [313] val sources = JSONObject(catJson).getJSONArray("sources")
      [315] val s = sources.getJSONObject(j)
      [317] val b = s.optJSONObject("boundary")
      [332] val filename = if (isFullSource)
      [335] val ts = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyyMMdd_HHmmss"))
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → File
      → JSONArray
      → JSONObject
      → LocalDateTime.now
      → Log.i
      → Log.w
      → Pair
      → arr.length
      → arr.opt
      → arr.optJSONArray
      → assets.open
      → bufferedReader
      → dir.exists
      → dir.mkdirs

  [277] private fun extractCoords(arr: JSONArray, out: MutableList<Pair<Double, Double>>) {
    WORK FIELDS (local computed values):
      [296] val dir = File(
      [302] val isFullSource = (south == null && west == null && north == null && east == null)
      [303] val json = JSONObject().apply {
      [312] val catJson = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [313] val sources = JSONObject(catJson).getJSONArray("sources")
      [315] val s = sources.getJSONObject(j)
      [317] val b = s.optJSONObject("boundary")
      [332] val filename = if (isFullSource)
      [335] val ts = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyyMMdd_HHmmss"))
      [348] val dir = File(
      [354] val json = JSONObject().apply {
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → File
      → JSONObject
      → LocalDateTime.now
      → Log.i
      → Log.w
      → Pair
      → arr.length
      → arr.opt
      → arr.optJSONArray
      → assets.open
      → bufferedReader
      → dir.exists
      → dir.mkdirs
      → extractCoords

  [291] private fun writeTrailAreaJson(
    WORK FIELDS (local computed values):
      [296] val dir = File(
      [302] val isFullSource = (south == null && west == null && north == null && east == null)
      [303] val json = JSONObject().apply {
      [312] val catJson = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [313] val sources = JSONObject(catJson).getJSONArray("sources")
      [315] val s = sources.getJSONObject(j)
      [317] val b = s.optJSONObject("boundary")
      [332] val filename = if (isFullSource)
      [335] val ts = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyyMMdd_HHmmss"))
      [348] val dir = File(
      [354] val json = JSONObject().apply {
      [371] val f = File(
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → File
      → JSONObject
      → LocalDateTime.now
      → Log.i
      → Log.w
      → assets.open
      → bufferedReader
      → dir.exists
      → dir.mkdirs
      → getJSONArray
      → it.readText
      → put
      → readPendingArea
      → s.optJSONObject

  [346] fun writePendingArea(north: Double, south: Double, east: Double, west: Double) {
    WORK FIELDS (local computed values):
      [348] val dir = File(
      [354] val json = JSONObject().apply {
      [371] val f = File(
      [394] val dir = File(
      [411] val f = File(
      [417] val j = JSONObject(f.readText())
      [425] val bounds = if (s != null) "{\"s\":$s,\"w\":$w,\"n\":$n,\"e\":$e}" else null
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → File
      → JSONObject
      → Log.i
      → Log.w
      → clearPendingArea
      → db.execSQL
      → dir.exists
      → dir.listFiles
      → dir.mkdirs
      → f.exists
      → isSourceFullyImported
      → it.delete
      → it.exists
      → put

  [369] fun readPendingArea(): JSONObject? {
    WORK FIELDS (local computed values):
      [371] val f = File(
      [394] val dir = File(
      [411] val f = File(
      [417] val j = JSONObject(f.readText())
      [425] val bounds = if (s != null) "{\"s\":$s,\"w\":$w,\"n\":$n,\"e\":$e}" else null
      [435] val db = SpatialDbManager.getSpatialDb() ?: return
      [436] var ver = 0
      [438] val cur = db.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [447] val sql = context.assets.open("schema_spatial_v1_to_v2.sql").bufferedReader().use { it.readText() }
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Instant.now
      → JSONObject
      → Log.i
      → Log.w
      → SpatialDbManager.getSpatialDb
      → UUID.randomUUID
      → applyMigrationIfNeeded
      → assets.open
      → bufferedReader
      → clearPendingArea
      → cur.close
      → cur.moveToFirst
      → db.execSQL
      → db.rawQuery

  [381] fun clearPendingArea() {
    WORK FIELDS (local computed values):
      [394] val dir = File(
      [411] val f = File(
      [417] val j = JSONObject(f.readText())
      [425] val bounds = if (s != null) "{\"s\":$s,\"w\":$w,\"n\":$n,\"e\":$e}" else null
      [435] val db = SpatialDbManager.getSpatialDb() ?: return
      [436] var ver = 0
      [438] val cur = db.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [447] val sql = context.assets.open("schema_spatial_v1_to_v2.sql").bufferedReader().use { it.readText() }
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Instant.now
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → SpatialDbManager.getSpatialDb
      → UUID.randomUUID
      → applyMigrationIfNeeded
      → assets.open
      → bufferedReader
      → cur.close
      → cur.moveToFirst
      → datetime
      → db.execSQL

  [392] fun scanTrailAreas(): List<JSONObject> {
    WORK FIELDS (local computed values):
      [394] val dir = File(
      [411] val f = File(
      [417] val j = JSONObject(f.readText())
      [425] val bounds = if (s != null) "{\"s\":$s,\"w\":$w,\"n\":$n,\"e\":$e}" else null
      [435] val db = SpatialDbManager.getSpatialDb() ?: return
      [436] var ver = 0
      [438] val cur = db.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [447] val sql = context.assets.open("schema_spatial_v1_to_v2.sql").bufferedReader().use { it.readText() }
      [470] val json = context.assets.open(assetFile).bufferedReader().use { it.readText() }
      [471] val root = JSONObject(json)
      [472] val features = root.getJSONArray("features")
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Instant.now
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → SpatialDbManager.getSpatialDb
      → SpatialDbManager.init
      → UUID.randomUUID
      → applyMigrationIfNeeded
      → assets.open
      → bufferedReader
      → cur.close
      → cur.moveToFirst
      → datetime

  [409] fun isSourceFullyImported(sourceId: String): Boolean {
    WORK FIELDS (local computed values):
      [411] val f = File(
      [417] val j = JSONObject(f.readText())
      [425] val bounds = if (s != null) "{\"s\":$s,\"w\":$w,\"n\":$n,\"e\":$e}" else null
      [435] val db = SpatialDbManager.getSpatialDb() ?: return
      [436] var ver = 0
      [438] val cur = db.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [447] val sql = context.assets.open("schema_spatial_v1_to_v2.sql").bufferedReader().use { it.readText() }
      [470] val json = context.assets.open(assetFile).bufferedReader().use { it.readText() }
      [471] val root = JSONObject(json)
      [472] val features = root.getJSONArray("features")
      [473] val total = features.length()
      [474] var inserted = 0
    CALLS OUT:
      → Environment.getExternalStoragePublicDirectory
      → Instant.now
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → SpatialDbManager.getSpatialDb
      → SpatialDbManager.init
      → UUID.randomUUID
      → applyMigrationIfNeeded
      → assets.open
      → bufferedReader
      → cur.close
      → cur.moveToFirst
      → datetime

  [423] private fun logIngestion(db: SQLiteDatabase, sid: String, count: Int, dupes: Int,
    WORK FIELDS (local computed values):
      [425] val bounds = if (s != null) "{\"s\":$s,\"w\":$w,\"n\":$n,\"e\":$e}" else null
      [435] val db = SpatialDbManager.getSpatialDb() ?: return
      [436] var ver = 0
      [438] val cur = db.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [447] val sql = context.assets.open("schema_spatial_v1_to_v2.sql").bufferedReader().use { it.readText() }
      [470] val json = context.assets.open(assetFile).bufferedReader().use { it.readText() }
      [471] val root = JSONObject(json)
      [472] val features = root.getJSONArray("features")
      [473] val total = features.length()
      [474] var inserted = 0
      [475] var skipped = 0
      [476] var outsideArea = 0
    CALLS OUT:
      → Instant.now
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → SpatialDbManager.getSpatialDb
      → SpatialDbManager.init
      → SpatialDbManager.insertWaypointWithId
      → UUID.randomUUID
      → applyMigrationIfNeeded
      → assets.open
      → bufferedReader
      → cur.close
      → cur.moveToFirst
      → datetime

  [434] private fun applyMigrationIfNeeded(context: Context) {
    WORK FIELDS (local computed values):
      [435] val db = SpatialDbManager.getSpatialDb() ?: return
      [436] var ver = 0
      [438] val cur = db.rawQuery("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1", null)
      [447] val sql = context.assets.open("schema_spatial_v1_to_v2.sql").bufferedReader().use { it.readText() }
      [470] val json = context.assets.open(assetFile).bufferedReader().use { it.readText() }
      [471] val root = JSONObject(json)
      [472] val features = root.getJSONArray("features")
      [473] val total = features.length()
      [474] var inserted = 0
      [475] var skipped = 0
      [476] var outsideArea = 0
      [478] val feature = features.getJSONObject(i)
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → Log.w
      → SpatialDbManager.getSpatialDb
      → SpatialDbManager.init
      → SpatialDbManager.insertWaypointWithId
      → assets.open
      → bufferedReader
      → cur.close
      → cur.moveToFirst
      → datetime
      → db.execSQL
      → db.rawQuery
      → feature.getJSONObject

  [459] fun importTrailheads(
    WORK FIELDS (local computed values):
      [470] val json = context.assets.open(assetFile).bufferedReader().use { it.readText() }
      [471] val root = JSONObject(json)
      [472] val features = root.getJSONArray("features")
      [473] val total = features.length()
      [474] var inserted = 0
      [475] var skipped = 0
      [476] var outsideArea = 0
      [478] val feature = features.getJSONObject(i)
      [479] val props = feature.getJSONObject("properties")
      [480] val geom = feature.getJSONObject("geometry")
      [481] val coords = geom.getJSONArray("coordinates")
      [482] val lon = coords.getDouble(0)
    CALLS OUT:
      → JSONObject
      → Log.e
      → Log.i
      → SpatialDbManager.init
      → SpatialDbManager.insertWaypointWithId
      → assets.open
      → bufferedReader
      → feature.getJSONObject
      → features.getJSONObject
      → features.length
      → geom.getJSONArray
      → it.readText
      → loadCatalog
      → root.getJSONArray

  [538] private fun loadCatalog(context: Context): List<Src>? {
    WORK FIELDS (local computed values):
      [540] val json = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
      [541] val root = JSONObject(json)
      [542] val sources = root.getJSONArray("sources")
      [544] val s = sources.getJSONObject(i)
      [546] val f = s.getJSONObject("fields")
      [547] val extras = mutableMapOf<String, String>()
    CALLS OUT:
      → JSONObject
      → Log.e
      → Src
      → assets.open
      → bufferedReader
      → f.keys
      → it.readText
      → listOf
      → root.getJSONArray
      → s.getJSONObject
      → sources.getJSONObject
      → sources.length

========================================
FILE: WaypointManager.kt
========================================

  [10] fun stub() { /* Pass 1 scaffold -- functions in Pass 2 */ }

================================================================================
SECTION 5: FIELD-LEVEL DATA FLOW — WRITERS vs READERS
================================================================================

--- 5A: StateFlow and MutableState Fields ---

  FIELD: _convoyState (public: convoyState)
  DECLARED: ConvoyViewModel.kt:55
  private val _convoyState = MutableStateFlow(ConvoyEngine.ConvoyState.empty())

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:199:            val name = _convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }?.callsign ?: nodeId
      [W] ConvoyViewModel.kt:406:        _convoyState.value = current.copy(nodes = current.nodes.filter { it.nodeId != nodeId })
      [W] ConvoyViewModel.kt:563:        _convoyState.value = state
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:114:    val convoyState by viewModel.convoyState.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:218:    LaunchedEffect(convoyState) {
      [R] convoy/ConvoyScreen.kt:220:        val validNodes = convoyState.nodes.filter { it.latitude != 0.0 && it.longitude != 0.0 }
      [R] convoy/ConvoyScreen.kt:235:    LaunchedEffect(convoyState) {
      [R] convoy/ConvoyScreen.kt:238:        val validNodes = convoyState.nodes.filter { it.latitude != 0.0 && it.longitude != 0.0 }
      [R] convoy/ConvoyScreen.kt:252:            val myCart = convoyState.nodes.firstOrNull { it.nodeId == myCartId }
      [R] convoy/ConvoyScreen.kt:261:    LaunchedEffect(hudMode, selectedNode, mapReady, autoPan, if (autoPan) convoyState else null) {
      [R] convoy/ConvoyScreen.kt:264:        val nodes = convoyState.nodes
      [R] convoy/ConvoyScreen.kt:522:                            val node = viewModel.convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }
      [R] convoy/ConvoyScreen.kt:642:                                val node = viewModel.convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }
      [R] convoy/ConvoyScreen.kt:777:                                val node = viewModel.convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }
      [R] convoy/ConvoyScreen.kt:833:            val dialogNodes = convoyState.nodes
      [R] convoy/ConvoyScreen.kt:910:                                        val meshNodes = viewModel.convoyState.value.nodes
      [R] convoy/ConvoyScreen.kt:999:        if (convoyState.hasLost && hudMode != HudMode.COLLAPSED) {
      [R] convoy/ConvoyScreen.kt:1000:            val lostNames = convoyState.nodes

  FIELD: _convoyState (public: convoyState)
  DECLARED: ConvoyViewModel.kt:56
  val convoyState: StateFlow<ConvoyEngine.ConvoyState> = _convoyState.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:199:            val name = _convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }?.callsign ?: nodeId
      [W] ConvoyViewModel.kt:406:        _convoyState.value = current.copy(nodes = current.nodes.filter { it.nodeId != nodeId })
      [W] ConvoyViewModel.kt:563:        _convoyState.value = state
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:114:    val convoyState by viewModel.convoyState.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:218:    LaunchedEffect(convoyState) {
      [R] convoy/ConvoyScreen.kt:220:        val validNodes = convoyState.nodes.filter { it.latitude != 0.0 && it.longitude != 0.0 }
      [R] convoy/ConvoyScreen.kt:235:    LaunchedEffect(convoyState) {
      [R] convoy/ConvoyScreen.kt:238:        val validNodes = convoyState.nodes.filter { it.latitude != 0.0 && it.longitude != 0.0 }
      [R] convoy/ConvoyScreen.kt:252:            val myCart = convoyState.nodes.firstOrNull { it.nodeId == myCartId }
      [R] convoy/ConvoyScreen.kt:261:    LaunchedEffect(hudMode, selectedNode, mapReady, autoPan, if (autoPan) convoyState else null) {
      [R] convoy/ConvoyScreen.kt:264:        val nodes = convoyState.nodes
      [R] convoy/ConvoyScreen.kt:522:                            val node = viewModel.convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }
      [R] convoy/ConvoyScreen.kt:642:                                val node = viewModel.convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }
      [R] convoy/ConvoyScreen.kt:777:                                val node = viewModel.convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }
      [R] convoy/ConvoyScreen.kt:833:            val dialogNodes = convoyState.nodes
      [R] convoy/ConvoyScreen.kt:910:                                        val meshNodes = viewModel.convoyState.value.nodes
      [R] convoy/ConvoyScreen.kt:999:        if (convoyState.hasLost && hudMode != HudMode.COLLAPSED) {
      [R] convoy/ConvoyScreen.kt:1000:            val lostNames = convoyState.nodes

  FIELD: _rideState (public: rideState)
  DECLARED: ConvoyViewModel.kt:57
  private val _rideState = MutableStateFlow(RideState.SOLO)

    WRITERS (functions that CHANGE this field):
    READERS (functions that USE this field):
      [R] convoy/ConvoyViewModel.kt:58:    val rideState: StateFlow<RideState> = _rideState.asStateFlow()

  FIELD: _rideState (public: rideState)
  DECLARED: ConvoyViewModel.kt:58
  val rideState: StateFlow<RideState> = _rideState.asStateFlow()

    WRITERS (functions that CHANGE this field):
    READERS (functions that USE this field):
      [R] convoy/ConvoyViewModel.kt:58:    val rideState: StateFlow<RideState> = _rideState.asStateFlow()

  FIELD: _activeRide (public: activeRide)
  DECLARED: ConvoyViewModel.kt:59
  private val _activeRide = MutableStateFlow<ConvoyEventConfig?>(null)

    WRITERS (functions that CHANGE this field):
    READERS (functions that USE this field):
      [R] convoy/ConvoyViewModel.kt:60:    val activeRide: StateFlow<ConvoyEventConfig?> = _activeRide.asStateFlow()

  FIELD: _activeRide (public: activeRide)
  DECLARED: ConvoyViewModel.kt:60
  val activeRide: StateFlow<ConvoyEventConfig?> = _activeRide.asStateFlow()

    WRITERS (functions that CHANGE this field):
    READERS (functions that USE this field):
      [R] convoy/ConvoyViewModel.kt:60:    val activeRide: StateFlow<ConvoyEventConfig?> = _activeRide.asStateFlow()

  FIELD: _myNodeInfo (public: myNodeInfo)
  DECLARED: ConvoyViewModel.kt:61
  private val _myNodeInfo = MutableStateFlow<MyNodeInfo?>(null)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:330:                _myNodeInfo.value = info
    READERS (functions that USE this field):
      [R] convoy/ConvoyApplyRadioScreen.kt:44:    val myNodeInfo  by uiViewModel.myNodeInfo.collectAsStateWithLifecycle()
      [R] convoy/ConvoyApplyRadioScreen.kt:47:    val isConnected = myNodeInfo != null
      [R] convoy/ConvoyApplyRadioScreen.kt:73:        else -> myNodeInfo?.let { "!%08x".format(it.myNodeNum) } ?: "—"
      [R] convoy/ConvoyApplyRadioScreen.kt:99:                        Text("  ${myNodeInfo?.model ?: ""}  fw ${myNodeInfo?.firmwareVersion ?: ""}",
      [R] convoy/ConvoyApplyRadioScreen.kt:263:                            Triple(LoraField.BANDWIDTH,     if ((myNodeInfo?.firmwareVersion ?: "").startsWith("2.6")) masterConfig?.loraBandwidth?.toString() ?: "—" else "—", masterConfig?.loraBandwidth?.toString() ?: "—"),
      [R] convoy/ConvoyApplyRadioScreen.kt:264:                            Triple(LoraField.SPREAD_FACTOR, if ((myNodeInfo?.firmwareVersion ?: "").startsWith("2.6")) masterConfig?.loraSpreadFactor?.toString() ?: "—" else "—", masterConfig?.loraSpreadFactor?.toString() ?: "—"),
      [R] convoy/ConvoyApplyRadioScreen.kt:265:                            Triple(LoraField.CODING_RATE,   if ((myNodeInfo?.firmwareVersion ?: "").startsWith("2.6")) masterConfig?.loraCodingRate?.toString() ?: "—" else "—", masterConfig?.loraCodingRate?.toString() ?: "—"),
      [R] convoy/ConvoyApplyRadioScreen.kt:401:                            val ni = myNodeInfo ?: return@clickable
      [R] convoy/ConvoyApplyRadioScreen.kt:500:                            val fwVersion = myNodeInfo?.firmwareVersion ?: ""
      [R] convoy/ConvoyArchiveRestoreScreen.kt:41: *   1. Read nodeId from myNodeInfo
      [R] convoy/ConvoyArchiveRestoreScreen.kt:59:    val myNodeInfo      by convoyViewModel.myNodeInfo.collectAsStateWithLifecycle()
      [R] convoy/ConvoyArchiveRestoreScreen.kt:62:    val nodeId   = myNodeInfo?.myNodeNum?.let { "!%08x".format(it) } ?: ""
      [R] convoy/ConvoyArchiveRestoreScreen.kt:112:                val nodeNum = myNodeInfo?.myNodeNum
      [R] convoy/ConvoyCreateEventScreen.kt:290:                                val nodeInfo = viewModel.myNodeInfo.value
      [R] convoy/ConvoyCreateRideScreen.kt:274:                                val nodeInfo = viewModel?.myNodeInfo?.value

  FIELD: _myNodeInfo (public: myNodeInfo)
  DECLARED: ConvoyViewModel.kt:62
  val myNodeInfo: StateFlow<MyNodeInfo?> = _myNodeInfo.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:330:                _myNodeInfo.value = info
    READERS (functions that USE this field):
      [R] convoy/ConvoyApplyRadioScreen.kt:44:    val myNodeInfo  by uiViewModel.myNodeInfo.collectAsStateWithLifecycle()
      [R] convoy/ConvoyApplyRadioScreen.kt:47:    val isConnected = myNodeInfo != null
      [R] convoy/ConvoyApplyRadioScreen.kt:73:        else -> myNodeInfo?.let { "!%08x".format(it.myNodeNum) } ?: "—"
      [R] convoy/ConvoyApplyRadioScreen.kt:99:                        Text("  ${myNodeInfo?.model ?: ""}  fw ${myNodeInfo?.firmwareVersion ?: ""}",
      [R] convoy/ConvoyApplyRadioScreen.kt:263:                            Triple(LoraField.BANDWIDTH,     if ((myNodeInfo?.firmwareVersion ?: "").startsWith("2.6")) masterConfig?.loraBandwidth?.toString() ?: "—" else "—", masterConfig?.loraBandwidth?.toString() ?: "—"),
      [R] convoy/ConvoyApplyRadioScreen.kt:264:                            Triple(LoraField.SPREAD_FACTOR, if ((myNodeInfo?.firmwareVersion ?: "").startsWith("2.6")) masterConfig?.loraSpreadFactor?.toString() ?: "—" else "—", masterConfig?.loraSpreadFactor?.toString() ?: "—"),
      [R] convoy/ConvoyApplyRadioScreen.kt:265:                            Triple(LoraField.CODING_RATE,   if ((myNodeInfo?.firmwareVersion ?: "").startsWith("2.6")) masterConfig?.loraCodingRate?.toString() ?: "—" else "—", masterConfig?.loraCodingRate?.toString() ?: "—"),
      [R] convoy/ConvoyApplyRadioScreen.kt:401:                            val ni = myNodeInfo ?: return@clickable
      [R] convoy/ConvoyApplyRadioScreen.kt:500:                            val fwVersion = myNodeInfo?.firmwareVersion ?: ""
      [R] convoy/ConvoyArchiveRestoreScreen.kt:41: *   1. Read nodeId from myNodeInfo
      [R] convoy/ConvoyArchiveRestoreScreen.kt:59:    val myNodeInfo      by convoyViewModel.myNodeInfo.collectAsStateWithLifecycle()
      [R] convoy/ConvoyArchiveRestoreScreen.kt:62:    val nodeId   = myNodeInfo?.myNodeNum?.let { "!%08x".format(it) } ?: ""
      [R] convoy/ConvoyArchiveRestoreScreen.kt:112:                val nodeNum = myNodeInfo?.myNodeNum
      [R] convoy/ConvoyCreateEventScreen.kt:290:                                val nodeInfo = viewModel.myNodeInfo.value
      [R] convoy/ConvoyCreateRideScreen.kt:274:                                val nodeInfo = viewModel?.myNodeInfo?.value

  FIELD: _hudMode (public: hudMode)
  DECLARED: ConvoyViewModel.kt:65
  private val _hudMode = MutableStateFlow(HudMode.GROUP)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:377:    fun setHudMode(mode: HudMode) { _hudMode.value = mode }
      [W] ConvoyViewModel.kt:378:    fun onMarkerTapped(node: ConvoyNode) { _selectedNode.value = node; _hudMode.value = HudMode.NODE }
      [W] ConvoyViewModel.kt:379:    fun dismissNodeHud() { _selectedNode.value = null; _hudMode.value = HudMode.GROUP }
      [W] ConvoyViewModel.kt:404:        _hudMode.value = HudMode.GROUP
      [W] ConvoyViewModel.kt:776:        if (_hudMode.value == HudMode.NODE && _selectedNode.value != null) {
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:115:    val hudMode by viewModel.hudMode.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:261:    LaunchedEffect(hudMode, selectedNode, mapReady, autoPan, if (autoPan) convoyState else null) {
      [R] convoy/ConvoyScreen.kt:265:        when (hudMode) {
      [R] convoy/ConvoyScreen.kt:999:        if (convoyState.hasLost && hudMode != HudMode.COLLAPSED) {
      [R] convoy/ConvoyScreen.kt:1383:            hudMode = hudMode,
      [R] convoy/ConvoyScreen.kt:1405:            when (hudMode) {
      [R] convoy/ConvoyScreen.kt:1933:    hudMode: HudMode,
      [R] convoy/ConvoyScreen.kt:1958:            val isActive = hudMode == mode
      [R] convoy/ConvoyViewModel.kt:66:    val hudMode: StateFlow<HudMode> = _hudMode.asStateFlow()

  FIELD: _hudMode (public: hudMode)
  DECLARED: ConvoyViewModel.kt:66
  val hudMode: StateFlow<HudMode> = _hudMode.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:377:    fun setHudMode(mode: HudMode) { _hudMode.value = mode }
      [W] ConvoyViewModel.kt:378:    fun onMarkerTapped(node: ConvoyNode) { _selectedNode.value = node; _hudMode.value = HudMode.NODE }
      [W] ConvoyViewModel.kt:379:    fun dismissNodeHud() { _selectedNode.value = null; _hudMode.value = HudMode.GROUP }
      [W] ConvoyViewModel.kt:404:        _hudMode.value = HudMode.GROUP
      [W] ConvoyViewModel.kt:776:        if (_hudMode.value == HudMode.NODE && _selectedNode.value != null) {
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:115:    val hudMode by viewModel.hudMode.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:261:    LaunchedEffect(hudMode, selectedNode, mapReady, autoPan, if (autoPan) convoyState else null) {
      [R] convoy/ConvoyScreen.kt:265:        when (hudMode) {
      [R] convoy/ConvoyScreen.kt:999:        if (convoyState.hasLost && hudMode != HudMode.COLLAPSED) {
      [R] convoy/ConvoyScreen.kt:1383:            hudMode = hudMode,
      [R] convoy/ConvoyScreen.kt:1405:            when (hudMode) {
      [R] convoy/ConvoyScreen.kt:1933:    hudMode: HudMode,
      [R] convoy/ConvoyScreen.kt:1958:            val isActive = hudMode == mode
      [R] convoy/ConvoyViewModel.kt:66:    val hudMode: StateFlow<HudMode> = _hudMode.asStateFlow()

  FIELD: _pendingImportBanner (public: pendingImportBanner)
  DECLARED: ConvoyViewModel.kt:67
  private val _pendingImportBanner = MutableStateFlow<String?>(null)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:69:    fun clearImportBanner() { _pendingImportBanner.value = null }
      [W] ConvoyViewModel.kt:370:                _pendingImportBanner.value = msg
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:189:    val pendingImportBanner by viewModel.pendingImportBanner.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1334:        if (showConvoyMenu && showImportSplash && pendingImportBanner != null) {
      [R] convoy/ConvoyScreen.kt:1352:                        androidx.compose.material3.Text(pendingImportBanner!!,
      [R] convoy/ConvoyViewModel.kt:68:    val pendingImportBanner: StateFlow<String?> = _pendingImportBanner.asStateFlow()

  FIELD: _pendingImportBanner (public: pendingImportBanner)
  DECLARED: ConvoyViewModel.kt:68
  val pendingImportBanner: StateFlow<String?> = _pendingImportBanner.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:69:    fun clearImportBanner() { _pendingImportBanner.value = null }
      [W] ConvoyViewModel.kt:370:                _pendingImportBanner.value = msg
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:189:    val pendingImportBanner by viewModel.pendingImportBanner.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1334:        if (showConvoyMenu && showImportSplash && pendingImportBanner != null) {
      [R] convoy/ConvoyScreen.kt:1352:                        androidx.compose.material3.Text(pendingImportBanner!!,
      [R] convoy/ConvoyViewModel.kt:68:    val pendingImportBanner: StateFlow<String?> = _pendingImportBanner.asStateFlow()

  FIELD: _selectedNode (public: selectedNode)
  DECLARED: ConvoyViewModel.kt:70
  private val _selectedNode = MutableStateFlow<ConvoyNode?>(null)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:378:    fun onMarkerTapped(node: ConvoyNode) { _selectedNode.value = node; _hudMode.value = HudMode.NODE }
      [W] ConvoyViewModel.kt:379:    fun dismissNodeHud() { _selectedNode.value = null; _hudMode.value = HudMode.GROUP }
      [W] ConvoyViewModel.kt:403:        _selectedNode.value = null
      [W] ConvoyViewModel.kt:776:        if (_hudMode.value == HudMode.NODE && _selectedNode.value != null) {
      [W] ConvoyViewModel.kt:778:            _selectedNode.value = refreshed
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:116:    val selectedNode by viewModel.selectedNode.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:261:    LaunchedEffect(hudMode, selectedNode, mapReady, autoPan, if (autoPan) convoyState else null) {
      [R] convoy/ConvoyScreen.kt:273:                selectedNode?.let {
      [R] convoy/ConvoyScreen.kt:1393:                onSelect = { selectedNode ->
      [R] convoy/ConvoyScreen.kt:1394:                    viewModel.onMarkerTapped(selectedNode)
      [R] convoy/ConvoyScreen.kt:1425:                HudMode.NODE -> selectedNode?.let { node ->
      [R] convoy/ConvoyViewModel.kt:71:    val selectedNode: StateFlow<ConvoyNode?> = _selectedNode.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:776:        if (_hudMode.value == HudMode.NODE && _selectedNode.value != null) {
      [R] convoy/ConvoyViewModel.kt:777:            val refreshed = state.nodes.firstOrNull { it.nodeId == _selectedNode.value?.nodeId }

  FIELD: _selectedNode (public: selectedNode)
  DECLARED: ConvoyViewModel.kt:71
  val selectedNode: StateFlow<ConvoyNode?> = _selectedNode.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:378:    fun onMarkerTapped(node: ConvoyNode) { _selectedNode.value = node; _hudMode.value = HudMode.NODE }
      [W] ConvoyViewModel.kt:379:    fun dismissNodeHud() { _selectedNode.value = null; _hudMode.value = HudMode.GROUP }
      [W] ConvoyViewModel.kt:403:        _selectedNode.value = null
      [W] ConvoyViewModel.kt:776:        if (_hudMode.value == HudMode.NODE && _selectedNode.value != null) {
      [W] ConvoyViewModel.kt:778:            _selectedNode.value = refreshed
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:116:    val selectedNode by viewModel.selectedNode.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:261:    LaunchedEffect(hudMode, selectedNode, mapReady, autoPan, if (autoPan) convoyState else null) {
      [R] convoy/ConvoyScreen.kt:273:                selectedNode?.let {
      [R] convoy/ConvoyScreen.kt:1393:                onSelect = { selectedNode ->
      [R] convoy/ConvoyScreen.kt:1394:                    viewModel.onMarkerTapped(selectedNode)
      [R] convoy/ConvoyScreen.kt:1425:                HudMode.NODE -> selectedNode?.let { node ->
      [R] convoy/ConvoyViewModel.kt:71:    val selectedNode: StateFlow<ConvoyNode?> = _selectedNode.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:776:        if (_hudMode.value == HudMode.NODE && _selectedNode.value != null) {
      [R] convoy/ConvoyViewModel.kt:777:            val refreshed = state.nodes.firstOrNull { it.nodeId == _selectedNode.value?.nodeId }

  FIELD: _offTrackIds (public: offTrackIds)
  DECLARED: ConvoyViewModel.kt:72
  private val _offTrackIds = MutableStateFlow<Set<String>>(emptySet())

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:760:            _offTrackIds.value = offTrack
      [W] ConvoyViewModel.kt:762:            _offTrackIds.value = emptySet()
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:119:    val offTrackIds by viewModel.offTrackIds.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:227:                val isOffTrack = offTrackIds.contains(node.nodeId)
      [R] convoy/ConvoyViewModel.kt:73:    val offTrackIds: StateFlow<Set<String>> = _offTrackIds.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:610:            val isOffTrack = _offTrackIds.value.contains(node.nodeId)
      [R] convoy/ConvoyViewModel.kt:688:                        if (_offTrackIds.value.contains(n.nodeId)) continue

  FIELD: _offTrackIds (public: offTrackIds)
  DECLARED: ConvoyViewModel.kt:73
  val offTrackIds: StateFlow<Set<String>> = _offTrackIds.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:760:            _offTrackIds.value = offTrack
      [W] ConvoyViewModel.kt:762:            _offTrackIds.value = emptySet()
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:119:    val offTrackIds by viewModel.offTrackIds.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:227:                val isOffTrack = offTrackIds.contains(node.nodeId)
      [R] convoy/ConvoyViewModel.kt:73:    val offTrackIds: StateFlow<Set<String>> = _offTrackIds.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:610:            val isOffTrack = _offTrackIds.value.contains(node.nodeId)
      [R] convoy/ConvoyViewModel.kt:688:                        if (_offTrackIds.value.contains(n.nodeId)) continue

  FIELD: _trackActive (public: trackActive)
  DECLARED: ConvoyViewModel.kt:74
  private val _trackActive = MutableStateFlow(false)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:171:        _trackActive.value = true
      [W] ConvoyViewModel.kt:177:        _trackActive.value = false
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:117:    val trackActive by viewModel.trackActive.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1410:                    trackActive = trackActive,
      [R] convoy/ConvoyScreen.kt:1462:    trackActive: Boolean = false,
      [R] convoy/ConvoyViewModel.kt:75:    val trackActive: StateFlow<Boolean> = _trackActive.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:573:        if (_trackActive.value) {
      [R] convoy/ConvoyViewModel.kt:623:                if (_trackActive.value) {
      [R] convoy/ConvoyViewModel.kt:705:                            if (_trackActive.value) {
      [R] convoy/ConvoyViewModel.kt:739:                    if (_trackActive.value) {
      [R] convoy/ConvoyViewModel.kt:750:if (_trackActive.value && _routeTrailSegments.value.isNotEmpty()) {
      [R] convoy/ConvoyViewModel.kt:853:                    if (_trackActive.value) {

  FIELD: _trackActive (public: trackActive)
  DECLARED: ConvoyViewModel.kt:75
  val trackActive: StateFlow<Boolean> = _trackActive.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:171:        _trackActive.value = true
      [W] ConvoyViewModel.kt:177:        _trackActive.value = false
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:117:    val trackActive by viewModel.trackActive.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1410:                    trackActive = trackActive,
      [R] convoy/ConvoyScreen.kt:1462:    trackActive: Boolean = false,
      [R] convoy/ConvoyViewModel.kt:75:    val trackActive: StateFlow<Boolean> = _trackActive.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:573:        if (_trackActive.value) {
      [R] convoy/ConvoyViewModel.kt:623:                if (_trackActive.value) {
      [R] convoy/ConvoyViewModel.kt:705:                            if (_trackActive.value) {
      [R] convoy/ConvoyViewModel.kt:739:                    if (_trackActive.value) {
      [R] convoy/ConvoyViewModel.kt:750:if (_trackActive.value && _routeTrailSegments.value.isNotEmpty()) {
      [R] convoy/ConvoyViewModel.kt:853:                    if (_trackActive.value) {

  FIELD: _leadLockedFlag (public: leadLockedFlag)
  DECLARED: ConvoyViewModel.kt:78
  private var _leadLockedFlag: Boolean = false

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:179:        _leadLockedFlag = false
      [W] ConvoyViewModel.kt:197:            _leadLockedFlag = true
      [W] ConvoyViewModel.kt:203:            _leadLockedFlag = false
    READERS (functions that USE this field):

  FIELD: _leadLocked (public: leadLocked)
  DECLARED: ConvoyViewModel.kt:88
  private val _leadLocked = MutableStateFlow(false)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:179:        _leadLockedFlag = false
      [W] ConvoyViewModel.kt:184:        _leadLocked.value = false
      [W] ConvoyViewModel.kt:197:            _leadLockedFlag = true
      [W] ConvoyViewModel.kt:198:            _leadLocked.value = true
      [W] ConvoyViewModel.kt:203:            _leadLockedFlag = false
      [W] ConvoyViewModel.kt:204:            _leadLocked.value = false
    READERS (functions that USE this field):
      [R] convoy/ConvoyEngine.kt:35:        leadLocked: Boolean = false,
      [R] convoy/ConvoyScreen.kt:904:                                    if (viewModel.leadLocked.value) {
      [R] convoy/ConvoyViewModel.kt:89:    val leadLocked: StateFlow<Boolean> = _leadLocked.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:559:            leadLocked = _leadLockedFlag,

  FIELD: _leadLocked (public: leadLocked)
  DECLARED: ConvoyViewModel.kt:89
  val leadLocked: StateFlow<Boolean> = _leadLocked.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:179:        _leadLockedFlag = false
      [W] ConvoyViewModel.kt:184:        _leadLocked.value = false
      [W] ConvoyViewModel.kt:197:            _leadLockedFlag = true
      [W] ConvoyViewModel.kt:198:            _leadLocked.value = true
      [W] ConvoyViewModel.kt:203:            _leadLockedFlag = false
      [W] ConvoyViewModel.kt:204:            _leadLocked.value = false
    READERS (functions that USE this field):
      [R] convoy/ConvoyEngine.kt:35:        leadLocked: Boolean = false,
      [R] convoy/ConvoyScreen.kt:904:                                    if (viewModel.leadLocked.value) {
      [R] convoy/ConvoyViewModel.kt:89:    val leadLocked: StateFlow<Boolean> = _leadLocked.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:559:            leadLocked = _leadLockedFlag,

  FIELD: _trackLeadOnly (public: trackLeadOnly)
  DECLARED: ConvoyViewModel.kt:91
  private val _trackLeadOnly = MutableStateFlow(true)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:219:        _trackLeadOnly.value = !_trackLeadOnly.value
      [W] ConvoyViewModel.kt:643:        android.util.Log.w("TRACK-DBG", "TICK: leadOnly=${_trackLeadOnly.value} leadId=$lockedLeadNodeId segs=${_routeTrailSegments.value.size} nodes=${state.nodes.size}")
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:118:    val trackLeadOnly by viewModel.trackLeadOnly.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:294:    val trackSegments = remember(rawSegments, gpsTrail, routeTrail, trackLeadOnly) {
      [R] convoy/ConvoyScreen.kt:295:        // Apply lead-only filter — if trackLeadOnly, skip routeTrail (all-cart overlay)
      [R] convoy/ConvoyScreen.kt:296:        val activeSegments = if (trackLeadOnly) rawSegments else (rawSegments + routeTrail)
      [R] convoy/ConvoyScreen.kt:1411:                    trackLeadOnly = trackLeadOnly,
      [R] convoy/ConvoyScreen.kt:1463:    trackLeadOnly: Boolean = true,
      [R] convoy/ConvoySettingsScreen.kt:59:    val trackLeadOnly by convoyViewModel.trackLeadOnly.collectAsStateWithLifecycle()
      [R] convoy/ConvoySettingsScreen.kt:192:                        text = if (trackLeadOnly) "Recording lead cart track only" else "Recording all carts",
      [R] convoy/ConvoySettingsScreen.kt:198:                    checked = trackLeadOnly,
      [R] convoy/ConvoyViewModel.kt:92:    val trackLeadOnly: StateFlow<Boolean> = _trackLeadOnly.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:643:        android.util.Log.w("TRACK-DBG", "TICK: leadOnly=${_trackLeadOnly.value} leadId=$lockedLeadNodeId segs=${_routeTrailSegments.value.size} nodes=${state.nodes.size}")
      [R] convoy/ConvoyViewModel.kt:645:        if (_trackLeadOnly.value) {
      [R] convoy/ConvoyViewModel.kt:767:        _leadTrackSegments.value = if (_trackLeadOnly.value) {

  FIELD: _trackLeadOnly (public: trackLeadOnly)
  DECLARED: ConvoyViewModel.kt:92
  val trackLeadOnly: StateFlow<Boolean> = _trackLeadOnly.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:219:        _trackLeadOnly.value = !_trackLeadOnly.value
      [W] ConvoyViewModel.kt:643:        android.util.Log.w("TRACK-DBG", "TICK: leadOnly=${_trackLeadOnly.value} leadId=$lockedLeadNodeId segs=${_routeTrailSegments.value.size} nodes=${state.nodes.size}")
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:118:    val trackLeadOnly by viewModel.trackLeadOnly.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:294:    val trackSegments = remember(rawSegments, gpsTrail, routeTrail, trackLeadOnly) {
      [R] convoy/ConvoyScreen.kt:295:        // Apply lead-only filter — if trackLeadOnly, skip routeTrail (all-cart overlay)
      [R] convoy/ConvoyScreen.kt:296:        val activeSegments = if (trackLeadOnly) rawSegments else (rawSegments + routeTrail)
      [R] convoy/ConvoyScreen.kt:1411:                    trackLeadOnly = trackLeadOnly,
      [R] convoy/ConvoyScreen.kt:1463:    trackLeadOnly: Boolean = true,
      [R] convoy/ConvoySettingsScreen.kt:59:    val trackLeadOnly by convoyViewModel.trackLeadOnly.collectAsStateWithLifecycle()
      [R] convoy/ConvoySettingsScreen.kt:192:                        text = if (trackLeadOnly) "Recording lead cart track only" else "Recording all carts",
      [R] convoy/ConvoySettingsScreen.kt:198:                    checked = trackLeadOnly,
      [R] convoy/ConvoyViewModel.kt:92:    val trackLeadOnly: StateFlow<Boolean> = _trackLeadOnly.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:643:        android.util.Log.w("TRACK-DBG", "TICK: leadOnly=${_trackLeadOnly.value} leadId=$lockedLeadNodeId segs=${_routeTrailSegments.value.size} nodes=${state.nodes.size}")
      [R] convoy/ConvoyViewModel.kt:645:        if (_trackLeadOnly.value) {
      [R] convoy/ConvoyViewModel.kt:767:        _leadTrackSegments.value = if (_trackLeadOnly.value) {

  FIELD: _isOfflineMode (public: isOfflineMode)
  DECLARED: ConvoyViewModel.kt:94
  private val _isOfflineMode = MutableStateFlow(false)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:96:    fun setOfflineMode(offline: Boolean) { _isOfflineMode.value = offline }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:197:    val isOfflineMode by viewModel.isOfflineMode.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1089:            isOffline = isOfflineMode,
      [R] convoy/ConvoyViewModel.kt:95:    val isOfflineMode: StateFlow<Boolean> = _isOfflineMode.asStateFlow()

  FIELD: _isOfflineMode (public: isOfflineMode)
  DECLARED: ConvoyViewModel.kt:95
  val isOfflineMode: StateFlow<Boolean> = _isOfflineMode.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:96:    fun setOfflineMode(offline: Boolean) { _isOfflineMode.value = offline }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:197:    val isOfflineMode by viewModel.isOfflineMode.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1089:            isOffline = isOfflineMode,
      [R] convoy/ConvoyViewModel.kt:95:    val isOfflineMode: StateFlow<Boolean> = _isOfflineMode.asStateFlow()

  FIELD: _mapTypeLabel (public: mapTypeLabel)
  DECLARED: ConvoyViewModel.kt:97
  private val _mapTypeLabel = MutableStateFlow("SAT")

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:99:    fun setMapTypeLabel(label: String) { _mapTypeLabel.value = label }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:156:    val mapTypeLabel by viewModel.mapTypeLabel.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1088:            activeSource = mapTypeLabel,
      [R] convoy/ConvoyViewModel.kt:98:    val mapTypeLabel: StateFlow<String> = _mapTypeLabel.asStateFlow()

  FIELD: _mapTypeLabel (public: mapTypeLabel)
  DECLARED: ConvoyViewModel.kt:98
  val mapTypeLabel: StateFlow<String> = _mapTypeLabel.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:99:    fun setMapTypeLabel(label: String) { _mapTypeLabel.value = label }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:156:    val mapTypeLabel by viewModel.mapTypeLabel.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1088:            activeSource = mapTypeLabel,
      [R] convoy/ConvoyViewModel.kt:98:    val mapTypeLabel: StateFlow<String> = _mapTypeLabel.asStateFlow()

  FIELD: _isLocalTiles (public: isLocalTiles)
  DECLARED: ConvoyViewModel.kt:100
  private val _isLocalTiles = MutableStateFlow(false)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:102:    fun setLocalTiles(local: Boolean) { _isLocalTiles.value = local }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:157:    val isLocalTiles by viewModel.isLocalTiles.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1093:                val url = if (isLocalTiles)
      [R] convoy/ConvoyScreen.kt:1097:                viewModel.setLocalTiles(isLocalTiles)
      [R] convoy/ConvoyViewModel.kt:101:    val isLocalTiles: StateFlow<Boolean> = _isLocalTiles.asStateFlow()

  FIELD: _isLocalTiles (public: isLocalTiles)
  DECLARED: ConvoyViewModel.kt:101
  val isLocalTiles: StateFlow<Boolean> = _isLocalTiles.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:102:    fun setLocalTiles(local: Boolean) { _isLocalTiles.value = local }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:157:    val isLocalTiles by viewModel.isLocalTiles.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1093:                val url = if (isLocalTiles)
      [R] convoy/ConvoyScreen.kt:1097:                viewModel.setLocalTiles(isLocalTiles)
      [R] convoy/ConvoyViewModel.kt:101:    val isLocalTiles: StateFlow<Boolean> = _isLocalTiles.asStateFlow()

  FIELD: _autoPan (public: autoPan)
  DECLARED: ConvoyViewModel.kt:103
  private val _autoPan = MutableStateFlow(false)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:105:    fun setAutoPan(pan: Boolean) { _autoPan.value = pan }
      [W] ConvoyViewModel.kt:172:        _autoPan.value = true   // autoPan ON during recording
      [W] ConvoyViewModel.kt:178:        _autoPan.value = false   // autoPan OFF when recording stops
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:202:    val autoPan by viewModel.autoPan.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:261:    LaunchedEffect(hudMode, selectedNode, mapReady, autoPan, if (autoPan) convoyState else null) {
      [R] convoy/ConvoyScreen.kt:263:        if (!autoPan) return@LaunchedEffect
      [R] convoy/ConvoyViewModel.kt:104:    val autoPan: StateFlow<Boolean> = _autoPan.asStateFlow()

  FIELD: _autoPan (public: autoPan)
  DECLARED: ConvoyViewModel.kt:104
  val autoPan: StateFlow<Boolean> = _autoPan.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:105:    fun setAutoPan(pan: Boolean) { _autoPan.value = pan }
      [W] ConvoyViewModel.kt:172:        _autoPan.value = true   // autoPan ON during recording
      [W] ConvoyViewModel.kt:178:        _autoPan.value = false   // autoPan OFF when recording stops
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:202:    val autoPan by viewModel.autoPan.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:261:    LaunchedEffect(hudMode, selectedNode, mapReady, autoPan, if (autoPan) convoyState else null) {
      [R] convoy/ConvoyScreen.kt:263:        if (!autoPan) return@LaunchedEffect
      [R] convoy/ConvoyViewModel.kt:104:    val autoPan: StateFlow<Boolean> = _autoPan.asStateFlow()

  FIELD: _radioInactive (public: radioInactive)
  DECLARED: ConvoyViewModel.kt:209
  private val _radioInactive = MutableStateFlow(false)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:214:        _radioInactive.value = false
    READERS (functions that USE this field):
      [R] convoy/ConvoyViewModel.kt:210:    val radioInactive: StateFlow<Boolean> = _radioInactive.asStateFlow()

  FIELD: _radioInactive (public: radioInactive)
  DECLARED: ConvoyViewModel.kt:210
  val radioInactive: StateFlow<Boolean> = _radioInactive.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:214:        _radioInactive.value = false
    READERS (functions that USE this field):
      [R] convoy/ConvoyViewModel.kt:210:    val radioInactive: StateFlow<Boolean> = _radioInactive.asStateFlow()

  FIELD: _workingConfig (public: workingConfig)
  DECLARED: ConvoyViewModel.kt:228
  private val _workingConfig = MutableStateFlow<WorkingConfig?>(null)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:230:    fun setWorkingConfig(config: WorkingConfig) { _workingConfig.value = config }
      [W] ConvoyViewModel.kt:282:    fun clearWorkingConfig() { _workingConfig.value = null }
    READERS (functions that USE this field):
      [R] convoy/ConvoyRadioConfigScreens.kt:239:                convoyViewModel.workingConfig.value?.let { wc ->
      [R] convoy/ConvoyRadioConfigScreens.kt:341:    workingConfig: WorkingConfig,
      [R] convoy/ConvoyRadioConfigScreens.kt:397:                ConvoyConfigRow("Long Name",      workingConfig.longName,        highlight = true)
      [R] convoy/ConvoyRadioConfigScreens.kt:399:                ConvoyConfigRow("Node Role",      workingConfig.nodeRole,        highlight = true)
      [R] convoy/ConvoyRadioConfigScreens.kt:401:                ConvoyConfigRow("Managed Mode",   workingConfig.isManaged.toString())
      [R] convoy/ConvoyRadioConfigScreens.kt:403:                ConvoyConfigRow("Serial Enabled", workingConfig.serialEnabled.toString())
      [R] convoy/ConvoyRadioConfigScreens.kt:440:                                org.meshtastic.proto.Config.DeviceConfig.Role.valueOf(workingConfig.nodeRole)
      [R] convoy/ConvoyRadioConfigScreens.kt:447:                                    is_managed = workingConfig.isManaged
      [R] convoy/ConvoyRadioConfigScreens.kt:471:    workingConfig: WorkingConfig,
      [R] convoy/ConvoyRadioConfigScreens.kt:531:                ConvoyConfigRow("Region",       workingConfig.loraRegion,             highlight = true)
      [R] convoy/ConvoyRadioConfigScreens.kt:533:                ConvoyConfigRow("Modem Preset", workingConfig.loraModemPreset,        highlight = true)
      [R] convoy/ConvoyRadioConfigScreens.kt:535:                ConvoyConfigRow("Hop Limit",    workingConfig.loraHopLimit.toString())
      [R] convoy/ConvoyRadioConfigScreens.kt:537:                ConvoyConfigRow("TX Enabled",   workingConfig.loraTxEnabled.toString())
      [R] convoy/ConvoyRadioConfigScreens.kt:539:                ConvoyConfigRow("TX Power",     "${workingConfig.loraTxPower} dBm")
      [R] convoy/ConvoyRadioConfigScreens.kt:576:                                org.meshtastic.proto.Config.LoRaConfig.RegionCode.valueOf(workingConfig.loraRegion)

  FIELD: _workingConfig (public: workingConfig)
  DECLARED: ConvoyViewModel.kt:229
  val workingConfig: StateFlow<WorkingConfig?> = _workingConfig.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:230:    fun setWorkingConfig(config: WorkingConfig) { _workingConfig.value = config }
      [W] ConvoyViewModel.kt:282:    fun clearWorkingConfig() { _workingConfig.value = null }
    READERS (functions that USE this field):
      [R] convoy/ConvoyRadioConfigScreens.kt:239:                convoyViewModel.workingConfig.value?.let { wc ->
      [R] convoy/ConvoyRadioConfigScreens.kt:341:    workingConfig: WorkingConfig,
      [R] convoy/ConvoyRadioConfigScreens.kt:397:                ConvoyConfigRow("Long Name",      workingConfig.longName,        highlight = true)
      [R] convoy/ConvoyRadioConfigScreens.kt:399:                ConvoyConfigRow("Node Role",      workingConfig.nodeRole,        highlight = true)
      [R] convoy/ConvoyRadioConfigScreens.kt:401:                ConvoyConfigRow("Managed Mode",   workingConfig.isManaged.toString())
      [R] convoy/ConvoyRadioConfigScreens.kt:403:                ConvoyConfigRow("Serial Enabled", workingConfig.serialEnabled.toString())
      [R] convoy/ConvoyRadioConfigScreens.kt:440:                                org.meshtastic.proto.Config.DeviceConfig.Role.valueOf(workingConfig.nodeRole)
      [R] convoy/ConvoyRadioConfigScreens.kt:447:                                    is_managed = workingConfig.isManaged
      [R] convoy/ConvoyRadioConfigScreens.kt:471:    workingConfig: WorkingConfig,
      [R] convoy/ConvoyRadioConfigScreens.kt:531:                ConvoyConfigRow("Region",       workingConfig.loraRegion,             highlight = true)
      [R] convoy/ConvoyRadioConfigScreens.kt:533:                ConvoyConfigRow("Modem Preset", workingConfig.loraModemPreset,        highlight = true)
      [R] convoy/ConvoyRadioConfigScreens.kt:535:                ConvoyConfigRow("Hop Limit",    workingConfig.loraHopLimit.toString())
      [R] convoy/ConvoyRadioConfigScreens.kt:537:                ConvoyConfigRow("TX Enabled",   workingConfig.loraTxEnabled.toString())
      [R] convoy/ConvoyRadioConfigScreens.kt:539:                ConvoyConfigRow("TX Power",     "${workingConfig.loraTxPower} dBm")
      [R] convoy/ConvoyRadioConfigScreens.kt:576:                                org.meshtastic.proto.Config.LoRaConfig.RegionCode.valueOf(workingConfig.loraRegion)

  FIELD: _simulationMode (public: simulationMode)
  DECLARED: ConvoyViewModel.kt:286
  private val _simulationMode = MutableStateFlow(false)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:410:        _simulationMode.value = enabled
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:120:    val simulationMode by viewModel.simulationMode.collectAsStateWithLifecycle()
      [R] convoy/ConvoyViewModel.kt:287:    val simulationMode: StateFlow<Boolean> = _simulationMode.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:544:        val nodes: List<ConvoyNode> = if (_simulationMode.value) {

  FIELD: _simulationMode (public: simulationMode)
  DECLARED: ConvoyViewModel.kt:287
  val simulationMode: StateFlow<Boolean> = _simulationMode.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:410:        _simulationMode.value = enabled
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:120:    val simulationMode by viewModel.simulationMode.collectAsStateWithLifecycle()
      [R] convoy/ConvoyViewModel.kt:287:    val simulationMode: StateFlow<Boolean> = _simulationMode.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:544:        val nodes: List<ConvoyNode> = if (_simulationMode.value) {

  FIELD: _myCartId (public: myCartId)
  DECLARED: ConvoyViewModel.kt:289
  private val _myCartId = MutableStateFlow(ConvoySimulation.MY_CART_ID)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:158:        _myCartId.value = if (myNum != null) "!%08x".format(myNum) else "!phone"
      [W] ConvoyViewModel.kt:173:        convoyLog("TRACK START: myCart=${_myCartId.value} lead=$lockedLeadNodeId nodes=${nodes.size}")
      [W] ConvoyViewModel.kt:414:    fun setMyCartId(nodeId: String) { _myCartId.value = nodeId }
    READERS (functions that USE this field):
      [R] convoy/ConvoyEngine.kt:33:        myCartId: String = "",
      [R] convoy/ConvoyEngine.kt:47:        val withProximity = computeProximity(withRoles, myCartId)
      [R] convoy/ConvoyEngine.kt:135:    fun computeProximity(nodes: List<ConvoyNode>, myCartId: String): List<ConvoyNode> {
      [R] convoy/ConvoyEngine.kt:143:                isMyCart = node.nodeId == myCartId,
      [R] convoy/ConvoyScreen.kt:251:            val myCartId = viewModel.myCartId.value
      [R] convoy/ConvoyScreen.kt:252:            val myCart = convoyState.nodes.firstOrNull { it.nodeId == myCartId }
      [R] convoy/ConvoyScreen.kt:1421:                    myCartId = viewModel.myCartId.collectAsStateWithLifecycle().value,
      [R] convoy/ConvoyScreen.kt:1563:    myCartId: String,
      [R] convoy/ConvoyScreen.kt:1570:        Text("My Cart  ★ ${myCart?.callsign ?: myCartId.takeLast(8)}", color = Color(0xFF111111), fontSize = 13.sp,
      [R] convoy/ConvoySimulation.kt:115:        return ConvoyEngine.compute(nodes, myCartId = MY_CART_ID, nowMs = nowMs)
      [R] convoy/ConvoyViewModel.kt:165:                nodes.isEmpty() -> setLeadCart(_myCartId.value)
      [R] convoy/ConvoyViewModel.kt:166:                else -> setLeadCart(_myCartId.value)
      [R] convoy/ConvoyViewModel.kt:173:        convoyLog("TRACK START: myCart=${_myCartId.value} lead=$lockedLeadNodeId nodes=${nodes.size}")
      [R] convoy/ConvoyViewModel.kt:292:        return if (num != null) "!%08x".format(num) else _myCartId.value
      [R] convoy/ConvoyViewModel.kt:294:    val myCartId: StateFlow<String> = _myCartId.asStateFlow()

  FIELD: _myCartId (public: myCartId)
  DECLARED: ConvoyViewModel.kt:294
  val myCartId: StateFlow<String> = _myCartId.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:158:        _myCartId.value = if (myNum != null) "!%08x".format(myNum) else "!phone"
      [W] ConvoyViewModel.kt:173:        convoyLog("TRACK START: myCart=${_myCartId.value} lead=$lockedLeadNodeId nodes=${nodes.size}")
      [W] ConvoyViewModel.kt:414:    fun setMyCartId(nodeId: String) { _myCartId.value = nodeId }
    READERS (functions that USE this field):
      [R] convoy/ConvoyEngine.kt:33:        myCartId: String = "",
      [R] convoy/ConvoyEngine.kt:47:        val withProximity = computeProximity(withRoles, myCartId)
      [R] convoy/ConvoyEngine.kt:135:    fun computeProximity(nodes: List<ConvoyNode>, myCartId: String): List<ConvoyNode> {
      [R] convoy/ConvoyEngine.kt:143:                isMyCart = node.nodeId == myCartId,
      [R] convoy/ConvoyScreen.kt:251:            val myCartId = viewModel.myCartId.value
      [R] convoy/ConvoyScreen.kt:252:            val myCart = convoyState.nodes.firstOrNull { it.nodeId == myCartId }
      [R] convoy/ConvoyScreen.kt:1421:                    myCartId = viewModel.myCartId.collectAsStateWithLifecycle().value,
      [R] convoy/ConvoyScreen.kt:1563:    myCartId: String,
      [R] convoy/ConvoyScreen.kt:1570:        Text("My Cart  ★ ${myCart?.callsign ?: myCartId.takeLast(8)}", color = Color(0xFF111111), fontSize = 13.sp,
      [R] convoy/ConvoySimulation.kt:115:        return ConvoyEngine.compute(nodes, myCartId = MY_CART_ID, nowMs = nowMs)
      [R] convoy/ConvoyViewModel.kt:165:                nodes.isEmpty() -> setLeadCart(_myCartId.value)
      [R] convoy/ConvoyViewModel.kt:166:                else -> setLeadCart(_myCartId.value)
      [R] convoy/ConvoyViewModel.kt:173:        convoyLog("TRACK START: myCart=${_myCartId.value} lead=$lockedLeadNodeId nodes=${nodes.size}")
      [R] convoy/ConvoyViewModel.kt:292:        return if (num != null) "!%08x".format(num) else _myCartId.value
      [R] convoy/ConvoyViewModel.kt:294:    val myCartId: StateFlow<String> = _myCartId.asStateFlow()

  FIELD: _showLeadTrack (public: showLeadTrack)
  DECLARED: ConvoyViewModel.kt:296
  private val _showLeadTrack = MutableStateFlow(true)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:415:    fun toggleLeadTrack() { _showLeadTrack.value = !_showLeadTrack.value }
      [W] ConvoyViewModel.kt:416:    fun setShowLeadTrack(visible: Boolean) { _showLeadTrack.value = visible }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:121:    val showLeadTrack by viewModel.showLeadTrack.collectAsStateWithLifecycle()
      [R] convoy/ConvoyViewModel.kt:297:    val showLeadTrack: StateFlow<Boolean> = _showLeadTrack.asStateFlow()

  FIELD: _showLeadTrack (public: showLeadTrack)
  DECLARED: ConvoyViewModel.kt:297
  val showLeadTrack: StateFlow<Boolean> = _showLeadTrack.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:415:    fun toggleLeadTrack() { _showLeadTrack.value = !_showLeadTrack.value }
      [W] ConvoyViewModel.kt:416:    fun setShowLeadTrack(visible: Boolean) { _showLeadTrack.value = visible }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:121:    val showLeadTrack by viewModel.showLeadTrack.collectAsStateWithLifecycle()
      [R] convoy/ConvoyViewModel.kt:297:    val showLeadTrack: StateFlow<Boolean> = _showLeadTrack.asStateFlow()

  FIELD: _leadTrackSegments (public: leadTrackSegments)
  DECLARED: ConvoyViewModel.kt:299
  private val _leadTrackSegments = MutableStateFlow<List<ConvoyEngine.LeadTrackSegment>>(emptyList())

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:146:        _leadTrackSegments.value = emptyList()
      [W] ConvoyViewModel.kt:767:        _leadTrackSegments.value = if (_trackLeadOnly.value) {
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:291:    val rawSegments by viewModel.leadTrackSegments.collectAsStateWithLifecycle()
      [R] convoy/ConvoyViewModel.kt:300:    val leadTrackSegments: StateFlow<List<ConvoyEngine.LeadTrackSegment>> = _leadTrackSegments.asStateFlow()

  FIELD: _leadTrackSegments (public: leadTrackSegments)
  DECLARED: ConvoyViewModel.kt:300
  val leadTrackSegments: StateFlow<List<ConvoyEngine.LeadTrackSegment>> = _leadTrackSegments.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:146:        _leadTrackSegments.value = emptyList()
      [W] ConvoyViewModel.kt:767:        _leadTrackSegments.value = if (_trackLeadOnly.value) {
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:291:    val rawSegments by viewModel.leadTrackSegments.collectAsStateWithLifecycle()
      [R] convoy/ConvoyViewModel.kt:300:    val leadTrackSegments: StateFlow<List<ConvoyEngine.LeadTrackSegment>> = _leadTrackSegments.asStateFlow()

  FIELD: _gpsTrailSegments (public: gpsTrailSegments)
  DECLARED: ConvoyViewModel.kt:301
  private val _gpsTrailSegments = MutableStateFlow<List<ConvoyEngine.LeadTrackSegment>>(emptyList())

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:147:        _gpsTrailSegments.value = emptyList()
      [W] ConvoyViewModel.kt:442:                        _gpsTrailSegments.value = _gpsTrailSegments.value + seg
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:292:    val gpsTrail by viewModel.gpsTrailSegments.collectAsStateWithLifecycle()
      [R] convoy/ConvoyViewModel.kt:302:    val gpsTrailSegments: StateFlow<List<ConvoyEngine.LeadTrackSegment>> = _gpsTrailSegments.asStateFlow()

  FIELD: _gpsTrailSegments (public: gpsTrailSegments)
  DECLARED: ConvoyViewModel.kt:302
  val gpsTrailSegments: StateFlow<List<ConvoyEngine.LeadTrackSegment>> = _gpsTrailSegments.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:147:        _gpsTrailSegments.value = emptyList()
      [W] ConvoyViewModel.kt:442:                        _gpsTrailSegments.value = _gpsTrailSegments.value + seg
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:292:    val gpsTrail by viewModel.gpsTrailSegments.collectAsStateWithLifecycle()
      [R] convoy/ConvoyViewModel.kt:302:    val gpsTrailSegments: StateFlow<List<ConvoyEngine.LeadTrackSegment>> = _gpsTrailSegments.asStateFlow()

  FIELD: _routeTrailSegments (public: routeTrailSegments)
  DECLARED: ConvoyViewModel.kt:303
  private val _routeTrailSegments = MutableStateFlow<List<ConvoyEngine.LeadTrackSegment>>(emptyList())

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:141:        _routeTrailSegments.value = emptyList()
      [W] ConvoyViewModel.kt:643:        android.util.Log.w("TRACK-DBG", "TICK: leadOnly=${_trackLeadOnly.value} leadId=$lockedLeadNodeId segs=${_routeTrailSegments.value.size} nodes=${state.nodes.size}")
      [W] ConvoyViewModel.kt:741:                        _routeTrailSegments.value = _routeTrailSegments.value + seg
      [W] ConvoyViewModel.kt:768:            _routeTrailSegments.value.map { it.copy(color = "#000000") }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:293:    val routeTrail by viewModel.routeTrailSegments.collectAsStateWithLifecycle()
      [R] convoy/ConvoyViewModel.kt:304:    val routeTrailSegments: StateFlow<List<ConvoyEngine.LeadTrackSegment>> = _routeTrailSegments.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:643:        android.util.Log.w("TRACK-DBG", "TICK: leadOnly=${_trackLeadOnly.value} leadId=$lockedLeadNodeId segs=${_routeTrailSegments.value.size} nodes=${state.nodes.size}")
      [R] convoy/ConvoyViewModel.kt:740:                        android.util.Log.w("TRACK-DBG", "SEG-WRITE: node=${seg.nodeId} from=${String.format("%.5f,%.5f",seg.startLat,seg.startLon)} to=${String.format("%.5f,%.5f",seg.endLat,seg.endLon)} proxy=$proxySnapped total=${_routeTrailSegments.value.size+1}")
      [R] convoy/ConvoyViewModel.kt:750:if (_trackActive.value && _routeTrailSegments.value.isNotEmpty()) {
      [R] convoy/ConvoyViewModel.kt:754:                val minDist = _routeTrailSegments.value.minOf { seg ->
      [R] convoy/ConvoyViewModel.kt:768:            _routeTrailSegments.value.map { it.copy(color = "#000000") }
      [R] convoy/ConvoyViewModel.kt:771:                segments = _routeTrailSegments.value,

  FIELD: _routeTrailSegments (public: routeTrailSegments)
  DECLARED: ConvoyViewModel.kt:304
  val routeTrailSegments: StateFlow<List<ConvoyEngine.LeadTrackSegment>> = _routeTrailSegments.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:141:        _routeTrailSegments.value = emptyList()
      [W] ConvoyViewModel.kt:643:        android.util.Log.w("TRACK-DBG", "TICK: leadOnly=${_trackLeadOnly.value} leadId=$lockedLeadNodeId segs=${_routeTrailSegments.value.size} nodes=${state.nodes.size}")
      [W] ConvoyViewModel.kt:741:                        _routeTrailSegments.value = _routeTrailSegments.value + seg
      [W] ConvoyViewModel.kt:768:            _routeTrailSegments.value.map { it.copy(color = "#000000") }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:293:    val routeTrail by viewModel.routeTrailSegments.collectAsStateWithLifecycle()
      [R] convoy/ConvoyViewModel.kt:304:    val routeTrailSegments: StateFlow<List<ConvoyEngine.LeadTrackSegment>> = _routeTrailSegments.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:643:        android.util.Log.w("TRACK-DBG", "TICK: leadOnly=${_trackLeadOnly.value} leadId=$lockedLeadNodeId segs=${_routeTrailSegments.value.size} nodes=${state.nodes.size}")
      [R] convoy/ConvoyViewModel.kt:740:                        android.util.Log.w("TRACK-DBG", "SEG-WRITE: node=${seg.nodeId} from=${String.format("%.5f,%.5f",seg.startLat,seg.startLon)} to=${String.format("%.5f,%.5f",seg.endLat,seg.endLon)} proxy=$proxySnapped total=${_routeTrailSegments.value.size+1}")
      [R] convoy/ConvoyViewModel.kt:750:if (_trackActive.value && _routeTrailSegments.value.isNotEmpty()) {
      [R] convoy/ConvoyViewModel.kt:754:                val minDist = _routeTrailSegments.value.minOf { seg ->
      [R] convoy/ConvoyViewModel.kt:768:            _routeTrailSegments.value.map { it.copy(color = "#000000") }
      [R] convoy/ConvoyViewModel.kt:771:                segments = _routeTrailSegments.value,

  FIELD: _routeRecording (public: routeRecording)
  DECLARED: ConvoyViewModel.kt:315
  private val _routeRecording = MutableStateFlow(false)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:459:        bindGpsService(context) { svc -> svc.startTrack(); _routeRecording.value = true }
      [W] ConvoyViewModel.kt:461:    fun pauseRecording() { gpsService?.pauseTrack(); _routeRecording.value = false }
      [W] ConvoyViewModel.kt:468:            bindGpsService(context) { svc -> svc.resumeTrack(); _routeRecording.value = true }
      [W] ConvoyViewModel.kt:469:        } else { gpsService?.resumeTrack(); _routeRecording.value = true }
      [W] ConvoyViewModel.kt:482:        lastGpsLat = null; lastGpsLon = null; _distanceMiles.value = 0.0; _routeRecording.value = false
    READERS (functions that USE this field):
      [R] convoy/ConvoyViewModel.kt:316:    val routeRecording: StateFlow<Boolean> = _routeRecording.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:566:        if (gpsService?.useRadioGps == true && _routeRecording.value) {

  FIELD: _routeRecording (public: routeRecording)
  DECLARED: ConvoyViewModel.kt:316
  val routeRecording: StateFlow<Boolean> = _routeRecording.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:459:        bindGpsService(context) { svc -> svc.startTrack(); _routeRecording.value = true }
      [W] ConvoyViewModel.kt:461:    fun pauseRecording() { gpsService?.pauseTrack(); _routeRecording.value = false }
      [W] ConvoyViewModel.kt:468:            bindGpsService(context) { svc -> svc.resumeTrack(); _routeRecording.value = true }
      [W] ConvoyViewModel.kt:469:        } else { gpsService?.resumeTrack(); _routeRecording.value = true }
      [W] ConvoyViewModel.kt:482:        lastGpsLat = null; lastGpsLon = null; _distanceMiles.value = 0.0; _routeRecording.value = false
    READERS (functions that USE this field):
      [R] convoy/ConvoyViewModel.kt:316:    val routeRecording: StateFlow<Boolean> = _routeRecording.asStateFlow()
      [R] convoy/ConvoyViewModel.kt:566:        if (gpsService?.useRadioGps == true && _routeRecording.value) {

  FIELD: _offTrackNodes (public: offTrackNodes)
  DECLARED: ConvoyViewModel.kt:317
  private val _offTrackNodes = MutableStateFlow<List<ConvoyNode>>(emptyList())

    WRITERS (functions that CHANGE this field):
    READERS (functions that USE this field):
      [R] convoy/ConvoyViewModel.kt:318:    val offTrackNodes: StateFlow<List<ConvoyNode>> = _offTrackNodes.asStateFlow()

  FIELD: _offTrackNodes (public: offTrackNodes)
  DECLARED: ConvoyViewModel.kt:318
  val offTrackNodes: StateFlow<List<ConvoyNode>> = _offTrackNodes.asStateFlow()

    WRITERS (functions that CHANGE this field):
    READERS (functions that USE this field):
      [R] convoy/ConvoyViewModel.kt:318:    val offTrackNodes: StateFlow<List<ConvoyNode>> = _offTrackNodes.asStateFlow()

  FIELD: _currentIntervalSecs (public: currentIntervalSecs)
  DECLARED: ConvoyViewModel.kt:381
  private val _currentIntervalSecs = MutableStateFlow(5)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:385:        _currentIntervalSecs.value = secs
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:1402:        val currentIntervalSecs by viewModel.currentIntervalSecs.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1416:                        currentIntervalSecs = currentIntervalSecs,
      [R] convoy/ConvoyScreen.kt:1468:    currentIntervalSecs: Int = 5,
      [R] convoy/ConvoyScreen.kt:1482:            Text("${currentIntervalSecs}s", color = Color(0xFF111111), fontSize = 10.sp,
      [R] convoy/ConvoyScreen.kt:1486:                value = currentIntervalSecs.toFloat(),
      [R] convoy/ConvoyViewModel.kt:382:    val currentIntervalSecs: StateFlow<Int> = _currentIntervalSecs.asStateFlow()

  FIELD: _currentIntervalSecs (public: currentIntervalSecs)
  DECLARED: ConvoyViewModel.kt:382
  val currentIntervalSecs: StateFlow<Int> = _currentIntervalSecs.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:385:        _currentIntervalSecs.value = secs
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:1402:        val currentIntervalSecs by viewModel.currentIntervalSecs.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1416:                        currentIntervalSecs = currentIntervalSecs,
      [R] convoy/ConvoyScreen.kt:1468:    currentIntervalSecs: Int = 5,
      [R] convoy/ConvoyScreen.kt:1482:            Text("${currentIntervalSecs}s", color = Color(0xFF111111), fontSize = 10.sp,
      [R] convoy/ConvoyScreen.kt:1486:                value = currentIntervalSecs.toFloat(),
      [R] convoy/ConvoyViewModel.kt:382:    val currentIntervalSecs: StateFlow<Int> = _currentIntervalSecs.asStateFlow()

  FIELD: _distanceMiles (public: distanceMiles)
  DECLARED: ConvoyViewModel.kt:419
  private val _distanceMiles = MutableStateFlow(0.0)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:446:                    _distanceMiles.value = svc.totalDistanceMiles
      [W] ConvoyViewModel.kt:482:        lastGpsLat = null; lastGpsLon = null; _distanceMiles.value = 0.0; _routeRecording.value = false
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:1014:          val distanceMiles by viewModel.distanceMiles.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1026:                      Text("%.2f".format(distanceMiles), color = Color(0xFFFF0000).copy(alpha = 0.75f),
      [R] convoy/ConvoyViewModel.kt:420:    val distanceMiles: StateFlow<Double> = _distanceMiles.asStateFlow()

  FIELD: _distanceMiles (public: distanceMiles)
  DECLARED: ConvoyViewModel.kt:420
  val distanceMiles: StateFlow<Double> = _distanceMiles.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:446:                    _distanceMiles.value = svc.totalDistanceMiles
      [W] ConvoyViewModel.kt:482:        lastGpsLat = null; lastGpsLon = null; _distanceMiles.value = 0.0; _routeRecording.value = false
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:1014:          val distanceMiles by viewModel.distanceMiles.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1026:                      Text("%.2f".format(distanceMiles), color = Color(0xFFFF0000).copy(alpha = 0.75f),
      [R] convoy/ConvoyViewModel.kt:420:    val distanceMiles: StateFlow<Double> = _distanceMiles.asStateFlow()

  FIELD: _avgChannelUtil (public: avgChannelUtil)
  DECLARED: ConvoyViewModel.kt:421
  private val _avgChannelUtil = MutableStateFlow(0f)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:921:        _avgChannelUtil.value = avgUtil
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:1401:        val avgChannelUtil by viewModel.avgChannelUtil.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1415:                        avgChannelUtil = avgChannelUtil,
      [R] convoy/ConvoyScreen.kt:1467:    avgChannelUtil: Float = 0f,
      [R] convoy/ConvoyScreen.kt:1472:        avgChannelUtil > 40f -> Color(0xFFFF4444)
      [R] convoy/ConvoyScreen.kt:1473:        avgChannelUtil > 25f -> Color(0xFFFFAA00)
      [R] convoy/ConvoyScreen.kt:1535:                        Text("%.0f%%".format(avgChannelUtil), color = chColor,
      [R] convoy/ConvoyViewModel.kt:422:    val avgChannelUtil: StateFlow<Float> = _avgChannelUtil.asStateFlow()

  FIELD: _avgChannelUtil (public: avgChannelUtil)
  DECLARED: ConvoyViewModel.kt:422
  val avgChannelUtil: StateFlow<Float> = _avgChannelUtil.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:921:        _avgChannelUtil.value = avgUtil
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:1401:        val avgChannelUtil by viewModel.avgChannelUtil.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:1415:                        avgChannelUtil = avgChannelUtil,
      [R] convoy/ConvoyScreen.kt:1467:    avgChannelUtil: Float = 0f,
      [R] convoy/ConvoyScreen.kt:1472:        avgChannelUtil > 40f -> Color(0xFFFF4444)
      [R] convoy/ConvoyScreen.kt:1473:        avgChannelUtil > 25f -> Color(0xFFFFAA00)
      [R] convoy/ConvoyScreen.kt:1535:                        Text("%.0f%%".format(avgChannelUtil), color = chColor,
      [R] convoy/ConvoyViewModel.kt:422:    val avgChannelUtil: StateFlow<Float> = _avgChannelUtil.asStateFlow()

  FIELD: _downloadState (public: downloadState)
  DECLARED: ConvoyViewModel.kt:945
  private val _downloadState = MutableStateFlow<DownloadState>(DownloadState.Idle)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:972:                _downloadState.value = when (entry.status) {
      [W] ConvoyViewModel.kt:994:        _downloadState.value = DownloadState.Cancelled
      [W] ConvoyViewModel.kt:997:            _downloadState.value = DownloadState.Idle
    READERS (functions that USE this field):
      [R] convoy/ConvoyMapViewerScreen.kt:197:    val downloadState by convoyViewModel.downloadState.collectAsState()
      [R] convoy/ConvoyScreen.kt:191:    val downloadState by viewModel.downloadState.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:417:    if (downloadState is ConvoyViewModel.DownloadState.Error) {
      [R] convoy/ConvoyScreen.kt:421:            text = { Text((downloadState as ConvoyViewModel.DownloadState.Error).message) },
      [R] convoy/ConvoyScreen.kt:458:    LaunchedEffect(downloadState) {
      [R] convoy/ConvoyScreen.kt:459:        if (downloadState is ConvoyViewModel.DownloadState.Complete) {
      [R] convoy/ConvoyScreen.kt:460:            val summary = (downloadState as ConvoyViewModel.DownloadState.Complete).summary
      [R] convoy/ConvoyViewModel.kt:946:    val downloadState: StateFlow<DownloadState> = _downloadState.asStateFlow()

  FIELD: _downloadState (public: downloadState)
  DECLARED: ConvoyViewModel.kt:946
  val downloadState: StateFlow<DownloadState> = _downloadState.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:972:                _downloadState.value = when (entry.status) {
      [W] ConvoyViewModel.kt:994:        _downloadState.value = DownloadState.Cancelled
      [W] ConvoyViewModel.kt:997:            _downloadState.value = DownloadState.Idle
    READERS (functions that USE this field):
      [R] convoy/ConvoyMapViewerScreen.kt:197:    val downloadState by convoyViewModel.downloadState.collectAsState()
      [R] convoy/ConvoyScreen.kt:191:    val downloadState by viewModel.downloadState.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:417:    if (downloadState is ConvoyViewModel.DownloadState.Error) {
      [R] convoy/ConvoyScreen.kt:421:            text = { Text((downloadState as ConvoyViewModel.DownloadState.Error).message) },
      [R] convoy/ConvoyScreen.kt:458:    LaunchedEffect(downloadState) {
      [R] convoy/ConvoyScreen.kt:459:        if (downloadState is ConvoyViewModel.DownloadState.Complete) {
      [R] convoy/ConvoyScreen.kt:460:            val summary = (downloadState as ConvoyViewModel.DownloadState.Complete).summary
      [R] convoy/ConvoyViewModel.kt:946:    val downloadState: StateFlow<DownloadState> = _downloadState.asStateFlow()

  FIELD: _pendingDownload (public: pendingDownload)
  DECLARED: ConvoyViewModel.kt:947
  private val _pendingDownload = MutableStateFlow<PendingDownload?>(null)

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:952:    fun setPendingDownload(pending: PendingDownload) { _pendingDownload.value = pending }
      [W] ConvoyViewModel.kt:953:    fun clearPendingDownload() { _pendingDownload.value = null }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:190:    val pendingDownload by viewModel.pendingDownload.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:356:    pendingDownload?.let { pending ->
      [R] convoy/ConvoyViewModel.kt:948:    val pendingDownload: StateFlow<PendingDownload?> = _pendingDownload.asStateFlow()

  FIELD: _pendingDownload (public: pendingDownload)
  DECLARED: ConvoyViewModel.kt:948
  val pendingDownload: StateFlow<PendingDownload?> = _pendingDownload.asStateFlow()

    WRITERS (functions that CHANGE this field):
      [W] ConvoyViewModel.kt:952:    fun setPendingDownload(pending: PendingDownload) { _pendingDownload.value = pending }
      [W] ConvoyViewModel.kt:953:    fun clearPendingDownload() { _pendingDownload.value = null }
    READERS (functions that USE this field):
      [R] convoy/ConvoyScreen.kt:190:    val pendingDownload by viewModel.pendingDownload.collectAsStateWithLifecycle()
      [R] convoy/ConvoyScreen.kt:356:    pendingDownload?.let { pending ->
      [R] convoy/ConvoyViewModel.kt:948:    val pendingDownload: StateFlow<PendingDownload?> = _pendingDownload.asStateFlow()

--- 5B: StateFlow Fields in Other Files ---

  FILE: ConvoyGpsService.kt
    [54] var state: State = State.IDLE
        [R] convoy/ConvoyApplyRadioScreen.kt:354:                if (writerState.step != WriteStep.IDLE) {
        [R] convoy/ConvoyEmailGateScreen.kt:125:                // ── IDLE / entry ──────────────────────────────────────────
        [R] convoy/ConvoyGpsService.kt:30: * States: IDLE → RECORDING → PAUSED → RECORDING → STOPPED
        [R] convoy/ConvoyGpsService.kt:53:    enum class State { IDLE, RECORDING, PAUSED }
        [R] convoy/ConvoyGpsService.kt:54:    var state: State = State.IDLE
    [57] var currentTempFile: File? = null
        [W] ConvoyGpsService.kt:149:        currentTempFile = tempFile
        [W] ConvoyGpsService.kt:200:        currentTempFile = null
        [R] convoy/ConvoyGpsService.kt:199:        val file = currentTempFile
    [61] var onLocationUpdate: ((lat: Double, lon: Double, alt: Double) -> Unit)? = null
        [W] ConvoyViewModel.kt:434:                svc.onLocationUpdate = { lat, lon, _ ->
        [R] convoy/ConvoyGpsService.kt:317:        onLocationUpdate?.invoke(lat, lon, alt)
    [63] var totalDistanceMiles: Double = 0.0
        [W] ConvoyGpsService.kt:203:        totalDistanceMiles = 0.0
        [W] ConvoyGpsService.kt:309:            totalDistanceMiles += haversineMiles(prevLat, prevLon, lat, lon)
        [R] convoy/ConvoyViewModel.kt:446:                    _distanceMiles.value = svc.totalDistanceMiles
    [78] private var wakeLock: PowerManager.WakeLock? = null
        [W] ConvoyGpsService.kt:419:            wakeLock = pm.newWakeLock(
        [W] ConvoyGpsService.kt:433:            if (wakeLock?.isHeld == true) {
        [W] ConvoyGpsService.kt:440:        wakeLock = null
        [R] convoy/ConvoyGpsService.kt:434:                wakeLock?.release()
    [79] private var locationManager: LocationManager? = null
        [W] ConvoyGpsService.kt:254:            locationManager = lm
        [W] ConvoyGpsService.kt:285:        locationManager = null
        [R] convoy/ConvoyGpsService.kt:280:            locationListener?.let { locationManager?.removeUpdates(it) }
        [R] service/MeshService.kt:80:    @Inject lateinit var locationManager: MeshLocationManager
        [R] service/MeshService.kt:324:                locationManager.start(serviceScope) { commandSender.sendPosition(it) }
        [R] service/MeshService.kt:328:                locationManager.stop()
    [80] private var locationListener: LocationListener? = null
        [W] ConvoyGpsService.kt:265:            locationListener = listener
        [W] ConvoyGpsService.kt:284:        locationListener = null
        [R] convoy/ConvoyGpsService.kt:280:            locationListener?.let { locationManager?.removeUpdates(it) }
    [81] private var kmlWriter: BufferedWriter? = null
        [W] ConvoyGpsService.kt:334:            kmlWriter = writer
        [W] ConvoyGpsService.kt:364:        kmlWriter = null
        [R] convoy/ConvoyGpsService.kt:348:            kmlWriter?.write("$lon,$lat,$alt\n")
        [R] convoy/ConvoyGpsService.kt:349:            kmlWriter?.flush()
        [R] convoy/ConvoyGpsService.kt:357:            kmlWriter?.write("</coordinates></LineString></Placemark>\n")
        [R] convoy/ConvoyGpsService.kt:358:            kmlWriter?.write("</Document>\n</kml>\n")
        [R] convoy/ConvoyGpsService.kt:359:            kmlWriter?.flush()
    [82] var lastLat: Double? = null
        [W] ConvoyGpsService.kt:201:        lastLat = null
        [W] ConvoyGpsService.kt:306:        lastLat = lat
        [R] convoy/ConvoyGpsService.kt:304:        val prevLat = lastLat
        [R] convoy/ConvoyViewModel.kt:785:        val svcLat = gpsService?.lastLat
    [84] private var lastMovementMs: Long = System.currentTimeMillis()
        [W] ConvoyGpsService.kt:183:        lastMovementMs = System.currentTimeMillis()
        [W] ConvoyGpsService.kt:293:            lastMovementMs = System.currentTimeMillis()
        [W] ConvoyViewModel.kt:215:        lastMovementMs = System.currentTimeMillis()
        [R] convoy/ConvoyGpsService.kt:296:        } else if (System.currentTimeMillis() - lastMovementMs > SLEEP_THRESHOLD_MS) {
    [85] private var lastMoveLat: Double = 0.0
        [W] ConvoyGpsService.kt:184:        lastMoveLat = 0.0
        [W] ConvoyGpsService.kt:291:        val distFeet = if (lastMoveLat != 0.0) haversineFeet(lastMoveLat, lastMoveLon, lat, lon) else 999.0
        [W] ConvoyGpsService.kt:294:            lastMoveLat = lat
    [86] private var lastMoveLon: Double = 0.0
        [W] ConvoyGpsService.kt:185:        lastMoveLon = 0.0
        [W] ConvoyGpsService.kt:295:            lastMoveLon = lon
        [R] convoy/ConvoyGpsService.kt:291:        val distFeet = if (lastMoveLat != 0.0) haversineFeet(lastMoveLat, lastMoveLon, lat, lon) else 999.0
    [87] private var isSleeping: Boolean = false
        [W] ConvoyGpsService.kt:182:        isSleeping = false
        [W] ConvoyGpsService.kt:297:            isSleeping = true
        [R] convoy/ConvoyGpsService.kt:290:        if (isSleeping) return
    [88] var onSleepTriggered: (() -> Unit)? = null
        [W] ConvoyViewModel.kt:433:                svc.onSleepTriggered = { recordingState.value = RecordingState.SLEEPING }
        [R] convoy/ConvoyGpsService.kt:300:            onSleepTriggered?.invoke()
    [91] var lastLon: Double? = null
        [W] ConvoyGpsService.kt:202:        lastLon = null
        [W] ConvoyGpsService.kt:307:        lastLon = lon
        [R] convoy/ConvoyGpsService.kt:305:        val prevLon = lastLon
        [R] convoy/ConvoyViewModel.kt:786:        val svcLon = gpsService?.lastLon
    [94] private const val TAG = "ConvoyGpsService"
        [W] ConvoyArtifactOps.kt:71:        Log.d(TAG, "SET TH route=$routeId waypoint=$waypointId — Pass 1 stub")
        [W] ConvoyDownloadQueue.kt:141:        android.util.Log.i(TAG, "Enqueued: ${entry.label} (${entry.totalTiles} tiles) id=${entry.id}")
        [W] ConvoyDownloadQueue.kt:186:        android.util.Log.i(TAG, "Enqueued area: $slotName ${cells.size} cells replace=$replaceExisting")
        [W] ConvoyDownloadQueue.kt:235:        android.util.Log.i(TAG, "Cancelled and removed: ${entry.label} id=$entryId")
        [W] ConvoyDownloadQueue.kt:281:        android.util.Log.i(TAG, "Removed: ${entry.label} id=$entryId")
        [R] convoy/ConvoyApiClient.kt:62:            Log.d(TAG, "registerUser: ${response.code} $responseBody")
        [R] convoy/ConvoyApiClient.kt:73:            Log.e(TAG, "registerUser error: ${e.message}")
        [R] convoy/ConvoyApiClient.kt:98:            Log.d(TAG, "acceptTerms: ${response.code}")
        [R] convoy/ConvoyApiClient.kt:101:            Log.e(TAG, "acceptTerms error: ${e.message}")
        [R] convoy/ConvoyApiClient.kt:124:            Log.d(TAG, "becomeOrganizer: ${response.code}")
    [95] const val CHANNEL_ID = "convoy_gps_recording"
        [W] ConvoyApplyList.kt:33:    CHANNEL_ID      ("Channel ID",          "Channel index (0=primary)"),
        [R] convoy/ConvoyApplyRadioScreen.kt:444:                                channelId                = if (ChannelField.CHANNEL_ID in al.channelFields) m?.channelId ?: 0 else ch?.id ?: 0,
        [R] convoy/ConvoyDownloadNotification.kt:47:                .getNotificationChannel(CHANNEL_ID)
        [R] convoy/ConvoyDownloadNotification.kt:50:                CHANNEL_ID, CHANNEL_NAME, NotificationManager.IMPORTANCE_LOW
        [R] convoy/ConvoyDownloadNotification.kt:64:        return NotificationCompat.Builder(context, CHANNEL_ID)
        [R] convoy/ConvoyDownloadNotification.kt:77:        return NotificationCompat.Builder(context, CHANNEL_ID)
    [96] const val NOTIFICATION_ID = 1001
        [R] convoy/ConvoyGpsService.kt:127:        startForeground(NOTIFICATION_ID, buildNotification())
        [R] convoy/ConvoyGpsService.kt:503:        nm.notify(NOTIFICATION_ID, buildNotification())
    [99] const val ACTION_PAUSE  = "com.geeksville.mesh.convoy.GPS_PAUSE"
        [R] convoy/ConvoyGpsService.kt:122:            ACTION_PAUSE  -> pauseTrack()
        [R] convoy/ConvoyGpsService.kt:461:            Intent(this, ConvoyGpsService::class.java).apply { action = ACTION_PAUSE },
    [100] const val ACTION_RESUME = "com.geeksville.mesh.convoy.GPS_RESUME"
        [R] convoy/ConvoyGpsService.kt:123:            ACTION_RESUME -> resumeTrack()
        [R] convoy/ConvoyGpsService.kt:466:            Intent(this, ConvoyGpsService::class.java).apply { action = ACTION_RESUME },

  FILE: ConvoySettingsViewModel.kt
    [30] private val _uiState = MutableStateFlow(ConvoySettingsUiState())
        [W] ConvoySettingsViewModel.kt:54:            }.collect { _uiState.value = it }
        [W] ConvoySettingsViewModel.kt:58:                _uiState.update { it.copy(leadLockFeet = feet) }
        [W] ConvoySettingsViewModel.kt:86:            _uiState.update { it.copy(userMessage = "$callsign reinstated") }
        [W] ConvoySettingsViewModel.kt:91:        _uiState.update { it.copy(userMessage = null) }
        [R] convoy/ConvoySettingsViewModel.kt:31:    val uiState: StateFlow<ConvoySettingsUiState> = _uiState.asStateFlow()
        [R] convoy/ConvoySettingsViewModel.kt:47:                _uiState.value.copy(

  FILE: ConvoySessionManager.kt
    [6] private const val TAG = "ConvoySession"
        [W] ConvoyArtifactOps.kt:71:        Log.d(TAG, "SET TH route=$routeId waypoint=$waypointId — Pass 1 stub")
        [W] ConvoyDownloadQueue.kt:141:        android.util.Log.i(TAG, "Enqueued: ${entry.label} (${entry.totalTiles} tiles) id=${entry.id}")
        [W] ConvoyDownloadQueue.kt:186:        android.util.Log.i(TAG, "Enqueued area: $slotName ${cells.size} cells replace=$replaceExisting")
        [W] ConvoyDownloadQueue.kt:235:        android.util.Log.i(TAG, "Cancelled and removed: ${entry.label} id=$entryId")
        [W] ConvoyDownloadQueue.kt:281:        android.util.Log.i(TAG, "Removed: ${entry.label} id=$entryId")
        [R] convoy/ConvoyApiClient.kt:62:            Log.d(TAG, "registerUser: ${response.code} $responseBody")
        [R] convoy/ConvoyApiClient.kt:73:            Log.e(TAG, "registerUser error: ${e.message}")
        [R] convoy/ConvoyApiClient.kt:98:            Log.d(TAG, "acceptTerms: ${response.code}")
        [R] convoy/ConvoyApiClient.kt:101:            Log.e(TAG, "acceptTerms error: ${e.message}")
        [R] convoy/ConvoyApiClient.kt:124:            Log.d(TAG, "becomeOrganizer: ${response.code}")
    [7] private const val PREFS_NAME = "grouptrack_user"
        [R] convoy/ConvoySessionManager.kt:116:        ctx.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)
        [R] convoy/ConvoySignInScreen.kt:68:    context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE).edit().apply {
        [R] convoy/ConvoySignInScreen.kt:80:    context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)
        [R] convoy/ConvoySignInScreen.kt:91:    context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE).edit()
        [R] convoy/ConvoySignInScreen.kt:96:    context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE).edit()
    [9] const val TERMS_VERSION   = "1.0"
        [W] ConvoyTermsScreen.kt:24:            Text(text = "RIDER TERMS V$TERMS_VERSION", color = GroupTrackColors.SkyBlue, fontSize = 11.sp, fontWeight = FontWeight.Bold, letterSpacing = 2.sp, modifier = Modifier.padding(bottom = 16.dp))
        [R] convoy/ConvoySessionManager.kt:30:        prefs(ctx).getString("terms_version", null) == TERMS_VERSION
        [R] convoy/ConvoySessionManager.kt:83:        prefs(ctx).edit().putString("terms_version", TERMS_VERSION).apply()
    [10] const val PRIVACY_VERSION = "1.0"
        [W] ConvoyPrivacyScreen.kt:27:            Text(text = "PRIVACY POLICY V$PRIVACY_VERSION", color = GroupTrackColors.SkyBlue, fontSize = 11.sp, fontWeight = FontWeight.Bold, letterSpacing = 2.sp, modifier = Modifier.padding(bottom = 16.dp))
        [R] convoy/ConvoySessionManager.kt:33:        prefs(ctx).getString("privacy_version", null) == PRIVACY_VERSION
        [R] convoy/ConvoySessionManager.kt:87:        prefs(ctx).edit().putString("privacy_version", PRIVACY_VERSION).apply()

  FILE: ConvoyConfig.kt
    [6] @Volatile var trackDisplayState: Int = 0
        [W] ConvoyMapViewerScreen.kt:396:                                        if (ConvoyConfig.trackDisplayState != DS_OFF) {
        [W] ConvoyMapViewerScreen.kt:660:                        "Tracks" -> { ConvoyConfig.trackDisplayState = newState; trackState = newState; if (newState != DS_SELECTED) { trackCheckedIds = null; ConvoyConfig.trackChecked = null } }
        [W] ConvoyMapViewerScreen.kt:725:                            "Tracks" -> { ConvoyConfig.trackDisplayState = newState; trackState = newState; trackCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.trackChecked = if (newState == DS_SELECTED) checked else null }
        [W] ConvoyScreen.kt:1165:                            "Tracks" -> { ConvoyConfig.trackDisplayState = newState; trackState = newState }
        [W] ConvoyScreen.kt:1205:                                "Tracks" -> { trackState = newState; ConvoyConfig.trackDisplayState = newState; trackCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.trackChecked = if (newState == DS_SELECTED) checked else null }
        [R] convoy/ConvoyMapViewerScreen.kt:180:    var trackState by remember { mutableStateOf(ConvoyConfig.trackDisplayState) }
        [R] convoy/ConvoyMapViewerScreen.kt:446:                                val capTrackState = ConvoyConfig.trackDisplayState
        [R] convoy/ConvoyScreen.kt:481:                            val capTrackState = ConvoyConfig.trackDisplayState
        [R] convoy/ConvoyScreen.kt:652:                                val capTrackState = ConvoyConfig.trackDisplayState
    [7] @Volatile var waypointDisplayState: Int = 0
        [W] ConvoyMapViewerScreen.kt:401:                                        if (ConvoyConfig.waypointDisplayState != DS_OFF) {
        [W] ConvoyMapViewerScreen.kt:661:                        "Waypoints" -> { ConvoyConfig.waypointDisplayState = newState; waypointState = newState; if (newState != DS_SELECTED) { waypointCheckedIds = null; ConvoyConfig.waypointChecked = null } }
        [W] ConvoyMapViewerScreen.kt:726:                            "Waypoints" -> { ConvoyConfig.waypointDisplayState = newState; waypointState = newState; waypointCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.waypointChecked = if (newState == DS_SELECTED) checked else null }
        [W] ConvoyScreen.kt:1166:                            "Waypoints" -> { ConvoyConfig.waypointDisplayState = newState; waypointState = newState }
        [W] ConvoyScreen.kt:1206:                                "Waypoints" -> { waypointState = newState; ConvoyConfig.waypointDisplayState = newState; waypointCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.waypointChecked = if (newState == DS_SELECTED) checked else null }
        [R] convoy/ConvoyMapViewerScreen.kt:181:    var waypointState by remember { mutableStateOf(ConvoyConfig.waypointDisplayState) }
        [R] convoy/ConvoyMapViewerScreen.kt:447:                                val capWaypointState = ConvoyConfig.waypointDisplayState
        [R] convoy/ConvoyScreen.kt:482:                            val capWaypointState = ConvoyConfig.waypointDisplayState
        [R] convoy/ConvoyScreen.kt:653:                                val capWaypointState = ConvoyConfig.waypointDisplayState
    [8] @Volatile var routeDisplayState: Int = 0
        [W] ConvoyMapViewerScreen.kt:406:                                        if (ConvoyConfig.routeDisplayState != DS_OFF) {
        [W] ConvoyMapViewerScreen.kt:662:                        "Routes" -> { ConvoyConfig.routeDisplayState = newState; routeState = newState; if (newState != DS_SELECTED) { routeCheckedIds = null; ConvoyConfig.routeChecked = null } }
        [W] ConvoyMapViewerScreen.kt:727:                            "Routes" -> { ConvoyConfig.routeDisplayState = newState; routeState = newState; routeCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.routeChecked = if (newState == DS_SELECTED) checked else null }
        [W] ConvoyScreen.kt:1167:                            "Routes" -> { ConvoyConfig.routeDisplayState = newState; routeState = newState }
        [W] ConvoyScreen.kt:1207:                                "Routes" -> { routeState = newState; ConvoyConfig.routeDisplayState = newState; routeCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.routeChecked = if (newState == DS_SELECTED) checked else null }
        [R] convoy/ConvoyMapViewerScreen.kt:182:    var routeState by remember { mutableStateOf(ConvoyConfig.routeDisplayState) }
        [R] convoy/ConvoyMapViewerScreen.kt:448:                                val capRouteState = ConvoyConfig.routeDisplayState
        [R] convoy/ConvoyScreen.kt:483:                            val capRouteState = ConvoyConfig.routeDisplayState
        [R] convoy/ConvoyScreen.kt:654:                                val capRouteState = ConvoyConfig.routeDisplayState
    [9] @Volatile var trailChecked: Set<String>? = null
        [W] ConvoyMapViewerScreen.kt:659:                        "Trails" -> { ConvoyConfig.trailDisplayState = newState; trailState = newState; if (newState != DS_SELECTED) { trailCheckedIds = null; ConvoyConfig.trailChecked = null } }
        [W] ConvoyMapViewerScreen.kt:724:                            "Trails" -> { ConvoyConfig.trailDisplayState = newState; trailState = newState; trailCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.trailChecked = if (newState == DS_SELECTED) checked else null }
        [W] ConvoyScreen.kt:1204:                                "Trails" -> { trailState = newState; ConvoyConfig.trailDisplayState = newState; trailCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.trailChecked = if (newState == DS_SELECTED) checked else null }
        [R] convoy/ConvoyMapViewerScreen.kt:183:    var trailCheckedIds by remember { mutableStateOf(ConvoyConfig.trailChecked) }
        [R] convoy/ConvoyMapViewerScreen.kt:449:                                val capTrailIds = ConvoyConfig.trailChecked
        [R] convoy/ConvoyScreen.kt:484:                            val capTrailIds = ConvoyConfig.trailChecked
        [R] convoy/ConvoyScreen.kt:658:                                val capTrailIds = ConvoyConfig.trailChecked
    [10] @Volatile var trackChecked: Set<String>? = null
        [W] ConvoyMapViewerScreen.kt:660:                        "Tracks" -> { ConvoyConfig.trackDisplayState = newState; trackState = newState; if (newState != DS_SELECTED) { trackCheckedIds = null; ConvoyConfig.trackChecked = null } }
        [W] ConvoyMapViewerScreen.kt:725:                            "Tracks" -> { ConvoyConfig.trackDisplayState = newState; trackState = newState; trackCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.trackChecked = if (newState == DS_SELECTED) checked else null }
        [W] ConvoyScreen.kt:1205:                                "Tracks" -> { trackState = newState; ConvoyConfig.trackDisplayState = newState; trackCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.trackChecked = if (newState == DS_SELECTED) checked else null }
        [R] convoy/ConvoyMapViewerScreen.kt:184:    var trackCheckedIds by remember { mutableStateOf(ConvoyConfig.trackChecked) }
        [R] convoy/ConvoyMapViewerScreen.kt:450:                                val capTrackIds = ConvoyConfig.trackChecked
        [R] convoy/ConvoyScreen.kt:485:                            val capTrackIds = ConvoyConfig.trackChecked
        [R] convoy/ConvoyScreen.kt:659:                                val capTrackIds = ConvoyConfig.trackChecked
    [11] @Volatile var waypointChecked: Set<String>? = null
        [W] ConvoyMapViewerScreen.kt:661:                        "Waypoints" -> { ConvoyConfig.waypointDisplayState = newState; waypointState = newState; if (newState != DS_SELECTED) { waypointCheckedIds = null; ConvoyConfig.waypointChecked = null } }
        [W] ConvoyMapViewerScreen.kt:726:                            "Waypoints" -> { ConvoyConfig.waypointDisplayState = newState; waypointState = newState; waypointCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.waypointChecked = if (newState == DS_SELECTED) checked else null }
        [W] ConvoyScreen.kt:1206:                                "Waypoints" -> { waypointState = newState; ConvoyConfig.waypointDisplayState = newState; waypointCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.waypointChecked = if (newState == DS_SELECTED) checked else null }
        [R] convoy/ConvoyMapViewerScreen.kt:185:    var waypointCheckedIds by remember { mutableStateOf(ConvoyConfig.waypointChecked) }
        [R] convoy/ConvoyMapViewerScreen.kt:451:                                val capWaypointIds = ConvoyConfig.waypointChecked
        [R] convoy/ConvoyScreen.kt:486:                            val capWaypointIds = ConvoyConfig.waypointChecked
        [R] convoy/ConvoyScreen.kt:660:                                val capWaypointIds = ConvoyConfig.waypointChecked
    [12] @Volatile var routeChecked: Set<String>? = null
        [W] ConvoyMapViewerScreen.kt:662:                        "Routes" -> { ConvoyConfig.routeDisplayState = newState; routeState = newState; if (newState != DS_SELECTED) { routeCheckedIds = null; ConvoyConfig.routeChecked = null } }
        [W] ConvoyMapViewerScreen.kt:727:                            "Routes" -> { ConvoyConfig.routeDisplayState = newState; routeState = newState; routeCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.routeChecked = if (newState == DS_SELECTED) checked else null }
        [W] ConvoyScreen.kt:1207:                                "Routes" -> { routeState = newState; ConvoyConfig.routeDisplayState = newState; routeCheckedIds = if (newState == DS_SELECTED) checked else null; ConvoyConfig.routeChecked = if (newState == DS_SELECTED) checked else null }
        [R] convoy/ConvoyMapViewerScreen.kt:186:    var routeCheckedIds by remember { mutableStateOf(ConvoyConfig.routeChecked) }
        [R] convoy/ConvoyMapViewerScreen.kt:452:                                val capRouteIds = ConvoyConfig.routeChecked
        [R] convoy/ConvoyScreen.kt:487:                            val capRouteIds = ConvoyConfig.routeChecked
        [R] convoy/ConvoyScreen.kt:661:                                val capRouteIds = ConvoyConfig.routeChecked
    [14] const val MAP_DEFAULT_ZOOM = 18.0
    [53] var moved = 0
        [W] ConvoyDownloadQueue.kt:235:        android.util.Log.i(TAG, "Cancelled and removed: ${entry.label} id=$entryId")
        [W] ConvoyDownloadQueue.kt:281:        android.util.Log.i(TAG, "Removed: ${entry.label} id=$entryId")
        [W] ConvoyDownloadQueue.kt:318:            android.util.Log.d(TAG, "Refresh cell complete, removed: id=$entryId downloaded=$downloaded")
        [W] ConvoySettingsRepository.kt:53:    val removedCartsForToday: Flow<Map<String, String>> = context.convoyDataStore.data
        [W] ConvoySettingsViewModel.kt:21:    val removedCarts:         Map<String, String> = emptyMap(),
        [R] convoy/ConvoyConfig.kt:64:                    moved++
        [R] convoy/ConvoyConfig.kt:75:            android.util.Log.d("TileMigrate", "Migration complete: $moved source(s) moved")
        [R] convoy/ConvoyScreen.kt:1156:                // QUEUES panel moved above maps bar
    [82] const val MAP_GROUP_ZOOM_PADDING = 1.4f
    [83] const val MAP_CART_ZOOM = 18.0
        [R] convoy/ConvoyScreen.kt:255:                    wv.evaluateJavascript("setView(${it.latitude}, ${it.longitude}, ${ConvoyConfig.MAP_CART_ZOOM})", null)
        [R] convoy/ConvoyScreen.kt:269:                    wv.evaluateJavascript("setView(${it.latitude}, ${it.longitude}, ${ConvoyConfig.MAP_CART_ZOOM})", null)
        [R] convoy/ConvoyScreen.kt:274:                    wv.evaluateJavascript("setView(${it.latitude}, ${it.longitude}, ${ConvoyConfig.MAP_CART_ZOOM})", null)
    [84] const val MAP_MIN_ZOOM = 16.0
    [86] var SHOW_DOWNLOADED_ON_OPEN = false
        [R] convoy/ConvoyScreen.kt:720:                                    if (!ConvoyConfig.SHOW_DOWNLOADED_ON_OPEN) return@post
    [87] const val BLINK_LOST_MS = 3000L
    [88] const val BLINK_DROP_MS = 150L
    [89] const val MARKER_SIZE_LARGE_DP = 24
    [90] const val MARKER_SIZE_MEDIUM_DP = 16
    [91] const val TICK_MS = 5000L
        [R] convoy/ConvoySimulation.kt:57:     * Call this every TICK_MS (5 seconds).
        [R] convoy/ConvoyViewModel.kt:508:            while (true) { tick(); delay(ConvoySimulation.TICK_MS) }
    [92] var SIGNAL_DROP_MINUTES = 2f
        [W] ConvoySettingsRepository.kt:26:        val KEY_SIGNAL_DROP_MINUTES    = floatPreferencesKey("signal_drop_minutes")
        [W] ConvoySettingsRepository.kt:67:        context.convoyDataStore.edit { it[KEY_SIGNAL_DROP_MINUTES] = value }
        [W] ConvoySettingsRepository.kt:68:        ConvoyConfig.SIGNAL_DROP_MINUTES = value
        [R] convoy/ConvoyEngine.kt:65:        val dropMs = (ConvoyConfig.SIGNAL_DROP_MINUTES * 60_000f).toLong()
    [93] var LOST_MINUTES = 10f
        [W] ConvoySettingsRepository.kt:27:        val KEY_SIGNAL_LOST_MINUTES    = floatPreferencesKey("signal_lost_minutes")
        [W] ConvoySettingsRepository.kt:72:        context.convoyDataStore.edit { it[KEY_SIGNAL_LOST_MINUTES] = value }
        [W] ConvoySettingsRepository.kt:73:        ConvoyConfig.LOST_MINUTES = value
        [R] convoy/ConvoyEngine.kt:64:        val lostMs = (ConvoyConfig.LOST_MINUTES * 60_000f).toLong()

--- 5C: SharedPreferences ---

  WRITERS:
    [W] ConvoyApplyListMaintenanceScreen.kt:51:        ConvoyApplyList.save(context, applyList)
    [W] ConvoyApplyListMaintenanceScreen.kt:207:                    ConvoyApplyList.save(context, applyList)
    [W] ConvoyApplyListScreen.kt:198:                            ConvoyApplyList.save(context, applyList)
    [W] ConvoyApplyListScreen.kt:219:                                ConvoyApplyList.save(context, applyList)
    [W] ConvoyApplyListScreen.kt:238:                            ConvoyApplyList.save(context, applyList)
    [W] ConvoyApplyListScreen.kt:256:                            ConvoyApplyList.save(context, applyList)
    [W] ConvoyApplyListScreen.kt:274:                            ConvoyApplyList.save(context, applyList)
    [W] ConvoyApplyListScreen.kt:292:                            ConvoyApplyList.save(context, applyList)
    [W] ConvoyConfig.kt:80:        prefs.edit().putBoolean("tiles_migrated", true).apply()
    [W] ConvoyCreateEventScreen.kt:323:                                ConvoyEventStore.save(context, event)
    [W] ConvoyCreateRideScreen.kt:296:                                ConvoyEventStore.save(context, event)
    [W] ConvoyDashboardScreen.kt:78:        prefs.edit().putStringSet("followed_ids", followedOrganizers).apply()
    [W] ConvoyDevSeeder.kt:31:            ctx.getSharedPreferences(it, Context.MODE_PRIVATE).edit().clear().apply()
    [W] ConvoyDevSeeder.kt:85:        val p = ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).edit()
    [W] ConvoyDevSeeder.kt:88:        p.putString("notif_001_ride_id", "ride-001")
    [W] ConvoyDevSeeder.kt:89:        p.putString("notif_001_ride", "Sunday Desert Run — Gooseberry Mesa")
    [W] ConvoyDevSeeder.kt:90:        p.putString("notif_001_organizer_id", "dev-user-001")
    [W] ConvoyDevSeeder.kt:91:        p.putString("notif_001_visibility", "PRIVATE")
    [W] ConvoyDevSeeder.kt:92:        p.putString("notif_001_ride_status", "OPEN")
    [W] ConvoyDevSeeder.kt:93:        p.putString("notif_001_date", "April 13, 2026")
    [W] ConvoyDevSeeder.kt:94:        p.putString("notif_001_trailhead", "Gooseberry Trailhead")
    [W] ConvoyDevSeeder.kt:95:        p.putString("notif_001_location", "Springdale, UT")
    [W] ConvoyDevSeeder.kt:96:        p.putString("notif_001_arrive_time", "7:30 AM")
    [W] ConvoyDevSeeder.kt:97:        p.putString("notif_001_depart_time", "8:00 AM")
    [W] ConvoyDevSeeder.kt:98:        p.putString("notif_001_description", "Moderate terrain, UTV recommended. Bring water and snacks. Meet at staging area.")
    [W] ConvoyDevSeeder.kt:99:        p.putInt("notif_001_invited", 8)
    [W] ConvoyDevSeeder.kt:100:        p.putInt("notif_001_accepted", 5)
    [W] ConvoyDevSeeder.kt:101:        p.putInt("notif_001_maybe", 2)
    [W] ConvoyDevSeeder.kt:102:        p.putInt("notif_001_declined", 1)
    [W] ConvoyDevSeeder.kt:104:        p.putString("notif_001_accepted_001_name", "Dave H"); p.putString("notif_001_accepted_001_callsign", "DELTA-4"); p.putString("notif_001_accepted_001_vehicle", "RZR Pro XP")
    [W] ConvoyDevSeeder.kt:105:        p.putString("notif_001_accepted_002_name", "Mike T"); p.putString("notif_001_accepted_002_callsign", "MIKE-7"); p.putString("notif_001_accepted_002_vehicle", "Can-Am Maverick")
    [W] ConvoyDevSeeder.kt:106:        p.putString("notif_001_accepted_003_name", "Sarah K"); p.putString("notif_001_accepted_003_callsign", "SIERRA-3"); p.putString("notif_001_accepted_003_vehicle", "Polaris General")
    [W] ConvoyDevSeeder.kt:107:        p.putString("notif_001_accepted_004_name", "Tom R"); p.putString("notif_001_accepted_004_callsign", "TANGO-2"); p.putString("notif_001_accepted_004_vehicle", "RZR Turbo R")
    [W] ConvoyDevSeeder.kt:108:        p.putString("notif_001_accepted_005_name", "Lisa M"); p.putString("notif_001_accepted_005_callsign", "LIMA-1"); p.putString("notif_001_accepted_005_vehicle", "Can-Am Defender")
    [W] ConvoyDevSeeder.kt:110:        p.putString("notif_001_maybe_001_name", "Chris B"); p.putString("notif_001_maybe_001_callsign", "CHARLIE-5"); p.putString("notif_001_maybe_001_vehicle", "RZR 1000")
    [W] ConvoyDevSeeder.kt:111:        p.putString("notif_001_maybe_002_name", "Ann W"); p.putString("notif_001_maybe_002_callsign", "ALPHA-8"); p.putString("notif_001_maybe_002_vehicle", "Polaris RZR")
    [W] ConvoyDevSeeder.kt:113:        p.putString("notif_001_declined_001_name", "Bob L"); p.putString("notif_001_declined_001_callsign", "BRAVO-6"); p.putString("notif_001_declined_001_vehicle", "Can-Am X3")
    [W] ConvoyDevSeeder.kt:116:        p.putString("notif_002_ride_id", "ride-005")
    [W] ConvoyDevSeeder.kt:117:        p.putString("notif_002_ride", "Hurricane Cliffs Bash")
    [W] ConvoyDevSeeder.kt:118:        p.putString("notif_002_organizer_id", "dev-user-001")
    [W] ConvoyDevSeeder.kt:119:        p.putString("notif_002_visibility", "PUBLIC")
    [W] ConvoyDevSeeder.kt:120:        p.putString("notif_002_ride_status", "CLOSED")
    [W] ConvoyDevSeeder.kt:121:        p.putString("notif_002_date", "April 22, 2026")
    [W] ConvoyDevSeeder.kt:122:        p.putString("notif_002_trailhead", "Hurricane Trailhead")
    [W] ConvoyDevSeeder.kt:123:        p.putString("notif_002_location", "Hurricane, UT")
    [W] ConvoyDevSeeder.kt:124:        p.putString("notif_002_arrive_time", "6:30 AM")
    [W] ConvoyDevSeeder.kt:125:        p.putString("notif_002_depart_time", "7:00 AM")
    [W] ConvoyDevSeeder.kt:126:        p.putString("notif_002_description", "Full day ride. Hard terrain. Experienced riders only. Ride is now full.")
    [W] ConvoyDevSeeder.kt:127:        p.putInt("notif_002_invited", 20)
    [W] ConvoyDevSeeder.kt:128:        p.putInt("notif_002_accepted", 20)
    [W] ConvoyDevSeeder.kt:129:        p.putInt("notif_002_maybe", 0)
    [W] ConvoyDevSeeder.kt:130:        p.putInt("notif_002_declined", 0)
    [W] ConvoyDevSeeder.kt:132:        p.putInt("notif_count", 2)
    [W] ConvoyDevSeeder.kt:138:        val p = ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).edit()
    [W] ConvoyDevSeeder.kt:140:        p.putString("invite_001_ride_id", "ride-ext-001")
    [W] ConvoyDevSeeder.kt:141:        p.putString("invite_001_ride", "Wednesday Night Ride — Sand Hollow")
    [W] ConvoyDevSeeder.kt:142:        p.putString("invite_001_organizer", "Dave H")
    [W] ConvoyDevSeeder.kt:143:        p.putString("invite_001_organizer_id", "org-dave-001")
    [W] ConvoyDevSeeder.kt:144:        p.putString("invite_001_email", "dave.h@grouptrack.org")
    [W] ConvoyDevSeeder.kt:145:        p.putString("invite_001_date", "April 16, 2026")
    [W] ConvoyDevSeeder.kt:146:        p.putString("invite_001_time", "6:00 PM")
    [W] ConvoyDevSeeder.kt:147:        p.putString("invite_001_visibility", "PRIVATE")
    [W] ConvoyDevSeeder.kt:148:        p.putString("invite_001_ride_status", "OPEN")
    [W] ConvoyDevSeeder.kt:149:        p.putString("invite_001_trailhead", "Sand Hollow Trailhead")
    [W] ConvoyDevSeeder.kt:150:        p.putString("invite_001_location", "Hurricane, UT")
    [W] ConvoyDevSeeder.kt:151:        p.putString("invite_001_my_status", "INVITED")
    [W] ConvoyDevSeeder.kt:153:        p.putString("invite_002_ride_id", "ride-ext-002")
    [W] ConvoyDevSeeder.kt:154:        p.putString("invite_002_ride", "Zion Narrows Approach")
    [W] ConvoyDevSeeder.kt:155:        p.putString("invite_002_organizer", "Red Rock Riders")
    [W] ConvoyDevSeeder.kt:156:        p.putString("invite_002_organizer_id", "org-rrr-001")
    [W] ConvoyDevSeeder.kt:157:        p.putString("invite_002_email", "rides@redrockriders.com")
    [W] ConvoyDevSeeder.kt:158:        p.putString("invite_002_date", "April 20, 2026")
    [W] ConvoyDevSeeder.kt:159:        p.putString("invite_002_time", "7:00 AM")
    [W] ConvoyDevSeeder.kt:160:        p.putString("invite_002_visibility", "PUBLIC")
    [W] ConvoyDevSeeder.kt:161:        p.putString("invite_002_ride_status", "OPEN")
    [W] ConvoyDevSeeder.kt:162:        p.putString("invite_002_trailhead", "Zion Canyon Trailhead")
    [W] ConvoyDevSeeder.kt:163:        p.putString("invite_002_location", "Springdale, UT")
    [W] ConvoyDevSeeder.kt:164:        p.putString("invite_002_my_status", "MAYBE")
    [W] ConvoyDevSeeder.kt:166:        p.putInt("invite_count", 2)
    [W] ConvoyDevSeeder.kt:172:        val p = ctx.getSharedPreferences(PREFS_RIDES, Context.MODE_PRIVATE).edit()
    [W] ConvoyDevSeeder.kt:175:        p.putString("ride_001_id", "ride-001")
    [W] ConvoyDevSeeder.kt:176:        p.putString("ride_001_name", "Sunday Desert Run — Gooseberry Mesa")
    [W] ConvoyDevSeeder.kt:177:        p.putString("ride_001_date", "April 13, 2026")
    [W] ConvoyDevSeeder.kt:178:        p.putString("ride_001_time", "8:00 AM")
    [W] ConvoyDevSeeder.kt:179:        p.putString("ride_001_my_status", "ORGANIZED")
    [W] ConvoyDevSeeder.kt:180:        p.putString("ride_001_ride_status", "OPEN")
    [W] ConvoyDevSeeder.kt:181:        p.putString("ride_001_visibility", "PRIVATE")
    [W] ConvoyDevSeeder.kt:182:        p.putString("ride_001_organizer", "Fred Dev")
    [W] ConvoyDevSeeder.kt:183:        p.putString("ride_001_organizer_id", "dev-user-001")
    [W] ConvoyDevSeeder.kt:184:        p.putString("ride_001_email", "fred@grouptrack.org")
    [W] ConvoyDevSeeder.kt:185:        p.putString("ride_001_trailhead", "Gooseberry Trailhead")
    [W] ConvoyDevSeeder.kt:186:        p.putString("ride_001_location", "Springdale, UT")
    [W] ConvoyDevSeeder.kt:187:        p.putInt("ride_001_enrolled", 5)
    [W] ConvoyDevSeeder.kt:190:        p.putString("ride_002_id", "ride-002")
    [W] ConvoyDevSeeder.kt:191:        p.putString("ride_002_name", "Wednesday Night Ride — Sand Hollow")
    [W] ConvoyDevSeeder.kt:192:        p.putString("ride_002_date", "April 16, 2026")
    [W] ConvoyDevSeeder.kt:193:        p.putString("ride_002_time", "6:00 PM")
    [W] ConvoyDevSeeder.kt:194:        p.putString("ride_002_my_status", "ACCEPTED")
    [W] ConvoyDevSeeder.kt:195:        p.putString("ride_002_ride_status", "OPEN")
    [W] ConvoyDevSeeder.kt:196:        p.putString("ride_002_visibility", "PUBLIC")
    [W] ConvoyDevSeeder.kt:197:        p.putString("ride_002_organizer", "Dave H")
    [W] ConvoyDevSeeder.kt:198:        p.putString("ride_002_organizer_id", "org-dave-001")
    [W] ConvoyDevSeeder.kt:199:        p.putString("ride_002_email", "dave.h@grouptrack.org")
    [W] ConvoyDevSeeder.kt:200:        p.putString("ride_002_trailhead", "Sand Hollow Trailhead")
    [W] ConvoyDevSeeder.kt:201:        p.putString("ride_002_location", "Hurricane, UT")
    [W] ConvoyDevSeeder.kt:202:        p.putInt("ride_002_enrolled", 6)
    [W] ConvoyDevSeeder.kt:205:        p.putString("ride_003_id", "ride-003")
    [W] ConvoyDevSeeder.kt:206:        p.putString("ride_003_name", "Zion Narrows Approach")
    [W] ConvoyDevSeeder.kt:207:        p.putString("ride_003_date", "April 20, 2026")
    [W] ConvoyDevSeeder.kt:208:        p.putString("ride_003_time", "7:00 AM")
    [W] ConvoyDevSeeder.kt:209:        p.putString("ride_003_my_status", "MAYBE")
    [W] ConvoyDevSeeder.kt:210:        p.putString("ride_003_ride_status", "OPEN")
    [W] ConvoyDevSeeder.kt:211:        p.putString("ride_003_visibility", "PUBLIC")
    [W] ConvoyDevSeeder.kt:212:        p.putString("ride_003_organizer", "Red Rock Riders")
    [W] ConvoyDevSeeder.kt:213:        p.putString("ride_003_organizer_id", "org-rrr-001")
    [W] ConvoyDevSeeder.kt:214:        p.putString("ride_003_email", "rides@redrockriders.com")
    [W] ConvoyDevSeeder.kt:215:        p.putString("ride_003_trailhead", "Zion Canyon Trailhead")
    [W] ConvoyDevSeeder.kt:216:        p.putString("ride_003_location", "Springdale, UT")
    [W] ConvoyDevSeeder.kt:217:        p.putInt("ride_003_enrolled", 12)
    [W] ConvoyDevSeeder.kt:220:        p.putString("ride_004_id", "ride-004")
    [W] ConvoyDevSeeder.kt:221:        p.putString("ride_004_name", "Escalante Canyons — May Run")
    [W] ConvoyDevSeeder.kt:222:        p.putString("ride_004_date", "May 3, 2026")
    [W] ConvoyDevSeeder.kt:223:        p.putString("ride_004_time", "TBD")
    [W] ConvoyDevSeeder.kt:224:        p.putString("ride_004_my_status", "ORGANIZED")
    [W] ConvoyDevSeeder.kt:225:        p.putString("ride_004_ride_status", "PENDING")
    [W] ConvoyDevSeeder.kt:226:        p.putString("ride_004_visibility", "PUBLIC")
    [W] ConvoyDevSeeder.kt:227:        p.putString("ride_004_organizer", "Fred Dev")
    [W] ConvoyDevSeeder.kt:228:        p.putString("ride_004_organizer_id", "dev-user-001")
    [W] ConvoyDevSeeder.kt:229:        p.putString("ride_004_email", "fred@grouptrack.org")
    [W] ConvoyDevSeeder.kt:230:        p.putString("ride_004_trailhead", "")
    [W] ConvoyDevSeeder.kt:231:        p.putString("ride_004_location", "Escalante, UT")
    [W] ConvoyDevSeeder.kt:232:        p.putInt("ride_004_enrolled", 0)
    [W] ConvoyDevSeeder.kt:235:        p.putString("ride_005_id", "ride-005")
    [W] ConvoyDevSeeder.kt:236:        p.putString("ride_005_name", "Hurricane Cliffs Bash")
    [W] ConvoyDevSeeder.kt:237:        p.putString("ride_005_date", "April 22, 2026")
    [W] ConvoyDevSeeder.kt:238:        p.putString("ride_005_time", "7:00 AM")
    [W] ConvoyDevSeeder.kt:239:        p.putString("ride_005_my_status", "ORGANIZED")
    [W] ConvoyDevSeeder.kt:240:        p.putString("ride_005_ride_status", "CLOSED")
    [W] ConvoyDevSeeder.kt:241:        p.putString("ride_005_visibility", "PUBLIC")
    [W] ConvoyDevSeeder.kt:242:        p.putString("ride_005_organizer", "Fred Dev")
    [W] ConvoyDevSeeder.kt:243:        p.putString("ride_005_organizer_id", "dev-user-001")
    [W] ConvoyDevSeeder.kt:244:        p.putString("ride_005_email", "fred@grouptrack.org")
    [W] ConvoyDevSeeder.kt:245:        p.putString("ride_005_trailhead", "Hurricane Trailhead")
    [W] ConvoyDevSeeder.kt:246:        p.putString("ride_005_location", "Hurricane, UT")
    [W] ConvoyDevSeeder.kt:247:        p.putInt("ride_005_enrolled", 20)
    [W] ConvoyDevSeeder.kt:249:        p.putInt("ride_count", 5)
    [W] ConvoyDevSeeder.kt:255:        val p = ctx.getSharedPreferences(PREFS_PUBLIC, Context.MODE_PRIVATE).edit()
    [W] ConvoyDevSeeder.kt:261:            p.putString("${k}name", name); p.putString("${k}organizer", org)
    [W] ConvoyDevSeeder.kt:262:            p.putString("${k}organizer_id", orgId); p.putString("${k}email", email)
    [W] ConvoyDevSeeder.kt:263:            p.putString("${k}date", date); p.putString("${k}time", time)
    [W] ConvoyDevSeeder.kt:264:            p.putString("${k}distance_miles", dist); p.putString("${k}location", location)
    [W] ConvoyDevSeeder.kt:265:            p.putString("${k}trailhead", trailhead); p.putString("${k}ride_status", status)
    [W] ConvoyDevSeeder.kt:266:            p.putString("${k}visibility", "PUBLIC")
    [W] ConvoyDevSeeder.kt:267:            p.putBoolean("${k}invite_required", false)
    [W] ConvoyDevSeeder.kt:318:        p.putInt("pub_count", 11)
    [W] ConvoyDownloadQueue.kt:358:            .putString("entry_id", entry.id)
    [W] ConvoyDownloadQueue.kt:363:            .putString("label", entry.label)
    [W] ConvoyDownloadQueue.kt:364:            .putBoolean("refresh_mode", entry.refreshMode)
    [W] ConvoyDownloadQueue.kt:365:            .putString("refresh_slot", entry.refreshSlot)
    [W] ConvoyDownloadRideConfigScreen.kt:262:                        ConvoyEventStore.save(context, event)
    [W] ConvoyDownloadWorker.kt:93:                                .putInt("downloaded", totalDownloaded)
    [W] ConvoyDownloadWorker.kt:94:                                .putInt("total", totalTiles)
    [W] ConvoyDownloadWorker.kt:95:                                .putInt("failed", totalFailed)
    [W] ConvoyDownloadWorker.kt:149:                                .putInt("downloaded", totalDownloaded)
    [W] ConvoyDownloadWorker.kt:150:                                .putInt("total", totalTiles)
    [W] ConvoyDownloadWorker.kt:151:                                .putInt("failed", totalFailed)
    [W] ConvoyEmailGateScreen.kt:294:                            ConvoyUserStore.save(context, upgraded)
    [W] ConvoyEnrollment.kt:253:                            ConvoyUserStore.save(context, user)
    [W] ConvoyEnrollmentQueue.kt:102:            .edit().putString("enrollment_${rideId}_${task}_status", status).apply()
    [W] ConvoyInviteHandler.kt:77:                    ConvoyEventStore.save(this@ConvoyInviteHandler, eventConfig)
    [W] ConvoyInviteHandler.kt:141:                .edit().apply {
    [W] ConvoyInviteHandler.kt:146:                    putString("pending_source",  tileSource)
    [W] ConvoyInviteHandler.kt:147:                    putInt("pending_zoom_max",   zoomMax)
    [W] ConvoyInviteHandler.kt:148:                    putBoolean("pending_download", true)
    [W] ConvoyInviteHandler.kt:171:                .edit().clear().apply()
    [W] ConvoyRideDetailScreen.kt:108:        prefs.edit().putStringSet("followed_ids", current).apply()
    [W] ConvoySessionManager.kt:49:        prefs(ctx).edit().putString("city", city).apply()
    [W] ConvoySessionManager.kt:53:        prefs(ctx).edit().putString("state", state).apply()
    [W] ConvoySessionManager.kt:62:        prefs(ctx).edit().putString("zip_code", zip).apply()
    [W] ConvoySessionManager.kt:66:        prefs(ctx).edit().putInt("search_radius_miles", miles).apply()
    [W] ConvoySessionManager.kt:71:        prefs(ctx).edit().apply {
    [W] ConvoySessionManager.kt:72:            putString("user_id",    userId)
    [W] ConvoySessionManager.kt:73:            putString("google_id",  googleId)
    [W] ConvoySessionManager.kt:74:            putString("email",      email)
    [W] ConvoySessionManager.kt:75:            putString("first_name", firstName)
    [W] ConvoySessionManager.kt:76:            putString("last_name",  lastName)
    [W] ConvoySessionManager.kt:83:        prefs(ctx).edit().putString("terms_version", TERMS_VERSION).apply()
    [W] ConvoySessionManager.kt:87:        prefs(ctx).edit().putString("privacy_version", PRIVACY_VERSION).apply()
    [W] ConvoySessionManager.kt:91:        prefs(ctx).edit().putBoolean("is_organizer", value).apply()
    [W] ConvoySessionManager.kt:95:        prefs(ctx).edit().putLong("subscription_expires_at", expiresAtMs).apply()
    [W] ConvoySessionManager.kt:99:        prefs(ctx).edit().clear().apply()
    [W] ConvoySettingsRepository.kt:67:        context.convoyDataStore.edit { it[KEY_SIGNAL_DROP_MINUTES] = value }
    [W] ConvoySettingsRepository.kt:72:        context.convoyDataStore.edit { it[KEY_SIGNAL_LOST_MINUTES] = value }
    [W] ConvoySettingsRepository.kt:77:        context.convoyDataStore.edit { it[KEY_OFF_TRACK_MILES] = value }
    [W] ConvoySettingsRepository.kt:82:        context.convoyDataStore.edit { it[KEY_ADMISSION_WINDOW_HOURS] = value }
    [W] ConvoySettingsRepository.kt:85:        context.convoyDataStore.edit { it[KEY_LEAD_LOCK_FEET] = value }
    [W] ConvoySettingsRepository.kt:91:        context.convoyDataStore.edit { prefs ->
    [W] ConvoySettingsRepository.kt:100:        context.convoyDataStore.edit { prefs ->
    [W] ConvoySettingsRepository.kt:111:        context.convoyDataStore.edit { prefs ->
    [W] ConvoySignInScreen.kt:68:    context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE).edit().apply {
    [W] ConvoySignInScreen.kt:69:        putString(PREF_USER_ID, userId)
    [W] ConvoySignInScreen.kt:70:        putString(PREF_GOOGLE_ID, googleId)
    [W] ConvoySignInScreen.kt:71:        putString(PREF_EMAIL, email)
    [W] ConvoySignInScreen.kt:72:        putString(PREF_FIRST_NAME, firstName)
    [W] ConvoySignInScreen.kt:73:        putString(PREF_LAST_NAME, lastName)
    [W] ConvoySignInScreen.kt:91:    context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE).edit()
    [W] ConvoySignInScreen.kt:92:        .putBoolean(PREF_DEV_SUBSCRIBED, subscribed)
    [W] ConvoySignInScreen.kt:96:    context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE).edit()
    [W] ConvoySignInScreen.kt:97:        .putLong("subscription_expires_at", expiresAt)
    [W] ConvoySubscriptionScreen.kt:397:        .edit()
    [W] ConvoySubscriptionScreen.kt:398:        .putLong("subscription_expires_at", expiresAtMs)
    [W] ConvoyUser.kt:154:            .edit().putString(ACTIVE_KEY, userId).apply()
    [W] ConvoyViewModel.kt:356:                            ConvoyEventStore.save(appContext, event)

  READERS:
    [R] ConvoyApplyList.kt:116:                    val names = (0 until arr.length()).map { arr.getString(it) }.toSet()
    [R] ConvoyConfig.kt:45:        if (prefs.getBoolean("tiles_migrated", false)) return
    [R] ConvoyDashboardScreen.kt:65:                .getStringSet("followed_ids", emptySet()) ?: emptySet()).toMutableSet()
    [R] ConvoyDevSeeder.kt:36:    fun getRideCount(ctx: Context)       = ctx.getSharedPreferences(PREFS_RIDES,  Context.MODE_PRIVATE).getInt("ride_count", 0)
    [R] ConvoyDevSeeder.kt:37:    fun getNotifCount(ctx: Context)      = ctx.getSharedPreferences(PREFS_NOTIF,  Context.MODE_PRIVATE).getInt("notif_count", 0)
    [R] ConvoyDevSeeder.kt:38:    fun getInviteCount(ctx: Context)     = ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).getInt("invite_count", 0)
    [R] ConvoyDevSeeder.kt:39:    fun getPublicRideCount(ctx: Context) = ctx.getSharedPreferences(PREFS_PUBLIC, Context.MODE_PRIVATE).getInt("pub_count", 0)
    [R] ConvoyDevSeeder.kt:42:        ctx.getSharedPreferences(PREFS_RIDES, Context.MODE_PRIVATE).getString("ride_${pad(i)}_$field", "") ?: ""
    [R] ConvoyDevSeeder.kt:44:        ctx.getSharedPreferences(PREFS_RIDES, Context.MODE_PRIVATE).getInt("ride_${pad(i)}_$field", 0)
    [R] ConvoyDevSeeder.kt:56:        ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).getString("notif_${pad(i)}_$field", "") ?: ""
    [R] ConvoyDevSeeder.kt:58:        ctx.getSharedPreferences(PREFS_NOTIF, Context.MODE_PRIVATE).getInt("notif_${pad(i)}_$field", 0)
    [R] ConvoyDevSeeder.kt:61:        ctx.getSharedPreferences(PREFS_INVITE, Context.MODE_PRIVATE).getString("invite_${pad(i)}_$field", "") ?: ""
    [R] ConvoyDevSeeder.kt:64:        ctx.getSharedPreferences(PREFS_PUBLIC, Context.MODE_PRIVATE).getString("pub_${pad(i)}_$field", "") ?: ""
    [R] ConvoyDevSeeder.kt:66:        ctx.getSharedPreferences(PREFS_PUBLIC, Context.MODE_PRIVATE).getBoolean("pub_${pad(i)}_invite_required", false)
    [R] ConvoyDownloadActionReceiver.kt:24:                val entryId = intent.getStringExtra(EXTRA_ENTRY_ID) ?: return
    [R] ConvoyDownloadWorker.kt:32:        val entryId = inputData.getString("entry_id") ?: return Result.failure()
    [R] ConvoyDownloadWorker.kt:37:        val label = inputData.getString("label") ?: "Map tiles"
    [R] ConvoyDownloadWorker.kt:38:        val refreshMode = inputData.getBoolean("refresh_mode", false)
    [R] ConvoyDownloadWorker.kt:39:        val refreshSlot = inputData.getString("refresh_slot") ?: ""
    [R] ConvoyEnrollmentQueue.kt:107:            .getString("enrollment_${rideId}_${task}_status", "PENDING") ?: "PENDING"
    [R] ConvoyEventConfig.kt:90:            eventId             = obj.getString("eventId"),
    [R] ConvoyEventConfig.kt:91:            eventName           = obj.getString("eventName"),
    [R] ConvoyEventConfig.kt:92:            eventDate           = obj.getString("eventDate"),
    [R] ConvoyEventConfig.kt:94:            organizerUserId     = obj.getString("organizerUserId"),
    [R] ConvoyEventConfig.kt:95:            organizerFirstName  = obj.getString("organizerFirstName"),
    [R] ConvoyEventConfig.kt:96:            organizerLastName   = obj.getString("organizerLastName"),
    [R] ConvoyEventConfig.kt:97:            organizerEmail      = obj.getString("organizerEmail"),
    [R] ConvoyEventConfig.kt:98:            organizerPhone      = obj.getString("organizerPhone"),
    [R] ConvoyEventConfig.kt:100:            hardwareId          = obj.getString("hardwareId"),
    [R] ConvoyEventConfig.kt:104:            channelName         = obj.getString("channelName"),
    [R] ConvoyEventConfig.kt:105:            channelPsk          = obj.getString("channelPsk"),
    [R] ConvoyEventConfig.kt:113:            createdDate         = obj.getString("createdDate"),
    [R] ConvoyEventConfig.kt:114:            expirationDate      = obj.getString("expirationDate"),
    [R] ConvoyInviteHandler.kt:158:            if (!prefs.getBoolean("pending_download", false)) return null
    [R] ConvoyInviteHandler.kt:164:                tileSource = prefs.getString("pending_source", "SAT") ?: "SAT",
    [R] ConvoyInviteHandler.kt:165:                zoomMax    = prefs.getInt("pending_zoom_max", 18)
    [R] ConvoyProfileScreen.kt:45:    var lastName by remember { mutableStateOf(context.getSharedPreferences("grouptrack_user", android.content.Context.MODE_PRIVATE).getString("last_name", "") ?: "") }
    [R] ConvoyRideDetailScreen.kt:88:                .getStringSet("followed_ids", emptySet()) ?: emptySet()).contains(orgId)
    [R] ConvoyRideDetailScreen.kt:100:        val current = (prefs.getStringSet("followed_ids", emptySet()) ?: emptySet()).toMutableSet()
    [R] ConvoySessionManager.kt:15:        prefs(ctx).getString("user_id", null)
    [R] ConvoySessionManager.kt:21:        if (prefs(ctx).getBoolean("dev_subscribed_override", false)) return true
    [R] ConvoySessionManager.kt:22:        val exp = prefs(ctx).getLong("subscription_expires_at", 0L)
    [R] ConvoySessionManager.kt:30:        prefs(ctx).getString("terms_version", null) == TERMS_VERSION
    [R] ConvoySessionManager.kt:33:        prefs(ctx).getString("privacy_version", null) == PRIVACY_VERSION
    [R] ConvoySessionManager.kt:36:        prefs(ctx).getBoolean("is_organizer", false)
    [R] ConvoySessionManager.kt:39:        prefs(ctx).getString("first_name", "") ?: ""
    [R] ConvoySessionManager.kt:42:        prefs(ctx).getString("email", "") ?: ""
    [R] ConvoySessionManager.kt:45:        prefs(ctx).getString("zip_code", "") ?: ""
    [R] ConvoySessionManager.kt:47:        prefs(ctx).getString("city", "") ?: ""
    [R] ConvoySessionManager.kt:51:        prefs(ctx).getString("state", "") ?: ""
    [R] ConvoySessionManager.kt:59:        prefs(ctx).getInt("search_radius_miles", 25)
    [R] ConvoySignInScreen.kt:81:        .getString(PREF_USER_ID, null)
    [R] ConvoySignInScreen.kt:104:        .getBoolean(PREF_DEV_SUBSCRIBED, false)
    [R] ConvoySubscriptionScreen.kt:387:    val expiresAt = prefs.getLong("subscription_expires_at", 0L)
    [R] ConvoyTrailSourceScreen.kt:618:                id = s.getString("id"),
    [R] ConvoyTrailSourceScreen.kt:619:                name = s.getString("name"),
    [R] ConvoyUser.kt:60:                for (i in 0 until arr.length()) devices.add(arr.getString(i))
    [R] ConvoyUser.kt:63:                userId        = obj.getString("userId"),
    [R] ConvoyUser.kt:64:                userType      = ConvoyUserType.valueOf(obj.getString("userType")),
    [R] ConvoyUser.kt:65:                firstName     = obj.getString("firstName"),
    [R] ConvoyUser.kt:66:                lastName      = obj.getString("lastName"),
    [R] ConvoyUser.kt:67:                email         = obj.getString("email"),
    [R] ConvoyUser.kt:68:                cellPhone     = obj.getString("cellPhone"),
    [R] ConvoyUser.kt:69:                vehicleType   = obj.getString("vehicleType"),
    [R] ConvoyUser.kt:70:                createdDate   = obj.getString("createdDate"),
    [R] ConvoyUser.kt:71:                expirationDate= obj.getString("expirationDate"),
    [R] ConvoyUser.kt:147:        val active = prefs.getString(ACTIVE_KEY, null) ?: return null
    [R] MapSourceManager.kt:89:                        role = l.getString("role"),
    [R] MapSourceManager.kt:90:                        urlTemplate = l.getString("url_template"),
    [R] MapSourceManager.kt:92:                        cacheDir = l.getString("cache_dir"),
    [R] MapSourceManager.kt:97:                    id = s.getString("id"),
    [R] MapSourceManager.kt:100:                    name = s.getString("name"),
    [R] MapSourceManager.kt:101:                    shortLabel = s.getString("short_label"),
    [R] MapSourceManager.kt:113:                    slot = sl.getInt("slot"),
    [R] MapSourceManager.kt:114:                    sourceId = sl.getString("source_id"),
    [R] MapSourceManager.kt:115:                    legacyKey = sl.getString("legacy_key")
    [R] MapSourceManager.kt:236:            json.keys().forEach { key -> apiKeys[key] = json.getString(key) }
    [R] SpatialDbManager.kt:230:            val wkt = cursor.getString(2)
    [R] SpatialDbManager.kt:233:                    "trail_id" to cursor.getString(0),
    [R] SpatialDbManager.kt:234:                    "name" to cursor.getString(1),
    [R] SpatialDbManager.kt:309:                    val wkt = it.getString(2) ?: continue
    [R] SpatialDbManager.kt:311:                        "track_id" to it.getString(0),
    [R] SpatialDbManager.kt:312:                        "name" to it.getString(1),
    [R] SpatialDbManager.kt:361:            cursor.use { while (it.moveToNext()) { existing.add(it.getString(0) ?: "") } }
    [R] SpatialDbManager.kt:415:        val count = cursor.getInt(0)
    [R] SpatialDbManager.kt:435:                    val wkt = it.getString(2) ?: continue
    [R] SpatialDbManager.kt:437:                        "waypoint_id" to it.getString(0),
    [R] SpatialDbManager.kt:438:                        "name" to it.getString(1),
    [R] SpatialDbManager.kt:440:                        "type" to (it.getString(3) ?: "other")
    [R] SpatialDbManager.kt:484:                    val wkt = it.getString(2) ?: continue
    [R] SpatialDbManager.kt:486:                        "route_id" to it.getString(0),
    [R] SpatialDbManager.kt:487:                        "name" to it.getString(1),
    [R] SpatialDbManager.kt:529:                    val wkt = it.getString(2) ?: continue
    [R] SpatialDbManager.kt:531:                        "track_id" to it.getString(0),
    [R] SpatialDbManager.kt:532:                        "name" to it.getString(1),
    [R] SpatialDbManager.kt:681:                    map["id"] = it.getString(0)
    [R] SpatialDbManager.kt:682:                    map["name"] = it.getString(1)
    [R] SpatialDbManager.kt:684:                        map["type"] = it.getString(2)
    [R] SpatialDbManager.kt:756:                val name = c.getString(0) ?: "Unnamed"
    [R] SpatialDbManager.kt:757:                val type = c.getString(1) ?: ""
    [R] SpatialDbManager.kt:758:                val geom = c.getString(2) ?: return null
    [R] SpatialDbManager.kt:759:                val desc = if (c.columnCount > 3) (c.getString(3) ?: "") else ""
    [R] SpatialDbManager.kt:789:                val name = c.getString(0) ?: "Unnamed"
    [R] SpatialDbManager.kt:790:                val geom = c.getString(1) ?: return null
    [R] SpatialDbManager.kt:791:                val desc = if (c.columnCount > 2) (c.getString(2) ?: "") else ""
    [R] SpatialDbManager.kt:828:                val name = c.getString(0) ?: "Unnamed"
    [R] SpatialDbManager.kt:829:                val geom = c.getString(1) ?: return null
    [R] SpatialDbManager.kt:940:            val v = cursor.getInt(0)
    [R] SpatialDbManager.kt:951:            val v = cursor.getInt(0)
    [R] TrailImporter.kt:439:            ver = if (cur.moveToFirst()) cur.getInt(0) else 0; cur.close()
    [R] TrailImporter.kt:548:                f.keys().forEach { k -> if (k !in listOf("id", "name", "type", "use")) extras[k] = f.getString(k) }
    [R] TrailImporter.kt:549:                Src(s.getString("id"), s.getString("name"), s.getString("query_url"),
    [R] TrailImporter.kt:551:                    f.getString("id"), f.getString("name"), f.optString("type", ""), f.optString("use", ""), extras)

--- 5D: File I/O ---

  FILE WRITERS:
    [W] ConvoyApplyList.kt:133:            java.io.File(context.filesDir, FILE_NAME).writeText(list.toJson().toString(2))
    [W] ConvoyConfig.kt:37:            if (!dir.exists()) dir.mkdirs()
    [W] ConvoyConfig.kt:62:                val ok = sourceDir.renameTo(dest)
    [W] ConvoyDownloadQueue.kt:405:            dir.mkdirs()
    [W] ConvoyDownloadQueue.kt:409:            file.writeText(arr.toString(2))
    [W] ConvoyEventConfig.kt:178:        java.io.File(context.filesDir, EVENTS_DIR).also { it.mkdirs() }
    [W] ConvoyEventConfig.kt:184:        file.parentFile?.mkdirs()
    [W] ConvoyEventConfig.kt:185:        file.writeText(event.toJson().toString(2))
    [W] ConvoyFileReceiver.kt:90:            destFile.writeText(content)
    [W] ConvoyGpsService.kt:81:    private var kmlWriter: BufferedWriter? = null
    [W] ConvoyGpsService.kt:221:            val success = tempFile.renameTo(finalFile)
    [W] ConvoyGpsService.kt:326:        if (!dir.exists()) dir.mkdirs()
    [W] ConvoyGpsService.kt:333:            val writer = BufferedWriter(FileWriter(file))
    [W] ConvoyGpsService.kt:335:            writer.write("<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n")
    [W] ConvoyGpsService.kt:337:            writer.write("<Document>\n")
    [W] ConvoyGpsService.kt:338:            writer.write("<name>Convoy Track</name>\n")
    [W] ConvoyGpsService.kt:339:            writer.write("<Placemark><name>Track</name><LineString><coordinates>\n")
    [W] ConvoyGpsService.kt:348:            kmlWriter?.write("$lon,$lat,$alt\n")
    [W] ConvoyGpsService.kt:357:            kmlWriter?.write("</coordinates></LineString></Placemark>\n")
    [W] ConvoyGpsService.kt:358:            kmlWriter?.write("</Document>\n</kml>\n")
    [W] ConvoyGpsService.kt:368:    private var gpxWriter: java.io.BufferedWriter? = null
    [W] ConvoyGpsService.kt:374:        if (!dir.exists()) dir.mkdirs()
    [W] ConvoyGpsService.kt:381:            val writer = java.io.BufferedWriter(java.io.FileWriter(file))
    [W] ConvoyGpsService.kt:383:            writer.write("<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n")
    [W] ConvoyGpsService.kt:384:            writer.write("<gpx version=\"1.1\" creator=\"GroupTrack\"\n")
    [W] ConvoyGpsService.kt:386:            writer.write("<trk><name>Convoy Track</name><trkseg>\n")
    [W] ConvoyGpsService.kt:396:            gpxWriter?.write("<trkpt lat=\"$lat\" lon=\"$lon\"><ele>$alt</ele><time>$ts</time></trkpt>\n")
    [W] ConvoyGpsService.kt:405:            gpxWriter?.write("</trkseg></trk>\n</gpx>\n")
    [W] ConvoyMapViewerScreen.kt:1160:                    val ok = ConvoyTrackOps.copyToDownloads(it)
    [W] ConvoyMasterApply.kt:44:        archiveFile.parentFile?.mkdirs()
    [W] ConvoyMasterCaptureScreen.kt:554:                                masterFile.writeText(masterJson)
    [W] ConvoyMasterCaptureScreen.kt:558:                                internalFile.writeText(masterJson)
    [W] ConvoyMasterCaptureScreen.kt:563:                                applyFile.writeText(applyList.toJson().toString(2))
    [W] ConvoyMasterCaptureScreen.kt:574:                                        .copyTo(masterCfgExternal, overwrite = true)
    [W] ConvoyMasterConfig.kt:250:                    it.parentFile?.mkdirs()
    [W] ConvoyMasterConfig.kt:251:                    it.writeText(json)
    [W] ConvoyProfileBuilder.kt:213:        outFile.parentFile?.mkdirs()
    [W] ConvoyRadioManager.kt:146:        val dir      = java.io.File(context.filesDir, "$BACKUPS_DIR/${snapshot.hardwareId}").also { it.mkdirs() }
    [W] ConvoyRadioManager.kt:148:        file.writeText(snapshot.toJson().toString(2))
    [W] ConvoyTileDownloader.kt:59:        dir.mkdirs()
    [W] ConvoyTrackExportSheet.kt:232:                    val ok = ConvoyTrackOps.copyToDownloads(it)
    [W] ConvoyTrackOps.kt:57:                file.renameTo(target) -> RenameResult.Success
    [W] ConvoyTrackOps.kt:68:    suspend fun copyToDownloads(file: File): Boolean = withContext(Dispatchers.IO) {
    [W] ConvoyTrackOps.kt:72:                target.outputStream().use { output -> input.copyTo(output) }
    [W] ConvoyTrackOps.kt:213:            if (!dir.exists()) dir.mkdirs()
    [W] ConvoyTrackOps.kt:241:                        dest.writeText(singleGpx)
    [W] ConvoyTrackOps.kt:285:                        dest.writeText(singleKml)
    [W] ConvoyTrackOps.kt:306:            dest.writeText(text)
    [W] ConvoyTrackOps.kt:504:            if (!dir.exists()) dir.mkdirs()
    [W] ConvoyTrackOps.kt:528:                        dest.writeText(singleGpx)
    [W] ConvoyTrackOps.kt:609:            tempFile.writeText(gpxContent, Charsets.UTF_8)
    [W] ConvoyTrackOps.kt:633:            file.writeText(gpxContent, Charsets.UTF_8)
    [W] ConvoyTransferRideScreen.kt:67:            outFile.writeText(rideJson.toString(2))
    [W] ConvoyUser.kt:133:        java.io.File(context.filesDir, FILE_NAME).writeText(arr.toString())
    [W] ConvoyViewModel.kt:236:        file.parentFile?.mkdirs()
    [W] ConvoyViewModel.kt:267:            dir.mkdirs()
    [W] ConvoyViewModel.kt:274:            dir.mkdirs()
    [W] ConvoyViewModel.kt:495:            it.writeText("=== GroupTrack Debug Log ${System.currentTimeMillis()} ===\n")
    [W] ConvoyViewModel.kt:499:        try { convoyLogFile.appendText("${System.currentTimeMillis() % 100000} $msg\n") }
    [W] MapSourceManager.kt:62:        if (!dir.exists()) dir.mkdirs()
    [W] MapSourceManager.kt:248:            apiKeysFile().writeText(json.toString(2), Charsets.UTF_8)
    [W] SpatialDbManager.kt:48:        if (!dir.exists()) dir.mkdirs()
    [W] SpatialDbManager.kt:51:            try { noMedia.createNewFile() } catch (_: Exception) {}
    [W] SpatialDbManager.kt:139:                    try { tileMapsNoMedia.createNewFile() } catch (_: Exception) {}
    [W] SpatialDbManager.kt:143:                    try { tileNoMedia.createNewFile() } catch (_: Exception) {}
    [W] TrailImporter.kt:301:            if (!dir.exists()) dir.mkdirs()
    [W] TrailImporter.kt:338:            File(dir, filename).writeText(json.toString(2))
    [W] TrailImporter.kt:353:            if (!dir.exists()) dir.mkdirs()
    [W] TrailImporter.kt:361:            File(dir, "pending_area.json").writeText(json.toString(2))

  FILE READERS:
    [R] ConvoyApplyList.kt:111:            if (!file.exists()) return ConvoyApplyList()
    [R] ConvoyApplyList.kt:113:                val obj = JSONObject(file.readText())
    [R] ConvoyArchiveRestoreScreen.kt:71:        archiveDir?.listFiles { f -> f.extension == "cfg" }
    [R] ConvoyArchiveRestoreScreen.kt:120:                val profileBytes = file.readBytes()
    [R] ConvoyConfig.kt:37:            if (!dir.exists()) dir.mkdirs()
    [R] ConvoyConfig.kt:50:        if (oldDir.exists() && oldDir.isDirectory) {
    [R] ConvoyConfig.kt:52:            val sources = oldDir.listFiles() ?: emptyArray()
    [R] ConvoyConfig.kt:57:                if (dest.exists()) {
    [R] ConvoyConfig.kt:71:            if ((oldDir.listFiles() ?: emptyArray()).isEmpty()) {
    [R] ConvoyDownloadQueue.kt:419:            if (!file.exists()) return
    [R] ConvoyDownloadQueue.kt:420:            val arr = JSONArray(file.readText())
    [R] ConvoyDownloadWorker.kt:62:                ConvoyTileDownloader.tilePath(appContext, refreshSlot, tile).exists()
    [R] ConvoyEventConfig.kt:188:        return eventsDir(context).listFiles { f -> f.extension == "json" }
    [R] ConvoyEventConfig.kt:190:                try { ConvoyEventConfig.fromJson(org.json.JSONObject(it.readText())) }
    [R] ConvoyFileReceiver.kt:44:            val content = contentResolver.openInputStream(uri)?.use { input ->
    [R] ConvoyFileReceiver.kt:45:                input.bufferedReader().readText()
    [R] ConvoyGpsService.kt:326:        if (!dir.exists()) dir.mkdirs()
    [R] ConvoyGpsService.kt:374:        if (!dir.exists()) dir.mkdirs()
    [R] ConvoyMapViewerScreen.kt:113:            val files = dir.listFiles()?.filter { f ->
    [R] ConvoyMapViewerScreen.kt:517:                                        if (file.exists()) {
    [R] ConvoyMapViewerScreen.kt:518:                                            return android.webkit.WebResourceResponse("image/png", null, java.io.FileInputStream(file))
    [R] ConvoyMapViewerScreen.kt:526:                                        if (file.exists()) {
    [R] ConvoyMapViewerScreen.kt:527:                                            return android.webkit.WebResourceResponse("image/png", null, java.io.FileInputStream(file))
    [R] ConvoyMapViewerScreen.kt:581:                                        if (sourceDir.exists()) {
    [R] ConvoyMapViewerScreen.kt:582:                                            sourceDir.listFiles()?.forEach { zDir ->
    [R] ConvoyMapViewerScreen.kt:1059:                                        if (tilesDir.exists()) {
    [R] ConvoyMapViewerScreen.kt:1061:                                            tilesDir.listFiles()?.forEach { xDir: java.io.File ->
    [R] ConvoyMapViewerScreen.kt:1063:                                                xDir.listFiles()?.forEach { yFile: java.io.File ->
    [R] ConvoyMapViewerScreen.kt:1224:    if (!dir.exists()) return emptyList()
    [R] ConvoyMapViewerScreen.kt:1225:    val files = dir.listFiles()
    [R] ConvoyMapViewerScreen.kt:1255:                if (!file.exists()) return@withContext null
    [R] ConvoyMapViewerScreen.kt:1256:                val text = file.readText()
    [R] ConvoyMasterApply.kt:56:            val bytes = inputStream.readBytes()
    [R] ConvoyMasterConfig.kt:220:            if (deviceFile.exists()) {
    [R] ConvoyMasterConfig.kt:222:                    val json = JSONObject(deviceFile.readText())
    [R] ConvoyMasterConfig.kt:248:                    .bufferedReader().use { it.readText() }
    [R] ConvoyMasterConfig.kt:265:            if (File(context.filesDir, DEVICE_FILE).exists()) return true
    [R] ConvoyProfileBuilder.kt:245:            context.assets.open("master.cfg").readBytes()
    [R] ConvoyRadioManager.kt:156:            .takeIf { it.exists() }
    [R] ConvoyRadioManager.kt:157:            ?.listFiles { f -> f.extension == "json" }
    [R] ConvoyScreen.kt:604:                                    android.util.Log.d("ConvoyIntercept", "TILE exists=${file.exists()} path=${file.absolutePath}")
    [R] ConvoyScreen.kt:605:                                    if (file.exists()) return android.webkit.WebResourceResponse("image/png", "utf-8", file.inputStream())
    [R] ConvoyScreen.kt:613:                                        if (file.exists()) {
    [R] ConvoyScreen.kt:614:                                            return android.webkit.WebResourceResponse("image/png", null, java.io.FileInputStream(file))
    [R] ConvoyScreen.kt:622:                                        if (file.exists()) {
    [R] ConvoyScreen.kt:623:                                            return android.webkit.WebResourceResponse("image/png", null, java.io.FileInputStream(file))
    [R] ConvoyScreen.kt:724:                                        if (tilesDir.exists()) {
    [R] ConvoyScreen.kt:731:                                            tilesDir.listFiles()?.forEach { xDir: java.io.File ->
    [R] ConvoyScreen.kt:733:                                                xDir.listFiles()?.forEach { yFile: java.io.File ->
    [R] ConvoyScreen.kt:1235:                                        dir.listFiles()?.map { it.name }?.sorted() ?: emptyList()
    [R] ConvoyScreen.kt:1241:                                                if (!file.exists()) return@withContext null
    [R] ConvoyScreen.kt:1242:                                                val text = file.readText()
    [R] ConvoyScreen.kt:1266:                                    val json = context.assets.open("utah_trails_stgeorge.geojson").bufferedReader().use { it.readText() }
    [R] ConvoyScreen.kt:1292:                                if (tilesDir.exists()) {
    [R] ConvoyScreen.kt:1294:                                    tilesDir.listFiles()?.forEach { xDir: java.io.File ->
    [R] ConvoyScreen.kt:1296:                                        xDir.listFiles()?.forEach { yFile: java.io.File ->
    [R] ConvoyScreen.kt:2053:    if (!dir.exists()) return emptyList()
    [R] ConvoyScreen.kt:2054:    return dir.listFiles()
    [R] ConvoyScreen.kt:2078:        if (!file.exists()) return null
    [R] ConvoyScreen.kt:2079:        val text = file.readText()
    [R] ConvoyScreen.kt:2101:        if (!file.exists()) return
    [R] ConvoyScreen.kt:2102:        val text = file.readText()
    [R] ConvoyTileDownloader.kt:122:                if (!forceOverwrite && dest.exists() && dest.length() > 0) {
    [R] ConvoyTileDownloader.kt:151:        if (!dir.exists()) return emptyList()
    [R] ConvoyTileDownloader.kt:154:        dir.listFiles()?.filter { it.isDirectory }?.forEach { zDir ->
    [R] ConvoyTileDownloader.kt:156:            zDir.listFiles()?.filter { it.isDirectory }?.forEach { xDir ->
    [R] ConvoyTileDownloader.kt:158:                xDir.listFiles()?.filter { it.isFile && it.name.endsWith(".png") }?.forEach { yFile ->
    [R] ConvoyTileDownloader.kt:210:        return if (dir.exists()) dir.deleteRecursively() else true
    [R] ConvoyTileDownloader.kt:216:        if (!dir.exists()) return Pair(0, 0f)
    [R] ConvoyTrackImportScreen.kt:95:            val found = dlDir.listFiles()
    [R] ConvoyTrackOps.kt:29:        if (!dir.exists()) return@withContext emptyList()
    [R] ConvoyTrackOps.kt:30:        dir.listFiles()?.filter { f ->
    [R] ConvoyTrackOps.kt:56:                target.exists() -> RenameResult.NameExists
    [R] ConvoyTrackOps.kt:198:            if (!sourceFile.exists()) {
    [R] ConvoyTrackOps.kt:202:            var text = sourceFile.readText()
    [R] ConvoyTrackOps.kt:213:            if (!dir.exists()) dir.mkdirs()
    [R] ConvoyTrackOps.kt:234:                        if (dest.exists()) {
    [R] ConvoyTrackOps.kt:278:                        if (dest.exists()) {
    [R] ConvoyTrackOps.kt:303:            if (dest.exists()) {
    [R] ConvoyTrackOps.kt:323:            if (!file.exists()) return@withContext false
    [R] ConvoyTrackOps.kt:324:            val content = file.readText()
    [R] ConvoyTrackOps.kt:490:            if (!sourceFile.exists()) {
    [R] ConvoyTrackOps.kt:494:            var text = sourceFile.readText()
    [R] ConvoyTrackOps.kt:504:            if (!dir.exists()) dir.mkdirs()
    [R] ConvoyTrackOps.kt:524:                    if (!dest.exists()) {
    [R] ConvoyTrailSourceScreen.kt:610:        val json = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
    [R] ConvoyUser.kt:120:        if (!file.exists()) return emptyList()
    [R] ConvoyUser.kt:122:            val arr = JSONArray(file.readText())
    [R] ConvoyViewModel.kt:351:                    val json = org.json.JSONObject(file.readText())
    [R] ConvoyViewModel.kt:477:        if (temp.exists()) {
    [R] MapSourceManager.kt:62:        if (!dir.exists()) dir.mkdirs()
    [R] MapSourceManager.kt:77:                .bufferedReader().use { it.readText() }
    [R] MapSourceManager.kt:234:            if (!file.exists()) return
    [R] MapSourceManager.kt:235:            val json = JSONObject(file.readText(Charsets.UTF_8))
    [R] SpatialDbManager.kt:48:        if (!dir.exists()) dir.mkdirs()
    [R] SpatialDbManager.kt:50:        if (!noMedia.exists()) {
    [R] SpatialDbManager.kt:136:            if (tileDir.exists()) {
    [R] SpatialDbManager.kt:138:                if (!tileMapsNoMedia.exists()) {
    [R] SpatialDbManager.kt:142:                if (!tileNoMedia.exists()) {
    [R] SpatialDbManager.kt:189:        val raw = context.assets.open(assetName).bufferedReader().use { it.readText() }
    [R] SpatialDbManager.kt:352:            if (!tracksDir.exists() || !tracksDir.isDirectory) {
    [R] SpatialDbManager.kt:356:            val gpxFiles = tracksDir.listFiles { f -> f.name.lowercase().endsWith(".gpx") } ?: return
    [R] SpatialDbManager.kt:368:                    val text = file.readText()
    [R] TrailImporter.kt:193:            conn.inputStream.bufferedReader().use { it.readText() }
    [R] TrailImporter.kt:301:            if (!dir.exists()) dir.mkdirs()
    [R] TrailImporter.kt:312:                    val catJson = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }
    [R] TrailImporter.kt:353:            if (!dir.exists()) dir.mkdirs()
    [R] TrailImporter.kt:376:            if (f.exists()) JSONObject(f.readText()) else null
    [R] TrailImporter.kt:387:            ).let { if (it.exists()) it.delete() }
    [R] TrailImporter.kt:399:            if (!dir.exists()) return emptyList()
    [R] TrailImporter.kt:400:            dir.listFiles()
    [R] TrailImporter.kt:402:                ?.mapNotNull { try { JSONObject(it.readText()) } catch (_: Exception) { null } }
    [R] TrailImporter.kt:416:            if (f.exists()) {
    [R] TrailImporter.kt:417:                val j = JSONObject(f.readText())
    [R] TrailImporter.kt:447:            val sql = context.assets.open("schema_spatial_v1_to_v2.sql").bufferedReader().use { it.readText() }
    [R] TrailImporter.kt:470:            val json = context.assets.open(assetFile).bufferedReader().use { it.readText() }
    [R] TrailImporter.kt:540:            val json = context.assets.open("trail_sources.json").bufferedReader().use { it.readText() }

--- 5E: Kotlin → JavaScript Bridge ---

    [W→JS] ConvoyMapViewerScreen.kt:143:                webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
    [W→JS] ConvoyMapViewerScreen.kt:156:            webViewRef?.evaluateJavascript("removeTrackFile('$safe')", null)
    [W→JS] ConvoyMapViewerScreen.kt:224:                        webViewRef?.evaluateJavascript("setTileUrl('" + url + "', '" + label + "')", null)
    [W→JS] ConvoyMapViewerScreen.kt:227:                            webViewRef?.evaluateJavascript("setOverlayLayers('" + ovJson.replace("'", "\\'") + "')", null)
    [W→JS] ConvoyMapViewerScreen.kt:302:                                            webViewRef?.evaluateJavascript(
    [W→JS] ConvoyMapViewerScreen.kt:305:                                            webViewRef?.evaluateJavascript(
    [W→JS] ConvoyMapViewerScreen.kt:414:                                                webViewRef?.evaluateJavascript(
    [W→JS] ConvoyMapViewerScreen.kt:430:                                    wv.evaluateJavascript("setTileUrl('" + url + "', '" + src + "')", null)
    [W→JS] ConvoyMapViewerScreen.kt:433:                                        wv.evaluateJavascript("setOverlayLayers('" + ovJson.replace("'", "\'") + "')", null)
    [W→JS] ConvoyMapViewerScreen.kt:461:                                            webViewRef?.evaluateJavascript("updateTrails(" + json + ")", null)
    [W→JS] ConvoyMapViewerScreen.kt:462:                                            if (capTrailState != DS_OFF) webViewRef?.evaluateJavascript("showTrails()", null)
    [W→JS] ConvoyMapViewerScreen.kt:472:                                                    webViewRef?.evaluateJavascript("updateTracks(" + trackJson + ")", null)
    [W→JS] ConvoyMapViewerScreen.kt:473:                                                    if (capTrackState != DS_OFF) webViewRef?.evaluateJavascript("showTracks()", null)
    [W→JS] ConvoyMapViewerScreen.kt:485:                                                    webViewRef?.evaluateJavascript("updateWaypoints(" + wptJson + ")", null)
    [W→JS] ConvoyMapViewerScreen.kt:486:                                                    if (capWaypointState != DS_OFF) webViewRef?.evaluateJavascript("showWaypoints()", null)
    [W→JS] ConvoyMapViewerScreen.kt:498:                                                    webViewRef?.evaluateJavascript("updateRoutes(" + routeJson + ")", null)
    [W→JS] ConvoyMapViewerScreen.kt:499:                                                    if (capRouteState != DS_OFF) webViewRef?.evaluateJavascript("showRoutes()", null)
    [W→JS] ConvoyMapViewerScreen.kt:550:                                    view?.evaluateJavascript(
    [W→JS] ConvoyMapViewerScreen.kt:563:                                            view?.evaluateJavascript(
    [W→JS] ConvoyMapViewerScreen.kt:589:                                                view?.evaluateJavascript("setMaxOfflineZoom($maxZ)", null)
    [W→JS] ConvoyMapViewerScreen.kt:599:                                    webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
    [W→JS] ConvoyMapViewerScreen.kt:665:                        webViewRef?.evaluateJavascript("hide" + typeName + "()", null)
    [W→JS] ConvoyMapViewerScreen.kt:667:                        webViewRef?.evaluateJavascript("show" + typeName + "()", null)
    [W→JS] ConvoyMapViewerScreen.kt:668:                        webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
    [W→JS] ConvoyMapViewerScreen.kt:730:                            webViewRef?.evaluateJavascript("hide" + t + "()", null)
    [W→JS] ConvoyMapViewerScreen.kt:732:                            webViewRef?.evaluateJavascript("show" + t + "()", null)
    [W→JS] ConvoyMapViewerScreen.kt:733:                            webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
    [W→JS] ConvoyMapViewerScreen.kt:769:                            webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
    [W→JS] ConvoyMapViewerScreen.kt:784:                            webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
    [W→JS] ConvoyMapViewerScreen.kt:830:                            webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
    [W→JS] ConvoyMapViewerScreen.kt:866:                                    webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
    [W→JS] ConvoyMapViewerScreen.kt:1031:                        webViewRef?.evaluateJavascript("activateDrawMode()", null)
    [W→JS] ConvoyMapViewerScreen.kt:1035:                        webViewRef?.evaluateJavascript("clearAreaBoundary()", null)
    [W→JS] ConvoyMapViewerScreen.kt:1075:                                            wv.evaluateJavascript("showDownloadedAreas($json)", null)
    [W→JS] ConvoyMapViewerScreen.kt:1084:                            webViewRef?.evaluateJavascript("clearDownloadedAreas()", null)
    [W→JS] ConvoyMapViewerScreen.kt:1096:                            wv.evaluateJavascript("showQueuedAreas(" + json + ")", null)
    [W→JS] ConvoyMapViewerScreen.kt:1098:                            webViewRef?.evaluateJavascript("clearQueuedAreas()", null)
    [W→JS] ConvoyMapViewerScreen.kt:1264:                webView?.evaluateJavascript("loadTrackFile('$safe', '${result.first}', '$color')", null)
    [W→JS] ConvoyScreen.kt:222:            wv.evaluateJavascript("clearMarkers()", null)
    [W→JS] ConvoyScreen.kt:228:                wv.evaluateJavascript("addMarker('${node.nodeId}', ${node.latitude}, ${node.longitude}, '$color', '$label', $isMine, $isOffTrack)", null)
    [W→JS] ConvoyScreen.kt:242:            wv.evaluateJavascript("fitBounds([$lats], [$lons])", null)
    [W→JS] ConvoyScreen.kt:255:                    wv.evaluateJavascript("setView(${it.latitude}, ${it.longitude}, ${ConvoyConfig.MAP_CART_ZOOM})", null)
    [W→JS] ConvoyScreen.kt:269:                    wv.evaluateJavascript("setView(${it.latitude}, ${it.longitude}, ${ConvoyConfig.MAP_CART_ZOOM})", null)
    [W→JS] ConvoyScreen.kt:274:                    wv.evaluateJavascript("setView(${it.latitude}, ${it.longitude}, ${ConvoyConfig.MAP_CART_ZOOM})", null)
    [W→JS] ConvoyScreen.kt:283:                    wv.evaluateJavascript("fitBounds([$lats], [$lons])", null)
    [W→JS] ConvoyScreen.kt:323:        wv.evaluateJavascript("drawTrack(" + json + ")", null)
    [W→JS] ConvoyScreen.kt:495:                                        android.os.Handler(android.os.Looper.getMainLooper()).post { webViewRef.value?.evaluateJavascript("updateTrails($json)", null) }
    [W→JS] ConvoyScreen.kt:501:                                        android.os.Handler(android.os.Looper.getMainLooper()).post { webViewRef.value?.evaluateJavascript("updateTracks($trackJson)", null) }
    [W→JS] ConvoyScreen.kt:507:                                        android.os.Handler(android.os.Looper.getMainLooper()).post { webViewRef.value?.evaluateJavascript("updateWaypoints($wptJson)", null) }
    [W→JS] ConvoyScreen.kt:513:                                        android.os.Handler(android.os.Looper.getMainLooper()).post { webViewRef.value?.evaluateJavascript("updateRoutes($routeJson)", null) }
    [W→JS] ConvoyScreen.kt:578:                                    view.evaluateJavascript("setTileUrl('$tileUrl', '${ConvoyConfig.ACTIVE_TILE_SOURCE}')", null)
    [W→JS] ConvoyScreen.kt:581:                                        view.evaluateJavascript("setOverlayLayers('${overlayJson.replace("'", "\'")}')", null)
    [W→JS] ConvoyScreen.kt:591:                                            view.evaluateJavascript("setView(${loc.latitude}, ${loc.longitude}, 15)", null)
    [W→JS] ConvoyScreen.kt:666:                                                webViewRef.value?.evaluateJavascript("updateTrails($json)", null)
    [W→JS] ConvoyScreen.kt:673:                                                webViewRef.value?.evaluateJavascript("updateTracks($trackJson)", null)
    [W→JS] ConvoyScreen.kt:680:                                                webViewRef.value?.evaluateJavascript("updateWaypoints($wptJson)", null)
    [W→JS] ConvoyScreen.kt:687:                                                webViewRef.value?.evaluateJavascript("updateRoutes($routeJson)", null)
    [W→JS] ConvoyScreen.kt:749:                                            wv.evaluateJavascript("showDownloadedAreas($json)", null)
    [W→JS] ConvoyScreen.kt:773:                        view.evaluateJavascript("findNearestMarker($x, $y)") { result ->
    [W→JS] ConvoyScreen.kt:1098:                webViewRef.value?.evaluateJavascript("setTileUrl('$url', '$label')", null)
    [W→JS] ConvoyScreen.kt:1100:                if (overlayJsonSc != "[]") { webViewRef.value?.evaluateJavascript("setOverlayLayers('${overlayJsonSc.replace("'", "\\'")}')", null) }
    [W→JS] ConvoyScreen.kt:1108:                webViewRef.value?.evaluateJavascript("setTileUrl('$url', '${ConvoyConfig.ACTIVE_TILE_SOURCE}')", null)
    [W→JS] ConvoyScreen.kt:1171:                            wv.evaluateJavascript("hide" + typeName + "()", null)
    [W→JS] ConvoyScreen.kt:1173:                            wv.evaluateJavascript("show" + typeName + "()", null)
    [W→JS] ConvoyScreen.kt:1174:                            wv.evaluateJavascript("try{var b=map.getBounds();Android.onViewportChanged(b.getNorth(),b.getSouth(),b.getEast(),b.getWest(),map.getZoom())}catch(e){}", null)
    [W→JS] ConvoyScreen.kt:1210:                            webViewRef.value?.evaluateJavascript("try{var b=map.getBounds();Android.onViewportChanged(b.getNorth(),b.getSouth(),b.getEast(),b.getWest(),map.getZoom())}catch(e){}", null)
    [W→JS] ConvoyScreen.kt:1225:                                webViewRef.value?.evaluateJavascript("showTracks()", null)
    [W→JS] ConvoyScreen.kt:1250:                                            wv?.evaluateJavascript("loadTrackFile('" + safe + "', '" + result + "', '" + trackColor + "')", null)
    [W→JS] ConvoyScreen.kt:1256:                            webViewRef.value?.evaluateJavascript("hideTracks()", null)
    [W→JS] ConvoyScreen.kt:1268:                                        webViewRef.value?.evaluateJavascript("loadTrails($json); showTrails();", null)
    [W→JS] ConvoyScreen.kt:1276:                        webViewRef.value?.evaluateJavascript("toggleTrails()", null)
    [W→JS] ConvoyScreen.kt:1283:                            webViewRef.value?.evaluateJavascript("clearDownloadedAreas()", null)
    [W→JS] ConvoyScreen.kt:1308:                                    wv.evaluateJavascript("showDownloadedAreas($json)", null)
    [W→JS] ConvoyScreen.kt:1431:                            webViewRef.value?.evaluateJavascript("removeMarker('${n.nodeId}')", null)
    [W→JS] ConvoyScreen.kt:2107:        webView?.evaluateJavascript("loadTrackFile('$safe', '$json', '$color')", null)

  JavaScript → Kotlin callbacks:
    [JS→KT] ConvoyMapViewerScreen.kt:368:                        addJavascriptInterface(object {
    [JS→KT] ConvoyMapViewerScreen.kt:369:                            @JavascriptInterface
    [JS→KT] ConvoyMapViewerScreen.kt:378:                            @JavascriptInterface
    [JS→KT] ConvoyMapViewerScreen.kt:380:                            @JavascriptInterface
    [JS→KT] ConvoyMapViewerScreen.kt:423:                            @JavascriptInterface
    [JS→KT] ConvoyMapViewerScreen.kt:437:                            @JavascriptInterface
    [JS→KT] ConvoyScreen.kt:474:                    existing.addJavascriptInterface(object : Any() {
    [JS→KT] ConvoyScreen.kt:639:                        addJavascriptInterface(object : Any() {

================================================================================
SECTION 6: LIFECYCLE PHASE MAP
================================================================================

--- INIT PHASE ---
  ConvoyApplyRadioScreen.kt:44:    val myNodeInfo  by uiViewModel.myNodeInfo.collectAsStateWithLifecycle()
  ConvoyArchiveRestoreScreen.kt:59:    val myNodeInfo      by convoyViewModel.myNodeInfo.collectAsStateWithLifecycle()
  ConvoyFileReceiver.kt:30:    override fun onCreate(savedInstanceState: Bundle?) {
  ConvoyGpsService.kt:114:    override fun onCreate() {
  ConvoyInviteHandler.kt:38:    override fun onCreate(savedInstanceState: Bundle?) {
  ConvoyMasterCaptureScreen.kt:110:    val nodeInfo        by viewModel.myNodeInfo.collectAsState()
  ConvoyMasterCaptureScreen.kt:114:    val myNodeInfo      by uiViewModel.myNodeInfo.collectAsStateWithLifecycle()
  ConvoyNodeFilter.kt:24:     * @param admissionWindowHours Max hours since lastHeard for initial admission.
  ConvoyNodeFilter.kt:31:        admissionWindowHours: Int,
  ConvoyNodeFilter.kt:35:        val admissionCutoff = nowEpochSeconds - (admissionWindowHours * 3600L)
  ConvoyRadioConfigScreens.kt:191:    val myNodeInfo      by uiViewModel.myNodeInfo.collectAsStateWithLifecycle()
  ConvoySessionManager.kt:107:    fun resolveLaunchRoute(ctx: Context, hasInternet: Boolean): LaunchRoute {
  ConvoySettingsRepository.kt:46:    val admissionWindowHours: Flow<Int> = context.convoyDataStore.data
  ConvoySettingsScreen.kt:145:                valueLabel  = "${uiState.admissionWindowHours} hr",
  ConvoySettingsScreen.kt:146:                value       = uiState.admissionWindowHours.toFloat(),
  ConvoySettingsViewModel.kt:18:    val admissionWindowHours: Int                = 1,
  ConvoySettingsViewModel.kt:33:    init {
  ConvoySettingsViewModel.kt:44:                repository.admissionWindowHours,
  ConvoySettingsViewModel.kt:51:                    admissionWindowHours = window,
  ConvoyViewModel.kt:321:    private var admissionWindowHours: Int = 1
  ConvoyViewModel.kt:323:    init {
  ConvoyViewModel.kt:325:            settingsRepository.admissionWindowHours.collect { admissionWindowHours = it }
  ConvoyViewModel.kt:329:            nodeRepository.myNodeInfo.collect { info ->
  ConvoyViewModel.kt:336:        startTick()
  ConvoyViewModel.kt:337:        viewModelScope.launch(Dispatchers.IO) { scanImportDirectory() }
  ConvoyViewModel.kt:340:    suspend fun scanImportDirectory() {
  ConvoyViewModel.kt:505:    private fun startTick() {
  ConvoyViewModel.kt:926:            admissionWindowHours = admissionWindowHours

--- TICK PHASE ---
  ConvoySimulation.kt:59:    fun tick(nowMs: Long = System.currentTimeMillis()): List<ConvoyNode> {
  ConvoySimulation.kt:115:        return ConvoyEngine.compute(nodes, myCartId = MY_CART_ID, nowMs = nowMs)
  ConvoyViewModel.kt:111:    private fun startPhoneGps() {
  ConvoyViewModel.kt:130:    private fun stopPhoneGps() {
  ConvoyViewModel.kt:159:        val nodes = readLiveNodes(System.currentTimeMillis())
  ConvoyViewModel.kt:173:        convoyLog("TRACK START: myCart=${_myCartId.value} lead=$lockedLeadNodeId nodes=${nodes.size}")
  ConvoyViewModel.kt:199:            val name = _convoyState.value.nodes.firstOrNull { it.nodeId == nodeId }?.callsign ?: nodeId
  ConvoyViewModel.kt:200:            convoyLog("LEAD SET: $name manually assigned as lead cart")
  ConvoyViewModel.kt:205:            convoyLog("LEAD CLEARED: no lead assigned")
  ConvoyViewModel.kt:290:    private fun resolveMyCartId(): String {
  ConvoyViewModel.kt:333:                // resolveMyCartId() reads radio info directly for pre-RECORD display
  ConvoyViewModel.kt:405:        val current = _convoyState.value
  ConvoyViewModel.kt:406:        _convoyState.value = current.copy(nodes = current.nodes.filter { it.nodeId != nodeId })
  ConvoyViewModel.kt:493:    private val convoyLogFile: java.io.File by lazy {
  ConvoyViewModel.kt:498:    private fun convoyLog(msg: String) {
  ConvoyViewModel.kt:499:        try { convoyLogFile.appendText("${System.currentTimeMillis() % 100000} $msg\n") }
  ConvoyViewModel.kt:536:    private fun tick() { try {
  ConvoyViewModel.kt:547:            readLiveNodes(nowMs)
  ConvoyViewModel.kt:555:        val state = ConvoyEngine.compute(
  ConvoyViewModel.kt:557:            myCartId = resolveMyCartId(),
  ConvoyViewModel.kt:563:        _convoyState.value = state
  ConvoyViewModel.kt:580:            convoyLog("tick | lead=$leadOut | trackFrom=$trackFrom | locked=$_leadLockedFlag | [$accumStr]")
  ConvoyViewModel.kt:624:                    convoyLog("CART SUB: ${node.callsign} silent — using trailing cart pos (${String.format("%.5f",resolvedLat)},${String.format("%.5f",resolvedLon)})")
  ConvoyViewModel.kt:706:                                convoyLog("PROXY SNAP: lead stale $leadStaleTicks ticks")
  ConvoyViewModel.kt:731:                        convoyLog("LEAD SEGMENT: dist=${String.format("%.3f",segDist)}mi${if (proxySnapped) " (PROXY)" else ""}")
  ConvoyViewModel.kt:783:    private fun getPhoneLocation(): android.location.Location? {
  ConvoyViewModel.kt:805:    private fun readLiveNodes(nowMs: Long): List<ConvoyNode> {
  ConvoyViewModel.kt:812:                startPhoneGps()
  ConvoyViewModel.kt:815:            val loc = getPhoneLocation()
  ConvoyViewModel.kt:849:                    convoyLog("STALE POS REJECTED: node=$callsign fixTime=$fixTimeMs rideStart=$rideStartTimeMs age=${(rideStartTimeMs - fixTimeMs)/1000}s lat=$lat lon=$lon → using lastKnown")
  ConvoyViewModel.kt:854:                        convoyLog("POS ACCEPTED: node=$callsign fixTime=$fixTimeMs lat=$lat lon=$lon")
  ConvoyViewModel.kt:861:                    convoyLog("ZERO POS: node=$callsign — using lastKnown lat=${fallback.first} lon=${fallback.second}")
  ConvoyViewModel.kt:865:                    val phoneLoc = getPhoneLocation()
  ConvoyViewModel.kt:867:                        convoyLog("ZERO POS: node=$callsign — using phone GPS lat=${phoneLoc.latitude} lon=${phoneLoc.longitude}")
  ConvoyViewModel.kt:870:                        convoyLog("ZERO POS: node=$callsign — no lastKnown, no phone GPS, dropping node this tick")

--- MAP-LOAD PHASE ---
  ConvoyMapViewerScreen.kt:143:                webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
  ConvoyMapViewerScreen.kt:156:            webViewRef?.evaluateJavascript("removeTrackFile('$safe')", null)
  ConvoyMapViewerScreen.kt:224:                        webViewRef?.evaluateJavascript("setTileUrl('" + url + "', '" + label + "')", null)
  ConvoyMapViewerScreen.kt:227:                            webViewRef?.evaluateJavascript("setOverlayLayers('" + ovJson.replace("'", "\\'") + "')", null)
  ConvoyMapViewerScreen.kt:302:                                            webViewRef?.evaluateJavascript(
  ConvoyMapViewerScreen.kt:305:                                            webViewRef?.evaluateJavascript(
  ConvoyMapViewerScreen.kt:414:                                                webViewRef?.evaluateJavascript(
  ConvoyMapViewerScreen.kt:430:                                    wv.evaluateJavascript("setTileUrl('" + url + "', '" + src + "')", null)
  ConvoyMapViewerScreen.kt:433:                                        wv.evaluateJavascript("setOverlayLayers('" + ovJson.replace("'", "\'") + "')", null)
  ConvoyMapViewerScreen.kt:461:                                            webViewRef?.evaluateJavascript("updateTrails(" + json + ")", null)
  ConvoyMapViewerScreen.kt:462:                                            if (capTrailState != DS_OFF) webViewRef?.evaluateJavascript("showTrails()", null)
  ConvoyMapViewerScreen.kt:472:                                                    webViewRef?.evaluateJavascript("updateTracks(" + trackJson + ")", null)
  ConvoyMapViewerScreen.kt:473:                                                    if (capTrackState != DS_OFF) webViewRef?.evaluateJavascript("showTracks()", null)
  ConvoyMapViewerScreen.kt:485:                                                    webViewRef?.evaluateJavascript("updateWaypoints(" + wptJson + ")", null)
  ConvoyMapViewerScreen.kt:486:                                                    if (capWaypointState != DS_OFF) webViewRef?.evaluateJavascript("showWaypoints()", null)
  ConvoyMapViewerScreen.kt:498:                                                    webViewRef?.evaluateJavascript("updateRoutes(" + routeJson + ")", null)
  ConvoyMapViewerScreen.kt:499:                                                    if (capRouteState != DS_OFF) webViewRef?.evaluateJavascript("showRoutes()", null)
  ConvoyMapViewerScreen.kt:533:                            override fun onPageFinished(view: WebView?, url: String?) {
  ConvoyMapViewerScreen.kt:534:                                super.onPageFinished(view, url)
  ConvoyMapViewerScreen.kt:550:                                    view?.evaluateJavascript(
  ConvoyMapViewerScreen.kt:563:                                            view?.evaluateJavascript(
  ConvoyMapViewerScreen.kt:589:                                                view?.evaluateJavascript("setMaxOfflineZoom($maxZ)", null)
  ConvoyMapViewerScreen.kt:599:                                    webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
  ConvoyMapViewerScreen.kt:604:                        loadUrl("file:///android_asset/grouptrack_map.html")
  ConvoyMapViewerScreen.kt:665:                        webViewRef?.evaluateJavascript("hide" + typeName + "()", null)
  ConvoyMapViewerScreen.kt:667:                        webViewRef?.evaluateJavascript("show" + typeName + "()", null)
  ConvoyMapViewerScreen.kt:668:                        webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
  ConvoyMapViewerScreen.kt:730:                            webViewRef?.evaluateJavascript("hide" + t + "()", null)
  ConvoyMapViewerScreen.kt:732:                            webViewRef?.evaluateJavascript("show" + t + "()", null)
  ConvoyMapViewerScreen.kt:733:                            webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
  ConvoyMapViewerScreen.kt:769:                            webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
  ConvoyMapViewerScreen.kt:784:                            webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
  ConvoyMapViewerScreen.kt:830:                            webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
  ConvoyMapViewerScreen.kt:866:                                    webViewRef?.evaluateJavascript("triggerViewportUpdate()", null)
  ConvoyMapViewerScreen.kt:1031:                        webViewRef?.evaluateJavascript("activateDrawMode()", null)
  ConvoyMapViewerScreen.kt:1035:                        webViewRef?.evaluateJavascript("clearAreaBoundary()", null)
  ConvoyMapViewerScreen.kt:1075:                                            wv.evaluateJavascript("showDownloadedAreas($json)", null)
  ConvoyMapViewerScreen.kt:1084:                            webViewRef?.evaluateJavascript("clearDownloadedAreas()", null)
  ConvoyMapViewerScreen.kt:1096:                            wv.evaluateJavascript("showQueuedAreas(" + json + ")", null)
  ConvoyMapViewerScreen.kt:1098:                            webViewRef?.evaluateJavascript("clearQueuedAreas()", null)
  ConvoyMapViewerScreen.kt:1264:                webView?.evaluateJavascript("loadTrackFile('$safe', '${result.first}', '$color')", null)
  ConvoyScreen.kt:222:            wv.evaluateJavascript("clearMarkers()", null)
  ConvoyScreen.kt:228:                wv.evaluateJavascript("addMarker('${node.nodeId}', ${node.latitude}, ${node.longitude}, '$color', '$label', $isMine, $isOffTrack)", null)
  ConvoyScreen.kt:242:            wv.evaluateJavascript("fitBounds([$lats], [$lons])", null)
  ConvoyScreen.kt:255:                    wv.evaluateJavascript("setView(${it.latitude}, ${it.longitude}, ${ConvoyConfig.MAP_CART_ZOOM})", null)
  ConvoyScreen.kt:269:                    wv.evaluateJavascript("setView(${it.latitude}, ${it.longitude}, ${ConvoyConfig.MAP_CART_ZOOM})", null)
  ConvoyScreen.kt:274:                    wv.evaluateJavascript("setView(${it.latitude}, ${it.longitude}, ${ConvoyConfig.MAP_CART_ZOOM})", null)
  ConvoyScreen.kt:283:                    wv.evaluateJavascript("fitBounds([$lats], [$lons])", null)
  ConvoyScreen.kt:323:        wv.evaluateJavascript("drawTrack(" + json + ")", null)
  ConvoyScreen.kt:495:                                        android.os.Handler(android.os.Looper.getMainLooper()).post { webViewRef.value?.evaluateJavascript("updateTrails($json)", null) }
  ConvoyScreen.kt:501:                                        android.os.Handler(android.os.Looper.getMainLooper()).post { webViewRef.value?.evaluateJavascript("updateTracks($trackJson)", null) }
  ConvoyScreen.kt:507:                                        android.os.Handler(android.os.Looper.getMainLooper()).post { webViewRef.value?.evaluateJavascript("updateWaypoints($wptJson)", null) }
  ConvoyScreen.kt:513:                                        android.os.Handler(android.os.Looper.getMainLooper()).post { webViewRef.value?.evaluateJavascript("updateRoutes($routeJson)", null) }
  ConvoyScreen.kt:557:                            override fun onPageFinished(view: android.webkit.WebView?, url: String?) {
  ConvoyScreen.kt:573:                                        viewModel.setLocalTiles(true)
  ConvoyScreen.kt:574:                                        viewModel.setOfflineMode(true)
  ConvoyScreen.kt:578:                                    view.evaluateJavascript("setTileUrl('$tileUrl', '${ConvoyConfig.ACTIVE_TILE_SOURCE}')", null)
  ConvoyScreen.kt:581:                                        view.evaluateJavascript("setOverlayLayers('${overlayJson.replace("'", "\'")}')", null)
  ConvoyScreen.kt:591:                                            view.evaluateJavascript("setView(${loc.latitude}, ${loc.longitude}, 15)", null)
  ConvoyScreen.kt:638:                        loadUrl("file:///android_asset/convoy_map.html")
  ConvoyScreen.kt:666:                                                webViewRef.value?.evaluateJavascript("updateTrails($json)", null)
  ConvoyScreen.kt:673:                                                webViewRef.value?.evaluateJavascript("updateTracks($trackJson)", null)
  ConvoyScreen.kt:680:                                                webViewRef.value?.evaluateJavascript("updateWaypoints($wptJson)", null)
  ConvoyScreen.kt:687:                                                webViewRef.value?.evaluateJavascript("updateRoutes($routeJson)", null)
  ConvoyScreen.kt:749:                                            wv.evaluateJavascript("showDownloadedAreas($json)", null)
  ConvoyScreen.kt:768:                        event.action == android.view.MotionEvent.ACTION_UP) { viewModel.setAutoPan(false) }
  ConvoyScreen.kt:773:                        view.evaluateJavascript("findNearestMarker($x, $y)") { result ->
  ConvoyScreen.kt:1091:                viewModel.setMapTypeLabel(label)
  ConvoyScreen.kt:1097:                viewModel.setLocalTiles(isLocalTiles)
  ConvoyScreen.kt:1098:                webViewRef.value?.evaluateJavascript("setTileUrl('$url', '$label')", null)
  ConvoyScreen.kt:1100:                if (overlayJsonSc != "[]") { webViewRef.value?.evaluateJavascript("setOverlayLayers('${overlayJsonSc.replace("'", "\\'")}')", null) }
  ConvoyScreen.kt:1103:                viewModel.setOfflineMode(goOffline)
  ConvoyScreen.kt:1108:                webViewRef.value?.evaluateJavascript("setTileUrl('$url', '${ConvoyConfig.ACTIVE_TILE_SOURCE}')", null)
  ConvoyScreen.kt:1171:                            wv.evaluateJavascript("hide" + typeName + "()", null)
  ConvoyScreen.kt:1173:                            wv.evaluateJavascript("show" + typeName + "()", null)
  ConvoyScreen.kt:1174:                            wv.evaluateJavascript("try{var b=map.getBounds();Android.onViewportChanged(b.getNorth(),b.getSouth(),b.getEast(),b.getWest(),map.getZoom())}catch(e){}", null)
  ConvoyScreen.kt:1210:                            webViewRef.value?.evaluateJavascript("try{var b=map.getBounds();Android.onViewportChanged(b.getNorth(),b.getSouth(),b.getEast(),b.getWest(),map.getZoom())}catch(e){}", null)
  ConvoyScreen.kt:1225:                                webViewRef.value?.evaluateJavascript("showTracks()", null)
  ConvoyScreen.kt:1250:                                            wv?.evaluateJavascript("loadTrackFile('" + safe + "', '" + result + "', '" + trackColor + "')", null)
  ConvoyScreen.kt:1256:                            webViewRef.value?.evaluateJavascript("hideTracks()", null)
  ConvoyScreen.kt:1268:                                        webViewRef.value?.evaluateJavascript("loadTrails($json); showTrails();", null)
  ConvoyScreen.kt:1276:                        webViewRef.value?.evaluateJavascript("toggleTrails()", null)
  ConvoyScreen.kt:1283:                            webViewRef.value?.evaluateJavascript("clearDownloadedAreas()", null)
  ConvoyScreen.kt:1308:                                    wv.evaluateJavascript("showDownloadedAreas($json)", null)
  ConvoyScreen.kt:1384:            onModeChange = { viewModel.setHudMode(it); viewModel.setAutoPan(true) },
  ConvoyScreen.kt:1431:                            webViewRef.value?.evaluateJavascript("removeMarker('${n.nodeId}')", null)
  ConvoyScreen.kt:2107:        webView?.evaluateJavascript("loadTrackFile('$safe', '$json', '$color')", null)
  ConvoyViewModel.kt:96:    fun setOfflineMode(offline: Boolean) { _isOfflineMode.value = offline }
  ConvoyViewModel.kt:99:    fun setMapTypeLabel(label: String) { _mapTypeLabel.value = label }
  ConvoyViewModel.kt:102:    fun setLocalTiles(local: Boolean) { _isLocalTiles.value = local }
  ConvoyViewModel.kt:105:    fun setAutoPan(pan: Boolean) { _autoPan.value = pan }

--- RECORD PHASE ---
  ConvoyGpsService.kt:38: *   On END: file is complete and ready. Caller renames via finalizeTrack().
  ConvoyGpsService.kt:41: * startTrack / pauseTrack / resumeTrack / stopTrack directly.
  ConvoyGpsService.kt:122:            ACTION_PAUSE  -> pauseTrack()
  ConvoyGpsService.kt:123:            ACTION_RESUME -> resumeTrack()
  ConvoyGpsService.kt:124:            ACTION_END    -> stopTrack()
  ConvoyGpsService.kt:134:        stopLocationUpdates()
  ConvoyGpsService.kt:145:    fun startTrack() {
  ConvoyGpsService.kt:152:        startLocationUpdates()
  ConvoyGpsService.kt:161:    fun pauseTrack() {
  ConvoyGpsService.kt:163:        stopLocationUpdates()
  ConvoyGpsService.kt:173:    fun resumeTrack() {
  ConvoyGpsService.kt:176:        startLocationUpdates()
  ConvoyGpsService.kt:194:    fun stopTrack(): File? {
  ConvoyGpsService.kt:196:        stopLocationUpdates()
  ConvoyGpsService.kt:212:     * Called after stopTrack() once user has entered a name.
  ConvoyGpsService.kt:214:    fun finalizeTrack(tempFile: File, trackName: String): File? {
  ConvoyGpsService.kt:230:            Log.e(TAG, "finalizeTrack error: ${e.message}")
  ConvoyGpsService.kt:247:            onGpsUpdate(lat, lon, alt)
  ConvoyGpsService.kt:251:    private fun startLocationUpdates() {
  ConvoyGpsService.kt:263:                onGpsUpdate(loc.latitude, loc.longitude, loc.altitude)
  ConvoyGpsService.kt:278:    private fun stopLocationUpdates() {
  ConvoyGpsService.kt:282:            Log.e(TAG, "stopLocationUpdates error: ${e.message}")
  ConvoyGpsService.kt:288:    private fun onGpsUpdate(lat: Double, lon: Double, alt: Double) {
  ConvoyGpsService.kt:298:            pauseTrack()
  ConvoyGpsService.kt:313:            writeGpxPoint(lat, lon, alt)
  ConvoyGpsService.kt:393:    private fun writeGpxPoint(lat: Double, lon: Double, alt: Double) {
  ConvoyScreen.kt:432:                viewModel.finalizeTrack("convoy_track", context)
  ConvoyScreen.kt:447:                    viewModel.finalizeTrack(name, context)
  ConvoyScreen.kt:858:                                        viewModel.startRecording(context)
  ConvoyScreen.kt:859:                                        viewModel.startGroupTrack()
  ConvoyScreen.kt:907:                                        viewModel.startRecording(context)
  ConvoyScreen.kt:908:                                        viewModel.startGroupTrack()
  ConvoyScreen.kt:916:                                            viewModel.startRecording(context)
  ConvoyScreen.kt:917:                                            viewModel.startGroupTrack()
  ConvoyScreen.kt:962:                                viewModel.resumeRecording(context)
  ConvoyScreen.kt:965:                                viewModel.pauseRecording()
  ConvoyScreen.kt:982:                            viewModel.stopRecording()
  ConvoyScreen.kt:983:                            viewModel.stopGroupTrack()
  ConvoyScreen.kt:1412:                    onStartTrack = { viewModel.startGroupTrack() },
  ConvoyScreen.kt:1413:                    onStopTrack = { viewModel.stopGroupTrack() },
  ConvoyViewModel.kt:140:    fun startGroupTrack() {
  ConvoyViewModel.kt:176:    fun stopGroupTrack() {
  ConvoyViewModel.kt:307:    private var rideStartTimeMs: Long = 0L  // Unix ms when startGroupTrack() was called
  ConvoyViewModel.kt:332:                // _myCartId no longer set here — set ONCE in startGroupTrack()
  ConvoyViewModel.kt:428:    private fun bindGpsService(context: android.content.Context, onBound: (ConvoyGpsService) -> Unit) {
  ConvoyViewModel.kt:457:    fun startRecording(context: android.content.Context) {
  ConvoyViewModel.kt:459:        bindGpsService(context) { svc -> svc.startTrack(); _routeRecording.value = true }
  ConvoyViewModel.kt:461:    fun pauseRecording() { gpsService?.pauseTrack(); _routeRecording.value = false }
  ConvoyViewModel.kt:464:        resumeRecording(context)
  ConvoyViewModel.kt:466:    fun resumeRecording(context: android.content.Context) {
  ConvoyViewModel.kt:468:            bindGpsService(context) { svc -> svc.resumeTrack(); _routeRecording.value = true }
  ConvoyViewModel.kt:469:        } else { gpsService?.resumeTrack(); _routeRecording.value = true }
  ConvoyViewModel.kt:471:    fun stopRecording() {
  ConvoyViewModel.kt:472:        pendingTempFile = gpsService?.stopTrack()
  ConvoyViewModel.kt:484:    fun finalizeTrack(name: String, context: android.content.Context) {
  ConvoyViewModel.kt:486:        gpsService?.finalizeTrack(temp, name)
  ConvoyViewModel.kt:552:        // Lead assigned at RECORD time in startGroupTrack() or via cart HUD
  ConvoyViewModel.kt:814:            // _myCartId no longer set here — set ONCE in startGroupTrack()

--- RADIO PHASE ---
  ConvoyApplyRadioScreen.kt:35:    channelViewModel: ChannelViewModel = hiltViewModel(),
  ConvoyApplyRadioScreen.kt:45:    val localConfig by channelViewModel.localConfig.collectAsStateWithLifecycle()
  ConvoyApplyRadioScreen.kt:46:    val channels    by channelViewModel.channels.collectAsStateWithLifecycle()
  ConvoyApplyRadioScreen.kt:490:                            val archiveResult = ConvoyMasterApply.archiveCurrentRadio(
  ConvoyApplyRadioScreen.kt:507:                                val profile27 = ConvoyProfileBuilder.buildProfile(wconfig).copy(short_name = null)
  ConvoyApplyRadioScreen.kt:510:                                convoyViewModel.installProfileToRadio(ni.myNodeNum, profile27)
  ConvoyApplyRadioScreen.kt:518:                                val profile = ConvoyProfileBuilder.buildProfile(wconfig).copy(short_name = null)
  ConvoyApplyRadioScreen.kt:521:                                convoyViewModel.installProfileToRadio(ni.myNodeNum, profile)
  ConvoyArchiveRestoreScreen.kt:45: *   5. User taps RESTORE — installProfileToRadio()
  ConvoyArchiveRestoreScreen.kt:122:                convoyViewModel.installProfileToRadio(nodeNum, profile)
  ConvoyMasterApply.kt:33:    suspend fun archiveCurrentRadio(
  ConvoyMasterApply.kt:72:        convoyViewModel.installProfileToRadio(nodeNum, profile)
  ConvoyMasterApply.kt:88:        val archivePath = archiveCurrentRadio(context, nodeId, convoyViewModel)
  ConvoyMasterCaptureScreen.kt:95:    channelViewModel: ChannelViewModel = hiltViewModel(),
  ConvoyMasterCaptureScreen.kt:111:    val localConfig     by channelViewModel.localConfig.collectAsStateWithLifecycle()
  ConvoyMasterCaptureScreen.kt:112:    val channels        by channelViewModel.channels.collectAsStateWithLifecycle()
  ConvoyProfileBuilder.kt:34:    fun buildProfile(config: ConvoyMasterConfig): DeviceProfile {
  ConvoyProfileBuilder.kt:201:    fun buildBytes(config: ConvoyMasterConfig): ByteArray = buildProfile(config).encode()
  ConvoyProfileBuilder.kt:304:    fun buildProfile(wconfig: WorkingConfig): DeviceProfile {
  ConvoyProfileBuilder.kt:366:        return buildProfile(config)
  ConvoyRadioConfigScreens.kt:186:    channelViewModel: ChannelViewModel = hiltViewModel(),
  ConvoyRadioConfigScreens.kt:192:    val localConfig     by channelViewModel.localConfig.collectAsStateWithLifecycle()
  ConvoyRadioConfigScreens.kt:193:    val channels        by channelViewModel.channels.collectAsStateWithLifecycle()
  ConvoyRadioConfigScreens.kt:228:                val snapshot = ConvoyRadioManager.buildSnapshot(
  ConvoyRadioConfigScreens.kt:344:    channelViewModel: ChannelViewModel = hiltViewModel(),
  ConvoyRadioConfigScreens.kt:444:                            channelViewModel.setConfig(org.meshtastic.proto.Config(
  ConvoyRadioConfigScreens.kt:474:    channelViewModel: ChannelViewModel = hiltViewModel(),
  ConvoyRadioConfigScreens.kt:581:                            channelViewModel.setConfig(org.meshtastic.proto.Config(
  ConvoyRadioConfigScreens.kt:615:    channelViewModel: ChannelViewModel = hiltViewModel(),
  ConvoyRadioConfigScreens.kt:673:                        channelViewModel.setConfig(org.meshtastic.proto.Config(
  ConvoyRadioConfigScreens.kt:707:    channelViewModel: ChannelViewModel = hiltViewModel(),
  ConvoyRadioConfigScreens.kt:809:                            channelViewModel.setChannels(org.meshtastic.proto.ChannelSet(
  ConvoyRadioManager.kt:19: *         Called via ChannelViewModel.setChannels() and setConfig()
  ConvoyRadioManager.kt:92:    fun buildSnapshot(
  ConvoyRadioWriter.kt:78:     * @param channelViewModel For writing config to radio
  ConvoyRadioWriter.kt:85:        channelViewModel: ChannelViewModel,
  ConvoyRadioWriter.kt:98:            channelViewModel.setConfig(Config(device = Config.DeviceConfig(role = Config.DeviceConfig.Role.CLIENT)))
  ConvoyRadioWriter.kt:115:            channelViewModel.setConfig(Config(lora = Config.LoRaConfig(hop_limit = workingConfig.loraHopLimit, tx_enabled = workingConfig.loraTxEnabled, tx_power = workingConfig.loraTxPower)))
  ConvoyRadioWriter.kt:131:            channelViewModel.setChannels(ChannelSet(settings = listOf(chSettings)))
  ConvoyScreen.kt:112:    val channelViewModel: ChannelViewModel = hiltViewModel()
  ConvoyScreen.kt:1417:                        onIntervalChange = { secs -> viewModel.setGpsInterval(secs, channelViewModel) },
  ConvoyScreen.kt:1430:                            viewModel.removeNode(n.nodeId)
  ConvoyVerifyConfigScreen.kt:38:    channelViewModel: ChannelViewModel = hiltViewModel(),
  ConvoyVerifyConfigScreen.kt:43:    val localConfig by channelViewModel.localConfig.collectAsStateWithLifecycle()
  ConvoyVerifyConfigScreen.kt:44:    val channels    by channelViewModel.channels.collectAsStateWithLifecycle()
  ConvoyViewModel.kt:213:    fun reconnectRadio() {
  ConvoyViewModel.kt:249:    fun installProfileToRadio(destNum: Int, profile: DeviceProfile) {
  ConvoyViewModel.kt:384:    fun setGpsInterval(secs: Int, channelViewModel: com.geeksville.mesh.ui.sharing.ChannelViewModel) {
  ConvoyViewModel.kt:388:                channelViewModel.setConfig(
  ConvoyViewModel.kt:402:    fun removeNode(nodeId: String) {

--- USER PHASE ---
  ConvoyApiClient.kt:41:    suspend fun registerUser(
  ConvoyApiClient.kt:62:            Log.d(TAG, "registerUser: ${response.code} $responseBody")
  ConvoyApiClient.kt:73:            Log.e(TAG, "registerUser error: ${e.message}")
  ConvoyApiClient.kt:162:    suspend fun createRide(
  ConvoyApiClient.kt:199:            Log.d(TAG, "createRide: ${response.code} $responseBody")
  ConvoyApiClient.kt:210:            Log.e(TAG, "createRide error: ${e.message}")
  ConvoyApiClient.kt:217:    suspend fun createInvite(
  ConvoyApiClient.kt:233:            Log.d(TAG, "createInvite: ${response.code} $responseBody")
  ConvoyApiClient.kt:244:            Log.e(TAG, "createInvite error: ${e.message}")
  ConvoyApiClient.kt:252:    suspend fun downloadRide(token: String): Result<String> = withContext(Dispatchers.IO) {
  ConvoyApiClient.kt:261:            Log.d(TAG, "downloadRide: ${response.code} ${responseBody.take(100)}")
  ConvoyApiClient.kt:266:            Log.e(TAG, "downloadRide error: ${e.message}")
  ConvoyApiClient.kt:273:    suspend fun enrollRider(
  ConvoyApiClient.kt:293:            Log.d(TAG, "enrollRider: ${response.code}")
  ConvoyApiClient.kt:296:            Log.e(TAG, "enrollRider error: ${e.message}")
  ConvoyApiClient.kt:347:    suspend fun followOrganizer(
  ConvoyApiClient.kt:362:            Log.d(TAG, "followOrganizer: ${response.code}")
  ConvoyApiClient.kt:365:            Log.e(TAG, "followOrganizer error: ${e.message}")
  ConvoyApiClient.kt:371:    suspend fun unfollowOrganizer(
  ConvoyApiClient.kt:386:            Log.d(TAG, "unfollowOrganizer: ${response.code}")
  ConvoyApiClient.kt:389:            Log.e(TAG, "unfollowOrganizer error: ${e.message}")
  ConvoyDashboardScreen.kt:72:            ConvoyEnrollmentQueue.unfollowOrganizer(context, organizerId, organizerName)
  ConvoyDashboardScreen.kt:75:            ConvoyEnrollmentQueue.followOrganizer(context, organizerId, organizerName)
  ConvoyDashboardScreen.kt:220:                        "CLEAR SESSION" to { ConvoySessionManager.clearSession(context); ConvoyDevSeeder.clear(context); seedKey++; android.widget.Toast.makeText(context, "Cleared", android.widget.Toast.LENGTH_SHORT).show() },
  ConvoyDevLaunchScreen.kt:72:                ConvoySessionManager.clearSession(context)
  ConvoyDevLaunchScreen.kt:80:                ConvoySessionManager.clearSession(context)
  ConvoyDevLaunchScreen.kt:82:                ConvoySessionManager.saveUser(context,
  ConvoyDevLaunchScreen.kt:91:                ConvoySessionManager.clearSession(context)
  ConvoyDevLaunchScreen.kt:99:                ConvoySessionManager.clearSession(context)
  ConvoyDevLaunchScreen.kt:110:                ConvoySessionManager.clearSession(context)
  ConvoyDevLaunchScreen.kt:132:    ConvoySessionManager.saveUser(context,
  ConvoyDevSeeder.kt:72:        ConvoySessionManager.saveUser(ctx,
  ConvoyEnrollmentQueue.kt:87:    fun followOrganizer(ctx: Context, organizerId: String, organizerName: String) {
  ConvoyEnrollmentQueue.kt:88:        Log.d(TAG, "followOrganizer — organizerId=$organizerId stub FOLLOW-01, Phase C")
  ConvoyEnrollmentQueue.kt:93:    fun unfollowOrganizer(ctx: Context, organizerId: String, organizerName: String) {
  ConvoyEnrollmentQueue.kt:94:        Log.d(TAG, "unfollowOrganizer — organizerId=$organizerId stub FOLLOW-01, Phase C")
  ConvoyInviteHandler.kt:22:// 2. Call ConvoyApiClient.downloadRide(token)
  ConvoyInviteHandler.kt:25:// 5. Enroll rider via ConvoyApiClient.enrollRider()
  ConvoyInviteHandler.kt:67:        val downloadResult = ConvoyApiClient.downloadRide(token)
  ConvoyInviteHandler.kt:83:                        val enrollResult = ConvoyApiClient.enrollRider(
  ConvoyMapViewerScreen.kt:1239:private fun loadTrackOnMap(
  ConvoyMyOrganizersScreen.kt:150:                                        ConvoyApiClient.unfollowOrganizer(userId, org.id)
  ConvoyMyOrganizersScreen.kt:153:                                        ConvoyApiClient.followOrganizer(userId, org.id)
  ConvoyRideDetailScreen.kt:102:            ConvoyEnrollmentQueue.unfollowOrganizer(context, orgId, orgName)
  ConvoyRideDetailScreen.kt:105:            ConvoyEnrollmentQueue.followOrganizer(context, orgId, orgName)
  ConvoyScreen.kt:401:                        viewModel.startDownload(context, pending)
  ConvoyScreen.kt:419:            onDismissRequest = { viewModel.cancelDownload() },
  ConvoyScreen.kt:423:                TextButton(onClick = { viewModel.cancelDownload() }) { Text("OK") }
  ConvoyScreen.kt:453:                    viewModel.deleteTempTrack()
  ConvoyScreen.kt:543:                                viewModel.setPendingDownload(pending)
  ConvoyScreen.kt:713:                                    viewModel.setPendingDownload(pending)
  ConvoyScreen.kt:855:                                        viewModel.setLeadCart(node.nodeId)
  ConvoyScreen.kt:914:                                            viewModel.setLeadCart(soloNode?.nodeId ?: "!phone")
  ConvoyScreen.kt:1434:                            viewModel.setLeadCart(n.nodeId)
  ConvoyScreen.kt:2047:fun convoyListTracks(context: android.content.Context): List<String> {
  ConvoySessionManager.kt:69:    fun saveUser(ctx: Context, userId: String, googleId: String,
  ConvoySessionManager.kt:98:    fun clearSession(ctx: Context) {
  ConvoySignInScreen.kt:50:// On success: calls ConvoyApiClient.registerUser()
  ConvoySignInScreen.kt:66:fun saveUserToPrefs(context: Context, userId: String, googleId: String,
  ConvoySignInScreen.kt:151:                    val result = ConvoyApiClient.registerUser(googleId, email, firstName, lastName)
  ConvoySignInScreen.kt:154:                            saveUserToPrefs(context, userId, googleId, email, firstName, lastName)
  ConvoySignInScreen.kt:161:                            Log.e(TAG, "registerUser failed: ${e.message}")
  ConvoySignInScreen.kt:260:                                saveUserToPrefs(context, uid, "google-dev-$uid",
  ConvoyViewModel.kt:164:                nodes.size == 1 -> setLeadCart(nodes[0].nodeId)
  ConvoyViewModel.kt:165:                nodes.isEmpty() -> setLeadCart(_myCartId.value)
  ConvoyViewModel.kt:166:                else -> setLeadCart(_myCartId.value)
  ConvoyViewModel.kt:194:    fun setLeadCart(nodeId: String?) {
  ConvoyViewModel.kt:475:    fun deleteTempTrack() {
  ConvoyViewModel.kt:551:        // Lead assignment REMOVED from tick — only through setLeadCart()
  ConvoyViewModel.kt:952:    fun setPendingDownload(pending: PendingDownload) { _pendingDownload.value = pending }
  ConvoyViewModel.kt:955:    fun startDownload(context: android.content.Context, pending: PendingDownload) {
  ConvoyViewModel.kt:992:    fun cancelDownload() {
  GroupTrackTheme.kt:150:                    ConvoySessionManager.clearSession(context)

================================================================================
SECTION 7: CODE ISSUES AND WARNINGS
================================================================================

  ConvoyApplyListScreen.kt:415:                            // TODO: wire radio write
  ConvoyCreateRideScreen.kt:78:        } else "CONVOY-XXXX"
  ConvoyMasterCaptureScreen.kt:462:                                    log.appendLine("\u26a0 WARNING: Channel name or PSK is missing.")


================================================================================
SECTION 8: CURRENT GIT STATE
================================================================================

Branch: feature/convoy-event-ride
Last 5 commits:
  d964a8bca V2.5: Convoy spatial display - tracks waypoints rendering, shared spatial_artifacts.js, ArtifactsPanel wired, artifact display design documented
  1d126d972 docs: recommit all project documents + cross-reference — 2026-05-29 00:12
  c0ad6b509 V2.5: Trail import cleanup, TrailImporter bbox, track Branch 2 removed + diagnostics, GPX import restored
  f231f5722 V2.5: trailhead import (568 UGRC waypoints), import infrastructure, import guard removed
  ff94d66c2 V2.5: GPX share/export wired, button styling, proximity Kotlin-side, refresh button, description column fix

Uncommitted changes:
   M docs/field_crossref_raw.txt

Stash list:
  stash@{0}: WIP on feature/convoy-event-ride: 5befd8707 Remove test DB files from repo, add to gitignore
  stash@{1}: WIP on feature/convoy-event-ride: 5befd8707 Remove test DB files from repo, add to gitignore
  stash@{2}: WIP on feature/convoy-event-ride: 97e5eacf8 V2 navigation wired - enrollment, create event, settings panel, master capture all routed

================================================================================
SECTION 9: FEATURE FLAGS AND CONFIG STATE
================================================================================

  15:    // TILE_SOURCES — reads from MapSourceManager (map_sources.json)
  17:    val TILE_SOURCES: Map<String, String>
  20:            .plus("SAT_LOCAL" to LOCAL_TILE_BASE + "SAT/{z}/{x}/{y}.png")
  21:    var ACTIVE_TILE_SOURCE: String
  26:    const val LOCAL_TILE_BASE = "convoy://tiles/"
  30:    val TILE_DIR: java.io.File
  48:        val newDir = TILE_DIR
  96:    const val V3_FEATURES_ENABLED = false
  97:    const val PAYWALL_ENABLED = false              // Flip to true when growth warrants paywall                  // Flip to true to expose V3 to testers

================================================================================
END OF FIELD CROSS-REFERENCE
================================================================================
