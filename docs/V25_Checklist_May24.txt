GroupTrack V2.5 — Feature Checklist
Updated: May 24, 2026 EOD
=====================================

PLANNING MAP — WORK WITH ARTIFACTS
=====================================

DISPLAY TOGGLES:
[X] Trail toggle → viewport query → map display (working)
[X] Track toggle → viewport query → map display (working)
[X] Waypoint toggle → viewport query → map display (wired, needs test data)
[X] Route toggle → viewport query → map display (wired, needs test data)

IMPORT:
[X] IMPORT TRAILS button → trail source management screen
[X] IMPORT ARTIFACTS button → file picker → GPX parser
[X] GPX track parsing (<trk>) → file + spatial DB
[X] GPX waypoint parsing (<wpt>) → spatial DB
[X] GPX route parsing (<rte>) → spatial DB
[X] Import summary toast (counts per type)
[X] Source file deletion after import
[ ] Test import with multi-type GPX files (Fred has test files)

SELECT/LIST MANAGER (ArtifactListPanel):
[ ] Reusable composable for all 4 types
[ ] List artifacts in map bounding box
[ ] Search/filter by name
[ ] Select/deselect all
[ ] Individual toggle show/hide on map
[ ] FIT (zoom to selected)
[ ] CLEAR (hide all)
[ ] Click name → maintenance panel

MAINTENANCE (per artifact type):
[ ] Rename (tracks: DB→file trigger | waypoints/routes: DB direct)
[ ] Delete (tracks: DB→file trigger | routes: blocked if on ride)
[ ] Share (tracks only: file via share sheet)
[ ] Copy to Downloads (tracks only)
[ ] Fix Date (tracks only)
[ ] Change Type (waypoints only: 10 types)
[ ] Alias management (all types)

DOWNLOADS:
[ ] Queue management controls (hold/resume/cancel wired to live data)
[ ] Download shading on map (blue=on disk, pink=queued, red=pending)
[ ] Download panel landscape fix (accordion segments)
[ ] Queue job summaries (records read/written)
[ ] Max tile count safety check (50K warn, 500K block)

UI CLEANUP:
[ ] Remove ConvoyDisplayPanel (replaced by Work With Artifacts)
[ ] Remove duplicate TRACKS button
[ ] Transparent bars with clickable text
[ ] Accordion SAT/TOPO/TOPO+ bar
[ ] Fix zoom display position (hidden behind header)
[X] Legend bar updated (8 entries)

GPS/MAP:
[ ] Planning Map GPS centering (not working — investigate)
[X] Planning Map fallback view (US center z4)
[X] Convoy Map zoom display (in pending build)
[ ] Convoy Map tile encoding fix (in pending build — utf-8→null)
[ ] maxNativeZoom from tile directory scan

=====================================
CONVOY MAP (AFTER PLANNING MAP COMPLETE)
=====================================

[ ] Wire ConvoyArtifactsPanel toggle handlers (all 4 types)
[ ] Add onViewportChanged to JavaScript interface
[ ] Switch trails from bundled GeoJSON to spatial DB lazy load
[ ] Switch tracks from file-by-file to spatial DB lazy load
[ ] Waypoint + route display via spatial DB
[ ] Display-only mode (no edit, no import)
[ ] Transparent bottom bar with text links
[ ] Remove DISPLAY button
[ ] Tile encoding fix (in pending build)

=====================================
DEFERRED TO LATER
=====================================

[ ] Waypoint creation (long-press on map)
[ ] Route creation (draw/snap-to-trail)
[ ] Track-to-route conversion
[ ] Trail properties in spatial DB (for click-to-action)
[ ] Non-lead cart track filter
[ ] Tile source investigation (z18 rural coverage)
