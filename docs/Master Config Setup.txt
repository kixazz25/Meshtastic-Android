Master Config Setup
1) Export Binary.cfg for master cfg base values. exporter master config in Jason.save both data store
Write Master config to new  radio process
2)  	a) Write Radio Archive using export binsay for the previous Radio config. 
	b) Store by card id and date and time of last chanhe. 
	c) Master file binary cfg will be applied to this radio. 
	d) the checklist process will identify difference in the son config vs current radio image an to identify vaLues tat must be changed            	7. an utomated process will submit changes and create a ride configuration in cfg format that can be attached to thr rifr requet and applied at any time.   data.  File rules will produce a list of field ld value cane to be sunnited over the top of the radio config. install corrected fileds in an automated lled corretclth the few fields that mus be changed based on rules engine analysis. d adjustemnyapply redimage of card to as archived on a given date in binry
2)  apply master file cfg using import process and install basic config values
2 Build Json build on current radio to be used with the rules engine to identify values that must be installed over the master config and save to naster binaryrestored after the master config binary is applied. create a processs to submit these cahngesbbto the new ride format. send the ride .ccfg file to all prticipnats, thh nadful of filds to be manually chngedm
2) Build json config of Master Config g indivial values specified as require  work cfg