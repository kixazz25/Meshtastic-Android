================================================================
V2.5 NOTE — GroupTrack Menu Architecture + 3.0 Gateway Role
2026-06-04 · GroupTrack V2.5 · architecture capture
================================================================

Captures the GroupTrack menu structure (launched from the GroupTrack icon in
Meshtastic), what stays vs goes in cleanup, and the button's future 3.0 role.

----------------------------------------------------------------
MENU STRUCTURE (current)
----------------------------------------------------------------
GroupTrack icon (in Meshtastic nav)
  -> GroupTrack menu
       -> EVENT / RIDE  sub-menu      [STAYS — still required]
       -> MAP SETTINGS  sub-menu      [QUARANTINE CANDIDATE — likely dead]

EVENT / RIDE  [STAYS]
  - Radio config writes: Apply Convoy Config, Archive, Restore (40/40 field
    validation, two-pass programming). Ride creation/config, ride file import.
  - These are the TRANSITIONAL radio-write functions (see
    V25_OpenItem_FirstLaunchNotes_OnlineManual_HelpIcon): alive + required in
    V2.5, slated for 3.0 cleanup, MUST be covered in the V2.5 manual.
  - The ? help icon on the Convoy radio-write screen links to this content.

MAP SETTINGS  [QUARANTINE CANDIDATE]
  - Believed fully dead: every function it held has already been MOVED elsewhere
    (Planning Map, Work-with-Artifacts, etc.).
  - >>> DO NOT REMOVE on belief alone. VERIFICATION happens INSIDE the cleanup
      item, BEFORE removal: where-used / xref every MAP SETTINGS function to
      confirm zero live callers AND no AllDocs feature mention (standing rule:
      verify dead via xref before removing; quarantine reversibly, never hard-
      delete). <<<
  - Until verified + removed, MAP SETTINGS stays as-is.

----------------------------------------------------------------
GROUPTRACK BUTTON — 3.0 GATEWAY (future, awareness only)
----------------------------------------------------------------
The GroupTrack button in Meshtastic nav becomes, in 3.0, the GATEWAY to online
+ offline functions: React web client (grouptrack.org), cloud sync, the
organizer subscription tier, ride engine / repository. Not a V2.5 item — noted
so the menu's eventual role is on record. V2.5 keeps it as the entry to the
current GroupTrack menu (Event/Ride + Map Settings).

----------------------------------------------------------------
CLEANUP / 3.0 IMPLICATIONS
----------------------------------------------------------------
  - V2.5 cleanup: verify-then-quarantine MAP SETTINGS (reversible, tagged
    branch/attic). Add to checklist §I dead-code quarantine candidate list.
  - 3.0: EVENT/RIDE radio-config functions are removed/replaced -> the
    corresponding V2.5 manual sections are removed/rewritten (carry as a 3.0
    doc task). The GroupTrack button is repurposed as the online/offline gateway.

----------------------------------------------------------------
GATE
----------------------------------------------------------------
Nothing removed here. MAP SETTINGS removal is gated on the cleanup item's
verification. This is the architecture record + quarantine flag. Fold the
MAP SETTINGS candidate into checklist §I and the menu/gateway note into §M
(architecture) on next checklist edit.
================================================================
