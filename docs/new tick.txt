Replacement functionaliaty s/b as follows (mostly follows existing process as it should be).

App open convoy map loads splash screen opens while convoy map draws gps from android or adio to postion map. 
Splash csreen shows with delay so app loads when tasks are completed.
My cart id is assigned. is node is connected after my carts is assigned node updates my cart id.
Record function prompts for user to  to slect tracks lead cart for node list unless node list is 1 cart. then lead is autoamted.
tick startw ith record function. radio broadcast every 5 seconds. tick should run every 2 so we do not end up with 9 second swings between whne carts broadacast. 
Tick should calculate each parts position for display with every tick. tick should use the leads cart new position to as leac cart tracks disply path snapto trails and track info.
tick should replace lead carts track on each android with the last android data point snpted to  my carts path. lead carts data point should nbe removed.

tick must calculate all hud measurement for my cart group, and selected cart info. i sould also calc all values like distance traveled etc, displayed on screens.