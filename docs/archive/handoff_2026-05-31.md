# Handoff — 2026-05-31

## Shipped this session
- Map-state independence + convoy SELECTED-filter parity — commit **e0182045a**.
- Waypoint creation on both maps (long-press → 12-type dialog → insert → draw) — committed.
- Trail DB dedupe recovery: 82,220 → 42,451 (3 agency imports collapsed by name+geometry). `spatial_BACKUP.db` retained on PC.
- Documentation system established: Google Drive `GroupTrack_docs/` working copy (Claude reads/writes live) + git `docs/` archive via recommit11.

## Docs produced/updated
- `grouptrack_manual.html` — 3-section spine (Online/Convoy/Planning).
- `v25_master_checklist.md` — primary tracker.
- Release-notes bullets for today's 3 functions (daily_doc_update_2026-05-31).

## Tomorrow's agenda (order)
1. **Cosmetic/quick-win pass** — Tier 1 (5 cosmetic, one patch) + Tier 2 (import/track-source cluster) + z12 separate. Hold the `!!`/safe-call tidy separate (not truly cosmetic).
2. **Dupe-creation roadblocks** — import (name,geometry) existence-check + post-import dedupe pass + DB UNIQUE constraint + source_id + null-naming. Trust/usability blockers.
3. **Route creation** — point-to-point with **snap-2** (trails+tracks as snap refs). Tester-chosen over freehand — do NOT revert to freehand.

## Standing reminders
- Convoy has TWO JS interfaces (494/622) + planning ONE (391); new bridge methods go in all three.
- DB pull/push: `MSYS_NO_PATHCONV=1` prefix (Git Bash mangles /sdcard). Force-stop + clear -journal before push.
- Build → test → commit each change. No stacking unverified.
- `./gradlew assembleGoogleRelease -x uploadCrashlyticsMappingFileGoogleRelease`; install `-r -d`.