GROUPTRACK — FEATURE LIST BY SURFACE
2026-09-04 · one folder per feature · capture every screen it opens

================================================================
RIDE MAP
================================================================

A  REC
     start recording
     recording in progress
     stop / name and save
     discard

B  Ride Planning
     (switches surface — no screens of its own)

C  SAT
D  TOPO
E  TOPO+
     (source switch — capture one example of the map redrawing)

F  NET
     online
     offline

G  QUEUES
     queue list
     item detail / progress
     empty state

H  Search
     search bar open
     results list
     result selected (map framed + record opens)
     no results

I  Map Features
     the panel
     Tracks — OFF / ALL / SELECT
     Tracks SELECT list
     Trails — OFF / ALL / SELECT
     Trails SELECT — the trail filter
     Waypoints — OFF / ALL / SELECT
     Waypoints SELECT list
     Routes — OFF / ALL / SELECT
     Routes SELECT list
     Create Route

J  Map Keys
     the key
     tap a category name — trail style panel
     colour / pattern / thickness
     apply

K  Help
     document chooser
     Quick Start open
     Release News open
     User Manual open

L  Mesh
     (only while the Meshtastic column is hidden)

M  Settings
     the settings screen
     map sources
     group-riding radio behaviour

N  HIDE
     button row hidden

O  SELECT CART
     cart list
     cart selected
     remove a cart

P  MY CART
     your cart centred
     distance ahead / behind

Q  GROUP
     whole group framed
     gaps and end-to-end length

R  Group readout
     span / carts / active / lost / lead / tail

S  Zoom
     (no screens of its own)

   TAP A TRAIL          the trail record
   TAP A TRACK          the track record + map downloads
   TAP A ROUTE          the ride description
   LONG PRESS           create a waypoint


================================================================
RIDE PLANNING MAP
================================================================

A  Back to Ride
     (switches surface — no screens of its own)

B  SAT
C  TOPO
D  TOPO+

E  QUEUES
     queue list
     item detail / progress

F  Search
     search bar open
     results list
     result selected

G  Map Features
     the panel
     Tracks / Trails / Waypoints / Routes — OFF / ALL / SELECT
     Trails SELECT — the trail filter
     ROUTE+
       method choice
       DRAW
         dropping pins
         snapping to trails
         straight line where there are none
         undo / review
         save — name the route
         discard
         resume an unfinished route
       AI ASSISTED
         welcome
         distance and speed
         trailhead
         drop waypoints
         summary
         searching
         COMPARE
           the table
           tick to keep
           save selected

H  Map Keys
     the key
     trail style panel

I  Help
     document chooser
     each document open

J  Mesh
     (only while the Meshtastic column is hidden)

K  MAPS
     download by area
     download along a route (corridor)
     confirm — size, tiles, megabytes
     area too large
     download running

L  Zoom

   TAP A ROUTE          the ride description
                        trails used (expanded)
                        points along the way (expanded)
                        ROUTE DETAILS
                        DOWNLOAD MAPS
   TAP A TRAIL          the trail record
   TAP A TRACK          the track record

   IMPORT               from Downloads — GPX from Gaia or onX
                        the import list
                        what came in


================================================================
MESHTASTIC NAVIGATION
================================================================

A  Conversations
B  Nodes
C  Mesh Map
D  Settings
E  Connection
F  GroupTrack Mesh Radio Setup
     Create Rides Unique Radio Config
     Send Meshtastic Ride Config via email
     Import Meshtastic Ride Config from Email
     Apply Meshtastic Config to your Radio
     Restore Saved config to your Radio
G  Hide Mesh
     folded — the Mesh word appears on the map


================================================================
FIRST RUN — not reached from any launcher
================================================================

   Permissions — all files
   Permissions — background location
   SYSTEM HOUSEKEEPING banner
   Access granted / Continue
   Home state — detected, confirm
   Home state — pick from the list
   Import running — the source list
   Import summary — what came in
