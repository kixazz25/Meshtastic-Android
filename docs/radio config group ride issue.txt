   radio config

new approach for configuring radio
we will mimic each of the radio config screens.
each config screen will be copied,  values copied in from our config file, no changes allowed the field values a red proceed button that save tracks Bluetooth connection and allows procced when the w have a connection status then on to the next screen until we have cycled through all screens. please adjust Monday stat up task for applying radio configuration return.


Field test results.

Two tracks in convoy app. First track is the lead track recorded to KML. The second track is a group track that displays on map screen. the display only group track worked fine the first half of the day. we paused for about twenty minutes and the second half of the day was not  a continuation of the group track, but a new track. the record button is what launches the group track the end record button stops it. I do not know why it restarted.

Also even though i ended record which should stop the group track a new group tack was recorded for my ride home.