================================================================
ROUTE TOOLBAR — CLEANUP ITEMS (v5 — RESOLVED, post-commit 89285b00a)
2026-06-04 · drag KEPT as-is with manual note
================================================================

Toolbar at pact compliance, committed 89285b00a.

----------------------------------------------------------------
HANDLE / DRAG — RESOLVED: KEEP AS-IS, document the behavior
----------------------------------------------------------------
DECISION (Fred): keep the function as it is. The drag works when the
accordion is OPEN; when collapsed, the tightened header routes the tap to
the accordion toggle instead. That's acceptable — accordion-open is a
reasonable precondition for repositioning.

MANUAL NOTE TO ADD (route creation / Route+ toolbar section):
  "To move the Route+ toolbar around the map, the panel must be EXPANDED
   (accordion open). Drag the ✥ grip handle in the header to reposition.
   When the panel is collapsed, tapping the header expands it rather than
   moving it."

No code change for the drag. NOT in the snap-2 patch. Manual-only.

----------------------------------------------------------------
STILL TO DO IN SNAP-2 PATCH
----------------------------------------------------------------
1. LABEL: "Amend In-Progress Routes" -> "In Progress"
   ConvoyRouteToolbar.kt: EntryBtnDisabled("Amend In-Progress Routes", ...)
   -> EntryBtnDisabled("In Progress", ...)

----------------------------------------------------------------
PLAY LAUNCH-GATE (§G, not now)
----------------------------------------------------------------
SpecifyForegroundServiceType lint — androidx.work SystemForegroundService
needs android:foregroundServiceType in AndroidManifest.xml. Pre-existing,
lint-only, build succeeded. Fix before Play submission with the ANR items.

----------------------------------------------------------------
NEXT SESSION: snap-2 slice #1 (Save-completed, free points) per
Snap2_Implementation_Plan_v2. Fold in: label rename only.
Manual: add the accordion-open-to-drag note to the Route+ section.
================================================================
